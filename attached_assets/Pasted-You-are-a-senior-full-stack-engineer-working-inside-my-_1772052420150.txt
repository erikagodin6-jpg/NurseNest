You are a senior full-stack engineer working inside my NurseNest codebase. Implement the ability to CONNECT my Facebook Page and Instagram account on my Social Post admin page, and then AUTO POST to both platforms. Right now my Social Post page does not provide any way to log in or connect accounts. Replace any placeholder “working on it” UI with a real, working implementation.

CRITICAL NOTE
You must use the official Meta OAuth flow and Meta Graph API. Do not ask for my password inside the app. The connection must happen via a “Connect Facebook” and “Connect Instagram” button that opens Meta’s authorization screen and returns to my site with approved permissions.

FEATURES REQUIRED
1) Connection UI on Social Post page
- A “Connections” section at the top of the Social Post page showing:
  - Facebook: Connected or Not connected
  - Instagram: Connected or Not connected
  - Connected Page name and IG account name
  - Token expiry status and “Reconnect” button if expired
  - “Disconnect” button
- Buttons:
  - Connect Facebook Page
  - Connect Instagram Business account (connected to the Page)

2) Posting UI on Social Post page
- “Post now” button for manual testing
- Draft editor:
  - caption text
  - choose platforms (Facebook, Instagram, or both)
  - image upload or select existing media
  - link field that defaults to: https://www.nursenest.ca
  - always include this line at bottom of caption:
    Want to learn more? Visit https://www.nursenest.ca
  - add UTM parameters automatically

3) Autopost twice per day
- Settings in the Social Post page:
  - enable or disable automation
  - set 2 daily posting times default 08:30 and 19:30 America/Toronto
  - choose content types (Question of the Day, Marketing post)
  - approval mode:
    - require approval before posting OR
    - auto post without approval
- Scheduler:
  - reliably posts at scheduled times in production
  - if cron is not available, implement a safe server polling worker that checks every 60 seconds for due posts and publishes them
  - use DB transactions or locks to prevent double posting

4) High engagement defaults
- Prefer image-first posts for Instagram
- Use best-performing sizes:
  - Instagram: 1080x1350 portrait
  - Facebook: 1080x1080 square
- Every post must include my logo on the image if the repo already has a logo asset. Place it in the bottom corner plus small “nursenest.ca” text at bottom.
- If branded image generation is not implemented yet, allow image upload and still include the website link line in the caption.

5) Official Meta API requirements
- Facebook Page publishing using Graph API
- Instagram Content Publishing API flow:
  - create media container
  - publish container
- Instagram requires Business or Creator account connected to a Facebook Page
- Handle errors and show them in the admin UI (permission missing, token expired, IG not connected, etc.)

PHASE 0: REPO DISCOVERY (DO THIS FIRST)
- Identify framework and routing (Next.js app router vs pages, etc.)
- Identify auth (how admins are identified)
- Identify DB/ORM and migrations
- Locate the existing Social Post page and any placeholder logic
- Identify if billing/social integrations already exist
Proceed without asking me questions.

PHASE 1: DATABASE MODELS
Create or extend tables using existing DB patterns:

SocialAccountConnection
- id
- userId
- facebookPageId
- facebookPageName
- instagramBusinessAccountId
- instagramUsername
- accessTokenEncrypted
- tokenExpiresAt
- scopesGranted
- connectedAt
- updatedAt

SocialPostDraft
- id
- createdByUserId
- caption
- linkUrl
- imageUrl
- platformTargets json (facebook, instagram)
- type (question|marketing)
- scheduledFor datetime
- status (draft|pending_approval|scheduled|publishing|published|failed)
- publishedAt
- errorMessage
- platformResponseJson

SocialAutomationSettings
- id
- isEnabled
- timezone = America/Toronto
- time1 = 08:30
- time2 = 19:30
- approvalMode required|auto

SocialPostLog
- id
- draftId
- platform
- status
- requestJson
- responseJson
- createdAt

Store tokens encrypted at rest using an ENCRYPTION_KEY env var.

PHASE 2: META OAUTH CONNECTION FLOW
Implement these routes:

GET /api/meta/connect
- starts OAuth and redirects to Meta authorization screen
- request required scopes for Page and IG publishing
- include redirect URI

GET /api/meta/callback
- exchanges code for access token
- fetch Facebook Pages the user manages
- choose the correct Page (if multiple, provide a UI selector in admin)
- fetch Page access token
- fetch Instagram Business Account ID connected to Page
- store encrypted tokens and IDs
- redirect back to Social Post page with success message

Include README instructions on how to create a Meta App and configure:
- App ID/Secret env vars
- Valid OAuth Redirect URIs
- Facebook Page + Instagram Business connection prerequisites

PHASE 3: PUBLISHING ENDPOINTS
Implement:
POST /api/social/publish
- admin auth required
- input: draftId
- loads connection tokens
- publishes to Facebook and/or Instagram based on targets
- updates status and logs results
- returns success/failure details for UI

PHASE 4: SCHEDULER
- Implement a scheduler that runs in production:
  - preferred: existing cron/job runner in repo
  - fallback: server loop checks every minute for due scheduled posts
- Ensure idempotency and prevent duplicates using DB transaction lock.

PHASE 5: ADMIN UI IMPLEMENTATION
- Replace placeholders with real UI
- Add:
  - Connections section (connect/reconnect/disconnect)
  - Draft editor
  - Draft queue (pending approval/scheduled)
  - Approve button if approvalMode is required
  - Logs list with errors and retry button
  - “Post now” for testing

SECURITY
- Only admin users can connect accounts or publish posts
- Do not expose access tokens to the client
- Validate all inputs server side

DELIVERABLES
1) Summary of what you implemented
2) List of files modified/added
3) DB migrations
4) Env vars needed:
   META_APP_ID
   META_APP_SECRET
   META_REDIRECT_URI
   ENCRYPTION_KEY
5) Manual test steps:
   - connect accounts
   - create a draft
   - post now to FB and IG
   - schedule and confirm it posts
6) Confirm no “working on it” placeholders remain

START NOW
- Do repo inspection first, then implement the connect buttons and OAuth flow so I can actually connect my accounts.
- Then implement posting and scheduling.
- Proceed without asking me questions and document assumptions briefly.
You are upgrading NurseNest Mock Exams to “Real Exam Mode” to match official pass/fail criteria by tier as closely as possible.

NON-NEGOTIABLE REQUIREMENTS
1) Users do NOT choose question count, difficulty, or domain mix.
2) Exam rules, stopping logic, scoring presentation, and UI must mirror the real exam experience by tier.
3) Passing is NOT percent-correct. Use ability/standard style outcomes for CAT exams (NCLEX/REx-PN) and scaled-score outcomes for NP boards (AANP/ANCC).
4) All premium mock exams are paywalled server-side. Free users only get a Readiness Exam (short) + limited review.

TIERS & PASS RULES TO IMPLEMENT
A) REx-PN (CAT-style)
- Minimum operational items gate: if candidate does not reach the minimum required operational items (90), outcome = FAIL (show “Minimum items not met”).
- Use CAT-style pass/fail: PASS if final ability estimate >= passing standard; FAIL otherwise.
- Implement stop rules that mimic the real exam: 95% confidence stop, max-length stop, run-out-of-time stop.
- UI must show: “Computer-Adaptive Exam (CAT)”, “Pass/Fail only”, no percent score.

B) NCLEX-PN and NCLEX-RN/NGN (CAT-style)
- Use CAT-style pass/fail: PASS if estimated ability >= passing standard; FAIL otherwise.
- Implement stop rules: 95% confidence stop, max-length stop, run-out-of-time stop.
- UI must present the exam like NCLEX:
  - “CAT Mode”
  - No question count selector
  - No immediate feedback
  - Review screen only after completion
  - Pass/Fail outcome presentation (no “you got 72%”)

C) NP Boards
- AANP: show scaled score 200–800, PASS if >= 500 (and show pass/fail).
- ANCC: show scaled score 0–500 (or 100–500 if your UI uses that), PASS if >= 350.
- Show domain breakdown as supportive analytics, but final result is still PASS/FAIL with scaled score.

EXAM GENERATION REQUIREMENTS (ALL TIERS)
1) Blueprint lock:
- Each tier has a fixed blueprint distribution table (domains + weights) stored in admin settings.
- Generator computes exact item counts per domain automatically.
- Ensure uniqueness: avoid repeating items recently seen by the user (recency window configurable).

2) Item-type realism:
- NCLEX/NGN: include NGN case sets and NGN item types consistent with your bank.
- REx-PN: keep item types aligned to your supported formats.
- NP: include board-style formats you support.

3) Difficulty + psychometrics simulation:
- For CAT tiers: implement an internal “ability estimate” progression model that updates after each response and selects the next item difficulty accordingly.
- Do not expose ability estimate to user during the exam.
- Ensure the stopping rule logic can reach pass/fail early, or proceed to max length if ability remains near standard.

SCORING & RESULTS UI REQUIREMENTS
1) During exam:
- Show only item navigation appropriate to real exam mode (no domain filters, no toggles, no customization).
- Show timer only (if the real exam is timed for that tier).
- Show “question x of y” ONLY if the real exam UI would show it; otherwise show “Item in progress” (admin configurable per tier).

2) End-of-exam results:
- Display: PASS or FAIL prominently.
- For CAT tiers: do NOT show percent-correct. Show “CAT result based on ability relative to passing standard.”
- Provide domain performance bands (e.g., Above / Near / Below) instead of exact percents for CAT tiers (admin configurable).
- For NP tiers: show scaled score and pass threshold line.
- Provide a remediation plan that targets weakest domains and recommends a schedule.

PAYWALL + PREVIEW REQUIREMENTS
1) Free users:
- Can access a 25-question Readiness Exam per tier with limited review.
- Must not access full mock exams, full rationales, or exports.
2) Paid users:
- Full Real Exam Mode mocks, full review, analytics, and remediation plans.

ADMIN CONTROLS REQUIRED
- Per tier: blueprint weights, min/max length, time limit, pass standard label, item-type mix, domain band thresholds, recency window, retake lockout.
- Ability model parameters for CAT tiers (tuning).

DELIVERABLES
Return:
1) Updated generator logic plan
2) Data model changes (tier config + blueprint tables)
3) UI changes to remove question count selection and enforce Real Exam Mode
4) Server-side gating updates
5) Example tier configs for REx-PN, NCLEX-PN, NCLEX-RN/NGN, AANP, ANCC
Do not redesign styling; reuse the current NurseNest UI components and layout.
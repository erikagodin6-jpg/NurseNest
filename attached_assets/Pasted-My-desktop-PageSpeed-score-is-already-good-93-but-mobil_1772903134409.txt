My desktop PageSpeed score is already good (93), but mobile is much worse (64). This means the site architecture is capable of performing well, but the mobile homepage is too heavy.

I want you to optimize the public homepage for mobile-first performance without harming the current strong desktop experience.

Context:
Desktop for https://www.nursenest.ca/en
- Performance: 93
- FCP: 0.9 s
- LCP: 1.5 s
- TBT: 50 ms
- CLS: 0.051
- Speed Index: 1.4 s

Mobile for the same page
- Performance: 64
- FCP: 4.6 s
- LCP: 7.2 s
- TBT: 110 ms
- CLS: 0
- Speed Index: 4.8 s

Shared Lighthouse issues on both:
- Reduce unused JavaScript (~187 KiB)
- 7 long main-thread tasks
- Render-blocking requests
- Optimize DOM size
- Non-composited animations
- Canonical issue on /en page
- Some accessibility issues

Your task:
Refactor the homepage and any shared landing-page components so mobile becomes much lighter while preserving the polished desktop experience.

Implement the following:

1. Mobile-first homepage simplification
- Reduce above-the-fold content on mobile
- Keep only the main headline, short supporting text, CTA, and one lightweight visual in the mobile hero
- Defer or lazy-load lower sections on mobile
- Avoid rendering large offscreen sections immediately on mobile
- Keep desktop layout largely intact unless a change clearly improves both

2. LCP optimization
- Identify the LCP element on the homepage
- If it is an image, serve an optimized responsive version for mobile
- Use WebP or AVIF where possible
- Add explicit width and height
- Preload only the true LCP image
- Do not lazy-load the LCP image
- Replace CSS background-image hero implementations with img/picture if that improves LCP

3. JavaScript reduction
- Code-split below-the-fold homepage sections with React.lazy or dynamic imports
- Lazy-load testimonial sliders, video embeds, modal components, chat widgets, and heavy marketing sections
- Remove unused imports and large icon bundle usage
- Ensure admin-only or subscriber-only code is not included in the public homepage bundle

4. Render-blocking cleanup
- Inline critical CSS for above-the-fold homepage content
- Defer noncritical CSS where safe
- Add font-display: swap to custom fonts
- Reduce font variants and weights on the homepage
- Defer nonessential scripts

5. Animation optimization
- Replace layout-affecting animations with transform and opacity only
- Reduce or disable expensive mobile animations
- Remove unnecessary hero and card animations on mobile

6. DOM size reduction
- Reduce the number of initially rendered cards/items/sections
- Do not mount hidden accordions, drawers, modals, or tab panels until opened
- Lazy-render long content sections

7. Fix canonical issue
- Ensure https://www.nursenest.ca/en has a self-referencing canonical
- Do not point /en canonical to the root homepage unless that is truly the canonical equivalent
- Audit multilingual canonical logic across homepage routes

8. Accessibility cleanup
- Add labels to select elements
- Fix heading hierarchy
- Improve insufficient contrast where practical
- Ensure icon-only buttons have accessible names

Deliverables:
- Identify the main mobile bottlenecks
- List the files/components changed
- Apply the refactor directly
- Preserve the strong desktop experience
- Focus on real implementation, not generic advice
Paste this from Replit (minimum needed)
In Replit AI, ask it to output just these two blocks (no whole file):
the validateSection() function (or whatever it’s named)
the per-section “shape contract” map/object and the decision-verb counter function
Use this prompt:
Output ONLY the unified diff hunks for server/routes.ts that include:
1) validateSection (entire function)
2) the new shape-contract enforcement logic (map/config + checks)
3) the decision-density counter (function or inline logic)
Nothing else.
Once you paste those hunks here, I can confirm it’ll behave exactly as you intend.
Two common gotchas that would still produce “generic but passing” content
Gotcha A: the “list block” kinds don’t match your generator
You’re validating kinds like:
bullets, checklist, steps
…but your generator might output:
list, ul, orderedList, numbered, points
If that mismatch exists, you’ll see repeated retries / 422 failures, or you’ll “fix” it by loosening validation again.
Fix: treat any block with an items array as a list, regardless of kind:
isListLike = Array.isArray(b.items) && b.items.length > 0
…and use that for the “2 qualified list blocks” requirement.
Gotcha B: decision verb count isn’t counting list/table text
If your decision-density checker only scans b.text, you’ll undercount verbs and fail good content.
Decision verbs must be counted across:
b.text
list items
table cell strings
callout text/captions
Otherwise, you’ll get false failures.
Recommended precise implementation (so it’s bulletproof)
List blocks qualification
Count a list as “qualified” if:
it has items.length >= 5
and combined item text length >= 150
Do not rely only on kind.
Table qualification
Count a table as qualified if:
rows length >= 3
and total cell chars >= 200 (prevents 3 empty rows)
Callout qualification
Callout qualifies if:
text length >= 80
and contains at least 1 decision verb OR 1 “exam trap” marker
Decision density
Count decision verbs on:
all block text
all list items text
all table cell text
Use whole-word matching to avoid counting “assessment” as “assess” unless you want that.
One more improvement (high impact)
Your “pharmacology keyword check” is smart, but use "contraindic" like you wrote so it matches contraindication/contraindicated.
Also include:
“hold parameters”
“monitor for”
“do not give if”
Because models sometimes avoid the exact word “contraindication.”
What success will look like in the output
After these shape contracts, your pathophysiology section should start looking like:
A short mechanism paragraph (ok)
A Recognition cues bullets block (>=5)
A What happens next steps block (>=5)
A Don’t confuse with table (>=3 rows)
Two callouts:
Red flags
Exam traps
Language loaded with assess/monitor/hold/escalate/prioritize
That’s exam-cram.

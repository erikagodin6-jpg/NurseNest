Keep the current NurseNest logo file exactly as it is for now and implement a theme-aware color system around it so the site still feels fully branded when a user changes themes.

Goal:
Do not replace or edit the current logo asset.
Instead, make the surrounding UI match the user’s selected theme, including:
- top announcement bars
- header background
- navigation menus
- dropdown menus
- hover states
- active menu states
- buttons
- tabs
- cards
- key accents

Important rules:
1. Keep the current logo file unchanged.
2. Do not change logo spacing, sizing, placement, padding, or alignment.
3. Do not modify the logo upload system.
4. Do not attempt to convert the current logo to SVG.
5. Build the theme system around the existing logo so the site still looks polished and cohesive.

Implementation requirements:

1. Create a centralized theme token system
Add shared theme variables for:
- primary brand color
- secondary brand color
- accent color
- top bar background
- top bar text
- navbar background
- navbar border
- menu text
- menu hover background
- menu hover text
- active menu color
- button primary background
- button primary text
- button secondary background
- button secondary text
- card background
- card border
- page background
- heading text
- body text
- muted text
- input background
- input border
- focus ring

Use CSS variables or the project’s existing theme system so colors are not hardcoded in individual components.

2. Add theme presets
Implement at least these theme options:
- Pastel Lilac
- Lavender Dream
- Clinical Light
- Soft Sage
- Dark Mode

Make them feel premium, soft, professional, and on-brand for NurseNest.
Avoid childish, oversaturated, or neon-looking colors.

3. Apply theme colors across the site
Refactor shared UI components so they use the theme tokens instead of hardcoded colors.

Apply to:
- top announcement bars
- navbar
- desktop dropdowns
- mobile menu
- footer accents
- buttons
- tabs
- chips/badges
- cards
- pricing highlights
- study dashboard accents
- search bars
- hover states
- selected states
- progress indicators where appropriate

4. Keep the current logo visually integrated
Since the logo file will remain unchanged, choose surrounding header and menu colors that complement the existing logo.
Do not place the current logo on backgrounds that make it look washed out, low-contrast, or mismatched.

5. Add a user theme switcher
Create or improve a theme picker so users can select their theme.
Requirements:
- clear theme names
- live preview on selection
- elegant UI
- mobile-friendly
- no clutter in the navbar

6. Save the user’s theme preference
Persist the selected theme in localStorage and also save it to the user profile if the app already supports user preferences.
Ensure the saved theme loads immediately on app startup.

7. Avoid layout regressions
Do not break:
- navbar height
- logo alignment
- menu spacing
- dropdown positioning
- responsive behavior
- mobile header
- sticky header behavior

8. Add accessibility safeguards
Ensure text remains readable in all themes.
Check contrast for:
- top bar text
- nav text
- dropdown text
- button text
- active states
- hover states

If a theme color causes poor contrast, automatically use a safer text color.

9. Make it work across the platform
Apply the theme system consistently to:
- main NurseNest pages
- pricing pages
- nursing tier pages
- allied health pages
- new grad pages
- dashboards
- auth pages
- admin pages where appropriate

10. Clean up hardcoded colors
Audit shared components and remove scattered hardcoded color values where possible.
Replace them with theme tokens for long-term maintainability.

Deliverable:
At the end, provide:
- the list of theme presets added
- which shared components were updated
- confirmation that the current logo file was left unchanged
- confirmation that the bars, menus, buttons, and accents now respond to theme choice

Final result:
The existing NurseNest logo stays exactly as it is, but the rest of the interface changes around it so the whole site feels cohesive, premium, and theme-aware.
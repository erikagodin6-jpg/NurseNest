You are a senior full-stack engineer. Scan my NurseNest codebase and implement a “site-wide quality pass” that fixes the highest-impact issues affecting: (1) language switching and translation completeness (especially FR), (2) admin content persistence and gating, (3) SEO/indexing hygiene, and (4) reliability of publish/schedule workflows.

Non-negotiable constraints
- Do NOT break the existing blog generator or blog scheduler; treat them as production-critical.
- Make minimal, safe changes; prefer additive utilities + small targeted patches.
- Preserve SEO: canonical, hreflang, stable URLs, no accidental noindex.
- Admin must always be able to view/edit any content regardless of tier/status when authenticated.
- Public users must only access published content they’re entitled to.

PART A — Translation & language switching must be 100% consistent
Problem to solve
- French switching is inconsistent: Pre-Nursing works but Anatomy/Physiology and other routes do not fully translate. When users switch languages, some pages remain in English or partially untranslated.

Tasks
1) Identify current i18n architecture
- Determine whether we use react-i18next/next-intl/custom dictionaries.
- Identify where locale is stored (cookie/localStorage/router path) and enforce a single source of truth.
- Confirm which routes/layouts are wrapped by the i18n provider. Find any routes (A&P, lessons, tools, pricing, etc.) that are not.

2) Fix the language switcher so ALL routes translate on switch
- Switching language must update:
  - URL locale prefix (e.g., /en → /fr) OR official locale mechanism
  - persisted cookie (preferred) so SSR loads correct language on first paint
  - client store (if used) without fighting the cookie/router
- Eliminate “flash of English” (hydration mismatch). Ensure SSR and client agree.

3) Ensure A&P and lesson pages load the correct translation namespaces
- Find hard-coded English strings on A&P pages/components and convert to translation keys.
- Ensure the correct dictionary namespaces load for A&P pages on initial render.
- Ensure all menus, category labels, lesson cards, breadcrumbs, buttons, and CTAs use i18n keys.

4) Localize dynamic content (lesson bodies) correctly
- Determine where lesson content lives (DB vs static vs CMS).
- Implement one of:
  - content_translations table keyed by (contentId, locale)
  - or localized fields (title_fr/body_fr/seo_fr, etc.)
- Update content fetch APIs to accept locale and return the localized variant.
- Controlled fallback: If FR content is missing, show EN content but:
  - log missing translation key/record
  - show an admin-only banner “French translation missing. Add now.”
- Add a nightly admin report endpoint or UI list: “Missing FR translations” with counts.

Acceptance checks
- Switching EN→FR on Anatomy and Physiology pages results in 100% French UI.
- Lesson detail pages in FR show FR content or controlled fallback + admin banner.
- Refresh on /fr pages stays French on first paint.

PART B — Admin gating & credentialed fetch must be consistent everywhere
Problem to solve
- Some content endpoints return 403 (tier gating) or 404 (draft/unpublished) and admin is blocked because GET requests aren’t credentialed and/or admin override isn’t supported consistently.
- UI sometimes auto-opens editor when dbContent is null, masking fetch failures.

Tasks
1) Create a consistent “adminView” contract
- For all content types (lessons, blog, exam questions, flashcards):
  - Public GET: published only + entitlement gating
  - Admin GET: allow adminView=true when authenticated; bypass tier/status gating
- Ensure auth is enforced on adminView. Never allow bypass without admin role.

2) Fix client fetch helpers
- Centralize fetch into one helper used by all admin pages:
  - fetch(url, { credentials: "include" })
  - adds adminView=true when admin is logged in
- Handle 403 vs 404 explicitly; do not treat failures as “open editor”.

3) Remove “auto-open editor on null content”
- Replace with explicit UI states:
  - Loading
  - Not found (404)
  - Restricted (403) with upgrade CTA for public
  - Admin: show “Unpublished/Draft” state + Edit button (never auto-open)

Acceptance checks
- Admin can view tier-gated and draft content reliably.
- Public cannot access drafts; paid content shows clear upgrade CTA.

PART C — Publish/save/schedule reliability (lessons, blog, questions, flashcards)
Problem to solve
- Reload-based flows cause race conditions and stale fetch states (especially with gating).

Tasks
1) Remove window.location.reload() after publish/save everywhere
- After POST/PUT returns saved item:
  - update local state from response
  - navigate to canonical view route using returned slug/id
  - invalidate query caches (SWR/React Query) if used

2) Ensure slug/id consistency
- Verify all create/update endpoints return slug/id and the client uses THAT for follow-up fetch.
- Prevent slug from changing unexpectedly on publish unless explicitly desired.

3) Scheduling parity across systems
- Ensure scheduler runner is idempotent and logs failures:
  - scheduled + publishAt <= now → publish
  - if validation fails → log and do not publish
- Ensure admin can schedule batches for questions/flashcards similar to blog.

PART D — SEO hygiene & indexing
Problem to solve
- Many localized routes exist; risk of duplicated/incorrect indexing if canonical/hreflang/sitemaps aren’t consistent.
- Add minimum blog word count enforcement for future and currently scheduled posts (not retroactive to already published).

Tasks
1) Canonical/hreflang audit across locales
- Ensure each locale page:
  - has canonical pointing to itself (or the chosen canonical strategy)
  - has hreflang links for alternates
  - avoids indexing thin/duplicate pages incorrectly
- Verify sitemap includes correct locale URLs and no junk locales.

2) Blog minimum words enforcement for all future + currently scheduled posts
- Server-enforced BLOG_MIN_WORDS (default 1500, configurable)
- Save draft: allowed, but compute/store wordCount and belowMinimum flag
- Manual publish: block with 422 if too short unless admin bypass
- Scheduler publish: also enforce; short scheduled posts must fail gracefully and not publish
- Provide admin UI: live word count + required minimum

3) Internal linking and crawl efficiency
- Ensure /lessons and category hubs link down to lesson pages and cross-link “What’s next?”
- Ensure breadcrumb markup and consistent title/meta for hubs
- Identify orphan pages and add internal links where appropriate.

Deliverables
- List: top 20 issues found (file + line or component names) grouped by Translation, Admin/Gating, Publish/Scheduler, SEO
- Implement fixes with minimal diffs
- Provide a regression checklist covering:
  - Language switching on A&P + lessons
  - Admin view of tier-gated and draft items
  - Publish without reload for lessons/questions/flashcards
  - Scheduled blog posts under minimum words do not publish
  - Canonical/hreflang correctness
Proceed now: start by mapping the routing + i18n provider coverage across Pre-Nursing vs Anatomy/Physiology and identify why those routes aren’t loading the same translation namespaces.
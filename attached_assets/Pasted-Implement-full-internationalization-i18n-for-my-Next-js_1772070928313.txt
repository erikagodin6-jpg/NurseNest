Implement full internationalization (i18n) for my Next.js / React website with multi-locale routing, translation extraction, hreflang, and sitemap support.

Locales to support (must be configured end-to-end):
en (English)
fr (French)
es (Spanish)
fil (Filipino/Tagalog)
hi (Hindi)
zh (Chinese)
ar (Arabic)
ko (Korean)
pt (Portuguese)
pa (Punjabi)
vi (Vietnamese)
ht (Haitian Creole)
ur (Urdu)
ja (Japanese)
fa (Farsi/Persian)

Routing:
Use locale path prefixes (no query params). Structure:
 /en/...
 /fr/...
 /es/...
 /fil/...
 /hi/...
 /zh/...
 /ar/...
 /ko/...
 /pt/...
 /pa/...
 /vi/...
 /ht/...
 /ur/...
 /ja/...
 /fa/...

Do NOT create a custom i18n engine. Use a well-supported Next.js-compatible library (prefer next-intl for App Router; react-i18next acceptable if already installed). Keep changes minimal and production-safe.

Primary tasks:
1) Extract ALL user-facing text from these files and replace hard-coded strings with i18n translation keys:
- lessons.tsx: all lesson system titles + all lesson names
- pre-nursing.tsx: ALL UI text (headings, buttons, labels, placeholders, helper text, empty states, filters, errors)
- anatomy.tsx: ALL UI text (same scope)

2) Create a stable, consistent translation key hierarchy. Examples:
pages.preNursing.title
pages.anatomy.title
common.startLesson
common.search
common.noResults
ui.filters.sortBy
lessons.systems.cardiology
lessons.items.hyperkalemia

Keys must be human-readable and consistent. Avoid random hashes.

3) Translation resources:
Create /locales/<locale>.json files for ALL locales above.
- en.json must contain the authoritative English strings.
- For all other locales: use existing translations if present in the repo; otherwise generate best-effort translations for missing strings only.
- Do NOT translate brand name “NurseNest”.
- Keep medical terms accurate and appropriate for nursing learners.

4) Locale switching:
Implement a language switcher component that:
- Changes locale while preserving the current path (same page in another locale).
- Falls back to English if a translated route/page does not exist.
- Persists the chosen locale (cookie or localStorage) without breaking SSR.

5) RTL support:
Arabic (ar) and Farsi (fa) must render in RTL direction.
Implement direction handling:
- Set dir="rtl" for ar and fa
- Set dir="ltr" for others
Ensure layout does not break (no major UI redesign).

6) SEO hreflang:
Add hreflang alternates on every indexable page:
- Include <link rel="alternate" hreflang="xx" href="..."> for every locale
- Include x-default pointing to the English version
- Canonical must be correct per-locale (each locale page canonicalizes to itself, not to /en)

7) Sitemaps:
Update sitemap generation so that:
- Only https://www.nursenest.ca URLs are included (no replit.app URLs)
- All localized URLs are included for all locales
- Output is valid XML and acceptable to Google Search Console
- If the site is large, implement a sitemap index (sitemap_index.xml) that references per-locale sitemaps

8) Indexing controls:
If any pages are paywalled or not meant for indexing, they must be excluded from sitemap and have noindex (but do not change business logic—only apply existing rules).

9) Safety and quality:
- Preserve styling and component structure; do not redesign the UI.
- Do not remove existing logic.
- Add fallback behavior: if a key is missing, show English.
- Ensure build passes TypeScript and lint checks.
- Provide a short summary of files changed and what was implemented.

Deliverables:
- Updated lessons.tsx, pre-nursing.tsx, anatomy.tsx using t("...") keys
- i18n configuration and routing
- /locales/*.json for all locales above
- Language switcher
- RTL direction handling for ar/fa
- hreflang + canonical tags
- Updated sitemap generation that includes all locales and only the correct domain
Your deployment is under-provisioned and unhealthy, and the exam path is amplifying it.
What the evidence says:
Memory at 98% (163MB / 167MB) means the service is effectively operating at the edge of OOM failure.
Health check reporting “down” means the app is not staying healthy enough to satisfy deployment monitoring.
/api/exams taking 3,102ms means exam startup is too heavy or blocked.
Repeated exam-incident-report calls means the frontend is likely looping or repeatedly retrying when exam init fails.
Deadlock + column errors during startup means startup/migrations/schema handling are also destabilizing the app before users even get a clean runtime.
So this is not just “memory is a little high.” It is a compound production failure with four layers:
Runtime memory exhaustion
Heavy/slow exam initialization
Frontend retry/report loop making load worse
Startup/database/schema issues causing instability before steady state
The real root cause
The clearest root cause is:
The app is running in too little memory for its current workload, while exam initialization and startup DB work are too heavy and error-prone, and the frontend is repeatedly hitting the backend during failure states.
That creates a failure spiral:
startup has column/deadlock issues
app becomes unhealthy
memory stays near max
/api/exams responds slowly
frontend retries / incident-reports repeatedly
more memory and request pressure
health checks fail
service is marked down
What Replit needs to do next
Tell them to stop “checking a few more files” and execute this plan in order:
You now have enough evidence to stop investigating broadly and move into incident remediation.

Confirmed facts:
- deployment memory is 98% utilized (163MB / 167MB)
- health checks report service down
- /api/exams is taking ~3102ms
- frontend is repeatedly calling exam-incident-report
- there is a deadlock
- there are startup column/schema errors

This is a live production failure affecting paying users.

Proceed with this exact priority order.

1. EMERGENCY STABILIZATION
- stop frontend retry/report loops for failed exam initialization
- rate-limit, debounce, or one-shot exam-incident-report calls
- prevent infinite or repeated reporting from the same client/session
- add hard timeout and circuit breaker around /api/exams
- return a lightweight fallback response instead of hanging
- ensure exam route cannot keep hammering the backend when init fails

2. STARTUP / DATABASE FIXES
- identify and fix the exact startup column errors immediately
- identify and fix the deadlock source
- ensure startup does not perform conflicting migration/repair logic
- ensure deploy/start path is schema-safe and does not run broken startup DB actions
- confirm the app can boot cleanly with no column mismatch and no deadlock condition

3. EXAMS ENDPOINT HARDENING
- inspect /api/exams and reduce its initial payload drastically
- do not load full exam datasets, rationales, translations, results, media, and extras at startup
- return only minimum bootstrap data needed for exam entry
- move question loading to windowed/question-by-question or small-batch mode
- defer all nonessential enrichments
- add defensive handling for malformed data and DB failure
- log timing for each stage inside /api/exams to identify the slow segment precisely

4. MEMORY REDUCTION
- identify what is consuming memory at startup and during /api/exams
- remove unbounded caches
- stop loading unnecessary modules on boot
- lazy-load heavy admin/generator/exam extras
- reduce oversized serialized objects
- reduce query result sizes
- confirm memory drops materially below current levels after fixes

5. HEALTH CHECK RECOVERY
- ensure health endpoint reflects actual readiness only after startup is stable
- make sure unhealthy startup conditions do not leave the app half-booted
- verify service remains healthy after deploy and after repeated exam access

6. PAID USER PROTECTION
- implement fallback exam mode
- if full exam engine is unhealthy, valid subscribers must still get a usable lightweight exam/practice experience
- no white screens
- no endless loaders
- no dead-end premium routes

Deliver back:
1. exact root cause
2. exact files changed
3. exact source of startup column errors
4. exact source of deadlock
5. exact source of /api/exams slowness
6. exact source of frontend repeat incident-report calls
7. before vs after memory
8. before vs after /api/exams latency
9. confirmation that health checks pass consistently
What each symptom most likely means
Based on what you pasted:
98% memory
your container is too small and/or the app is loading too much at startup or on exam init
even if they optimize, they may still need more RAM temporarily to stabilize production
health check down
the app is either not starting cleanly, taking too long to become ready, or becoming unhealthy under load
/api/exams at 3102ms
likely too much DB work, too much data loaded, blocking logic, or retries under degraded conditions
repeated exam incident report calls
frontend likely has a bad effect loop, retry loop, or failure handler that re-fires on each render/request failure
deadlock
probably concurrent startup DB work, competing writes, or a migration/repair/session creation conflict
column errors during startup
schema mismatch between code and deployed DB is very likely part of the boot instability
What matters most right now
The order should be:
stop retry loops
fix startup schema/deadlock
slim /api/exams
reduce memory
add fallback mode for subscribers
Because until startup is clean and exam retries stop, the system will keep hurting itself.
What not to accept from Replit
Do not accept:
“we increased RAM” as the only fix
“we found some files to inspect”
“the issue is memory” without exact file/route/query causes
“we added retries”
“we’ll monitor it”
You need:
exact failing migration/startup logic
exact slow code path in /api/exams
exact frontend loop causing repeated incident reports
exact before/after numbers
One-line diagnosis you can use
Send this to them if you want it blunt:
The production outage is being caused by a failure spiral: schema/startup errors and deadlocks are preventing a healthy boot, the app is running at 98% memory, /api/exams is too heavy and slow, and the frontend is repeatedly re-triggering failure/report flows, which compounds load and keeps the service unhealthy.
I can turn this into an even sharper “incident commander” prompt that forces them to fix the exact files
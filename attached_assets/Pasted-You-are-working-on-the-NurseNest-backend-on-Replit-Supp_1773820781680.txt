You are working on the NurseNest backend on Replit.

Support the mobile app by finalizing the PRODUCTION AUTH + PROFILE + ONBOARDING + PRACTICE API CONTRACT.

The mobile app is ready to replace mocks. The backend must now provide stable production endpoints and response shapes.

==================================================
PRIMARY GOAL
==================================================

Provide production-safe backend endpoints for:
- authentication
- current user/profile
- onboarding persistence
- answer submission
- premium/free usage state

==================================================
SECTION 1 — AUTH ENDPOINTS
==================================================

Ensure stable endpoints exist for:
- POST /auth/login
- POST /auth/signup
- POST /auth/logout
- GET /auth/me

Requirements:
- return consistent response shapes
- include user profile fields needed by app
- include onboardingCompleted
- include role/country/exam/goal fields if available
- include subscription/entitlement summary if available

==================================================
SECTION 2 — ONBOARDING PROFILE PERSISTENCE
==================================================

Create or finalize endpoint such as:
- POST /me/onboarding
or equivalent

Save:
- role
- country
- examType
- studyGoal
- dailyStudyTime
- onboardingCompleted

Requirements:
- validate payload
- update user profile
- return updated user object
- do not partially corrupt profile on bad input

==================================================
SECTION 3 — QUESTION / USAGE / LIMITS
==================================================

Create or finalize answer submission endpoint and usage tracking.

Support:
- submit question answer
- update question history
- update daily usage count / free tier usage
- return updated entitlement/remaining-question state if applicable

Requirements:
- backend should become source of truth for free-tier limits
- support app + website consistently
- no separate mobile-only usage logic

==================================================
SECTION 4 — RESPONSE CONTRACT
==================================================

Define stable response structures the mobile app can rely on.

At minimum, GET /auth/me should include:
- user
- onboardingCompleted
- onboarding data
- subscription info
- entitlement info
- usage summary if applicable

==================================================
SECTION 5 — SECURITY / SESSION
==================================================

Support token-based authenticated requests for mobile.

Requirements:
- secure auth validation
- expired-token handling
- logout invalidation if applicable
- clean unauthorized responses

==================================================
SECTION 6 — DELIVERABLES
==================================================

Implement and return:
1. endpoints added/updated
2. payload/response shapes
3. how onboarding is stored
4. how usage/free limits are tracked centrally
5. any missing backend pieces the app still depends on
Debug and permanently fix the CAT exam loading issue on the published NurseNest site, and also fix the recurring problem where newly added database content only appears in Replit preview/dev but does not appear on the actual published live site.

This is now a repeated production failure and it is not acceptable.
Do not do another preview-only fix.
Do not make another change that “works in Replit” but fails on the real deployed site.
Treat production/live-site visibility as a required success condition.

MAIN PROBLEMS

Problem 1:
On the published live site, I am logged in and can open the CAT exam page. The custom colour modal/window appears, but after that the actual exam does not populate.

Problem 2:
This is at least the third time new database content has been visible in Replit preview/dev but not on the actual live published site. This wastes time and money. I need this fixed at the root so future question bank, CAT, flashcard, and lesson content is actually stored and visible on production.

PRIMARY GOALS

1. Make the CAT exam fully load on the published site after the custom colour modal step
2. Identify the exact root cause of the preview-vs-production data mismatch
3. Fix the data/storage/deployment architecture so newly added content is truly available on the live site
4. Add safeguards so this does not keep happening again

CRITICAL INSTRUCTION

Do not stop after making preview work.
Do not mark the task complete unless the published site works.
The published production site is the source of truth.
A change is not successful unless:
- the live site loads correctly
- the live production API returns the expected data
- the live production database contains the expected records
- the live user-facing page displays the expected content

PART 1: DEBUG THE CAT EXAM LOADING ISSUE

Trace the full production start-exam flow from the CAT page to the first rendered question.

Investigate all of the following:

A. Frontend flow
- identify the CAT exam page component
- identify the custom colour modal component
- trace what happens when the user clicks continue/start after selecting colours
- confirm whether the modal submit handler actually triggers exam initialization
- confirm whether route navigation occurs
- confirm whether exam state/session is created
- confirm whether first question fetch is triggered
- confirm whether returned data reaches the rendered exam component

B. Add temporary logs around the modal submit step
Log:
- selected colours/theme payload
- exam tier
- exam slug or exam ID
- CAT config ID if applicable
- authenticated user/session info
- whether startExam() or equivalent runs
- whether createSession() runs
- whether fetchFirstQuestion() runs
- whether route push or state transition happens
- whether the response payload is valid

C. Inspect frontend failures
- check for JS errors after modal submit
- fix any null/undefined state issues
- fix failed promise handling
- inspect hydration/state issues
- inspect whether the exam container is waiting on missing properties and silently rendering blank

D. Inspect production API requests
- identify the endpoint used to initialize CAT exam
- identify the endpoint used to create the CAT session
- identify the endpoint used to fetch the first question
- log request payloads and responses in production-safe ways
- determine whether the published site gets:
  - 401
  - 403
  - 404
  - 500
  - empty JSON
  - invalid session object
  - no question payload
- fix any failing endpoint or bad request handling

E. Inspect auth/session issues
Even if user is logged in, verify:
- auth cookies/session are available to CAT endpoints
- middleware is not blocking CAT start
- subscription/tier checks are not silently failing
- session survives modal submit flow
- protected route logic is not causing a silent blank state

F. Inspect production CAT data/config
Verify production has:
- CAT question pool
- CAT blueprint/config
- adaptive settings
- valid exam slug/tier mapping
- nonzero question count
- ability to select a first question
- correct production filters

G. Inspect route and param mismatches
- verify exam slug, tier key, region key, and CAT identifiers after modal submit
- ensure modal state does not overwrite/drop required params
- normalize casing, slug mismatches, enum mismatches, trailing spaces, and route keys

H. Inspect loading-state logic
- determine if exam is loading but UI never exits loading/empty state
- fix conditions where the UI waits forever for partial data
- add explicit error states instead of silent blank content

PART 2: FIX THE REPEATED PREVIEW-ONLY DATABASE PROBLEM

This is a major issue and must be fixed permanently.

I need you to identify exactly why content keeps appearing in Replit preview/dev but not on the published site.

Investigate all of the following:

A. Compare preview/dev database vs production database
- identify exactly which database preview uses
- identify exactly which database production uses
- compare DATABASE_URL and any other storage config between environments
- confirm whether preview and production are using different DBs
- confirm whether content was inserted only into the preview/dev database

B. Compare environment variables and secrets
- compare preview/dev env vars vs deployed production env vars
- inspect:
  - DATABASE_URL
  - API base URLs
  - storage config
  - auth config
  - feature flags
  - deployment secrets
- identify anything missing or different in production
- fix mismatches

C. Check for local/mock/preview-only data sources
- determine whether preview is using:
  - mock JSON
  - local seed data
  - temporary in-memory state
  - admin-only cached data
  - local SQLite/file-based storage
while production uses a different source
- remove any architecture that causes preview-only visibility unless intentionally used

D. Check content insertion/import flows
- trace how questions/content are being inserted
- determine whether imports, seeds, or admin-created content write only to preview/dev DB
- ensure inserts and admin-created content go to the real production data source when intended
- ensure future uploads/imports do not silently write only to preview

E. Check production filtering
Make sure records are not being excluded on live site because of:
- isPublished false
- status draft
- visibility mismatch
- wrong examTier
- wrong region
- wrong specialtyTrack
- wrong slug
- archived/deleted flags
- missing flashcard flag
- missing storeReady flag
- role/subscription mismatch
- admin-only filters
- language mismatch

F. Check deployment/build issues
- verify production is running latest build
- verify content-creation changes happened before or after deployment
- check for stale builds/cached API responses
- invalidate stale cache if needed
- ensure the app is redeployed when necessary

PART 3: PERMANENT SAFEGUARDS

Add robust safeguards so this stops happening.

Implement the following:

1. Production data validation checks
At app startup or in admin diagnostics, validate:
- production DB connection exists
- required env vars are present
- production DB is not empty unexpectedly
- key tables return expected counts by tier

2. Clear environment diagnostics
Add a safe admin/dev diagnostics view or script that shows:
- current environment
- current DB target
- row counts by major content type
- row counts by tier
- row counts by published vs draft
This should make it obvious whether content exists in production

3. Content insertion safeguards
When content is inserted/imported/admin-created:
- log which database it was written to
- log whether isPublished/status/visibility were set
- validate required production fields
- prevent silent “success” if write goes to the wrong source

4. Production visibility verification step
After any future content import or question generation:
- automatically verify production DB counts
- verify production API counts
- verify live frontend counts if feasible
- report mismatch immediately instead of pretending success

5. Better error handling
If the CAT exam cannot initialize:
- do not leave the user stuck on blank screen
- show a clear error message
- log enough diagnostic detail to identify the failing step

6. Centralize tier/slug constants
- centralize examTier, region, specialtyTrack, slugs, and route keys
- eliminate casing mismatches and duplicated constants
- reduce production-only filtering mistakes

PART 4: FIX THE CURRENT ISSUE FULLY

I want you to actually fix the current production issue now.

Do all of the following:
- make the CAT exam load on the published site
- ensure CAT questions exist in the real production data source
- ensure production API returns them
- ensure live UI renders them
- ensure the colour modal correctly transitions into the active exam
- ensure future question imports do not repeat this preview-only mistake

PART 5: REQUIRED VERIFICATION

Do not mark complete until you verify:

CAT exam flow
- published site opens CAT exam page
- colour modal appears
- clicking continue/start initializes session
- first question loads
- exam UI populates
- no blank state remains
- progress/session is saved correctly

Production data checks
- production DB has expected CAT records
- production API returns expected counts
- live frontend displays expected counts
- records are published and publicly visible as intended

Future-proofing checks
- new content insertion path targets the correct production source
- environment mismatch is identified and fixed
- diagnostics exist so this can be caught immediately next time

OUTPUT REQUIRED

When finished, tell me clearly:
1. exact root cause of the CAT exam not loading
2. exact root cause of the repeated preview-only database visibility issue
3. why preview worked but published failed
4. files changed
5. database changes or migrations run
6. env/secrets changes needed
7. deployment changes needed
8. safeguards added to prevent recurrence
9. confirmation that the CAT exam now works on the published site
10. confirmation that production content is no longer preview-only

IMPORTANT
Do not give me a partial fix.
Do not say it is fixed unless the published site is actually working.
Do not optimize for Replit preview.
Optimize for the live production site.
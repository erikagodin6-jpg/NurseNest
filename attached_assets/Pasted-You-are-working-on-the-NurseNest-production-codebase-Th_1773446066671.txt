You are working on the NurseNest production codebase.

This is a high-priority architecture and cost-control task.

Goal:
Remove all always-on AI behavior and convert all AI generation into manual, admin-triggered queued jobs only. I do NOT want any continuous AI agents, polling loops, background generators, auto-regeneration, or hidden recurring tasks. The system must only generate content when I explicitly trigger it from the admin dashboard.

Critical rules:
1. Production only. Do not use development database for generation, counts, or publishing. All generation, writes, status updates, and dashboards must point to the published/production database and published site environment only.
2. Do not create any always-on AI workers.
3. Do not create any cron jobs, polling loops, recursive retries, or background listeners that keep running indefinitely.
4. AI generation must only run when started manually by an admin user from the admin dashboard.
5. Every job must stop cleanly when complete.
6. Add clear budget controls so the system cannot silently burn money.
7. Add ROI and spend tracking in the admin dashboard.
8. Preserve existing content. Do not delete or overwrite published questions, flashcards, lessons, images, mock exams, or user purchases.
9. Add robust logging and visible admin status indicators.
10. Keep UI polished and consistent with the existing NurseNest admin style.

What I want built

PART 1 — REMOVE ALWAYS-ON AI
Audit the codebase and remove or disable anything that behaves like:
- always-running AI agents
- automatic generation loops
- scheduled content generation
- silent background generation
- auto-retries that keep re-running forever
- interval timers for AI jobs
- watchers that restart generation continuously
- any feature that generates content without explicit admin initiation

Search for and remove/refactor:
- setInterval / polling loops
- background tasks that do not terminate
- cron-style generation
- workers that auto-start on app boot
- recursive job runners with no hard stop
- “keep alive” generation patterns
- automatic “top up content” logic
- any AI pipeline that re-runs because content is below some threshold unless manually triggered

At the end of the task, generation should only happen from an admin button click or explicit admin action.

PART 2 — MANUAL ADMIN JOB QUEUE
Create a manual AI job queue system in the admin dashboard.

Add a new admin section called:
AI Jobs

In this section, create:
1. A job creation panel
2. A job queue table
3. A job history table
4. A spending summary card
5. A job limits/settings panel

Job creation panel requirements:
Allow admin to manually create a job with:
- content type:
  - exam questions
  - CAT exam questions
  - flashcards
  - lessons
  - blog posts
  - rationale image linking
  - lesson image linking
  - conversion jobs (example: CAT questions -> test bank / flashcards)
- tier:
  - RPN/LVN
  - RN/NCLEX
  - NP
  - allied health
  - emergency nursing
  - imaging
  - new grad
- specialty/topic
- exam/framework if relevant
- batch size
- model selection
- publish destination
- dry run toggle
- duplicate protection toggle
- maximum spend cap for this job
- “stop after this many items” limit

Buttons:
- Start Job
- Save Draft Job Config
- Run Test Batch
- Cancel Pending Job

Queue table columns:
- job id
- created by
- job type
- tier
- topic
- model
- batch size
- status
- items requested
- items completed
- items failed
- estimated cost
- actual cost
- started at
- completed at
- actions: view / pause / resume / cancel / duplicate config

Statuses:
- pending
- validating
- queued
- running
- paused
- completed
- failed
- cancelled
- completed with warnings

Behavior:
- Jobs run only after admin starts them
- Jobs process finite batches only
- Jobs terminate automatically when finished
- No job restarts itself unless admin explicitly resumes it
- Support multiple jobs in queue, but each must remain finite and visible
- Add safe concurrency controls so multiple jobs can run without corrupting data
- If concurrency is risky, limit concurrency and show this in UI
- Never let hidden background processes continue after completion

PART 3 — SMALL BATCH GENERATION ONLY
Enforce safe batch sizes.

Default limits:
- exam questions: default 25, max 50 per batch
- CAT questions: default 25, max 50 per batch
- flashcards: default 50, max 100 per batch
- lessons: default 5, max 10 per batch
- blog posts: default 3, max 5 per batch
- image linking/enrichment: default 10, max 20 per batch

Do not allow giant unbounded runs.
If admin requests a huge generation task, split it into finite queued batches with visible progress.

PART 4 — BUDGET AND SPEND PROTECTION
This is extremely important.

Build a spend protection system with:
1. per-job spend cap
2. daily AI spend cap
3. optional weekly spend cap
4. hard stop when cap is reached
5. warning banners before jobs start if limits may be exceeded

Add admin settings for:
- daily AI budget
- weekly AI budget
- per-job max budget
- alert threshold percentages (50%, 75%, 90%, 100%)

Behavior:
- before a job starts, estimate cost and show it
- while job runs, track actual estimated spend in real time
- if spend cap is reached, stop job cleanly
- if daily budget is reached, prevent new jobs from starting
- show a clear reason if job is blocked or stopped
- do not allow silent overspending

Add logs:
- budget block events
- cap reached events
- job stopped due to budget
- admin overrides if implemented

PART 5 — MODEL STRATEGY TO REDUCE COST
Implement sensible model defaults.

Default model strategy:
- bulk question generation: cheapest acceptable model
- bulk flashcards: cheapest acceptable model
- lesson drafting: mid-tier if needed
- refinement/editing: stronger model only when explicitly selected
- never default to an expensive model for bulk generation

Add a model selection dropdown with admin descriptions like:
- cheapest / bulk generation
- balanced / standard
- premium / high-cost

Set the default for large-scale generation to the lowest-cost suitable model.

PART 6 — DUPLICATE PROTECTION AND CACHING
Prevent accidental regeneration.

Before generating content:
- check whether equivalent content already exists in production
- compare by topic, title, framework, subtype, and normalized prompt signature
- where appropriate, hash the generation configuration and content signature
- skip duplicates automatically if duplicate protection is enabled
- show skip counts in the job report

Examples:
- do not regenerate the same topic batch if sufficient published content already exists unless I explicitly force regenerate
- do not create near-identical flashcards repeatedly
- do not keep re-linking the same images to the same lesson/rationale if already linked

Add admin options:
- skip duplicates
- allow update of incomplete items only
- force regenerate selected items only

PART 7 — ROI / FINANCIAL DASHBOARD
Add a new admin dashboard section called:
Business Health

I want it to track:
- total invested
- Replit spend total
- AI generation spend estimate
- manually added business expenses
- domain cost
- software/subscription costs
- ads/marketing costs
- contractor/freelancer costs
- total revenue
- subscription revenue
- one-time purchase revenue
- total customers
- active subscribers
- total orders
- refund total if available
- gross profit/loss
- break-even remaining
- average revenue per user if data exists
- lifetime value if possible
- revenue this month
- spend this month

Add a manual expense entry form so I can input:
- category
- vendor
- description
- amount
- currency
- date
- recurring yes/no

Add an exchange-rate aware currency field if possible, but base dashboard totals in CAD with optional USD display.

At minimum I want:
- total invested in CAD
- total revenue in CAD
- break-even remaining in CAD

If direct Replit invoice sync is not available, build the dashboard to support manual invoice entry/import and make the structure ready for automated imports later.

PART 8 — SUBSCRIBER / PURCHASE COUNTS
Add production dashboard metrics that automatically count:
- total subscribers
- active subscribers
- cancelled subscribers
- total purchases
- purchases by product
- purchases by tier
- total sales
- sales by date range
- free users vs paid users
- conversion counts if possible

These must read from the live production data only.

PART 9 — ADMIN VISIBILITY AND CONTROL
In the AI Jobs admin area, add:
- live progress bars
- current stage text
- estimated tokens or usage if available
- cost estimate
- logs panel
- error panel
- retry failed items only
- export job summary
- filter by job type / tier / status / date

I want to clearly see:
- what is running
- why it is running
- how much it is costing
- how much has completed
- whether it wrote to production
- whether it stopped cleanly

PART 10 — FAILSAFE RULES
Implement these safety rules:
- never write generated content to dev database
- never silently fall back to dev database
- if production DB env vars are missing, block generation and show error
- if publish target is invalid, block the job
- if estimated cost exceeds cap, require admin adjustment before start
- if API/model call fails repeatedly, fail the job cleanly after limited retries
- no infinite retries
- no infinite loops
- no silent auto-resume

PART 11 — PERFORMANCE / ARCHITECTURE
Use a finite job architecture.

Recommended behavior:
- admin creates job
- server validates config
- job stored in DB
- worker processes finite batch
- progress updates saved
- job exits at completion/failure/cancel
- nothing remains running unnecessarily afterward

If a queue library or background job tool already exists, use it only if it supports finite on-demand jobs and does not create always-on uncontrolled AI behavior.

PART 12 — UI / UX
Keep design polished and consistent with the current NurseNest admin UI:
- clean cards
- readable tables
- progress chips
- spend warning badges
- clear red/yellow/green status indicators
- mobile-friendly where practical

Use wording that is easy for me to understand:
- “Runs only when you start it”
- “Stops automatically when finished”
- “Blocked by budget cap”
- “Production database verified”
- “Duplicate content skipped”

PART 13 — IMPLEMENTATION DELIVERABLES
When complete, provide:
1. Summary of every file changed
2. What always-on AI logic was found and removed/refactored
3. Where the production DB enforcement was added
4. Where the budget caps were implemented
5. Where the admin AI Jobs UI was added
6. Where the Business Health / ROI dashboard was added
7. How duplicate protection works
8. Any migrations added
9. Any environment variables required
10. Any manual steps I need to do in admin after deploy

PART 14 — ACCEPTANCE CRITERIA
This task is only complete if all of the following are true:
- no AI generation runs automatically
- no always-on AI agent exists
- no hidden recurring generation exists
- all generation must be manually triggered from admin
- all jobs are finite and terminate
- spending caps are enforced
- production-only safeguards exist
- ROI dashboard exists
- subscriber/purchase counts exist
- duplicate protection exists
- admin can see progress and stop jobs
- system is safer and cheaper to operate

Important:
Do not give me a vague plan only. Make the actual code changes.
Audit the current implementation, refactor it, and wire everything into the live admin system.
Use the existing code patterns where appropriate, but prioritize reliability, production safety, and cost reduction.
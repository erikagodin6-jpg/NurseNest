Build a robust admin-only Translation Coverage Dashboard and Missing Translation Scanner for the entire NurseNest platform.

Goal:
I need a production-grade system that detects incomplete translations across all pages, lessons, blogs, flashcards, rationales, exams, simulators, anatomy pages, and marketing pages. The system must show me exactly what is untranslated, what is partially translated, what is still falling back to English, and which pages are safe to index in Google.

Core requirement:
This must work for both static UI strings and dynamic database-driven content.
Do not only scan frontend translation keys. Also scan actual rendered content and database records.

PLATFORM CONTEXT
NurseNest has:
- multilingual routes like /en/, /es/, /fr/, /ar/, /fil/, /ht/, /ja/, etc.
- lessons
- lesson categories
- blogs
- anatomy pages
- flashcards
- rationales
- question banks
- CAT exams
- simulators
- static marketing pages
- pricing pages
- navigation/footer
- user dashboard areas
- modals/forms/errors/empty states
- admin content creation tools

I need one unified translation auditing system across all of it.

PART 1 — BUILD THE TRANSLATION AUDIT ENGINE

1. Create a central translation audit service that scans every locale and every route.
2. The audit engine must inspect:
   - route-level metadata
   - page title
   - meta title
   - meta description
   - H1/H2/H3 text
   - body content
   - CTA text
   - breadcrumbs
   - nav and footer
   - card labels
   - button labels
   - quiz labels
   - flashcard front and back
   - rationale content
   - exam UI
   - simulator UI
   - lesson content
   - blog content
   - form labels
   - validation messages
   - toast messages
   - paywall copy
   - error pages
   - structured data text fields where applicable
3. The audit engine must inspect both:
   - frontend translation keys
   - actual rendered page content
4. For database content, inspect records for all supported locales.
5. Support scanning for these content types at minimum:
   - lessons
   - lesson sections
   - lesson summaries
   - blog posts
   - blog categories
   - flashcard decks
   - flashcard cards
   - rationale blocks
   - exam questions
   - answer explanations
   - simulator scenarios
   - anatomy modules
   - FAQ entries
   - product descriptions
   - pricing descriptions
6. Design the scanner so new content types can be added easily later.

PART 2 — WHAT THE SCANNER MUST DETECT

7. Detect untranslated content where a locale field is missing entirely.
8. Detect English fallback content shown on non-English routes.
9. Detect mixed-language content on the same page.
10. Detect placeholder strings such as:
   - TODO
   - translate me
   - missing translation
   - key names
   - raw i18n keys
11. Detect content fields where the localized value equals the English value exactly when it should not.
12. Detect route pages where the UI chrome is translated but the main body is still English.
13. Detect pages where metadata is untranslated even if visible content is translated.
14. Detect pages where slugs are localized incorrectly or still using fallback route patterns when a localized slug exists.
15. Detect pages where dynamic content is translated but common UI elements remain English.
16. Detect pages where the route locale and HTML lang attribute do not match.
17. Detect pages where locale-specific schema or JSON-LD is missing or still English.

PART 3 — TRANSLATION SCORING MODEL

18. Create a weighted translation completeness score per page and per content item.
19. Score categories separately:
   - UI chrome
   - metadata
   - headings
   - primary body
   - supporting content
   - forms/errors
   - structured data
20. Weight major body content more heavily than minor UI labels.
21. Example scoring idea:
   - metadata 10%
   - core headings 10%
   - primary body 40%
   - secondary body/supporting content 20%
   - UI chrome 10%
   - forms/errors 5%
   - structured data 5%
22. Produce:
   - overall translation %
   - content translation %
   - UI translation %
   - metadata translation %
23. Mark status using clear rules:
   - 100% = Fully Translated
   - 95–99% = Ready for Indexing
   - 70–94% = Partial Translation
   - below 70% = Draft Translation
24. Make thresholds configurable in admin settings.

PART 4 — INDEXING SAFETY RULES

25. Add logic so only pages marked Ready for Indexing or Fully Translated are eligible to be included in sitemap.
26. Pages below indexing threshold must automatically be:
   - removed from sitemap
   - marked noindex
   - flagged in admin
27. Add a per-page override toggle for admins, but show a clear warning before allowing manual override.
28. Prevent accidental indexing of half-translated pages.

PART 5 — ADMIN TRANSLATION COVERAGE DASHBOARD

29. Build a new admin-only menu item:
   Translation Coverage
30. On that page, show a dashboard with:
   - total supported locales
   - total scanned routes
   - total scanned content items
   - fully translated count
   - ready for indexing count
   - partial translation count
   - draft translation count
   - pages excluded from sitemap count
31. Add summary widgets by locale:
   - locale code
   - total pages
   - average translation %
   - pages ready for indexing
   - pages incomplete
32. Add summary widgets by content type:
   - lessons
   - blogs
   - flashcards
   - exams
   - simulator pages
   - anatomy
   - static pages
33. Add filters:
   - locale
   - content type
   - translation status
   - sitemap inclusion
   - noindex status
   - last updated date
   - only show pages with English fallback
   - only show metadata issues
   - only show mixed-language pages
34. Add search by:
   - URL
   - title
   - slug
   - content ID
35. Add sortable columns:
   - URL
   - locale
   - content type
   - translation %
   - status
   - sitemap status
   - noindex status
   - last updated
   - untranslated field count

PART 6 — PAGE DETAIL INSPECTOR

36. Clicking any row should open a detail panel or detail page.
37. The detail view must show:
   - full URL
   - locale
   - content type
   - source language
   - translation %
   - status
   - sitemap inclusion
   - robots/noindex status
   - last scan date
   - last content update date
38. Show a field-by-field audit:
   - field name
   - English source value
   - localized value
   - issue type
   - status
39. Issue types should include:
   - missing
   - fallback English
   - mixed language
   - duplicate source text
   - placeholder text
   - key not resolved
40. Highlight exactly which parts failed.
41. Add a rendered preview comparison:
   - source language preview
   - target locale preview
42. Add a quick jump link to edit that content item in admin.

PART 7 — RENDERED PAGE SCANNING

43. Add a rendered output scanner, not just database comparison.
44. For each page, fetch the rendered route in each locale and inspect visible text blocks.
45. Compare rendered output against locale expectations.
46. Detect hydration problems where the server and client render different language content.
47. Detect if canonical, lang attribute, or hreflang do not match the rendered locale.
48. Log whether the problem is:
   - source data issue
   - frontend fallback issue
   - routing issue
   - hydration issue
   - missing translation key
   - missing localized database content

PART 8 — BULK FIX SUPPORT

49. Add bulk actions from the dashboard:
   - mark as draft translation
   - mark as ready after review
   - remove from sitemap
   - apply noindex
   - rescan selected items
50. Add export options:
   - CSV export
   - JSON export
51. Export must include:
   - URL
   - locale
   - content type
   - translation %
   - issue summary
   - untranslated fields
   - indexing status

PART 9 — AUTOMATED SCAN SCHEDULING

52. Run scans automatically:
   - after content publish
   - after translation update
   - after build/deploy
53. Add a nightly full scan job.
54. Add a manual “Run Full Translation Audit” button in admin.
55. Show scan progress and last completed timestamp.
56. Store audit results so the dashboard loads quickly.
57. Re-scan only changed records when possible for efficiency.

PART 10 — TRANSLATION DATA MODEL

58. Create a clean translation audit schema/table to store:
   - audit id
   - content id
   - content type
   - URL
   - locale
   - source language
   - translation %
   - status
   - issue count
   - issue breakdown
   - sitemap eligibility
   - noindex flag
   - last scanned at
   - last content updated at
59. Create a child table or structured JSON for field-level issues.
60. Keep the schema flexible enough for future locales and content types.

PART 11 — UX REQUIREMENTS

61. Match the existing NurseNest admin aesthetic.
62. Make it clean, premium, and easy to scan visually.
63. Use status pills and subtle color coding:
   - Fully Translated
   - Ready for Indexing
   - Partial Translation
   - Draft Translation
64. Add progress bars for translation completion.
65. Make the table responsive and usable on smaller screens.
66. Add pagination and virtualized rendering if needed for performance.

PART 12 — QUALITY GUARDS

67. Do not rely on naive string equality alone.
68. Use heuristics to detect language mismatch more intelligently.
69. Avoid false positives for medical terminology, brand names, abbreviations, or universal terms that may remain the same across languages.
70. Keep brand names like NurseNest unchanged unless explicitly intended.
71. Do not mark medical acronyms as untranslated if they are commonly used cross-language.
72. Create an allowlist for:
   - brand names
   - medical acronyms
   - accepted untranslated terms
73. Create an ignore list for fields that are intentionally locale-neutral.

PART 13 — DEVELOPER DELIVERABLES

74. Deliver:
   - translation audit service
   - rendered page scanner
   - admin Translation Coverage dashboard
   - page detail inspector
   - field-level issue tracking
   - sitemap/noindex integration
   - scheduled scan support
   - export support
   - documentation for how scoring works
75. Show me exactly which files were added/changed.
76. Add comments in code for the translation scoring logic and indexing eligibility rules.
77. Do not break the existing routing, paywall, analytics, or admin editor flows.

PART 14 — FINAL OUTPUT I WANT

78. After implementation, provide:
   - a summary of how the scanner works
   - list of audited content types
   - list of locales supported
   - sample audit results
   - examples of flagged untranslated items
   - examples of pages auto-removed from sitemap
   - examples of pages now safe for indexing
79. Also provide a short admin usage guide explaining:
   - how to run a scan
   - how to filter issues
   - how to decide when a page is ready for indexing
   - how to use the export
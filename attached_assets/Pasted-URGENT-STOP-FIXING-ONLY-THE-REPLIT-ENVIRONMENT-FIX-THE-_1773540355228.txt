URGENT: STOP FIXING ONLY THE REPLIT ENVIRONMENT. FIX THE ACTUAL PUBLISHED FRONTEND.

Problem summary:
The production database is NOT the primary issue. The published frontend is failing to render the available lessons correctly.

Evidence already confirmed:
- /api/lessons/meta returns lesson data
- local dev API works
- production API works and can return ~3453 lessons for admin
- published site still shows only 5 lessons or none
- this is production-specific
- there is a SchemaSync/runtime issue in production
- current fixes appear to work only inside Replit/dev, not on the actual published website

Goal:
Find and fix the PRODUCTION FRONTEND rendering issue so the published site displays lessons correctly across tiers.

Do NOT:
- do not focus only on the database
- do not regenerate lesson content
- do not report success based only on Replit preview/dev
- do not stop after API tests pass
- do not assume backend success means published frontend success

Required investigation steps:

1. Trace the actual published browser request flow for lessons
- identify exactly which endpoint the published site calls
- identify whether it calls /api/lessons/meta, /api/content/lessons, or another route
- compare dev vs published endpoint behavior
- verify whether the published frontend is hitting the correct origin and route

2. Inspect the actual published frontend render path
- determine which state variable drives visible lesson cards
- determine whether the page is rendering from:
  - dbLessons
  - static systems arrays
  - fallback arrays
  - free-tier lesson lists
  - filtered memoized values
- identify where real API data is discarded, replaced, or filtered out

3. Audit all frontend guard conditions
Check for production-only bugs involving:
- isFreeUser
- admin role detection
- subscription tier detection
- locale/language filter
- published flag
- category/system filter
- route params
- hydration guard
- client-only render guard
- empty array fallback logic
- “show free lessons only” logic
- auth token/cookie presence on published

4. Inspect auth/session behavior on the published site
- verify whether browser cookies/tokens are sent with lesson requests on production
- verify whether SSR/client hydration loses user auth state
- verify whether admin users on published are being treated as free users
- verify whether same-origin/cross-origin cookie settings differ between dev and production

5. Inspect production runtime errors
- check browser console on the published lessons pages
- check network tab for all lesson-related requests
- check built asset errors, hydration errors, JS exceptions, and schema sync errors
- identify whether data loads successfully but fails during mapping/render

6. Compare the actual published bundle against current source
- verify the newest frontend code is truly deployed
- verify published is not serving an older cached bundle
- verify env vars used during build are correct
- verify API base URL and public environment variables in production build
- invalidate stale asset/caching issues if needed

7. Add temporary production-safe diagnostics
Add admin-only or console diagnostics showing:
- current route
- detected user tier
- detected auth state
- locale
- raw lesson response count
- count after each filter step
- final rendered lesson count
- which data source is being used (dbLessons vs static array)
- whether fallback mode activated

8. Fix the actual render logic
- ensure published pages render from the live DB/API results when available
- prevent static fallback arrays from overriding valid DB results
- fix any filter chain that incorrectly collapses lesson lists to 0 or 5
- ensure admin sees full content
- ensure paid tiers see correct lessons
- ensure free users see only intended free lessons

9. Verify on the actual published site
Must test on published, not Replit preview:
- RPN lessons page
- RN lessons page
- NP lessons page
- allied pages if they use the same lesson pipeline
- admin account
- logged-out/free user
- mobile and desktop

Deliverables required:
- exact root cause
- exact files changed
- exact failing render condition or endpoint mismatch
- before/after lesson counts on published
- proof that published now renders correctly
- confirmation that this was verified on the live published website, not just in dev

Priority:
Fix the published frontend rendering path first. Backend/database work is secondary unless a production endpoint is proven broken.

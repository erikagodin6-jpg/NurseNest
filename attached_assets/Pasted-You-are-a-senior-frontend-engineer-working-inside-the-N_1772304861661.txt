You are a senior frontend engineer working inside the NurseNest codebase.

Add a “Store” navigation link to the primary top navigation bar and to any tier-based navigation menus.

This must integrate cleanly with:
- Existing routing
- Multilingual system (i18n)
- SSR if applicable
- Mobile navigation
- Active state styling
- Brand design

Do NOT break existing navigation logic.

==================================================
REQUIREMENTS
==================================================

1) TOP NAVIGATION BAR

Add a new nav item labeled:

English: Store  
French: Boutique  
(Use translation key system if present.)

Route:
- /shop (English)
- /fr/shop (French)
- Follow existing locale routing pattern

Placement:
- After “Flashcards” and before “Pricing” (if present)
- Maintain consistent spacing and styling

Active state:
- When user is on /shop or any /shop/* page, highlight the Store link as active.

==================================================
2) TIER NAVIGATION (RPN / RN / NP)
==================================================

If tier-based nav menus exist, add “Store” there as well.

If tier-specific routing exists, the Store link should NOT change by tier.
It should always point to the main marketplace route for that locale.

==================================================
3) MOBILE NAVIGATION
==================================================

Ensure Store appears in:
- Hamburger menu
- Collapsible nav
- Any mobile drawer

Maintain tap targets and spacing consistency.

==================================================
4) FOOTER (IF APPLICABLE)
==================================================

Add Store to footer quick links section.

==================================================
5) SEO
==================================================

Ensure:
- Proper anchor tag (not just button)
- Crawlable link
- No "nofollow"
- Included in sitemap if dynamic

==================================================
6) TRACKING
==================================================

Add event tracking:
- nav_store_click
- Include user tier (if logged in)
- Include locale

==================================================
7) VISUAL CONSISTENCY
==================================================

- Match typography
- Match hover state
- Match border radius
- No new colors
- No layout shifts

==================================================
DELIVERABLES
==================================================

1) Files modified
2) Nav component updated
3) Translation keys added
4) Confirmation Store appears on:
   - Desktop nav
   - Mobile nav
   - Tier nav
   - Footer

Implement now without refactoring unrelated components.
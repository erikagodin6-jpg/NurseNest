You are working on the live NurseNest production system.

This is a CRITICAL production hardening and permanent stabilization task.

The system has experienced:
- repeated memory exhaustion (≈98% usage at ~192MB)
- continuous emergency mode activation
- exam_open_flow failures ("operation aborted")
- invalid published content (zero valid items)
- alert storms (emails every few seconds)
- misclassified alerts ("Payment Failed" used incorrectly)
- unstable exam and content experience for users

Previous fixes have FAILED because they addressed symptoms, not root causes.

Your task is to implement a PERMANENT, PRODUCTION-GRADE FIX across architecture, memory, exam delivery, content integrity, and alerting.

==================================================
PRIMARY GOAL
==================================================

Make the system:
- stable under load
- memory-safe
- self-protecting
- resilient to failure
- incapable of publishing invalid content
- incapable of spamming alerts
- incapable of crashing core user flows

This is NOT a patch. This is a system redesign and hardening.

==================================================
SECTION 1 — INFRASTRUCTURE CORRECTION
==================================================

Fix the runtime environment:

1. Treat ~192MB as invalid for production
2. Define and implement a safe baseline (minimum 2GB, recommend 4GB+)
3. Ensure deployment supports scaling or multiple instances if needed
4. Restart system cleanly after changes

Then:

Separate workloads:
- Web app (lightweight, user-facing)
- Background workers (heavy jobs)

MOVE OUT of the web process:
- AI generation
- bulk question generation
- exam construction
- PDF/image generation
- content audits
- scheduled jobs

The web server must ONLY handle lightweight requests.

==================================================
SECTION 2 — MEMORY SAFETY SYSTEM
==================================================

Audit and eliminate all memory risks:

Identify and fix:
- large exam payloads loaded at once
- rationales/metadata loaded unnecessarily
- unbounded caches
- global arrays/maps growing over time
- retry loops
- interval leaks
- retained request objects
- oversized DB queries
- duplicate object creation
- frontend refetch loops
- synthetic test overloading

Implement:

- pagination / chunking everywhere
- lazy loading for heavy data (rationales, images)
- cache size limits + eviction policies
- strict request size limits
- concurrency limits for heavy endpoints
- request timeouts
- memory thresholds (70/80/90%)

At thresholds:
- reduce load
- block heavy routes
- switch to fallback mode

Add instrumentation:
- RSS
- heap used / total
- route-level memory usage
- payload sizes
- concurrent jobs
- request counts

==================================================
SECTION 3 — EXAM SYSTEM REDESIGN
==================================================

Completely harden exam delivery.

Rules:
- NEVER load full exam datasets at once
- NEVER send all rationales upfront
- NEVER allow exam route to crash the app

Implement:

- exam shell first (metadata only)
- load questions in chunks
- load rationales only after answering
- limit questions per request
- store attempt state in DB (NOT memory)
- prevent duplicate data expansion
- cap concurrent exam loads

Add:

- circuit breaker on exam routes
- timeout handling
- fallback exam experience if full load fails
- structured logging with request IDs

Fix exam_open_flow so it NEVER hard fails.

==================================================
SECTION 4 — CONTENT INTEGRITY SYSTEM
==================================================

Fix publishing permanently.

NO content can be published if invalid.

Implement:

Pre-publish validation:
- must have valid item count > 0
- required fields present
- valid answers exist
- rationales valid (if required)
- tier + language correct

If validation fails:
- BLOCK publish

For existing broken content:
- auto-unpublish
- quarantine
- remove from user-facing UI

Add admin visibility:
- show validation failures
- allow regeneration or repair

Investigate and fix why these were published:
- Restless Leg Syndrome Rpn
- Macular Degeneration Rpn
- Gestational Diabetes Rpn
- Bell's Palsy Rpn

==================================================
SECTION 5 — ALERT SYSTEM REBUILD
==================================================

Fix alert correctness and spam.

Current issues:
- alerts mislabeled as "Payment Failed"
- alerts firing every few seconds

Implement:

Correct alert types:
- memory_critical
- service_down
- synthetic_test_failure
- content_integrity_failure
- payment_failure (ONLY for actual payments)
- emergency_mode

Fix templates:
- titles must match alert type
- content must be accurate

Add deduplication:
- group repeated alerts into ONE incident
- include count, first_seen, last_seen

Add cooldown:
- prevent same alert firing repeatedly within seconds

Alerts must be:
- accurate
- grouped
- non-spammy
- actionable

==================================================
SECTION 6 — RESILIENCE / FAILSAFE SYSTEM
==================================================

The system must NEVER return a dead-end.

Implement:

If memory high:
- block heavy features
- return lightweight responses

If exam fails:
- show fallback exam or retry option

If content invalid:
- hide it automatically

If backend unstable:
- degrade gracefully (read-only or reduced mode)

Add:
- frontend error boundaries
- backend fallbacks
- circuit breakers
- safe retry logic (no infinite loops)

Emergency mode must:
- actively reduce load
- not just log alerts

==================================================
SECTION 7 — OBSERVABILITY
==================================================

Add full monitoring:

Track:
- memory trends over time
- route-level failures
- exam_open_flow success rate
- payload sizes
- alert frequency
- content validation failures

Make it possible to answer:
- what is causing memory spikes
- what route is failing
- whether system recovers after load

==================================================
SECTION 8 — DELIVERABLES
==================================================

Return:

1. Root causes for:
   - memory failure
   - emergency mode loop
   - exam_open_flow failure
   - zero-valid-item content
   - alert spam/mislabeling

2. Exact code changes

3. Architecture changes

4. Memory fixes

5. Exam system changes

6. Content validation fixes

7. Alerting fixes

8. Verification checklist proving:
   - memory stable
   - no alert spam
   - exam loads reliably
   - no invalid content published
   - alerts correctly labeled
   - system degrades gracefully

==================================================
NON-NEGOTIABLE RULES
==================================================

- Do NOT give superficial fixes
- Do NOT stop at "increase memory"
- Do NOT allow heavy routes to crash system
- Do NOT allow invalid content to be published
- Do NOT allow alert spam
- Do NOT allow misleading alerts
- PRIORITIZE reliability over features

Start with:
1. memory stabilization + alert suppression
2. exam route hardening
3. content validation enforcement
4. alert system correction
5. full system resilience
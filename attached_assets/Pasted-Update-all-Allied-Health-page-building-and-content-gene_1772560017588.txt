Update all Allied Health page-building and content-generation prompts to explicitly reuse overlapping content from the Nursing site.

GOAL:
If a lesson, concept, module, interactive, UI component, or logic already exists on the Nursing site, the AI must copy/port/reuse it for Allied pages instead of recreating it, then only add allied-specific differences.

NON-NEGOTIABLES:
- Preserve the exact NurseNest UI layout, styling, components, theme tokens, and navigation patterns from the Nursing site.
- Do not rewrite existing nursing content unless an allied-specific adaptation is required.
- Do not duplicate identical content into two separate sources of truth. Prefer shared modules, shared lesson templates, and shared data models.
- Server-side paywalls remain authoritative.
- Readiness exams remain free for all authenticated users.

STEP 1 — OVERLAP INVENTORY (AUTO-DETECT + CONFIRM)
1) Programmatically scan the Nursing site content repository and database for:
   - Lesson categories/modules
   - Shared clinical topics (infection control, shock, vitals, ABGs basics, electrolytes, documentation, safety, ethics)
   - Existing question templates and item types
   - Existing UI components (lesson pages, quizzes, mock exam flows, flashcards, dashboards)
   - Existing interactive activities (games, case sims)
2) Build an "OverlapMap" object:
   - key: normalized topic slug
   - value: { nursingSourceId, alliedTargetIds[], reuseMode }
Where reuseMode is one of:
   - SHARED_AS_IS (identical content)
   - SHARED_WITH_ADAPTATION (same core, allied additions)
   - ALLIED_ONLY (new content)

STEP 2 — SHARED CONTENT ARCHITECTURE
Create shared packages/modules so nursing + allied reuse the same implementation:
- shared/lessons (templates, renderers, schema)
- shared/questionTypes (SBA, SATA, case-based, calculations, etc.)
- shared/blueprints (domain weighting, readiness configs)
- shared/ui (cards, layouts, nav, CTA blocks)
- shared/paywall (gating rules + banners)
- shared/seo (meta generator, schema generator, hreflang, internal linking helpers)

STEP 3 — CONTENT REUSE RULES
For any Allied lesson page or exam content:
1) If topic exists on Nursing site and is relevant:
   - Clone reference to the shared lesson module (not copy-paste text).
   - Keep same title structure and layout blocks.
   - Add a short allied-specific "Scope Notes" section only if needed.
2) If topic partially overlaps:
   - Start from the nursing lesson (shared base).
   - Add allied-specific sections:
     - tools/equipment
     - scope-of-practice differences
     - workflow differences
     - exam blueprint differences
3) If topic is allied-only:
   - Create new module in shared/lessons if it will be reused by multiple allied careers.
   - Otherwise create it under allied-specific lessons.

STEP 4 — UI REUSE
All Allied pages must use:
- same layout components as Nursing pages
- same lesson card components
- same question bank UI
- same flashcard UI
- same mock exam UI (including Real Exam Mode patterns)

Only allow allied-specific differences via config flags:
- careerSlug
- blueprint
- domainWeights
- questionTypeAvailability
- contentLibraryIds

STEP 5 — MOCK EXAMS + TEST BANK REUSE
- Reuse Nursing question rendering, scoring, review flows, and report views.
- For allied careers, only swap:
  - blueprint/domain weights
  - question pools
  - pass criteria config
- Readiness exams must be available for all authenticated users across both Nursing and Allied.
- Ensure readiness exam generation can pull overlapping nursing questions when clinically appropriate, but must be tagged and filtered to remain within allied scope.

STEP 6 — SEO REUSE
- Reuse the nursing SEO meta/schema generator.
- Allied pages must automatically generate:
  - title/meta
  - canonical
  - breadcrumb schema
  - FAQ schema
  - internal link blocks that include links to overlapping nursing content where appropriate (cross-silo linking), but preserve canonical per page.

STEP 7 — IMPLEMENTATION OUTPUT
Return ONLY:
1) Files changed list
2) Unified diffs
3) Manual test checklist
4) Automated test checklist

NO narration.

START NOW by:
- Finding the existing nursing lesson templates, mock exam flows, flashcard components, paywall middleware, and SEO helpers
- Creating the shared module structure
- Wiring allied routes to reuse the shared modules
- Adding OverlapMap detection and config-based reuse
- Updating the allied page generator prompt templates to enforce reuse-first behavior
Debug and fix a production data mismatch where CAT exam questions appear correctly in Replit preview/dev, but the published site shows 0 questions.

Do not guess. Trace the issue end to end and identify the exact root cause.

Main problem:
- In Replit preview/dev, the CAT exam question bank displays questions correctly
- On the published/deployed site, the CAT exam page shows zero questions
- I need the published site to load the same CAT questions correctly

Your job:
1. trace the full data flow from frontend to backend to database
2. compare preview vs published behavior
3. identify the exact mismatch
4. fix it
5. tell me exactly what changed

INVESTIGATION CHECKLIST

A. TRACE THE FRONTEND REQUEST
- Find the exact page/component used for the CAT exam question count and question loading
- Identify the API route, loader, or fetch function used on preview and published
- Add temporary logs to verify:
  - route being called
  - request params
  - exam type slug
  - category
  - filters
  - returned count
- Confirm whether the published site is hitting the same endpoint as preview

B. TRACE THE BACKEND ENDPOINT
- Find the endpoint/service/controller that returns CAT questions
- Log:
  - incoming request params
  - parsed examType
  - parsed slug
  - publish filters
  - role/tier filters
  - visibility filters
  - final query
  - question count returned
- Check whether production is accidentally filtering out valid CAT questions due to:
  - isPublished
  - status
  - visible
  - archived
  - deleted
  - premium/free flags
  - category mismatch
  - examType mismatch
  - language filter
  - role/tier mismatch
  - product type mismatch
  - storeReady flag

C. VERIFY DATABASE SOURCE
- Determine exactly which database preview uses
- Determine exactly which database published production uses
- Check whether preview and published are using different DATABASE_URL or other storage config
- Confirm whether the CAT questions actually exist in production database
- Confirm whether they exist in the same table/collection expected by the live query
- Compare row counts in preview vs production for:
  - all questions
  - CAT questions only
  - published CAT questions only
  - CAT questions for the relevant exam/category slug

D. CHECK ENVIRONMENT VARIABLES AND SECRETS
- Compare preview/dev environment variables against deployed/published environment variables
- Especially inspect:
  - DATABASE_URL
  - API base URL
  - PUBLIC_API_URL
  - exam/content-related feature flags
  - auth/session config
  - deployment secrets
- Identify any missing or different values in production
- Fix any mismatches

E. CHECK FOR MOCK OR LOCAL-ONLY DATA
- Determine whether preview is showing:
  - mock data
  - local seed data
  - temporary frontend state
  - admin-cached content
  - file-based JSON instead of DB-backed data
- If so, remove that mismatch and make preview and published both use the same real persisted data source

F. CHECK ADMIN IMPORT / SEED FLOW
- Find how CAT questions were originally added
- Determine whether they were:
  - inserted only in dev
  - stored only locally
  - imported into a non-production database
  - missing a publish flag
  - missing the proper examType/category key
- If needed, create a safe production seed or migration to insert/update the missing CAT questions in production

G. CHECK ROUTE / SLUG / KEY MISMATCHES
- Confirm the CAT exam page slug matches the actual stored exam key
- Check for mismatches such as:
  - cat vs CAT
  - pharmtech-cat vs pharmacy-tech-cat
  - questionBankType mismatch
  - lesson category slug mismatch
  - uppercase/lowercase mismatch
  - trailing spaces
  - different enum values in dev vs prod
- Normalize these values if needed

H. CHECK DEPLOYMENT / BUILD ISSUES
- Confirm production is actually running the latest code
- Verify whether the site was redeployed after adding/importing the CAT questions
- Check for stale static builds or cached API responses
- Invalidate stale cache if needed
- Ensure the latest backend and frontend code are deployed

I. CHECK AUTH / PERMISSION FILTERS
- Confirm that live users are allowed to access these questions
- Check whether production is returning 0 because of:
  - subscription gate
  - user tier filter
  - auth session missing
  - published/public visibility logic
  - admin-only visibility
- Ensure the public or authorized user flow returns the expected CAT question count

J. ADD TEMPORARY DEBUGGING TOOLS
Create temporary debugging output for the CAT question pipeline:
- frontend logs for request payload
- backend logs for query inputs
- backend logs for returned rows
- debug helper that prints:
  - total CAT questions in DB
  - total CAT questions after publish filter
  - total CAT questions after visibility filter
  - total CAT questions after user/tier filter
  - final returned count
This should make it obvious where the questions are being lost

K. FIX THE ISSUE
Once root cause is found:
- correct the production data source or filters
- fix route/query logic
- fix env mismatches
- fix publish flags/status fields
- reseed or migrate production if needed
- redeploy if needed
- confirm live published site now shows the proper CAT question count

L. MAKE THE FIX ROBUST
Prevent this from happening again:
- centralize examType/category constants
- validate required environment variables at startup
- add better error logging when question count is zero unexpectedly
- add admin/dev diagnostics to compare question totals by exam type
- avoid silent failures that just return empty arrays without explanation

OUTPUT REQUIRED
When finished, show me:
1. exact root cause
2. why preview worked
3. why published failed
4. files changed
5. database changes or migrations run
6. env/secrets changes needed
7. whether I need to redeploy
8. whether I need to reseed/import production data
9. what permanent safeguards were added

IMPORTANT
- Do not stop at “I think it is the database”
- Actually trace and verify the full data path
- Do not patch blindly
- Prove the fix by confirming the published site now returns the correct CAT question count
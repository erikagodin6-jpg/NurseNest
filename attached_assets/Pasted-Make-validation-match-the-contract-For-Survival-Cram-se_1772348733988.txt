Make validation match the contract.
For Survival/Cram sections, set minimums to:
blocks >= 8
chars >= 800
substantive >= 6
Then your repair prompt requirements and your validator are aligned.
If you need to be slightly looser on client, that’s fine — but the server MUST enforce the contract or you’ll keep getting thin pages.
2) Your “generic title” detection is wrong (it will match almost everything)
This regex:
/^(survival|cram|study|exam|review|nursing|nclex|rex|guide|prep)/i
If the title starts with “Nursing …” (like yours), it flags as generic — fine.
But it also flags a lot of non-generic titles you might use (anything starting with “Exam …”, “Study …”, etc.). That’s okay if you always want the inventory anchors, but then…
Bigger issue
You defined:
const HIGH_YIELD_INVENTORY = `Use these high-yield conditions as anchors...`;
…but from what you pasted, you’re not actually showing it being injected into the section prompt. If you’re not explicitly appending HIGH_YIELD_INVENTORY into every section prompt in generic-title mode, the model still has nothing to write about and will keep outputting headings.
The inventory string must be literal and concrete (HF, ACS, COPD vs asthma, pneumonia, sepsis, hypo/hyperK, etc.), and it must be included in every section prompt.
The 3 changes that will actually stop “no information” output
Change A — Align validator thresholds to match your “CRITICAL RULES”
In validateSection (server):
blocks >= 8
chars >= 800
substantive >= 6
and include failure reasons like:
insufficient_blocks
insufficient_substantive_blocks
insufficient_chars
title_only (if all blocks are heading/divider)
Change B — Expand substantiveKinds beyond your current list
Right now you count substantive as only:
bullets, table, callout, paragraph
But your renderer likely uses other kinds like:
checklist
steps
flowchart
decisionTree
qa
case
comparisonGrid
chart
If those aren’t counted as substantive, you’ll falsely fail good output and over-repair, or your model will avoid those types.
So add them to substantiveKinds on both server and client.
Change C — The inventory must be real, and always included for generic titles
Make HIGH_YIELD_INVENTORY explicit and injected:
Example content:
Heart failure (acute decomp)
ACS/MI recognition
COPD exacerbation vs asthma
Pneumonia
Sepsis recognition
Hypo/hyperkalemia
Hypo/hyperglycemia + DKA/HHS recognition
Stroke recognition
AKI vs CKD
GI bleed
Fluid deficit vs overload
Anaphylaxis
Dysrhythmias (AF/brady/tachy)
UTI→sepsis
And then in the section prompt builder:
If isGenericTitle, append HIGH_YIELD_INVENTORY and tell it: “Every section must reference at least 8 of these anchors.”
Otherwise you’ll keep getting “Pathophysiology” with nothing.
The exact Replit AI prompt for the last-mile fix (copy/paste)
No narration. Output a unified diff for server/routes.ts and client/src/pages/product-builder.tsx only.

Make these changes:

1) Align server validateSection thresholds to the content contract:
   - Require blocks >= 8, chars >= 800, substantive >= 6 for survival/cram sections.
   - Add a specific failure reason when a section is title-only (all blocks are heading/divider or <10 chars).

2) Expand substantive block kinds on BOTH server and client:
   Include at least: bullets, table, callout, paragraph, checklist, steps, flowchart, decisiontree, case, qa, comparisongrid, algorithm.
   Ensure kind/type normalization is consistent.

3) Ensure HIGH_YIELD_INVENTORY is a real, explicit list of anchor conditions (not a placeholder string)
   and is appended into every section prompt whenever isGenericTitle is true.
   Add rule: each generated section must reference at least 8 anchor conditions across its blocks.

Return the diff and a 3-step test checklist.
You are working in my NurseNest codebase (Vite + React + Express). I already have Generator V2 that can create chunked generation jobs and save questions. I need a new system that can automatically scale each tier’s question bank to at least 4,000 questions.

NON-NEGOTIABLES
- Do NOT modify existing Generator V2 generation logic or prompts. Treat it as a black box.
- You may add new server modules, DB tables (or JSON storage), admin pages, and endpoints.
- Must support resume/retry. Must be safe with caps.
- Must deduplicate aggressively and run QA checks.
- Must be able to run in batches (chunked).

DEFINITIONS / GOAL
I need to reach at least:
- 4,000 questions for RPN tier (REx-PN Canada focus)
- 4,000 questions for RN tier (NCLEX-RN focus)
- 4,000 questions for NP tier (NP exams / advanced practice focus)
(If examTarget already exists in the system, keep it; otherwise store examTarget on each question.)

IMPLEMENT: BULK BANK BUILDER
Create a server-side “Bulk Builder” that can be run manually from an admin UI and can also run on a schedule.

It must:
1) Accept a target definition:
   - tierTarget: "rpn" | "rn" | "np"
   - examTarget: "rex-pn" | "nclex-pn" | "nclex-rn" | "np"
   - region: "CA" | "US" | "BOTH"
   - goalCount: number (default 4000)
   - batchSize: number (default 40)
   - chunkSize: number (default 10-15)
   - difficulty: "mixed" | "moderate" | "hard" | "very_challenging"
   - topicRotation: string[] (many short topics)
   - blueprintWeights: domain weights (default balanced)
   - typeWeights: MCQ/SATA/PRIORITY/DELEGATION (default 60/20/10/10)
2) Determine current inventory count for that bank (tier + exam + region).
3) Loop until goal met:
   - choose next topic from rotation
   - create generation job via existing endpoint:
     POST /api/generator-v2/generations with {template:"question_pack", exam, region, targetCount: batchSize, chunkSize, topic, difficulty, tier}
   - run it: POST /api/generator-v2/generations/:id/run
   - poll /status until complete or timeout (e.g., 30 minutes)
   - fetch all questions for that generation (paginate)
   - run QA checks (see below)
   - deduplicate against the entire bank using hashing:
       - normalizedStemHash
       - normalizedScenarioHash
     If duplicate: discard.
   - If after dedupe acceptedCount < 80% of batchSize:
       - automatically run a “top-up” generation for the missing count (same topic or next topic)
       - stop after 2 top-up attempts and mark batch partially successful if still short.
   - Save accepted questions to the bank and mark them with tierTarget + examTarget + region + topic + blueprint category + type + difficulty.
   - Write a run log entry.

QUALITY ASSURANCE RULES (SERVER-SIDE)
Reject a question if:
- required fields missing
- MCQ/PRIORITY/DELEGATION options != 4
- SATA options not in 6-8 range
- SATA correct answers not in 2-5 range
- uses “all of the above” or “none of the above”
- rationaleCorrect or clinicalPearl is empty or too short (e.g., < 140 chars)
- duplicates detected by stem hash or scenario hash

DATA MODEL
Add persistent models/tables (or JSON if no DB):
A) BulkBuildConfig
- id
- name
- tierTarget, examTarget, region
- goalCount (default 4000)
- batchSize, chunkSize, difficulty
- topicRotation (array)
- enabled (bool)
- scheduleTime (HH:MM) timezone America/Toronto
- caps: maxDailyQuestions, maxMonthlyQuestions, maxFailuresBeforeDisable

B) BulkBuildRunLog
- id
- configId
- startedAt finishedAt
- status "success" | "partial" | "failed" | "skipped"
- generationIds[]
- createdCount
- acceptedCount
- duplicateCount
- rejectedCount
- error

ADMIN ENDPOINTS
- GET /api/admin/bulk-builder/configs
- POST /api/admin/bulk-builder/configs
- PUT /api/admin/bulk-builder/configs/:id
- POST /api/admin/bulk-builder/run-now/:id
- POST /api/admin/bulk-builder/stop/:id
- GET /api/admin/bulk-builder/runs?configId=...&limit=100

ADMIN UI PAGE
Create /admin/bulk-builder:
- list configs
- create/edit config form (goalCount, batchSize, chunkSize, difficulty, topicRotation)
- show current inventory counts per bank
- Run Now button
- show runs log with accepted/duplicates/rejected
- Emergency Stop

SCHEDULING
Implement optional daily scheduling:
- If enabled, run at scheduleTime in America/Toronto.
- Ensure idempotency: do not run more than once per day per config unless manual “Run Now”.

IMPORTANT
- Do not break existing generator flows
- Use the existing admin auth pattern (adminFetch style)
- Add clear logs so I can see progress toward 4000
- Make it resumable after server restarts

DELIVERABLES
- server bulk builder module
- admin endpoints
- admin UI page
- models/storage
- dedupe + QA gates
- scheduling
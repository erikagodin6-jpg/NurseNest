Stop. Your plan is inconsistent.

You stated free lessons are loaded from client/src/data/lessons/*.ts and bypass the DB/API. If that is true, adding title_i18n/content_i18n columns and changing backend endpoints will NOT translate what users see.

Your task:
1) Confirm with code references whether lesson pages/lists render from:
   A) TypeScript imports (client/src/data/lessons/*.ts), OR
   B) API calls returning DB content
Provide the exact files and lines for where lesson title/body are sourced and rendered.

2) Implement the correct fix depending on the source:

IF (A) TypeScript imports:
- Implement localization in the lesson data layer:
  - Convert lesson fields to i18n objects (title_i18n/body_i18n) OR store per-language modules.
  - Add a helper getLocalized(value, lang, fallback="en").
  - Update all lesson rendering + activities to use getLocalized with current language from i18n.
  - Ensure language switch triggers rerender (no manual refresh) and applies to:
    - lesson list titles
    - lesson detail body
    - activity prompts/buttons/feedback
  - Do NOT add any DB changes for this path.

IF (B) API/DB:
- Implement language-aware API:
  - Add title_i18n/content_i18n JSON columns OR a translations table.
  - API must accept ?lang= and return localized fields with English fallback.
  - Update frontend to fetch lessons/activities from API, not TS imports.
  - Migrate existing TS lessons into DB (one-time script/migration).

3) Hard acceptance tests (must demonstrate all pass):
- Switch language -> lesson list titles change immediately.
- Switch language -> lesson page content changes immediately.
- Switch language -> activity text changes immediately.
- Refresh preserves language and content remains correct.
- Missing translation falls back to English (never blank).

Deliverables:
- Short root cause summary + chosen path
- List of files changed
- Evidence via console logs or screenshots showing content changes on language toggle
- Do not touch hero/nav again.
You are fixing a live production NurseNest issue.

CURRENT USER-FACING ERROR:
"Exam Start Failed — Something went wrong — please try again."

This is a P0 revenue-critical production issue.
The exam start/open flow must never hard fail with a dead-end message.

Your task is to trace and permanently fix the entire exam start pipeline end-to-end.

==================================================
PRIMARY GOAL
==================================================

A user clicking "Start Exam" must always get one of these outcomes:

1. Primary success:
- exam session opens normally

2. Safe fallback success:
- smaller exam opens
- cached published exam opens
- practice-by-category opens
- prior saved session resumes
- emergency lightweight exam mode opens

3. Clear blocked state:
- only if the user is truly not entitled or the exam is truly unavailable
- must show a precise reason, not generic failure text

A blank dead-end generic failure is not acceptable.

==================================================
INVESTIGATE ALL POSSIBLE FAILURE POINTS
==================================================

Audit and fix all exam start path stages:

1. client button/action handler
2. route/navigation transition
3. entitlement/tier access check
4. exam metadata fetch
5. exam session creation endpoint
6. exam content fetch
7. response parsing/validation
8. state initialization
9. restore/resume logic
10. fallback path if any upstream step fails

==================================================
REQUIRED FIXES
==================================================

1. TRACE THE FAILURE
Add full structured logging with correlation ids for exam start:
- user id
- tier
- exam id
- exam type
- region
- route name
- request id
- stage reached
- failure reason
- payload size
- response status
- validation outcome

Log exact failure stage:
- entitlement_failed
- exam_not_found
- invalid_payload
- oversized_payload
- session_create_failed
- parsing_failed
- restore_failed
- navigation_failed
- runtime_exception

2. HARDEN API CONTRACTS
Ensure exam start endpoints return schema-valid data only.

Required minimum contract:
- exam id
- session id
- title
- tier
- exam type
- question count
- initial question slice or valid fetch token
- timing metadata if applicable
- safe fallback metadata if full payload unavailable

Reject malformed payloads server-side before sending.

3. ELIMINATE OVERSIZED LOADS
Do not load full exams into memory on start.
Do not return all questions at once unless tiny and explicitly allowed.

Implement:
- chunked/paginated question delivery
- initial lightweight session bootstrap
- current-question or small-window loading only
- response size caps
- rationale stripping from start payload
- minimal metadata only at open time

4. FIX ENTITLEMENT/TIER LOGIC
Ensure:
- RPN users cannot open RN/NP exams
- RN users can open allowed RN/RPN exams
- NP users can open permitted NP/RN/RPN exams
- Allied users only open allied exams
- free users only open intended free previews/mock paths

If blocked, return precise structured reason:
- not_entitled
- exam_unpublished
- exam_unavailable_for_region
- subscription_required

Do not return generic server failure for entitlement issues.

5. BUILD FALLBACKS
If primary exam start fails, automatically attempt in order:
- restore existing in-progress session
- open cached published exam version
- open lightweight reduced-question variant
- open practice-by-category mode
- open emergency question mode

The user must land somewhere usable whenever safe.

6. CLIENT ERROR HANDLING
Replace generic:
"Something went wrong — please try again."

With actionable states:
- "We couldn't open this exam right now. Loading a lighter version..."
- "Your previous session was restored."
- "This exam is temporarily unavailable. Opening practice mode instead."
- "This exam requires a higher tier."
- "This exam is not currently published."

No blank dead ends.

7. VALIDATION
Before exam start succeeds, validate:
- exam exists
- exam published/live
- user entitled
- question set contains valid items
- first slice is non-empty
- answer structures are valid
- no zero-valid-item exam can start

8. SYNTHETIC TESTS
Add automated tests for:
- entitled user opens exam successfully
- oversized exam is chunked successfully
- invalid exam payload is blocked
- zero-valid-item exam is blocked
- unpublished exam is blocked with correct message
- wrong-tier access is blocked with correct message
- exam start fallback activates when primary fails
- resume session path works
- client does not show generic dead-end for handled failures

These tests must block deploy if failing.

9. OBSERVABILITY
Add dashboard/metrics for:
- exam start success rate
- exam start latency
- exam start failures by stage
- fallback activation count
- wrong-tier block count
- invalid payload count
- zero-valid-item block count
- average exam bootstrap payload size

10. ACCEPTANCE CRITERIA
Complete only when:
- Start Exam no longer dead-ends
- primary or fallback path always handles recoverable failures
- exam open flow is lightweight and memory-safe
- all tier and publication checks are correct
- malformed/empty exams cannot open
- deploy is blocked if exam start synthetic tests fail
- exact root cause is identified and documented

==================================================
DELIVERABLE FORMAT
==================================================

Do not give vague recommendations.

Provide:
1. root cause found
2. files changed
3. exact code/path changes
4. API contract changes
5. fallback logic added
6. validation rules added
7. tests added
8. how this prevents future exam start failures

Now fix the full exam start pipeline permanently.
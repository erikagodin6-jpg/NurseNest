URGENT PRODUCTION BUG FIX

The published NurseNest site currently shows NO lessons available across all tiers, even though lessons were previously present. This must be treated as a high-priority production incident.

Goal:
Restore lesson visibility on the published site for all tiers without deleting, overwriting, or corrupting existing content.

What to do:
1. Investigate the published production app only, not dev assumptions.
2. Find why all lesson libraries are empty across RPN, RN, and NP.
3. Determine whether this is caused by:
   - wrong production database connection
   - empty/incorrect API response
   - broken lesson query
   - broken tier filter
   - broken localization filter
   - route mismatch
   - publish/build issue
   - feature flag or access gating problem
   - accidental replacement of lesson data source
4. Add temporary diagnostic logging to production-safe server code so we can see:
   - lesson query input
   - lesson query output counts
   - counts per tier
   - counts before and after filters
   - locale being requested
   - user tier / access tier
   - whether fallback content exists
5. Verify the production database actually contains lessons by running count queries grouped by:
   - tier
   - published status
   - locale
   - lesson type
6. Check whether the published frontend is calling the correct endpoint and whether the endpoint returns empty arrays, errors, or filtered-out results.
7. Compare current production code against the last working deployment to find the exact regression.
8. If a recent lesson-generation task changed the lesson pipeline, isolate and roll back only the breaking change.
9. Preserve all existing lesson data. Do not regenerate or overwrite lessons unless absolutely necessary.
10. Restore a safe fallback so that if tier-specific filters fail, the library still shows published lessons rather than a blank state.
11. If tier mapping is broken, repair it so each tier sees only its correct lessons.
12. Check for slug/localization regressions caused by recent translation or route updates.
13. Confirm lesson pages render on published for:
   - main lesson library
   - tier-specific lesson views
   - individual lesson detail pages
14. After fixing, test on the published site:
   - RPN lessons visible
   - RN lessons visible
   - NP lessons visible
   - no cross-tier leakage
   - no blank-state message when lessons exist
   - filters, search, and pagination still work
15. If needed, immediately roll back to the last working deployment before applying a forward fix.

Deliverables:
- root cause summary
- exact files changed
- exact bug fixed
- production-safe rollback plan
- confirmation of lesson counts visible by tier after fix

Important constraints:
- Do not wipe or regenerate the lesson database
- Do not change Stripe, pricing, or unrelated pages
- Do not leave silent failures in place
- If API returns zero lessons, surface a clear debug reason in logs
- Prefer restoring visibility first, then refactor later

Add a temporary admin-only diagnostics panel if helpful showing:
- production DB connected: yes/no
- total lessons
- published lessons
- lessons by tier
- lessons by locale
- endpoint response count
- last deployment timestamp

Fix this completely and verify on the published site, not just in preview.

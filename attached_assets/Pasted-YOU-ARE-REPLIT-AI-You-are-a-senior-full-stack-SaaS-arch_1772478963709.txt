YOU ARE REPLIT AI. You are a senior full-stack SaaS architect + exam psychometrics implementer working inside the existing NurseNest codebase (Vite + React + Express + DB + Stripe). Implement a fully separated Allied Health Exam Prep SUBSITE on the SUBDOMAIN allied.nursenest.ca, including exam-like adaptive (“CAT-style”) practice exams that mimic each career’s licensing exam feel (question type mix, pacing, blueprint weighting) WITHOUT claiming affiliation with any licensing body and WITHOUT copying proprietary exam items.

NON-NEGOTIABLES
1) Do NOT break or refactor existing nursing routes, nursing tiers, nursing Stripe products, nursing sitemap, nursing layout, or nursing SEO.
2) Allied must be isolated by hostname (allied.nursenest.ca) + separate sitemap + separate robots + separate nav/branding + separate Stripe products.
3) Shared backend is allowed (same DB/services), but front-end must NEVER mix nursing and allied content.
4) Enforce all paywalls server-side. Never leak paid payloads to free users.
5) Performance: lazy load, paginate, cache where sensible, avoid huge payloads.
6) Content generation: fully original items only; no copyrighted exam questions.

TARGET CAREERS (Allied only)
- rrt
- paramedic
- pharmacy-tech
- mlt
- imaging

END STATE
- www.nursenest.ca remains Nursing-only and unchanged.
- allied.nursenest.ca becomes “NurseNest Allied” with its own careers, content, SEO, paywalls, and exam engine.
- Separate sitemaps and robots by host.
- Separate pricing products for Allied.

===============================================================================
A) SUBDOMAIN / HOSTNAME SUBSITE SHELL + ROUTE ISOLATION
===============================================================================
1) Implement hostname detection at app bootstrap:
   - If window.location.hostname startsWith("allied.") OR equals "allied.nursenest.ca":
       render <AlliedApp/> with AlliedLayout + AlliedRoutes
     else:
       render existing nursing app/layout/routes exactly as-is.

2) Implement server middleware for host enforcement:
   - On NON-allied host, any request path that starts with:
     /careers, /pricing, /rrt, /paramedic, /pharmacy-tech, /mlt, /imaging, /admin/allied
     MUST redirect (301) to https://allied.nursenest.ca{path}
   - On allied host, any request to known nursing-only paths MUST redirect (301) to https://www.nursenest.ca{path}
   - Handle x-forwarded-host and x-forwarded-proto safely.

3) AlliedRoutes (must exist):
   / (Allied homepage)
   /pricing
   /careers
   /:careerSlug
   /:careerSlug/lessons
   /:careerSlug/lessons/:moduleSlug
   /:careerSlug/qbank
   /:careerSlug/mock-exams
   /:careerSlug/cat-exams         (adaptive exams)
   /:careerSlug/study-plan
   /:careerSlug/flashcards
   /:careerSlug/sims
   /:careerSlug/tools
   /account
   /admin/allied
   /admin/allied/factory

4) AlliedLayout:
   - Same overall NurseNest design language (pastel, rounded), but clearly branded:
     “NurseNest Allied” / “Healthcare Exam Academy”
   - Separate nav and footer; no nursing links in top nav.
   - Footer only: small link back to nursing root.

===============================================================================
B) DATA MODEL (SHARED ENGINE, SEPARATE CONTENT)
===============================================================================
1) Add/ensure careerType enum/string across all relevant models:
   values: nursing, rrt, paramedic, pharmacyTech, mlt, imaging

2) Ensure/extend tables:
   - questions (with all fields needed below)
   - flashcards
   - modules/lessons (allied lesson content)
   - caseSims
   - blueprints (versioned)
   - examTemplates (mock/cat configs)
   - examSessions (attempts)
   - performanceStats (per user + career)
   - leads (email capture; allied only)

3) Every content query must filter by careerType. Never return nursing content on allied endpoints and vice versa.

===============================================================================
C) ALLIED PRICING + PAYWALLS (SEPARATE FROM NURSING)
===============================================================================
1) Create separate Stripe products for Allied ONLY:
   - allied_pro_monthly (recommended default price: 29.99)
   - allied_pro_annual  (recommended default price: 239.00)
   - optional: allied_single_career_monthly (19.99) & annual (149.00) by careerType
   - optional: allied_cram_addon_one_time (69.00)

2) Access control:
   FREE:
   - limited free sample questions (is_free=true; rate limited)
   - 1 mini mock per career
   - limited flashcards
   PRO:
   - full QBank
   - unlimited mock exams
   - CAT-style adaptive exams
   - analytics + readiness
   - study plan
   - case sims + tools

3) Enforce all access server-side; client must never receive locked question payloads or full banks.

===============================================================================
D) EXAM BLUEPRINTS + “CAT-STYLE” EXAMS (EXAM-LIKE, CONFIGURABLE)
===============================================================================
IMPORTANT LANGUAGE:
- Implement “CAT-style adaptive exams” that are “exam-like” and configurable. Do NOT claim they are the official exam algorithm.

1) Create versioned blueprint JSON per career (editable in /admin/allied):
   - domains/categories
   - weights (%)
   - subtopics
   - allowed question types
   - difficulty distribution targets
   - time-per-question target (for pacing)

2) Create exam modes:
   a) Practice QBank mode (tutor/exam)
   b) Mock exam (blueprint-weighted, fixed length)
   c) CAT-style adaptive exam (blueprint-weighted + adaptive difficulty)

3) CAT-style adaptive exam engine requirements:
   - Start difficulty at 3 (configurable).
   - Update a rolling “ability estimate” based on correctness + question difficulty (simple IRT-like or heuristic ok).
   - Choose next question difficulty using ability estimate, bounded [1..5].
   - MUST still respect blueprint weights over the whole exam (use weighted sampling buckets).
   - Stop rules (configurable in admin):
     * fixed length (e.g., 75/100/125)
     * OR confidence threshold after minimum items (e.g., after 50 items)
   - Anti-repeat: avoid recently seen items (per user + career) for 30–90 days.
   - End-of-exam report:
     * readinessScore (0–100)
     * estimated pass readiness band (low/medium/high; avoid absolute claims)
     * weak domains list
     * recommended drill sets
     * time management metrics

4) “Just like licensing exams” requirement:
   - For each career, implement the same STYLE and MIX of question types commonly tested for that profession, controlled by blueprint config in admin.
   - Provide default “ExamTypeMix” per career (below) but make it adjustable (do not present as official).

===============================================================================
E) CAREER-SPECIFIC QUESTION TYPES + FEATURE MAP (DEFAULTS)
===============================================================================
1) RRT (careerType=rrt, slug=rrt)
Question types enabled:
- MCQ, SATA, OrderedResponse, CaseSet, Calculation, ImageInterpretation (optional)
Default type mix target:
- 60% MCQ, 15% SATA, 10% Ordered, 10% CaseSet, 5% Calc/Image
Tools/features:
- ABG interpretation trainer (guided + quiz)
- Ventilator mode decision engine (AC/SIMV/CPAP/BiPAP basics)
- Oxygen device selector drill
Lessons/modules (MVP modules list):
- Resp physiology & gas exchange
- Acid–base & ABGs
- Oxygen therapy/devices
- Mechanical ventilation basics
- COPD/asthma/pneumonia/PE/ARDS
- Airway/emergencies
- PFTs + imaging basics
- Infection control & safety

2) Paramedic (careerType=paramedic, slug=paramedic)
Question types enabled:
- MCQ, OrderedResponse, Calculation, ECGRecognition (placeholder OK), CaseBranch/CaseSet
Default type mix:
- 55% MCQ, 15% Ordered, 10% Calc, 10% ECG, 10% Case
Tools/features:
- ECG rapid drill
- Trauma algorithm flow simulator
- Emergency drug dosage calculator
Lessons/modules:
- Assessment/triage
- Airway/breathing
- Cardiac + ECG
- Trauma/shock
- Neuro (stroke/seizure)
- Medical emergencies (anaphylaxis, tox, endocrine)
- Peds/OB basics (paramedic context)
- Ops/safety/communication

3) Pharmacy Tech (careerType=pharmacyTech, slug=pharmacy-tech)
Question types enabled:
- Calculation, MCQ, SATA, LabelInterpretation, Matrix/Matching
Default type mix:
- 40% Calc, 35% MCQ, 10% SATA, 10% Label, 5% Matrix
Tools/features:
- Pharm math generator
- IV rate calculator
- Compounding simulator (conceptual + math)
Lessons/modules:
- Pharm math core
- Dosage forms/routes
- Sterile vs non-sterile compounding
- Controlled substance handling (general)
- Medication safety/workflow
- Inventory/storage
- Drug classes foundations

4) MLT (careerType=mlt, slug=mlt)
Question types enabled:
- MCQ, SATA, CaseInterpretation, QCScenario, CriticalValueID, ImageMorph (optional)
Default type mix:
- 45% MCQ, 15% SATA, 20% CaseInterp, 10% QC, 10% Critical/Image
Tools/features:
- Lab trend simulator
- QC failure analyzer
- Micro ID decision tree (MVP)
Lessons/modules:
- Hematology
- Chemistry
- Microbiology
- Urinalysis/body fluids
- Transfusion foundations
- QC/QA
- Safety/pre-analytics

5) Imaging (careerType=imaging, slug=imaging)
Question types enabled:
- MCQ, OrderedResponse, ImageLabeling (placeholder OK), SafetyScenario, CaseSet
Default type mix:
- 55% MCQ, 20% Ordered, 15% Labeling, 10% Safety/Case
Tools/features:
- Anatomy labeling trainer
- Radiation/MRI safety drills
- Positioning technique engine (MVP)
Lessons/modules:
- Anatomy/positioning
- Physics/radiation safety
- Image quality/artifacts concepts
- Contrast reactions workflow
- Modalities overview (XR/CT/MRI/US)
- Patient care/communication

===============================================================================
F) “CONTENT FACTORY” ADMIN (MONEY MACHINE)
===============================================================================
Implement /admin/allied/factory (admin-only):
1) Select careerType, blueprint domain/subtopic, difficulty distribution, question type mix, count.
2) Generate batch via AI → strict JSON schema.
3) Validation gates:
   - duplicate/near-duplicate detection (stem similarity)
   - required tags present (careerType, domain, subtopic, difficulty, questionType)
   - rationale length minimums:
     * standard >= 350 words
     * CaseSet or difficulty 4–5 >= 600 words
   - option quality (no “all of the above”, no ambiguous stems)
   - safety disclaimer present when relevant
4) Preview/edit UI and “Commit to DB”.
5) Bulk import/export JSON.
6) Separate generators:
   - Generate Modules (12–20/career) with lessons + objectives + most-tested + red flags
   - Generate Flashcards (spaced repetition)
   - Generate Case Sims (unfolding)
7) Seed MVP content automatically (minimum):
   - 250 questions per career with correct type mix
   - 5 modules per career
   - 50 flashcards per career
   - 5 sims per career
   - Mark ~10% questions as is_free=true for lead magnet pool

STRICT CONTENT RULES:
- Original content only. No copyrighted questions. No “official exam” claims.

===============================================================================
G) STUDY PLAN + ANALYTICS + RETENTION (ALLIED ONLY)
===============================================================================
1) Add Allied study plan generator:
   - inputs: exam date, hours/week, baseline diagnostic score
   - outputs: daily schedule of modules + drills + sims
   - weekly recalibration based on performance

2) Analytics dashboard per career:
   - mastery heatmap by blueprint domain
   - readiness trend
   - time-on-task
   - weak area list + “Generate Drill Set” CTA

3) Retention:
   - exam countdown timer
   - daily streak
   - weekly summary (store events; email sending can be scaffolded)

===============================================================================
H) SEO INFRA (SEPARATE SITEMAP + ROBOTS BY HOST)
===============================================================================
1) Implement on allied host ONLY:
   - GET /sitemap-allied.xml
     * Content-Type: application/xml
     * include only public allied routes:
       /, /careers, /pricing, /:careerSlug, /:careerSlug/lessons, /:careerSlug/lessons/:moduleSlug (published only)
     * do NOT include qbank/exam session URLs
   - GET /robots.txt
     * Allow: /, /careers, /pricing, /:careerSlug, /:careerSlug/lessons*
     * Disallow: /api, /admin, /account, /checkout, /:careerSlug/qbank (if it serves payload),
                 /:careerSlug/mock-exams (if it serves payload),
                 /:careerSlug/cat-exams (if it serves payload),
                 any user/session routes
     * Include sitemap line:
       Sitemap: https://allied.nursenest.ca/sitemap-allied.xml

2) Nursing host:
   - Do NOT change existing sitemap/robots.

3) Canonicals:
   - Allied pages canonical to https://allied.nursenest.ca/...
   - Nursing pages canonical to https://www.nursenest.ca/...

===============================================================================
I) LEAD CAPTURE FUNNEL (ALLIED)
===============================================================================
1) Add “Free Diagnostic” funnel per career:
   - 15-question diagnostic using is_free=true pool, blueprint-sampled, exam-like pacing
   - end screen shows readiness band + weak areas preview
   - CTA: create account for study plan; subscribe for full bank
2) Store leads with consent (email, careerType, timestamp).

===============================================================================
J) ACCEPTANCE TESTS / SMOKE CHECKS
===============================================================================
Add checks:
1) allied sitemap contains NO nursing URLs.
2) nursing sitemap contains NO allied URLs.
3) allied robots disallows /admin and /api and any payload routes.
4) paywall enforcement: free user cannot fetch paid question payloads.
5) CAT exam generator respects:
   - blueprint weights within tolerance
   - allowed question type mix per career within tolerance
   - adaptive difficulty moves based on correctness

===============================================================================
K) DELIVERABLES
===============================================================================
1) Implement all above.
2) Provide a concise “Files changed” summary.
3) Document where to edit blueprint weights/type mix per career in Admin.
4) Seed MVP content for all five careers so pages render immediately.
5) Ensure the CAT-style mode is labeled “Adaptive Exam (CAT-style practice)” and includes disclaimer:
   “Exam-like practice; not affiliated with or identical to official licensing exams.”

BUILD NOW. END.
You are a senior React/Next.js engineer. Continue from the current RCA.

Known facts:
- POST /api/content (line 1319) upserts correctly and returns the saved item.
- GET /api/content/slug/:slug (line 1280) fetches by slug, but returns 404 unless item.status === "published" (line 1299).
- Client publish handler AdminLessonCreator.handleSave reloads the page via window.location.reload().
- The visible bug happens because the lesson detail page rendering logic routes/returns to the editor when dbLoading is false and dbContent is null (not found case).

Goal:
After clicking Publish:
1) The saved lesson should be immediately viewable at the lesson detail route.
2) The detail page should NOT auto-open the editor when the content is simply not fetched yet, stale, or in a draft state.
3) The “not found” state should show a NotFound UI (or a “Draft/Unpublished” message for admins) rather than redirecting to editor.

Tasks (do in this order):

A) Identify WHY dbContent is null after publish
1) Log the exact slug used in:
   - POST /api/content request payload and returned item.slug
   - The subsequent GET /api/content/slug/:slug request
2) Confirm the slug is identical and stable. If slug is derived from title, ensure publish doesn’t change slug unexpectedly.
3) Confirm the saved item.status is exactly "published" (string match, case sensitive) in DB and in POST response.
4) Confirm GET route is hitting the same environment/database as POST (no dev/prod mismatch).
5) Check auth/tenant scoping: ensure GET is allowed to retrieve the same record that POST saved (e.g., orgId/userId/siteId mismatch).
6) If the client fetches immediately after publish, check cache:
   - If using SWR/React Query, confirm the query key matches and that publish triggers invalidate/mutate.
   - If using Next.js app router fetch caching, ensure GET uses no-store/revalidate=0 where appropriate.

B) Fix the detail page rendering logic (this is the user-facing issue)
1) Find the lesson detail page component where it does:
   - if (!dbLoading && dbContent == null) { open editor OR redirect to editor }
2) Replace this behavior:
   - Do NOT redirect to editor automatically.
   - Show an explicit “Lesson not found” view.
   - If the current user is an admin/author and the API returns 404 because status != published, show:
     "This lesson exists but is not published yet" + [Edit] button.
3) Change API behavior if needed:
   - Option 1 (preferred): Add a separate admin endpoint GET /api/content/slug/:slug?includeDraft=true (auth protected) that returns drafts.
   - Option 2: Modify existing GET to allow admins to fetch drafts.
   - But keep public GET restricted to published only.

C) Fix the publish flow so it navigates to the view route and does not hard reload
1) Remove window.location.reload().
2) On successful POST response:
   - Ensure status is set to "published" in the payload OR server sets it on publish.
   - Use returned item.slug (source of truth).
   - Navigate to the view route: router.push(`/lessons/${item.slug}`)
   - Update local state to reflect saved content.
3) If using SWR/React Query, mutate/invalidate:
   - lessons list query
   - lesson detail query for that slug
4) Add UI feedback:
   - Disable publish button while saving.
   - Toast on success/failure.

D) Hardening
1) Make status transitions explicit:
   - Save Draft sets status="draft"
   - Publish sets status="published"
2) Ensure GET /slug only 404s for non-published for public users, but for admins it should return 200 with status and content.
3) Ensure list page click goes to the view route, not editor route. If inline edit exists, only show editor when user clicks Edit.

Deliverables:
- Point to exact file + lines in the detail page where dbContent null triggers editor route.
- Provide a patch implementing:
  - detail page: not-found handling + admin draft handling + no auto editor redirect
  - publish handler: remove reload, push to view using returned slug, cache invalidation
  - optional API change: admin draft fetch or includeDraft param with auth check
- Include a small test checklist:
  - Create new lesson -> publish -> redirected to view -> refresh -> still visible
  - Open from lessons list -> view shows content (not editor)
  - Draft lesson -> public sees not found; admin sees “unpublished” with edit button
Proceed to implement now.
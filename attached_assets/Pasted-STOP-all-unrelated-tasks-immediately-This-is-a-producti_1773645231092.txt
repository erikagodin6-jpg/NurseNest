STOP all unrelated tasks immediately.

This is a production outage. The site has been down for hours across multiple devices, multiple networks, and private browsing. Do not mention cache, stale service workers, or local browser state again unless you can prove it from production evidence.

I do NOT have a good deployment to roll back to, so your job is to recover the current codebase by restoring rendering with the smallest safe code change.

NON-NEGOTIABLE RULES
- Do not work on SEO
- Do not work on content
- Do not work on styling or layout polish
- Do not work on Allied Health or New Grad audits
- Do not work on conversion improvements
- Do not keep multiple background tasks running
- Do not make broad speculative edits
- Do not say “likely” or “probably”
- Work on rendering recovery only

PRIMARY GOAL
Get /en to visibly render again from the current codebase, then identify the exact root cause.

PHASE 1 — FREEZE AND NARROW
1. Stop all active unrelated AI tasks.
2. Inspect the most recently changed files related to:
   - i18n / locale loading
   - App.tsx
   - main entry
   - router
   - providers
   - homepage
   - service worker
   - head/meta handling
3. Build a short suspect list of the files most likely to have broken first paint.

PHASE 2 — PROVE WHERE RENDERING FAILS
Use the current codebase and production build path, not the dev server.

1. Run a production build.
2. Serve the production build on a clean port, not the dev port.
3. Open /en.
4. Capture:
   - first console error
   - first failed network request
   - whether #root exists
   - whether #root receives children
   - whether any visible text appears

PHASE 3 — EMERGENCY MINIMAL RENDER RECOVERY
Temporarily force /en to render the simplest possible visible page while preserving the same route.

Target output:
- plain visible text on screen such as:
  NURSENEST RECOVERY TEST
- rendered through the real production route /en

Interpretation:
- If even that does not render, the failure is above the page level: entry file, router, provider, or build/runtime boot
- If that does render, the failure is inside the homepage tree

PHASE 4 — BINARY ISOLATION OF THE BREAK
If the recovery test page renders, re-enable the real /en page pieces one at a time in this order:
1. route shell
2. layout
3. i18n/bootstrap
4. header/nav
5. hero
6. stats/cards
7. main sections
8. FAQ/schema/meta
9. footer
10. analytics/third-party code

Stop at the FIRST reintroduced piece that breaks rendering.
Do not re-enable multiple major sections at once.

PHASE 5 — I18N FAILURE CHAIN
You already have evidence that /api/assets/i18n/en.json returns 404 in production.

Trace this exactly:
1. which file requests it
2. whether the app blocks render waiting for it
3. whether failure leaves locale/messages undefined
4. whether that directly causes the white screen

Then fix only one of the following, whichever is proven:
- wrong fetch path
- missing production asset copy/upload
- express/static serving issue
- no safe fallback when translation fetch fails

Requirement:
A failed translation request must never white-screen the site.
If translation loading fails, fall back to bundled/default English and still render.

PHASE 6 — HOOK/RUNTIME ERROR
If a hook/runtime error still remains after the i18n/render fix:
1. identify the exact error text
2. identify the exact file and line/code path
3. determine whether it is:
   - invalid hook call
   - duplicate React
   - hook called conditionally
   - component invoked as plain function
   - mismatched React/ReactDOM
   - provider misuse

Fix that exact issue only after first paint is restored or while preserving the recovery test render.

PHASE 7 — DIFF-BASED RECOVERY
If the root cause is still unclear:
1. inspect recent edits in the suspect files
2. revert them in small groups
3. test /en after each revert
4. stop as soon as visible rendering returns
5. identify the exact offending change

This is a code-level rollback strategy inside the current codebase.
Do not wait for a previous deployment.

PHASE 8 — COMPLETION CRITERIA
Task is not done until:
- /en visibly renders
- first paint occurs
- no blank white page remains
- production build succeeds
- the first fatal error is identified
- the minimal fix is documented

FINAL REPORT
Return exactly:
1. whether /en now visibly renders
2. exact first fatal error
3. exact failing file(s)
4. exact fix applied
5. whether the i18n 404 was causal
6. whether any second hook/runtime error remains
7. exact files changed
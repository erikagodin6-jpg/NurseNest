2) The minimum implementation that works reliably
Required labels as token sets, not single strings
Instead of:
require "Recognition Cues"
Do:
require token "recognition cue"
require token "red flag"
require token "first action"
require token "exam trap"
require token "confuse with" OR "differential" OR "vs"
Validation strategy
For each section:
build one big searchText from all blocks:
title/heading/text/caption/label
items
table headers and cell text
then test each required token with .includes(token)
This avoids brittle “block must contain label in its title” rules.
3) What I recommend you change right now (even if your implementation “works”)
Replace exact label names with REQUIRED_TOKENS per section
This avoids apostrophe and phrasing issues.
For pathophysiology required tokens:
"recognition cue"
"red flag"
"first action"
"exam trap"
one of: "confuse with", "differential", "vs"
For pharmacology required tokens:
"hold" (or "hold parameter")
"monitor"
"contraindic" (covers contraindication)
"exam trap"
"red flag" (optional but useful)
4) The one ask: paste the actual diff hunk
To be able to confirm you’re not about to hit a hidden bug, paste just the new validation code. Ask Replit AI:
Output ONLY the diff hunk in server/routes.ts that adds REQUIRED_LABELS / semantic label validation and the updated pass condition.
Drop that here and I’ll sanity-check it for:
string matching pitfalls
missing fields in extraction
scanning items/tables
false positives/negatives
risk of infinite retries
5) Reality check on output quality
If semantic labels are enforced, your Pathophysiology section should now include blocks like:
Recognition cues: dyspnea, orthopnea, crackles, JVD, edema, weight gain…
Red flags: pink frothy sputum, new confusion, hypotension, SpO₂ dropping…
First actions: sit up, assess airway/breathing, SpO₂, O₂ as ordered, notify provider, IV access, strict I&O…
Exam traps: “give IV fluids for crackles,” “ignore daily weights,” “delay escalation…”
Don’t confuse with table: COPD vs HF, pneumonia vs HF, PE vs HF, nephrotic edema vs HF…
That’s the “exam cram” you want.
If you paste the diff hunk, I’ll confirm whether your validator will reliably force those blocks without causing endless retries.
ood—your direction is correct (object storage keys, stream from storage, no base64). The remaining risk is you’re letting the implementation drift into inconsistent patterns (“previewUrl used as key,” “parseStoragePath helper,” “two adminFetch copies”). That’s where bugs and security leaks happen.

Here’s how to lock this down cleanly, with exact decisions and a QA checklist. No more meta.

1) Decide what previewUrl actually is (and enforce it)

Right now you have a column named previewUrl. You want it to hold the object storage location, not a public URL and not base64.

Pick one canonical format:

Option A (recommended): store a storage key only

previewUrl = "products/{id}/preview-3.pdf"

Streaming endpoint knows the bucket and uses the key

Pros: simplest, no parsing, portable across envs
Cons: column name “previewUrl” is misleading but workable

Option B: store full gs://bucket/key

previewUrl = "gs://your-bucket/products/{id}/preview-3.pdf"

Pros: explicit
Cons: requires parsing; more fragile

Recommendation: Option A (key only).
If you keep “parseStoragePath,” you are implicitly choosing Option B—be consistent.

✅ Action: Rename in code to previewStorageKey variable even if DB column stays previewUrl.

2) Object storage integration: don’t invent helpers unless needed

If you already have an objectStorageClient pattern elsewhere, copy it exactly. The only two operations you need:

putObject(key, bytes, contentType)

getObject(key) -> bytes/stream

Generate preview (admin-only): key write + DB update

Must do:

read full PDF bytes from full PDF key

watermark first N pages

write preview bytes to previewKey

set previewUrl = previewKey

Stream preview (public): read bytes and send

Must do:

fetch previewKey from DB

getObject(previewKey)

stream bytes with Content-Type: application/pdf

Hard rule: never redirect to object storage directly, unless you generate a signed URL for preview only. Streaming is simplest and safest.

3) Fix the admin POST pattern immediately (you already found it)

This is critical:

getAdminParams() puts username/password in query params for a POST request

That’s not just ugly—it’s risky (leaks in logs, browser history, proxies).

✅ Correct: all admin mutations must use adminFetch with:

credentials: "include" (cookie session) or

Authorization: Bearer …

No admin secrets in query params. Ever.

4) The real bug you identified: two adminFetch implementations

This will absolutely cause inconsistent auth headers/credentials and intermittent failures.

Fix (minimum viable)

Create one client helper, e.g.:

src/lib/adminFetch.ts

Export it and import everywhere (shop page, product builder, image lab, etc.)

Delete local copies.

Acceptance criteria

Grep for function adminFetch and it appears exactly once in the codebase.

All admin endpoints are called through this one helper.

5) Verify your schema “already added” is actually correct for the preview system

You said you added:

previewUrl

previewPageCount

That’s enough. But you must also ensure:

it is nullable (older products won’t have previews yet)

product page handles previewUrl == null gracefully (shows “Preview coming soon” or hides the iframe)

Also: make sure full PDF key is not stored in a publicly exposed field.

6) Mandatory QA pass (PASS/FAIL list)

Run these checks now; do not move to translations until all pass.

A) Admin download

As admin: click “Download” → PDF downloads (attachment)

As non-admin: request /api/admin/products/:id/download → 401/403

B) Generate preview (admin)

Click “Generate/Regenerate Preview”

Confirm DB previewUrl now contains a storage key (not base64, not http)

Confirm object exists in bucket at that key

C) Public preview

Logged out: open /api/products/:slug/preview

It renders PDF inline

It shows watermark on every page

It is limited to previewPageCount pages

D) Security sanity checks

Search the network tab / HTML source:

no full PDF storage keys exposed publicly

no admin username/password in URLs

Ensure previewUrl never equals a signed URL that expires (unless you regenerate it every time)

E) Image Lab

Generate an image

Save to assets

Insert to canvas

Export PDF includes image

7) What I would change in your current plan (to prevent future pain)

Stop calling it previewUrl in code. Treat it as previewStorageKey.

Kill parseStoragePath unless you truly store gs://… strings.

Unify adminFetch now (this is your highest-leverage stability fix).

If you paste one snippet, I can pinpoint the remaining issues

Paste your current adminFetch implementations (both copies) and I’ll tell you exactly what to standardize (headers, credentials, error handling) so downloads and preview generation don’t fail on one page but work on another.
You are a senior full-stack engineer and CMS architect working inside my existing NurseNest codebase. The current admin/blog system is NOT truly implemented. Your task is to build a production-grade Blog CMS with AI-assisted scholarly writing, citation controls, scheduling, approval workflows, and inline editing.

You must reuse my existing framework, database, auth system, UI components, Tailwind tokens, layout, and NurseNest aesthetic exactly. No placeholder features. No mock implementations.

PRIMARY OBJECTIVES (NON-NEGOTIABLE)
1) Implement a fully working Blog Management System.
2) Implement AI-assisted blog generation based on topics I input.
3) Enforce scholarly writing structure with APA 7 citations and references.
4) Implement draft → approval → scheduling → publishing workflow.
5) Add a contribution prompt on the public blog page.
6) Remove ALL “working on it” placeholders.

PHASE 0 — REPOSITORY INSPECTION
- Identify framework/router, auth model, database/ORM, storage.
- Locate existing blog/admin code and diagnose why blog editing/posting is not functional.
- Output a short technical diagnosis and proceed immediately.

PHASE 1 — DATA MODELS (USE EXISTING ORM/MIGRATIONS)
Create or extend models:

BlogPost:
- id
- title
- slug (unique)
- topic (string)
- excerpt
- bodyContent (rich text or structured blocks)
- coverImageUrl
- status = 'draft' | 'pending_review' | 'approved' | 'scheduled' | 'published'
- publishAt (nullable datetime)
- citationsJson (stores structured citation data)
- referenceList (formatted APA 7)
- seoMetaTitle
- seoMetaDescription
- canonicalUrl
- createdAt, updatedAt
- authorId
- reviewerId (nullable)
- aiGenerated = boolean
- aiVerificationStatus = 'unchecked' | 'needs_review' | 'verified'

AuditLog:
- actorId, entityType, entityId, beforeJson, afterJson, timestamp

Optional: Comment/ContributionIntent model if needed.

PHASE 2 — ADMIN AUTHORIZATION
- Enforce server-side admin checks on ALL blog mutations.
- Only admins can approve/publish/schedule posts.
- If roles missing → add user.role with secure bootstrap via env ADMIN_EMAILS.

PHASE 3 — BLOG MANAGER (REAL CMS)
Implement /admin/blog:

Capabilities:
- List posts by status (draft / pending_review / scheduled / published)
- Create new post manually
- Edit existing post
- Inline preview
- Delete/unpublish
- Approval controls
- Scheduling controls

Status workflow:
draft → pending_review → approved → scheduled OR published

Admins can move posts between states.

PHASE 4 — AI BLOG GENERATION ENGINE (CRITICAL)
Implement server endpoint:

POST /api/blog/ai-generate

Input:
- topic
- targetAudience (RPN / RN / NP)
- desiredDepth (concise / standard / comprehensive)
- wordCountRange

AI output MUST:
- Produce scholarly, structured educational articles
- Include headings and logical academic flow
- Use APA 7 in-text citations
- Include APA 7 reference list
- Prefer sources within last 5 years
- NEVER fabricate citations

If citation verification cannot be guaranteed automatically:

Implement a SAFETY MECHANISM:
- Mark aiVerificationStatus = 'needs_review'
- Store citations separately
- Display warning in admin editor:
  "References require verification before approval."

Admins must explicitly approve.

User flow:
- Admin inputs topic
- Clicks “Generate AI Draft”
- Draft created with status='draft' and aiGenerated=true

PHASE 5 — APPROVAL & VERIFICATION SYSTEM
- Admin UI must clearly display:
  - AI generated badge
  - Verification status
- Approval button must:
  - Require verification confirmation OR allow override with audit log

PHASE 6 — SCHEDULING ENGINE
Implement reliable scheduled publishing:

Preferred:
- Use existing cron/scheduler if available

Fallback:
- Implement server publishing loop:
  - Every X minutes check posts where status='scheduled' AND publishAt <= now
  - Transition to 'published'
  - Use DB locking/transactions

Ensure no duplicates or race conditions.

PHASE 7 — INLINE EDITING FOR BLOG POSTS
- Admin can open blog post page and edit content inline
- Editor supports text formatting + images + SEO fields
- Save writes to BlogPost model

PHASE 8 — PUBLIC BLOG PAGE ENHANCEMENTS
On /blog main page:

Add CTA section at bottom:

Title:
"Want to contribute or suggest a topic?"

Buttons:
• Suggest a Topic
• Contribute Content

Behavior:
- Opens modal/form
- Stores submission OR sends email notification OR logs intent
- Must NOT require full user auth unless system already supports it

Tone:
Academic, professional, aligned with NurseNest brand.

PHASE 9 — SEO SAFETY
Ensure published posts:

- Are publicly accessible
- Have correct meta tags
- Appear in sitemap if generator exists
- Are not blocked by robots/noindex

PHASE 10 — REMOVE PLACEHOLDERS
Search for placeholder text:
- “working on it”
- “coming soon”
Remove or replace with real implementations.

DELIVERABLES (MANDATORY)
1) Repo diagnosis summary
2) Files changed list
3) Migrations / schema updates
4) Route & API list
5) Manual test script:
   - Generate AI draft → Edit → Approve → Schedule → Publish
6) Confirm blog system is fully operational

START NOW
- Perform repo inspection first.
- Implement Blog Manager + AI Generation Engine before any other features.
- Make reasonable assumptions and document briefly.
- Do not leave stubs or fake UI logic.
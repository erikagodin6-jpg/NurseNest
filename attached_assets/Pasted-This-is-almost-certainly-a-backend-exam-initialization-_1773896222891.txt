This is almost certainly a backend exam-initialization failure, not just a UI glitch.
Because it is specifically the CAT exams that still will not start, the failure point is usually one of these:
CAT session creation is crashing
The frontend clicks “Start Exam,” calls a CAT start endpoint, and that endpoint throws before returning a valid attempt/session payload.
Question selection returns empty or malformed data
CAT logic often expects a minimum pool of calibrated questions. If the query returns zero rows, duplicate rows, missing difficulty values, missing IDs, or bad option structures, the session fails.
Entitlement/tier filtering is blocking CAT content
If CAT exams are now behind role/tier filtering, the CAT endpoint may be filtering out all eligible questions for that user.
Payload contract mismatch
The frontend expects one response shape, but the backend now returns something different, such as:
attemptId missing
questions missing
currentQuestion null
examSession renamed
error wrapped incorrectly
Memory/cache/query timeout
CAT generation sometimes loads too much content at once or recomputes too much before first render, causing timeout or process restart.
CAT-specific validation is too strict
For example:
requires difficulty ladder but data does not have it
requires min 75 questions but bank has fewer eligible
requires stats/calibration fields that older content lacks
Frontend is swallowing the real backend error
Your UI message is generic, so the actual exception is probably in logs.
What needs to happen now is not another cosmetic patch. You need a hard fail-safe CAT exam recovery implementation.
Use this master prompt in Replit/Emergent exactly as written:
You are working on the NurseNest production codebase.

URGENT: CAT exams still fail to start and users see:
“Unable to Start Exam — An unexpected issue occurred. Please retry — if it persists, contact support.”

I need a PERMANENT FIX for CAT exam startup across the full stack.

GOAL
Make CAT exams always produce one of these outcomes:
1. successfully start a CAT session, OR
2. gracefully fall back to a usable non-CAT exam/session with clear logging,
BUT NEVER leave the user at a dead end.

==================================================
SECTION 1 — TRACE THE ENTIRE CAT START FLOW
==================================================

Audit and fix the complete CAT startup pipeline end-to-end:

- CAT exam card/button click
- frontend handler
- API client
- exam start route
- CAT session creation logic
- question pool fetch
- entitlement/tier filtering
- attempt/session persistence
- first question assembly
- frontend response parsing/rendering

Find every CAT-related route, service, helper, query, adapter, and component.

Search for terms like:
- CAT
- catExam
- adaptive
- startExam
- createExamAttempt
- beginAttempt
- exam session
- question selection
- difficulty
- theta
- exam attempt
- mock exams
- /api/exams
- /api/cat
- /api/mock-exams

==================================================
SECTION 2 — ADD STRUCTURED LOGGING AT EVERY FAILURE POINT
==================================================

Add structured logs with a shared requestId / traceId for every CAT startup request.

Must log:
- user id
- role/tier/subscription
- exam id / exam type
- endpoint hit
- number of candidate questions found
- number of questions after entitlement filtering
- number after language filtering
- number after validity checks
- whether session row was created
- whether first question was selected
- exact thrown error
- response status code
- fallback used or not

Do NOT log sensitive secrets/tokens.

==================================================
SECTION 3 — HARDEN THE BACKEND CAT START ENDPOINT
==================================================

Refactor CAT startup so it NEVER returns a vague unhandled failure.

Implement a single safe orchestration flow:

1. Validate user entitlement
2. Validate exam exists
3. Load eligible CAT question pool
4. Validate minimum usable pool size
5. Validate every question has:
   - stable id
   - stem/content
   - 4 valid answer options if MCQ
   - correct answer
   - rationale structure where required
   - difficulty / domain metadata if CAT requires it
6. Create exam attempt/session
7. Select first question
8. Return normalized response shape

If any step fails:
- catch it
- log exact root cause
- return typed error code
- attempt fallback if possible

==================================================
SECTION 4 — NORMALIZED RESPONSE CONTRACT
==================================================

Create and enforce one canonical CAT start response contract.

Success example:
{
  "ok": true,
  "mode": "cat",
  "attemptId": "...",
  "sessionId": "...",
  "examId": "...",
  "question": {...},
  "progress": {...},
  "meta": {...}
}

Fallback success example:
{
  "ok": true,
  "mode": "fallback_standard_exam",
  "attemptId": "...",
  "sessionId": "...",
  "examId": "...",
  "question": {...},
  "progress": {...},
  "meta": {
    "reason": "cat_pool_unavailable"
  }
}

Handled failure example:
{
  "ok": false,
  "errorCode": "CAT_POOL_EMPTY",
  "message": "This exam is temporarily unavailable.",
  "retryable": true
}

Frontend must not rely on inconsistent field names.

==================================================
SECTION 5 — BUILD A SAFE FALLBACK SYSTEM
==================================================

If CAT startup cannot proceed, automatically fall back in this priority order:

1. Same exam in fixed-form mode using valid questions from same tier/region/language
2. If unavailable, a safe mixed adaptive-ready practice block
3. If still unavailable, a user-friendly unavailable state with incident logged

Never leave the user on a generic crash toast.

If fallback occurs, show a clear user-safe message like:
“Adaptive mode is temporarily unavailable. We’ve started a standard exam instead.”

==================================================
SECTION 6 — VERIFY ENTITLEMENT / TIER FILTERING
==================================================

Audit all CAT filtering rules so they match the site-wide paywall model exactly:

- RPN users can access only RPN content
- RN users can access RN + permitted lower-tier content only if intended by product rules
- NP users only according to paid entitlements
- Allied users only their own stream
- free users must not receive premium CAT content

Fix any place where filtering accidentally removes the entire CAT pool.

Log counts before and after each filter:
- by tier
- by region
- by language
- by active/published status
- by exam type
- by premium entitlement

==================================================
SECTION 7 — QUESTION POOL VALIDATION + SELF-HEAL
==================================================

Before CAT starts, run a validation layer over the candidate pool.

Reject and log any question missing:
- id
- text/content
- choices/options
- correct answer
- difficulty / blueprint tags required for CAT
- active/published status

If enough valid questions remain, continue.
If not enough remain, trigger fallback.

Add a background/admin repair report for invalid CAT questions.

==================================================
SECTION 8 — MEMORY / PERFORMANCE FIXES
==================================================

Make CAT startup lightweight and safe under load.

Requirements:
- never load huge full banks into memory if not needed
- query only required fields for first-question selection
- paginate or batch where needed
- add sane LIMITs
- avoid N+1 queries
- memoize/cache only safe small metadata, not giant question blobs
- add timeout guards
- avoid repeated full recomputation on every start
- ensure DB indexes exist for exam/tier/type/published filters
- ensure pool selection does not scan the whole table unnecessarily

If current architecture loads all questions into memory to start a CAT exam, refactor it.

==================================================
SECTION 9 — FRONTEND HARDENING
==================================================

On the frontend:
- capture and display structured handled errors
- support success mode = "cat"
- support success mode = "fallback_standard_exam"
- do not assume missing nested fields
- do not crash on null question/session values
- prevent duplicate start submissions
- disable button during startup
- show loading state
- show user-friendly message on fallback
- retry only when retryable = true

Remove any generic “Unexpected issue occurred” dead-end if a better handled state exists.

==================================================
SECTION 10 — ADD TESTS
==================================================

Create or update tests for these cases:

1. CAT exam starts successfully with valid pool
2. pool empty after entitlement filter
3. pool empty after language filter
4. invalid/malformed questions in pool
5. session creation DB failure
6. first question selection failure
7. frontend receives fallback_standard_exam and renders correctly
8. duplicate button tap does not create two attempts
9. premium/free access rules enforced correctly
10. large pool does not cause excessive memory/time usage

==================================================
SECTION 11 — ADMIN / SUPPORT VISIBILITY
==================================================

Add an admin/support diagnostic endpoint or dashboard card for CAT health showing:
- CAT starts attempted
- CAT starts succeeded
- CAT fallbacks used
- CAT failures by errorCode
- average startup time
- empty pool counts by tier/exam/language
- malformed question counts

==================================================
SECTION 12 — OUTPUT REQUIRED
==================================================

After implementation, provide:

1. Exact root cause(s) found
2. Files changed
3. Endpoints changed
4. Response contract used
5. Fallback behavior added
6. Performance improvements added
7. Test cases added
8. Remaining risks if any

IMPORTANT:
Do not do a superficial patch.
Do not just change frontend text.
Do not leave generic catch-all failures.
Do not break existing non-CAT exams.

The result must make CAT exam startup resilient, observable, entitlement-safe, and non-brittle in production.
Also give them this fast diagnostic prompt first, because you need the real root cause before anything else:
Audit why CAT exams cannot start in the NurseNest production codebase.

I need a precise root-cause report, not guesses.

Trace the exact CAT startup flow from frontend click to backend response and identify the first failing point.

Return:
1. exact files/functions/routes involved
2. exact error/exception being thrown
3. whether failure is caused by:
   - empty question pool
   - entitlement filter removing all questions
   - malformed CAT question data
   - DB/session creation failure
   - timeout/memory issue
   - frontend/backend response mismatch
4. counts of questions before/after all filters
5. whether fallback exists today
6. minimal code fix and permanent architecture fix

Do not stop at generic UI errors. Find the first real failing line/path.
Most likely, based on everything you have described across the last day, the real issue is one of these three:
CAT pool becomes empty after filtering
CAT startup loads too much / times out / crashes
frontend expects a session payload that backend no longer returns
If you want, paste the CAT start route or the error logs here and I’ll pinpoint the likely failing block directly.
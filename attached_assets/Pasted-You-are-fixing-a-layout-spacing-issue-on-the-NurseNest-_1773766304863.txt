You are fixing a layout spacing issue on the NurseNest homepage hero section.

PROBLEM:
There is excessive vertical spacing between the top navigation/header and the hero content ("Study Smarter. Pass Faster.") on desktop.

GOAL:
Tighten the hero layout so content sits closer to the header while maintaining clean, premium spacing.

DO NOT:
- Do not remove breathing room entirely
- Do not affect mobile spacing negatively
- Do not break existing layout structure

---

STEP 1 — CHECK HEADER BEHAVIOR

If header is fixed or sticky:
- Ensure the hero accounts for header height properly

Add or confirm:

.hero-section {
  padding-top: calc(var(--header-height, 72px) + 32px);
}

If header is NOT fixed:
- Remove unnecessary top offset logic

---

STEP 2 — REDUCE HERO TOP SPACING

Locate hero container (likely something like):

<section className="hero ...">

Reduce vertical padding:

Change:
py-20 / pt-24 / pt-28

TO:
pt-12 pb-16 (desktop)
pt-10 pb-12 (tablet)
pt-8 pb-10 (mobile)

Tailwind example:
className="pt-12 pb-16 md:pt-10 md:pb-12 sm:pt-8 sm:pb-10"

---

STEP 3 — REMOVE EXTRA STACKED MARGINS

Check top elements inside hero:

- badge pills (Designed for Modern Nursing Exams, Evidence-Based)
- small labels (Evidence-Based)

Ensure:

.hero-badges {
  margin-bottom: 16px;  // not 32–48px
}

.hero-title {
  margin-top: 0;
}

Remove any:
mt-8, mt-10, mt-12 on the title

---

STEP 4 — FIX VERTICAL ALIGNMENT

Ensure hero is NOT artificially centered with min-height forcing spacing:

If you have:
min-h-screen
or
h-screen

Change to:
min-h-[auto]

OR:
min-h-[70vh]

---

STEP 5 — CONTAINER WIDTH + ALIGNMENT

Ensure content starts near top, not centered:

.hero-container {
  display: flex;
  flex-direction: column;
  justify-content: flex-start;
}

NOT:
justify-center

---

STEP 6 — DESKTOP-SPECIFIC TUNING

Apply slightly tighter spacing only on large screens:

@media (min-width: 1024px) {
  .hero-section {
    padding-top: 48px;
  }
}

---

STEP 7 — FINAL VISUAL TARGET

After fix:
- Logo → small gap → badges → title (tight grouping)
- Title should appear within top 25–30% of viewport
- No large empty white space above content

---

STEP 8 — DO NOT TOUCH

- Do not change typography scale
- Do not change card layout below
- Do not affect mobile UX negatively

---

VALIDATION

After changes:
- Desktop: no excessive white space above hero
- Mobile: still readable and spaced
- Lighthouse layout stability unchanged
- No CLS introduced

Commit changes cleanly.
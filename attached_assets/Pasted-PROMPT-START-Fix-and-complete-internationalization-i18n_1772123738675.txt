PROMPT START
Fix and complete internationalization (i18n) across my NurseNest app. Right now only the hero section translates; I need ALL core pages and UI strings to translate properly using the existing translation setup.
HARD RULE: Do NOT modify, refactor, or touch any code related to the Blog Generator (blog generator logic, blog admin, blog routes/pages, blog database tables, blog endpoints). The blog generator is perfect and must remain unchanged. If blog pages already exist and must display translated UI chrome (nav/footer/buttons), that’s fine, but do not alter the blog generator features or logic.
GOAL
Ensure every page and every UI component displays in the selected language, not just the hero.
This includes:
Navigation labels
Buttons, forms, placeholders, tooltips
All page headings, sections, empty states
Error messages (404, unauthorized, validation messages)
Lessons index/detail UI chrome
Flashcards index/detail UI chrome
Exams/QBank UI chrome (start/submit/results)
Admin dashboard UI chrome (labels, buttons, validation)
Footer and legal/help links
Important: I am asking for “basic translations” for all pages. If full academic content translation is not possible, translate all UI and static text and add a clear fallback for user-generated content.
REQUIRED IMPLEMENTATION
1) Audit + Coverage
Scan the codebase and identify all strings currently hardcoded in components/pages. Replace them with the translation function (e.g., t("...")) so they are driven by i18n everywhere.
Do not leave hardcoded English strings except for:
user-generated content (lesson text I write)
proper nouns (NurseNest)
technical codes (IDs/slugs)
2) Translation Architecture
Use or fix my existing i18n approach (do not introduce a second i18n framework). Make sure:
i18n is initialized once
language selection persists (localStorage or cookie)
SSR/CSR hydration issues are avoided (if Next.js)
there is a clean fallback language (en)
3) Translation Files Structure
Create structured translation namespaces, for example:
common.json (global UI)
nav.json
footer.json
auth.json
lessons.json
flashcards.json
exams.json
admin.json
errors.json
Add the necessary keys for all pages. Keep key naming consistent.
4) Basic Languages to Support
Implement at minimum:
English (en)
French (fr)
If my app already has more languages configured, keep them and extend coverage consistently.
5) Content Translation Behavior (Important)
For user-generated educational content (lessons/flashcards/exams) created in admin:
Do NOT auto-translate existing content unless I explicitly request it.
Add a “Content language” field on each content item (default en).
On public pages, if the selected UI language differs from the content language:
Show the content in its original language
Show a small notice: “This lesson is currently available in English.”
Provide an optional button “AI Translate (Optional)” ONLY if the AI Assist system exists and is enabled (API key set). If AI is disabled, hide the button.
This ensures my UI is translated even if content is not.
6) Component-Level Requirements
Ensure these are translated everywhere they appear:
Search and filter labels
Sort dropdown options
“Loading…”, “No results”, “Try adjusting filters”
“Save draft”, “Publish”, “Unpublish”, “Archive”
“Create new lesson”, “Add block”, “Delete block”
Exam buttons: “Start”, “Next”, “Previous”, “Submit”, “Review”, “Show rationales”
Toasts/alerts and validation errors
Breadcrumbs (if used)
Modals: confirm delete, unsaved changes
7) Routing + Links
If the app uses locale-prefixed routes (e.g., /en, /fr), ensure ALL routes are generated correctly. If not using locale prefixes, ensure language switching does not break navigation.
8) Testing Checklist (Must Pass)
After implementation:
Switching language updates ALL visible UI on every page, not just the hero.
Refresh keeps the selected language.
No untranslated strings remain in Lessons/Flashcards/Exams/Admin UI.
Error pages (404/500) translate.
Blog generator untouched.
Provide a quick internal “i18n Coverage” dev checklist or a small script/log to help identify remaining hardcoded strings.
Proceed to implement now with minimal visual redesign.
PROMPT END
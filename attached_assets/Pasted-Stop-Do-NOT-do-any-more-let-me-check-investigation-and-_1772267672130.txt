Stop. Do NOT do any more “let me check” investigation, and do NOT work on unrelated tickets (T003–T010, CAT, SEO, dashboards). This task is ONLY:

PUBLIC USER LANGUAGE FIX:
When a user changes language, the lesson/module titles + lesson body + activity text must change immediately. Site must default to user’s language on first visit.

You must deliver an end-to-end working result with proof.

Acceptance criteria (must all pass):
1) First visit default language:
   - Choose language priority: URL locale > saved preference (localStorage/cookie) > browser language (navigator.language) > English.
   - Must work for users (not admin).

2) Language switch:
   - When user clicks language switcher, update:
     a) static lesson list card titles
     b) static lesson detail title + at least one paragraph of body
     c) at least one activity/module prompt/button/feedback string
   - Must update immediately (reload is OK if your router design requires it) but it must land in the chosen language.
   - Missing translations must fall back to English, never blank.

3) No runtime AI:
   - No AI calls for public users, ever.
   - Do not auto-generate translations on GET.

Required process (must follow exactly):
STEP A — Pick 3 concrete lesson IDs and demonstrate current failure
- Choose 3 lesson IDs that are present in contentMap.
- Print the IDs in your response.
- Add TEMP debug logs (remove at end) to show:
   - current language
   - renderedTitle for each ID on the list page
   - renderedTitle + first 80 chars of renderedBody for one ID on the detail page
   - one activity/module string
- Show logs BEFORE fix proving language changes but content stays English (or stays partial).

STEP B — Fix the actual cause (choose one path only)
IF the pipeline works but translations are missing:
- Generate/produce COMPLETE translation coverage for the chosen target language(s) for ALL lesson IDs.
- “Complete” means: every user-facing string used by the renderer exists for that lesson (not just title/overview).
- Ensure fr.json (or lessons.i18n.json) includes the same field structure expected by getLessonI18n/getLessonTitle and activities.

IF translations exist but lookup/render is wrong:
- Fix the source-of-truth:
   - Lesson list must not use hardcoded diseases[].name.
   - List items must reference lessonId, and display name must come from translations map with fallback.
   - lesson-detail must render localized fields from getLessonI18n and not cache stale results (fix translationsReady reset, memo deps, and language switch flow).
- Ensure activity/module UI strings use the same localization layer.

STEP C — Prove success
- Provide AFTER logs showing for the same 3 lesson IDs:
   - renderedTitle changes in list
   - renderedTitle + renderedBody changes in detail
   - activity/module string changes
- Provide screenshots of list + one detail page in two languages.
- Remove TEMP debug logs.

Deliverables:
- List of files changed
- Translation file path(s) and a sample entry showing full structure for one lesson
- Confirmation that unrelated tickets were NOT touched

Constraint:
If you only have translations for 10 lessons, you must either:
- Expand translations to cover ALL lessons (for at least one language like French), OR
- Implement a clear UI indicator “English fallback” per lesson so users are not misled.
But you cannot claim the feature works if 90% of lessons remain English.
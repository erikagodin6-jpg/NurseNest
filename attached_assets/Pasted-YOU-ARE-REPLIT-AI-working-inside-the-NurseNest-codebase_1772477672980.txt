YOU ARE REPLIT AI working inside the NurseNest codebase (Vite + React + Express). Implement Allied subdomain content generation + SEO infrastructure WITHOUT impacting nursing SEO.

GOAL
- Nursing stays on main host (www/nursenest.ca) with existing sitemap/robots unchanged.
- Allied lives on allied.nursenest.ca with its own sitemap and robots rules.
- Generate initial seed content for Allied careers: rrt, paramedic, pharmacy-tech, mlt, imaging.
- Ensure server-side paywalls prevent indexed pages from exposing paid content.

A) HOST + ROUTING (do not break existing)
1) Ensure hostname detection already routes allied.nursenest.ca to AlliedLayout + AlliedRoutes only.
2) Add server middleware to enforce host separation:
   - If request host is NOT allied.* and path begins with any allied career slug (/rrt, /paramedic, /pharmacy-tech, /mlt, /imaging, /careers, /pricing on allied):
     redirect to https://allied.nursenest.ca{path}
   - If request host IS allied.* and path is nursing-only (existing nursing routes list):
     redirect to https://www.nursenest.ca{path}

B) ALLIED SEO PAGES (indexable, safe)
3) Create “public/preview” Allied pages that are indexable and do NOT contain paid QBank payloads:
   - /careers
   - /pricing
   - /rrt (landing)
   - /paramedic (landing)
   - /pharmacy-tech (landing)
   - /mlt (landing)
   - /imaging (landing)
   - Optional: /rrt/free-practice-questions, etc. (landing-style pages only)
4) Each career landing must include:
   - H1, short value prop
   - Blueprint topic list (public)
   - Feature list (public)
   - “Start free diagnostic” CTA
   - FAQ section (public)
   - Internal links to that career’s public routes
5) DO NOT render full questions/answers/rationales on any indexable page unless it is explicitly “free sample” and rate-limited.

C) CONTENT SEEDING (database + admin-safe)
6) Add an admin-only script/route to seed Allied content:
   - For each careerType: create a blueprint JSON with categories + weights + subtopics.
   - Create 20 “Lesson” stubs per career (title + summary + tags) as public marketing content.
   - Create 100 “sample” free questions per career (flag is_free=true), mixed types, short rationales OK.
   - Create placeholders for paid question banks (counts shown, but content locked).
7) Ensure all seeded records have:
   - careerType
   - topic/subtopic
   - difficulty
   - questionType
   - is_free boolean
   - countryMode (global default)

D) SITEMAPS (separate!)
8) Implement a dynamic Allied sitemap endpoint on the server:
   - GET https://allied.nursenest.ca/sitemap-allied.xml
   - It must include ONLY allied public routes and ONLY for published careers/pages.
   - It must NOT include any nursing routes.
   - Include lastmod from DB updated_at where available.
   - Include: /, /careers, /pricing, and each published career landing + public topic pages (if created).
9) DO NOT modify the existing nursing sitemap. Leave it exactly as-is.

E) ROBOTS.TXT (separate by host)
10) Serve different robots.txt depending on hostname:
   - On allied host: allow indexing of public landing pages and sitemap-allied.xml.
   - Disallow crawling of:
     /admin
     /account
     /checkout
     /api
     /qbank (if it returns question payloads)
     /mock-exams (if it returns exam payloads)
     Any routes that expose paid content or user-specific content.
   - On nursing host: keep current robots.txt behavior unchanged.

11) Robots for allied should include:
   - Sitemap: https://allied.nursenest.ca/sitemap-allied.xml

F) META + CANONICAL
12) Ensure canonical tags:
   - Allied pages canonical to allied.nursenest.ca
   - Nursing pages canonical to www.nursenest.ca
13) Add OG/Twitter metadata for allied with “NurseNest Allied” branding.

G) VALIDATION + SAFETY
14) Add automated checks:
   - A test that confirms allied sitemap contains no nursing URLs.
   - A test that confirms nursing sitemap contains no allied URLs.
   - A test that robots on allied disallows /admin and /api.
15) Ensure free sample questions are rate-limited and do not leak paid bank content.

DELIVERABLES
- Implemented sitemap-allied.xml endpoint, host-based robots.txt, content seed script, and public career landing pages.
- Provide a short list of files changed and where to edit career SEO copy going forward.

END.
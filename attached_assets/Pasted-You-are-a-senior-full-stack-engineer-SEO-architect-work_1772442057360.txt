You are a senior full-stack engineer + SEO architect working inside the NurseNest codebase.

GOAL
Create 4 “exam silo” hub pages and programmatically pull/link existing content into them (question banks, printable tests, mock exams, lessons/blog, flashcards where relevant) WITHOUT changing the layout, styling, or behavior of any existing pages/components outside these new silo routes.

DO NOT
- Do NOT refactor global layout, theme, or shared components used elsewhere.
- Do NOT change existing routes, URLs, slugs, or page layouts.
- Do NOT alter Stripe products, pricing logic, paywalls, or entitlements.
- Do NOT move or rename existing content in the database.
- Do NOT regenerate content or rewrite user-facing content body.
- Do NOT change existing SEO/hreflang system except adding metadata for new pages.
- Do NOT change the “printable tests” generator output format.

DO
- Add new silo routes/pages only.
- Reuse existing list/grid/card components where available (import and use them as-is).
- Pull existing content by querying existing APIs/data sources and display it in organized sections.
- If tagging/categorization does not exist, implement a minimal mapping layer (configuration map) that only affects these silo pages.

====================================================================
CREATE THESE 4 NEW SILO ROUTES (HUB PAGES)
====================================================================

1) /nclex-rn-practice-questions
2) /nclex-pn-practice-questions
3) /rex-pn-practice-questions
4) /np-exam-practice-questions

Each page should:
- Have a clear H1 (keyword-targeted)
- Include a short intro paragraph (do not overwrite existing marketing copy elsewhere)
- Include 4–6 sections that pull existing content into the hub
- Include internal links out to the existing pages (NOT duplicating full content)
- Be indexable, with canonical tags pointing to itself
- Include an FAQ accordion (static content is fine initially)
- Add FAQ schema JSON-LD on the page only

====================================================================
SECTION REQUIREMENTS (PULL EXISTING CONTENT, DON’T REWRITE IT)
====================================================================

On each silo page, create these sections and populate them from existing content sources:

A) “Start Here” (CTAs)
- Button 1: Start Free Practice -> link to existing free entry route (or /free-practice if exists)
- Button 2: Take a Mock Exam -> /mock-exams (or existing)
- Button 3: View Printables -> existing printables/store category route

B) “Question Banks” (List/Grid)
- Show the relevant question bank products/pages for that exam
- Use existing product cards/list components
- Each card links to the existing product page (no new product pages)

C) “Mock Exams” (List/CTA)
- Show relevant mock exam sets (or a featured CTA if only one route exists)
- Link to existing /mock-exams filtering if supported; otherwise link to /mock-exams

D) “Printable Tests” (List/Grid)
- Show relevant printable packs for that exam
- Link to existing printable product pages

E) “Pharmacology by System” (Optional if exists)
- Show pharm packs relevant to that exam tier
- Link out to existing pharm products/pages

F) “Lessons / Guides” (Blog/Learn content)
- Show recent or most relevant lesson/blog posts for that exam
- Link to existing posts (do not duplicate post bodies)

G) “Flashcards” (Only if exam silo includes them)
- If you have standalone flashcards subscription, show a small module and link to that page.
- IMPORTANT: Do NOT rename the standalone flashcards product globally; only label correctly within this page.

====================================================================
HOW TO SELECT “RELEVANT” CONTENT PER SILO
====================================================================

Prefer using existing metadata or categories if present (examType, tier, tags, language, productCategory, etc).

If no metadata exists:
- Create a minimal config mapping file ONLY used by silo pages, e.g.:
  config/silos.ts
  with arrays of product slugs/ids for each silo:
  NCLEX_RN: { questionBanks: [...], printables: [...], lessons: [...], pharm: [...] }
  NCLEX_PN: { ... }
  REX_PN: { ... }
  NP: { ... }

This mapping must not affect any other page.

====================================================================
IMPLEMENTATION CONSTRAINTS
====================================================================

- Make silo pages visually consistent with the site by reusing existing layout wrappers and components.
- Do not modify global navigation; if you add links to silos, add them ONLY if there is already a safe place to add links without layout changes (otherwise skip).
- Do not increase bundle size significantly; lazy-load where appropriate.
- Ensure these pages work across current language routing strategy (if you have /en, /fr etc), but do not create duplicate English content across folders. Follow the existing i18n approach.

====================================================================
SEO + INDEXING (NEW PAGES ONLY)
====================================================================

For each silo page:
- Title tag + meta description targeting the exam
- Canonical self
- Add FAQ schema JSON-LD
- Add internal links to 10–30 relevant existing URLs
- Add these 4 silo URLs to the sitemap generator (HIGH PRIORITY)

====================================================================
DELIVERABLES
====================================================================

1) Create the 4 silo page route files and any minimal helper modules used ONLY by them.
2) If needed, create config/silos.ts mapping file with placeholders and document how to add slugs.
3) Confirm: No existing routes/pages/components had their layout changed.
4) Provide a list of files changed and a brief summary of what each change does.

Proceed to implement now.
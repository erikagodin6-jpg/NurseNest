You are working on the live NurseNest production system.

THIS IS AN ACTIVE PRODUCTION INCIDENT WITH ALERT FLOODING AND SYSTEM INSTABILITY.

The system is currently in a failure loop:
- memory exceeding limits (RSS > 500MB)
- emergency mode repeatedly activating
- deploy freeze triggered
- exam and service failures
- alerts firing every few seconds
- system not stabilizing between incidents

YOUR FIRST OBJECTIVE IS TO STOP THE INCIDENT LOOP IMMEDIATELY.

==================================================
PHASE 1 — STOP THE BLEEDING (DO THIS FIRST)
==================================================

1. DISABLE ALERT SPAM
- Immediately implement alert deduplication
- Same alert type within a time window must NOT send new emails
- Group alerts into a single active incident
- Add cooldown (minimum 5–10 minutes per identical alert)

2. FORCE LOAD SHEDDING
When memory exceeds threshold:
- BLOCK heavy routes completely:
  - exam generation
  - large exam loading
  - AI generation
  - PDF/image generation
- Return lightweight fallback responses instead
- DO NOT allow system to attempt heavy operations while unstable

3. HARD LIMIT CONCURRENCY
- Cap concurrent requests to heavy endpoints
- Reject or queue excess requests
- Prevent request pileups that amplify memory usage

4. STOP SYNTHETIC TEST HAMMERING
- If synthetic tests are repeatedly hitting failing endpoints:
  - throttle them
  - pause them temporarily
- Prevent monitoring from worsening the outage

5. FORCE CLEAN STATE
- Restart the runtime cleanly after above protections are in place
- Ensure no stuck processes or memory fragmentation

==================================================
PHASE 2 — STABILIZE MEMORY (IMMEDIATE)
==================================================

1. Audit what is consuming memory RIGHT NOW
2. Identify top offending routes/jobs
3. Temporarily disable or degrade:
   - large exam endpoints
   - bulk content loading
   - heavy generation features

4. Ensure:
- memory drops after requests complete
- system is no longer stuck at critical threshold

==================================================
PHASE 3 — VERIFY SYSTEM IS STABLE
==================================================

System is considered stabilized ONLY if:
- memory no longer spikes to critical repeatedly
- emergency mode is not re-triggering continuously
- alerts are no longer firing every few seconds
- core routes respond successfully

DO NOT PROCEED until stability is confirmed.

==================================================
IMPORTANT RULES
==================================================

- Do NOT add new features
- Do NOT attempt full system redesign yet
- Do NOT allow alerts to continue spamming
- Do NOT allow heavy routes to execute during instability
- PRIORITIZE STOPPING THE LOOP over everything else

RETURN:
- what was causing the loop
- what was disabled or throttled
- current memory behavior
- confirmation that alert spam has stopped
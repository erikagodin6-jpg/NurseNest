Fix the NurseNest pricing system where the tier pricing pages load but no pricing cards render for RPN, RN, NP, or Allied Health.

Current problem:
- The pricing page shell loads correctly.
- Tier pages show the title, description, guarantee text, and footer.
- But the actual plan cards do not appear.
- This is affecting multiple tiers, so the issue is likely in shared pricing data, shared rendering logic, tier filtering, or Stripe plan mapping.

Primary goal:
Restore visible pricing cards and checkout actions for all pricing tiers:
- RPN
- RN
- NP
- Allied Health

Pricing to restore exactly:

RPN
- 1 Month: CAD 24.99
- 3 Months: CAD 59.99
- 6 Months: CAD 99.99
- 1 Year: CAD 149.99

RN
- 1 Month: CAD 29.99
- 3 Months: CAD 74.99
- 6 Months: CAD 119.99
- 1 Year: CAD 179.99

NP
- 1 Month: CAD 39.99
- 3 Months: CAD 99.99
- 6 Months: CAD 159.99
- 1 Year: CAD 239.99

Allied Health
- 1 Month: CAD 24.99
- 3 Months: CAD 59.99
- 6 Months: CAD 99.99
- 1 Year: CAD 149.99

Required tasks:

1. Find the shared pricing page component and the shared pricing plan renderer.
2. Find the current pricing data source and determine why no plan cards are rendering.
3. Restore or create a central pricing config file used by all pricing pages.
4. Make all tier pages render plan cards dynamically from the central pricing config.
5. Ensure each tier page displays 4 plan cards:
   - 1 Month
   - 3 Months
   - 6 Months
   - 1 Year
6. Each plan card must show:
   - duration label
   - price in CAD
   - short supporting text if applicable
   - CTA button labeled “Unlock Full Access”
7. Highlight the 6 Month plan with a badge:
   - “Most Popular”
8. Ensure the free one-day pass remains separate and is not shown as a paid plan.
9. Remove any lingering paid trial plans from the tier pages:
   - 1 Day Trial
   - 3 Day Trial
   - Buy 1 Day Trial
   - Buy 3 Day Trial

Technical requirements:

10. Do not hardcode price markup directly inside JSX across multiple files. Use one central config source.
11. Do not break the existing Stripe checkout integration.
12. If Stripe price IDs are already mapped elsewhere, reconnect the visible cards to the existing checkout function instead of removing Stripe logic.
13. If the UI is filtering plans by tier slug, normalize and verify the tier keys:
   - RPN
   - RN
   - NP
   - ALLIED
   or Allied Health if that is the actual key used in the codebase
14. If duration labels are used in routing or Stripe mapping, normalize them consistently:
   - monthly
   - 3month
   - 6month
   - yearly
15. If the current code expects Stripe prices from API data and nothing is returned, add a safe fallback to local pricing config so the cards still render.
16. Ensure there are no silent null returns or empty array states hiding the cards.
17. Add an empty-state debug guard for development so if plans fail to load again, the UI shows a visible warning rather than rendering nothing.

Tier-specific requirements:

18. Ensure the following pricing routes render plans correctly:
   - /pricing/rpn
   - /pricing/rn
   - /pricing/np
   - /pricing/allied
   or the actual allied pricing route if different
19. If Allied Health has its own subdomain or separate pricing component, ensure the same fix is applied there too.
20. Keep existing intro copy, guarantee text, FAQ links, and supporting sections unless they conflict with rendering the plan cards.
21. Keep the text:
   - “Prices shown in CAD.”
   - “No contracts. Cancel anytime.”
   - “Secure checkout powered by Stripe.”

UX requirements:

22. Keep the pricing pages visually premium and clean.
23. Ensure plan cards render correctly on desktop and mobile.
24. Maintain good spacing, button hierarchy, and responsive stacking.
25. Keep the “Most Popular” emphasis on the 6 Month plan.

Validation requirements:

26. Confirm these routes all render exactly 4 paid plans each:
   - /pricing/rpn
   - /pricing/rn
   - /pricing/np
   - /pricing/allied
27. Confirm the 6 Month plan is highlighted on each page.
28. Confirm each CTA still triggers the correct checkout path.
29. Output a summary with:
   - root cause found
   - files changed
   - pricing config location
   - components fixed
   - Stripe mappings reviewed
   - any manual review still needed

Important:
- Do not remove webhook logic
- Do not expose Stripe secrets
- Do not change subscription verification logic
- Do not invent extra plans
- Do not leave the pricing sections blank
- Preserve production-safe behavior
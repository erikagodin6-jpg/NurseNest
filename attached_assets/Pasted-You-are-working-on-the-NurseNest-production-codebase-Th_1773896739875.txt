You are working on the NurseNest production codebase.

This is a FULL PLATFORM STABILITY, MEMORY PROTECTION, EXAM RESILIENCE, FAILOVER, AND ALWAYS-AVAILABLE DELIVERY HARDENING INITIATIVE.

The platform has been experiencing:
- CAT exams failing to start
- question pool unavailable errors
- memory pressure / memory alerts
- slow or unstable publish/runtime behavior
- risk of loading too much data at once
- user-facing dead ends when exams fail

I need a PERMANENT, PRODUCTION-GRADE solution.

==================================================
PRIMARY GOAL
==================================================

NO VALID USER SHOULD EVER HIT A DEAD END WHEN TRYING TO START AN EXAM.

The system must guarantee:

1. no full-bank loading into memory during exam start
2. no unlimited queries
3. no fragile CAT-only startup path
4. no single point of failure for question delivery
5. no dead end if primary question source fails
6. graceful fallback to a usable exam experience
7. observability into exact failure reasons
8. resilience under load and concurrency spikes
9. question availability even if one storage/query path fails
10. strict entitlement and tier safety

==================================================
SECTION 1 — AUDIT ALL CRASH-RISK AND MEMORY-HEAVY PATHS
==================================================

Perform a full audit of the production codebase and identify every code path that can overload memory, block requests, or crash exam delivery.

Audit at minimum:

- CAT exam startup
- standard exam startup
- question pool building
- question fetching/hydration
- exam session creation
- attempt persistence
- admin content lists
- publish/build steps
- AI generation endpoints
- content audit endpoints
- any route that loads large banks or large arrays
- all caches that may hold large blobs

Find:
- unlimited queries
- missing LIMITs
- loading full question banks into memory
- loading full rationales/options/images when not needed
- repeated cloning/transformation of large arrays
- giant caches
- N+1 query paths
- slow queries
- expensive synchronous work inside request handlers
- duplicate request pathways
- missing fallbacks
- single points of failure

Return exact files/functions/routes involved.

==================================================
SECTION 2 — REFACTOR EXAM DELIVERY TO INCREMENTAL LOADING
==================================================

Refactor the exam/CAT architecture so the system NEVER loads all questions at once.

At exam start:
- query only lightweight candidate metadata
- do not hydrate full question content
- database must perform filtering
- choose the first question from metadata only
- fetch full content only for the selected question

Metadata-only fields may include:
- id
- exam type
- tier
- profession/stream
- region
- language
- difficulty
- blueprint/domain/category
- active/published flags
- premium/entitlement flags

Do NOT load at startup:
- full question stem/content for large banks
- full options for all questions
- rationales for all questions
- media/assets
- giant nested content blobs

For each next question:
- use persisted attempt/session state
- compute next candidate from metadata/query logic
- fetch only the next selected question in full

Persist only lightweight attempt state:
- attempt id
- session id
- current question id
- served question ids
- answered question ids
- theta/difficulty/adaptive state
- timing/progress
- score state

==================================================
SECTION 3 — QUERY SAFETY AND DATABASE FILTERING
==================================================

Move all filtering into the database wherever possible.

Add explicit LIMITs and selected-column queries to all question/exam/content endpoints.

Never:
- fetch a giant pool and filter in application memory
- SELECT *
- leave list endpoints unbounded

Add or verify indexes for common filters:
- exam_type
- tier
- stream/profession
- language
- region
- published
- active
- premium/entitlement fields
- difficulty
- blueprint/domain
- exam_id / bank_id mappings
- attempt/session foreign keys

Audit query plans if needed and optimize heavy query paths.

==================================================
SECTION 4 — CAT QUESTION POOL HARDENING
==================================================

Trace the exact CAT startup and question pool pipeline.

Find:
- frontend start button flow
- API client
- CAT start endpoint
- pool builder
- exam-to-bank mapping
- tier/entitlement filters
- region/language filters
- active/published filters
- CAT validation rules
- minimum pool threshold logic
- first-question selector

For every CAT start request, log counts after each step:
- raw candidate count
- after exam mapping
- after tier filter
- after entitlement filter
- after region filter
- after language filter
- after active/published filter
- after CAT validation
- threshold required
- final usable count

Group rejection reasons:
- missing difficulty
- missing correct answer
- fewer than 4 options
- missing stem/content
- unpublished
- inactive
- wrong tier
- wrong exam type
- wrong language
- wrong region
- malformed metadata
- stale cache
- invalid mapping

Return the exact root cause if the final pool is unusable.

==================================================
SECTION 5 — NORMALIZED RESPONSE CONTRACTS
==================================================

Create one canonical contract for exam start responses.

CAT success:
{
  "ok": true,
  "mode": "cat",
  "attemptId": "...",
  "sessionId": "...",
  "examId": "...",
  "question": {...},
  "progress": {...},
  "meta": {...}
}

Fallback success:
{
  "ok": true,
  "mode": "fallback_standard_exam",
  "attemptId": "...",
  "sessionId": "...",
  "examId": "...",
  "question": {...},
  "progress": {...},
  "meta": {
    "reason": "cat_pool_unavailable"
  }
}

Handled failure:
{
  "ok": false,
  "errorCode": "CAT_POOL_EMPTY",
  "message": "This exam is temporarily unavailable.",
  "retryable": true
}

Frontend must support these cleanly and must not rely on inconsistent field names.

==================================================
SECTION 6 — GRACEFUL DEGRADATION AND FALLBACKS
==================================================

Never leave users at a dead end.

If CAT startup fails, fallback in this order:
1. same exam in fixed-form standard mode
2. smaller adaptive-ready practice block from eligible questions
3. emergency static validated fallback bank for that tier/stream
4. only then a specific unavailable state with incident logged

User-facing copy should be clear:
“Adaptive mode is temporarily unavailable. We’ve started a standard exam instead.”

Failures in nonessential systems must never block exam start:
- analytics
- recommendations
- badges
- side widgets
- secondary dashboards
- notification hooks

==================================================
SECTION 7 — BACKUP, FAILOVER, AND ALWAYS-AVAILABLE QUESTION DELIVERY
==================================================

Implement a multi-layer question storage and failover strategy.

The system must support:

A. PRIMARY SOURCE OF TRUTH
- main database remains the canonical write source

B. SECONDARY READ SOURCE
- add/read from a read replica or secondary read path for exam delivery where appropriate

C. PREVALIDATED QUESTION SNAPSHOTS
- generate validated snapshots of question banks / exam pools
- store them separately from the primary DB (for example in object storage or equivalent)
- snapshots must be versioned, integrity-checked, and scoped by tier/region/language/exam type

D. EMERGENCY FALLBACK EXAM BANKS
- maintain curated, validated fallback banks for:
  - RPN
  - RN
  - NP
  - Allied
- these must be entitlement-safe and always usable for standard exam delivery

E. FAILOVER READ LOGIC
- if primary live pool generation fails, try:
  1. replica/read source
  2. prevalidated snapshot
  3. emergency fallback bank

F. CLEAR SOURCE LABELING
- log which source served the exam:
  - primary_db
  - read_replica
  - validated_snapshot
  - emergency_fallback_bank

Do NOT create a second uncontrolled source of truth.
The backup/failover sources must be read-only or generated from the primary source in a controlled way.

==================================================
SECTION 8 — CACHE DISCIPLINE
==================================================

Audit and fix all caches.

Safe to cache:
- counts
- IDs
- eligibility summaries
- difficulty distributions
- exam availability metadata
- lightweight validated pool metadata

Unsafe to cache:
- giant full question-bank payloads
- full exam sessions per user
- huge hydrated question objects
- entire content libraries in memory

Every cache must have:
- max size
- TTL
- eviction policy
- observability
- memory-safe defaults

Remove caches that can grow unbounded or store giant blobs.

==================================================
SECTION 9 — TIMEOUTS, CONCURRENCY, BACKPRESSURE
==================================================

Add safety controls system-wide.

1. TIMEOUTS
- DB statement timeouts
- API handler timeouts
- background job timeouts
- external AI timeout guards

2. CONCURRENCY LIMITS
- per-endpoint concurrency caps for expensive routes
- exam start concurrency protection
- AI generation concurrency limits
- publish/build concurrency limits

3. RATE LIMITING
- per-user rate limit for start exam
- per-IP / global limit where appropriate
- prevent burst abuse and duplicate spikes

4. BACKPRESSURE
- if the system is stressed, reject/downgrade nonessential work
- protect critical exam/user routes first

5. CIRCUIT BREAKERS
- if a dependency is failing, stop hammering it
- fallback instead of repeated expensive retries

==================================================
SECTION 10 — WORKER ISOLATION
==================================================

Separate live user traffic from heavy background work as much as possible.

Move off the main request path:
- AI content generation
- audits/scans
- export/PDF/image generation
- cache warmups
- statistics aggregation
- bulk repair jobs
- large content normalization

Live requests should only do what is necessary to serve the current user action.

==================================================
SECTION 11 — CONTENT VALIDATION BEFORE PRODUCTION USE
==================================================

Do not let malformed content reach live exam startup.

Add validation during generation, ingestion, publish, and admin workflows.

Validate at minimum:
- stable id
- non-empty stem/content
- answer options present and valid
- correct answer present
- difficulty metadata where CAT requires it
- blueprint/domain/category tags if required
- published/active status
- valid language/region/tier mappings
- valid exam/bank mapping

Reject, quarantine, or repair malformed rows before they affect production.

Add repair tools or scripts if data normalization is needed.

==================================================
SECTION 12 — DUPLICATE ACTION PROTECTION / IDEMPOTENCY
==================================================

Protect critical actions from duplicate execution.

Implement:
- disable exam start buttons during request
- idempotency keys or equivalent protection for attempt creation
- request deduplication for rapid repeat starts
- duplicate purchase/publish/start protection

No double-tap or retry burst should create multiple attempts or heavy duplicate work.

==================================================
SECTION 13 — FRONTEND HARDENING
==================================================

Refactor frontend exam start UX to be resilient.

Requirements:
- loading state while exam starts
- no duplicate submissions
- support mode = cat
- support mode = fallback_standard_exam
- handle structured error codes
- do not crash on missing nested fields
- do not assume currentQuestion/session values always exist
- display safe, specific messages
- retry only if retryable = true

Remove generic dead-end messages wherever a better handled state exists.

==================================================
SECTION 14 — OBSERVABILITY / DIAGNOSTICS
==================================================

Add structured logging and health visibility.

For exam startup log:
- requestId/traceId
- user id
- tier/subscription/role
- exam id / exam type
- source used (primary_db / read_replica / snapshot / fallback_bank)
- row counts after each filter
- validation rejection reasons
- whether full hydration occurred
- startup duration
- query durations
- fallback used
- exact error code / root cause

Add admin/system diagnostic visibility for:
- CAT starts attempted
- CAT starts succeeded
- fallbacks used
- failures by error code
- empty pool counts by tier/language/region
- malformed question counts
- largest payload endpoints
- slowest queries
- most memory-heavy routes
- queue depth
- concurrency spikes
- startup times

==================================================
SECTION 15 — FEATURE FLAGS / KILL SWITCHES
==================================================

Add emergency toggles for unstable subsystems.

Allow rapid enable/disable of:
- CAT mode
- AI-enhanced explanations
- heavy export features
- expensive diagnostics
- large background jobs
- nonessential widgets

One unstable subsystem must not bring down the whole platform.

==================================================
SECTION 16 — TEST COVERAGE
==================================================

Create or update tests for:

1. CAT exam starts successfully with valid pool
2. pool empty after tier filter
3. pool empty after language filter
4. invalid/malformed questions in pool
5. DB/session creation failure
6. first question selection failure
7. fallback_standard_exam returns and renders correctly
8. emergency fallback bank works if live pool fails
9. start exam does not load full bank into memory
10. one-question-at-a-time fetching works
11. large banks do not crash startup
12. duplicate start tap does not create two attempts
13. replica/snapshot/fallback source selection works
14. entitlement rules are enforced correctly
15. nonessential subsystem failure does not block exam start
16. cache limits and TTL behavior are safe
17. concurrency limits prevent endpoint overload

==================================================
SECTION 17 — OUTPUT REQUIRED
==================================================

After implementation, provide:

1. exact root causes found
2. exact crash-risk code paths found
3. files changed
4. endpoints changed
5. old memory-heavy code paths removed
6. query strategy changes
7. indexes added
8. timeouts added
9. concurrency / rate-limit protections added
10. fallback behavior added
11. backup/failover question storage strategy implemented
12. how replica/snapshot/fallback selection works
13. cache fixes made
14. admin diagnostics added
15. tests added
16. remaining risks, if any

IMPORTANT:
- Do not do a superficial patch
- Do not just improve the text of the error message
- Do not keep any code path that hydrates full question banks at startup
- Do not create a second uncontrolled source of truth
- Do not leave users at a dead end
- Do not break existing non-CAT exams
- Do not weaken entitlement/paywall rules
- The final system must be resilient, memory-safe, failover-capable, and production-ready
Fix the critical CAT exam launch bug immediately. Right now when a user tries to start a CAT exam, the system returns “exam start failed. contact admin.” This is a production-blocking issue and must be fully traced and fixed at the root cause, not hidden behind better error text.
Priority
Treat this as a critical P0 bug.
CAT exams must start successfully for eligible users across all intended tiers.
Do not stop at a frontend patch. Diagnose frontend, API, auth, database, exam session creation, and CAT configuration logic.
Bug to fix
Current behavior:
User clicks Start CAT Exam
System returns: “exam start failed. contact admin.”
Expected behavior:
CAT exam session initializes successfully
User is routed into the CAT exam
Exam session, adaptive settings, and first question load correctly
If there is a real eligibility/config issue, show a useful human-readable message instead of a generic admin error
Required investigation
Audit the full CAT exam start flow end to end:
Frontend launch flow
inspect the Start CAT Exam button handler
inspect request payload being sent
confirm required params are present such as tier, exam type, user id, mode, blueprint, language, question pool, or exam config id
confirm no null or undefined values are being passed
confirm the frontend is calling the correct CAT start endpoint
confirm auth headers/session cookies are included
Backend/API exam start endpoint
locate the CAT exam creation endpoint and trace why it is returning failure
log the full failure reason server-side
inspect validation logic for:
missing user
missing subscription/access
missing exam blueprint
missing CAT config
missing question pool
invalid tier mapping
failed session insert
failed transaction
bad exam state defaults
replace silent catch blocks or vague generic errors with structured internal logging
Database/session creation
verify CAT exam session records can be created successfully
verify required tables/columns exist and match current code expectations
inspect whether schema drift, missing migration, or null constraints are breaking session creation
verify exam attempts, question assignments, adaptive state, and timing metadata are stored correctly
check foreign keys and required relations for:
user
tier
exam blueprint
CAT config
question set/pool
active exam session
CAT-specific configuration
verify CAT exams have valid adaptive configuration loaded for every supported tier
confirm there is a valid blueprint and question source for RPN, RN, NP, and any other supported tracks where CAT is offered
verify minimum question counts and domain coverage requirements are met
verify the start logic does not fail when one topic bucket is undersupplied
confirm confidence tracking, rationale settings, and exam mode flags are not interfering with startup
Auth/subscription/access control
verify the user has the correct access and the check is not incorrectly blocking valid users
verify free vs paid gating is not breaking CAT startup
verify role/tier resolution is correct
if access is denied, return a clear message such as:
“Your current plan does not include CAT exams.”
“This CAT exam is not yet configured for this tier.”
do not return “contact admin” unless absolutely unavoidable
Routing and state
verify successful exam creation returns a valid exam session id and route
verify the app navigates correctly to the CAT exam screen after creation
verify no client-side state bug, stale store, or invalid redirect is causing the failure message
confirm race conditions are not causing the exam start response to be lost or overwritten
Implementation requirements
Add robust logging around CAT exam startup on both client and server
Surface precise internal error codes in logs, but show clean human-readable messages to users
Add defensive validation before attempting session creation
Ensure the start endpoint returns actionable error details to the frontend
Remove any generic catch-all that swallows the real root cause
If a required CAT config is missing, fail gracefully and clearly
If the issue is missing seeded data, create or repair the missing CAT config/data
User-facing behavior
If CAT exam startup succeeds:
immediately create session
route user into exam
load first question
show proper loading state during startup
If startup fails for a real reason:
show specific messages such as:
CAT exam configuration is not available for this tier yet
Your subscription does not include CAT exams
Unable to create exam session, please retry
include retry handling
never show the vague “exam start failed. contact admin.” message again
Add diagnostics
Please add temporary debug instrumentation to identify:
exact endpoint called
payload sent
auth state at time of launch
response status and error body
database insert failure details
missing config ids or blueprint ids
question pool availability counts
Likely root causes to check first
missing or broken CAT config record
frontend sending wrong exam type or tier key
exam session insert failing due to schema mismatch
invalid access-control check
missing question pool or blueprint mapping
stale API route after recent refactor
bad environment/config variable
null examId/sessionId returned and treated as failure
Hard requirements
Fix the root cause, not just the symptom
CAT exam must be testable after repair
do not break standard exams, quiz mode, question bank mode, or analytics
preserve exam attempt tracking and adaptive logic
keep tier routing intact
Acceptance criteria
Before finishing, verify all of the following:
clicking Start CAT Exam successfully launches a CAT exam
a valid CAT exam session record is created
first question loads correctly
no generic “contact admin” error remains
server logs identify exact startup failures if any remain
valid users can launch CAT exams for supported tiers
unsupported tiers show a clear message instead of crashing
no regressions to regular exams or test bank flows
After fixing, run a full launch test for:
RPN CAT
RN CAT
NP CAT if supported
desktop and mobile
authenticated paid user
any free user state if CAT should be gated
If missing seed/config data is the cause, create the necessary CAT exam config, blueprint mapping, question pool linkage, and default exam initialization logic so CAT startup works reliably going forward.
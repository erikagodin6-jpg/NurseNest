You are a senior full-stack engineer. Fix the issue where edits made in my browser-based admin editor show on the live site but do NOT show in the Replit preview.

Context:
- I can edit content in the browser at home (admin editor) and it renders correctly on the live site.
- When I open Replit and use the Replit preview, those edits do not appear.
- This indicates Replit preview is reading from a different data source (different DB/API) or serving cached/static content.

Hard rules:
1) Do not guess. Diagnose with evidence first.
2) Before changing code, provide a 10-line root cause summary with:
   - Which API host the live site uses for content fetch
   - Which API host the Replit preview uses for content fetch
   - Which database each environment points to (prod vs dev)
   - Whether the preview is rendering static TS content instead of API content
3) Provide proof with screenshots/logs from the Network tab or server logs.
4) Then implement the minimum fix.

Diagnosis steps you MUST perform:
A) Identify where the page content comes from in both environments
- In the live site: inspect Network calls to find the content endpoint(s) used by the page and copy the request URL + response snippet.
- In Replit preview: do the same.
- If Replit preview makes NO content API call, find the TypeScript/JSON data file being used (e.g., contentMap) and list the exact import chain.

B) Compare environment configuration
- Print relevant env variables and where they’re read in code:
  - DATABASE_URL (server)
  - VITE_API_BASE_URL / NEXT_PUBLIC_API_URL (client)
  - Any PROD_API_URL, CONTENT_API_URL, etc.
- Confirm whether Replit preview server is connecting to a different DB than production.

Implement the fix (choose the correct one based on findings):

CASE 1: Different DB or different API host
- Make Replit preview point to the intended data source.
Options (choose and implement the safest):
  Option A (recommended): Keep dev and prod separate, BUT add an admin “Export content” (prod) and “Import content” (dev) tool so I can sync intentionally.
  Option B: Point Replit dev to production DB for preview parity (only if explicitly safe; add big warnings and read-only mode).
- Ensure the client uses a consistent API base URL in Replit (no hardcoded production URL in one place and relative path in another).

CASE 2: Static fallback content is being rendered in Replit preview
- Ensure editable pages always fetch from API for their content when available.
- Keep TS/static content only as explicit fallback and show a visible “Fallback mode” badge to admins when API data isn’t used.
- Remove/disable any logic that prefers static content over DB content in dev.

CASE 3: Caching/service worker/query cache
- Identify whether SW/PWA caching exists; disable in dev or add cache-busting for content fetch.
- If React Query/SWR is used, ensure save mutations invalidate the exact query keys used in the preview.
- Add Cache-Control headers on content endpoints in dev to prevent stale preview.

Required deliverables:
1) Root cause summary (10 lines max) with evidence.
2) List of files changed.
3) Exact env vars to set in Replit Secrets (names + example values, no secrets exposed).
4) A simple /api/health endpoint that returns:
   - environment name (dev/prod)
   - git commit hash
   - sanitized DB host identifier (e.g., “prod-db” vs “dev-db”)
So we can verify immediately which environment Replit preview is using.
5) Proof after fix: show that changing content in the browser editor appears in Replit preview after refresh.
You are a senior full-stack engineer. The current “cram product design” tool is not a real design builder and image generation is not working. Implement a Canva-like Product Builder MVP inside NurseNest TODAY and ensure image generation works.

Do NOT defer this to a future session. Build the MVP now.

Hard Constraints
- Must not break blog generator/scheduler.
- Must be production-ready enough to create sellable PDFs.
- Must persist projects and allow editing later (no lost work).
- Must support medically correct image generation with NO TEXT in the image.
- Must export:
  1) print-quality PDF
  2) thumbnails (600x600 and 1200x630)
- Must integrate with Store: create a product using exported PDF + thumbnail.

========================================================
PHASE 0 — ROOT CAUSE: IMAGE GENERATION & “GENERATE” BROKEN
========================================================
1) Identify why “Generate” does nothing:
- missing click handler
- API route not implemented
- request blocked by auth
- missing env key
- error swallowed
Add:
- visible loading state
- success/failure toast
- console logging for dev
- server logging with request id

2) Identify why image generation does not work:
- missing endpoint
- wrong payload
- CORS/auth issues
- provider key missing
Fix it end-to-end.

========================================================
PHASE 1 — PRODUCT BUILDER MVP (CANVA-LIKE)
========================================================
Add Admin route:
- /admin/product-builder

A) Project Dashboard
- Create Project
- Duplicate Project
- Edit Project
- Export Project (PDF + thumbnails)
- “Create Store Product from Export”

B) Editor UI Layout
Left sidebar tabs:
1) Elements (shapes, boxes, borders, lines)
2) Text (heading, subheading, body, lists)
3) Uploads (images)
4) Icons (basic medical icon set)
5) AI Images (medical image generator)
6) Brand (colors, logo, components)
7) Pages (multi-page)

Center:
- Canvas workspace with page size selector
  - Letter 8.5x11, A4, 5x7, US Letter landscape
- Zoom controls (50%–200%)
- Grid + snap-to-grid toggle
- Rulers optional

Right panel:
- Properties editor for selected element:
  - position (x,y), size (w,h), rotation
  - fill color, border color, border width
  - corner radius
  - shadow toggle
  - opacity
  - text: font, size, weight, line height, alignment, color
  - layer actions: bring forward/back, lock/unlock

C) Core Editing Features (Must-have)
- Drag/drop elements
- Resize/rotate
- Multi-select
- Copy/paste
- Undo/redo (at least 20 history steps)
- Layers panel
- Autosave every 10 seconds + manual save
- “Saved” indicator

D) Multi-page Support
- Add page
- Duplicate page
- Reorder pages
- Delete page
- Set “Cover Page” (used for thumbnail)

Implementation guidance:
Use Fabric.js OR Konva (choose one). Implement cleanly.

========================================================
PHASE 2 — MEDICAL IMAGE GENERATOR (NO TEXT)
========================================================
Inside “AI Images” tab:

Inputs:
- Subject (e.g., COPD lungs, nephron, heart chambers)
- Style: “clean vector medical illustration” / “pastel clinical diagram” / “realistic anatomy”
- Background: transparent/white
- Palette: match brand

When generating, ALWAYS enforce:
- “no text”
- “no labels”
- “no letters”
- “no watermark”
- “no numbers”

Output:
- Generate 4 variations
- Click to insert into canvas
- Save to asset library with tags

If generation fails:
- show error toast with reason
- log full server error (without secrets)
- fallback: allow upload of images manually

========================================================
PHASE 3 — ASSET LIBRARY + LOGO INSERTION
========================================================
Add asset library:
- Upload images (png/jpg/webp/svg if supported)
- Store generated images
- Store icon set
- Store NurseNest logo variants (preloaded)
- Tagging + search
- Drag assets into canvas

========================================================
PHASE 4 — EXPORT (PDF + THUMBNAILS)
========================================================
Implement reliable WYSIWYG export:

A) PDF Export
- Export all pages to one PDF
- Correct page size
- High resolution render
- Embed fonts if possible
- Save export record in DB

B) Thumbnail Export
- 600x600 square (cropped from cover)
- 1200x630 OG image (cropped from cover)
- Allow admin to adjust crop area with a simple crop UI
- Save to exports

Show export progress state.

========================================================
PHASE 5 — STORE INTEGRATION
========================================================
Add “Create Store Product from Project Export”:
- Pre-fill product:
  - title
  - slug
  - descriptions template
  - tier target
  - exam target
- Attach exported PDF as File URL
- Attach thumbnail as Cover Image
- Create product in DB and show it in Manage Products list immediately

========================================================
PHASE 6 — QUALITY OF LIFE FUNCTIONS (ADD THESE)
========================================================
Add components/templates:
- “Clinical Pearl” box component
- “Red Flags” box component
- “Medication Table” header row component
- “Lab Values” table component
- “Step-by-step” numbered layout component

Add formatting helpers:
- align left/center/right
- distribute evenly
- group/ungroup
- lock background
- duplicate element
- consistent padding presets

========================================================
PHASE 7 — RELIABILITY & TESTING (NON-NEGOTIABLE)
========================================================
Add a manual test checklist and confirm pass:

1) Create project, add text/box/image, autosave, refresh, confirm persists
2) Generate AI image (no text), insert, save
3) Multi-page add/reorder
4) Export PDF and open file to confirm correct sizing
5) Export thumbnails and confirm look correct
6) Create store product from export
7) View store product page and confirm thumbnail and download file are attached
8) No console errors; errors show to admin if any

DELIVERABLES
- Files changed/added
- New endpoints
- DB migrations
- Screenshot not required, but describe UI and flows clearly
- Confirm image generation works (or clearly describe configuration needed like env vars)

Proceed now. Start by fixing the broken Generate handlers and image generation endpoint, then implement the MVP editor with Fabric/Konva, then export pipeline, then store integration.
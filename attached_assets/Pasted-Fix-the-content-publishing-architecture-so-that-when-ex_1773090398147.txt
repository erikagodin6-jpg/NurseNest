Fix the content publishing architecture so that when exam questions or flashcards are created in admin/dev mode, they are saved to the same persistent production database and become accessible on the live site.

Goal:
Right now generated questions/flashcards appear only inside Replit preview/dev, but they do not show up on the live deployed website. I need a true create → save → publish → live display workflow.

Please do the following:

1. Audit the full content pipeline
- Find exactly where exam questions and flashcards are currently being generated.
- Identify whether they are being stored only in:
  - local component state
  - temporary server memory
  - a dev-only JSON file
  - a preview-only database
  - an admin-only cache
- Trace the full path from:
  admin generator → API route → database write → publish state → frontend fetch on live site.

2. Make all generated content persist in the production database
- Ensure generated exams and flashcards are written to the real persistent database used by the deployed app.
- Do not store generated content only in memory, mock arrays, or temporary files.
- If there are separate dev/prod environments, make sure the production deployment points to the correct production database.
- Add proper schema fields if missing.

3. Add a proper publish workflow
For both exam questions and flashcards, create a content lifecycle with fields such as:
- id
- title
- slug
- type (exam, flashcard, lesson, etc.)
- tier (free, RPN, RN, NP)
- category
- subcategory
- language
- status (draft, published, archived)
- visibility (private, admin, public, subscriber)
- createdAt
- updatedAt
- publishedAt
- createdBy
- source
- isStoreReady
- isLive

Generated content should not stay trapped in draft/admin state unless intentionally set that way.
There must be a clear way for admin to mark content as published/live.

4. Make the live site query only published content
- Update frontend/API queries so the live site fetches published/live records from the production database.
- Ensure the public exam pages and flashcard pages read from database records, not hardcoded mock content.
- Make sure role filtering still applies:
  - free sees only free
  - RPN sees only RPN + free
  - RN sees only RN + free
  - NP sees only NP + free
- Ensure unpublished admin drafts do not appear publicly.

5. Fix deployment/environment issues
- Verify all environment variables used in deployment.
- Confirm the live site is connected to the same intended database as the admin generator.
- Check whether Replit preview and deployed production are using different DATABASE_URL values or different storage backends.
- If they are different, refactor so production-generated content is stored where the live app can access it.
- Print safe server logs confirming:
  - content created
  - DB row inserted
  - status assigned
  - live query returns the item

6. Create admin publishing controls
In the admin dashboard add:
- Save Draft
- Publish Now
- Unpublish
- Archive
- Preview
- Duplicate
- Delete
Show publication status clearly for each generated exam/flashcard.

7. Add automatic cache invalidation / refresh
- If the site uses caching, ISR, or static generation, revalidate relevant routes after publish.
- Ensure newly published content appears on the live site without needing manual code edits.
- Refresh category pages, exam lists, flashcard lists, and search indexes after publishing.

8. Add validation tests
Create tests or debug verification for this workflow:
- Generate new exam
- Save to DB
- Publish it
- Confirm it appears on live exam listing
- Confirm it opens on the live site
- Confirm role restrictions work
- Repeat same flow for flashcards

9. Remove fake/demo-only behavior
- Eliminate any logic where generated questions are only appended to local arrays or shown only in preview mode.
- Replace all mock/demo content paths with real database-backed CRUD.
- If mock data is still needed for fallback, separate it clearly so it never overrides production content.

10. Give me a final report in code comments or admin notes
Show:
- what was broken
- what was using dev-only storage
- what was changed
- which tables/models/endpoints were updated
- how published content now reaches the live site

Important implementation rules:
- Do not break the current site styling.
- Preserve current tier logic and brand structure.
- Use the existing stack and patterns already in this project.
- Keep the solution production-safe and scalable.
- Make minimal but complete schema/API/frontend changes necessary to make generated content truly live.

At the end, provide:
1. the exact files changed
2. the schema changes
3. the API routes involved
4. the admin publish flow
5. a short explanation of how I can test it on the live site
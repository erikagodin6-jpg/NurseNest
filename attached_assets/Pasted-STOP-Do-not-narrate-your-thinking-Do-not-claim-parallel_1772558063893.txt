STOP. Do not narrate your thinking. Do not claim parallel subagents. Do not say you “read files” unless you quote actual file paths and the code you found.

You are implementing “Real Exam Mode” for mock exams in the existing NurseNest codebase AND you must also add:
- a persistent RealExamModeConfig store (DB-backed),
- deterministic seeded CAT behavior,
- and integration tests for paywall + stop rules.

OUTPUT RULES (NON-NEGOTIABLE):
1) Your response must be ONLY:
   - Files changed list
   - Unified diffs for each file (git diff format)
   - A short manual test checklist
   - A short automated test checklist (commands + what should pass)
2) No commentary, no session plans, no “I will…”, no “now I’m going to…”.
3) Do not invent file paths. Use the actual repo structure.
4) If you need to inspect files, the diff must show the edits that prove it.

CORE PRODUCT REQUIREMENTS:
A) Remove user choice of question count for official mock exams.
B) Official mock exams must feel like real exams (UI + flow).
C) Paywall: official mock exams are premium; free users can only take readiness exams with limited review.
D) Passing is not percent-correct for CAT exams. Show PASS/FAIL only + domain bands.
E) NP boards use scaled scoring (AANP vs ANCC separately).

SCOPE (IMPLEMENT EXACTLY THESE 7 TASKS):
T001 - Blueprint Types & Configs
- Extend blueprint config to include examType = "CAT" | "SCALED"
- Split NP boards into AANP and ANCC
- Add Readiness exam configs (shorter fixed count, limited review)
- Store blueprint weights by domain in a tier config object

T002 - CAT Engine (new file/module)
- Implement a simplified adaptive engine that:
  - Maintains an internal ability estimate (theta)
  - Selects next item difficulty based on theta and blueprint needs
  - Implements stop rules:
      1) 95% confidence rule around passing standard
      2) max length rule
      3) run-out-of-time rule (only if your system tracks time; otherwise omit)
- Return pass/fail outcome relative to configurable passing standard.
- Must be deterministic given a seeded RNG (seed stored per session).

T003 - RealExamModeConfig (DB-backed)
- Create a new DB table (or equivalent persistent store) to hold:
  - per examId or examType: minLength, maxLength, timeLimitSeconds (nullable), passingStandardLabel, passingStandardValue (numeric), scaledScoreMin/Max, scaledPassCut, domainBandCutoffs
  - feature toggles: showTotalCount (false for CAT), allowReviewForFree (false), allowRationalesForFree (false)
- Add server routes to read this config and use defaults if none exist.
- Add admin-only ability to update this config (server-side route; UI optional).

T004 - Setup Page UI
- Rename mock exam setup to “Real Exam Mode”
- Hide question-count and customization selectors for official mode
- Provide two modes:
   1) “Official Mock” (locked, paywalled)
   2) “Readiness Exam” (free preview)
- Display badges: CAT vs Scaled
- Ensure the exam type automatically matches selected tier/exam.

T005 - Session Page Behavior
- Official Mock (CAT):
   - Serve one item at a time
   - Do NOT show total question count
   - Auto-stop as soon as stop rule triggers
- Official Mock (Scaled):
   - Fixed-length per blueprint
- Readiness exam:
   - Fixed short count
   - Limited review depending on role
- Persist session state in DB as currently done; do not break existing session tables.
- Ensure no leakage of premium content in free mode (no full rationales; block exports).

T006 - Report Page Output
- Official Mock (CAT):
   - PASS/FAIL banner only (no percent correct)
   - Domain performance bands: Above / Near / Below standard (based on band cutoffs in config)
   - Remediation plan for weakest 2–3 domains
- Official Mock (Scaled):
   - Display scaled score + pass threshold
   - Show domain bands (not raw percent unless admin config says so)
- Readiness:
   - Show “Readiness Result” label + CTA to upgrade

T007 - Integration Tests
- Add tests that cover:
  1) Paywall: free user cannot start Official Mock (403)
  2) Free user can start Readiness exam (200)
  3) CAT engine determinism: same seed + same answers => same outcome
  4) Stop rule: 95% confidence triggers early stop in a controlled test scenario
- Use the project’s existing test stack (Vitest/Jest/Supertest/etc). Do NOT introduce a new framework unless none exists.

IMPORTANT DEFAULT CONFIG VALUES (admin-editable later):
- NCLEX-RN passing standard label: 0.00 logits
- NCLEX-PN passing standard label: -0.18 logits
- REx-PN minLength = 90
- AANP scaled score: 200–800 pass >= 500
- ANCC scaled score: max 500 pass >= 350

ALSO REMOVE FAKE LOG STRINGS:
- Remove any “parallel subagents”, “loaded delegation skill”, or similar text from UI/logs if present.

IMPLEMENTATION METHOD:
STEP 1: Locate mock exam feature files by searching for:
- “mock”, “mock exam”, “exam setup”, “report”, “session”, “blueprint”, “readiness”
STEP 2: Implement all changes with minimal disruption.
STEP 3: Ensure server-side gating is authoritative (never rely on client-only checks).

Return unified diffs + checklists only.
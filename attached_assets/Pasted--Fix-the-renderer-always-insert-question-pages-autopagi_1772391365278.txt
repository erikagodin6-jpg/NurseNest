“Fix the renderer: always insert question pages + autopaginate overflow”
Paste this into Replit:
The PDF renderer currently outputs cover + TOC + section dividers but does NOT render the question pages. I need you to fix the rendering layer so the validated JSON question set is always laid out into pages.

REQUIREMENTS:
1) Create a dedicated “QuestionPage” layout component/template that renders:
   - header with: NurseNest + “REx-PN QBank” + page number
   - question number + category + type label
   - scenario + stem
   - options (A–D or SATA list)
   - on the next page (or below if space): Correct Answer + Rationale + Clinical Pearl
2) Implement automatic pagination:
   - If a question block does not fit on the current page, split it across pages.
   - Never drop content. Never overlap. Never render off-canvas.
3) Implement a “content audit” step before export:
   - Confirm that the PDF contains N questions.
   - Confirm that the first pages are in this order: Cover (page 1), TOC (page 2), then questions start on page 3 (or page 4 if you include an Instructions page).
4) Add a debug export mode:
   - Overlay bounding boxes and show “remaining space” so we can visually confirm pagination correctness.
5) Ensure the questions can be rendered in two modes:
   - “Questions only” (no answers/rationales)
   - “Questions + rationales” (full workbook)
   The mode should be a toggle.

DELIVERABLE:
Update the renderer so question pages actually appear, and pagination prevents truncation.
This addresses the big issue your sample shows: questions never got inserted (or got dropped).
Prompt 3 — “Fix page ordering: cover is page 1, not page 2”
Paste this into Replit:
My exports sometimes have the title/cover on page 2. Fix the export ordering so the PDF always starts with the cover on page 1.

REQUIREMENTS:
1) Identify where pages are collected before PDF assembly (array order, filesystem glob, sort, etc.).
2) Ensure stable ordering:
   - Use explicit page indices from the rendering pipeline (not filename sorting).
   - If filenames are used, enforce zero-padded numbering and numeric sort (page-01, page-02…).
3) Add an assertion before PDF write:
   - pages[0].type must be "cover"
   - pages[1].type must be "toc"
   - if not, throw an error and log the actual sequence.

DELIVERABLE:
Implement stable ordering and a preflight assertion that prevents wrong ordering from shipping.
Prompt 4 — “Make it sellable: qbank-style formatting + consistent difficulty + anti-fluff”
Paste this into Replit when the above is fixed:
Now that questions render correctly, upgrade the generator to produce “question bank” quality (UWorld/Archer style) and consistent formatting.

REQUIREMENTS:
1) Enforce consistent difficulty: medium-to-hard clinical judgment
2) Enforce REx-PN style:
   - prioritization, safety, delegation, scope, infection control, meds, mental health, LTC/community
3) Each set must include:
   - 60% MCQ, 20% SATA, 10% PRIORITY, 10% DELEGATION
4) Add an “Exam Blueprint Mix” summary page at the end:
   - counts by category and question type
5) Add unique rationales (no copy/paste patterns). Add a “trap explanation” for at least one distractor per question.

DELIVERABLE:
Upgrade prompts + validators to enforce the distribution and uniqueness checks.
If you want the fastest single-shot prompt (minimal refactor)
If you can’t refactor everything yet, use this as an emergency patch:
Add a hard post-processing step after AI returns:
- Count how many questions were generated.
- If fewer than requested, automatically call the AI again to generate only the missing questions (e.g., “generate questions 31–50 only”), then append.
- Do NOT export until the combined set hits N exactly.
Also add overflow splitting: any question block that exceeds the page height must be auto-split onto a new page (never dropped).

PROMPT START
Build a “content creator” system inside my NurseNest app so I can create and publish lessons and flashcards myself without using AI generation.
Current problem: I can add content in an admin area, but it doesn’t reliably show on the public Lessons pages or Flashcards pages. I need a real content pipeline and a template-driven UI.
Goal: After this is built, I should be able to create and publish most content manually (like a CMS) using forms, and it should automatically appear on the website.
Do NOT create a new subscription tier system. Do NOT hardcode lesson lists. Do NOT rely on AI-generated content. This is about content management and rendering.
CORE REQUIREMENTS (MUST)
1) Single Source of Truth Data Model
Create or fix a unified content model for Lessons and Flashcards.
Lesson fields:
id (uuid)
title
slug (auto-generated, unique)
category (select)
system (select; optional)
level (select: pre-nursing / RPN / RN / NP)
tier (select: free / paid)
status (select: draft / published / archived)
excerpt (short description for menus)
objectives (array of strings)
sections (array of structured blocks, see below)
clinicalPearls (array of strings)
references (array of strings/links; optional)
createdAt, updatedAt, publishedAt
Flashcard set fields:
id (uuid)
title
slug
category
level
tier
status
description
cards (array of { front, back, tags(optional) })
linkedLessonIds (optional)
Store these in a real database (preferred) or in JSON files if database isn’t available, but it MUST be queryable by the frontend. Pick the simplest robust option already compatible with the repo.
2) Block-Based Lesson Template (So I Can Fill It In)
Implement “sections” as content blocks so I can build lessons without coding.
Section/block types must include:
heading
paragraph
bulletList
callout (type: tip / warning / exam / clinical)
table (rows/cols)
image (url + caption; optional)
divider (optional)
quiz (references quiz objects; optional)
Frontend must render all block types safely.
3) Admin Content Builder UI (No AI)
Create an admin dashboard that lets me:
Create/edit/delete lessons
Create/edit/delete flashcard sets
Edit objectives, pearls, references
Add/reorder sections via drag-and-drop or up/down buttons
Add block types from a dropdown
Save as draft
Publish/unpublish with one click
Preview the public lesson view while in admin (preview mode)
4) Public Pages Must Render Dynamically
Update the public Lessons index page to:
Fetch lessons from the data source
Show only status = published for public users
Filter/sort by category, level, tier
Include search
Use excerpt and metadata for cards
Update the public Lesson detail page to:
Load by slug
Render blocks in order
Handle missing/empty fields without breaking
Show 404 or friendly “not found” for invalid slugs
Update the Flashcards index and detail pages similarly:
Only show published sets
Render cards from stored data
If a set links to a lesson, show a “Related lesson” link
5) Drafts & Access Rules
Draft lessons must be visible ONLY in admin and preview mode
Published lessons visible publicly (respecting free/paid gating if already exists)
Archived lessons hidden everywhere except admin
If my paid gating already exists, integrate with it. If not, stub the check with a simple placeholder but do not redesign my auth system.
6) Reliability & Verification
Add a simple admin “Content Diagnostics” panel that checks:
lessons without slugs
duplicate slugs
published lessons with empty title/empty sections
flashcard sets with zero cards
This panel should show fix suggestions.
7) Keep Design Consistent
Do not redesign NurseNest branding. Keep existing layout/components where possible. Only add necessary admin UI.
IMPLEMENTATION INSTRUCTIONS
Inspect current codebase for:
existing lesson routes/pages
current admin system
current data storage (db/prisma/supabase/json/etc)
Choose the least invasive approach:
Prefer using existing DB if present
If none exists, implement SQLite + Prisma (or the simplest already supported stack)
Refactor so the admin and public pages read/write the SAME tables/collections/files.
Remove hardcoded lesson lists and replace with dynamic queries.
Create seed/example content:
2 sample lessons (one draft, one published)
1 sample flashcard set linked to a lesson
DEFINITION OF DONE (MUST PASS)
Creating a lesson in admin → saving → publishing → appears in Lessons menu immediately
Draft lessons are hidden from public but visible in admin preview
Flashcard sets render cards and appear publicly when published
No hardcoded lesson menus remain
No runtime crashes when a lesson is missing optional fields
Proceed to implement this now with clean, maintainable code.
PROMPT END
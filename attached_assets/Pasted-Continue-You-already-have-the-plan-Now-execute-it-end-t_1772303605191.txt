Continue. You already have the plan. Now execute it end-to-end with a hard, testable output.

PHASE 1 — Produce a concrete audit (no code yet)
1) Output a prioritized list of issues found, grouped by:
   A) Translation/i18n coverage
   B) Language switcher + SSR/first paint issues (if applicable)
   C) Admin credentialed fetch + adminView consistency
   D) Publish/save/schedule reliability
   E) SEO/canonical/hreflang/sitemaps/indexing hygiene
2) For each issue, include:
   - File path(s)
   - Function/component name
   - Exact condition causing the bug
   - Risk level (High/Med/Low)
   - Fix approach (1–2 sentences)

PHASE 2 — Implement fixes in this order
Order (do not skip):
1) Translation coverage for Anatomy/Physiology and all lesson detail pages
2) Language switcher single source of truth and no “flash of English”
3) Centralized credentialed fetch helper + adminView contract applied everywhere
4) Remove any “dbContent null => auto-open editor” logic site-wide
5) Replace reload-based publish flows with response-based state update + navigation
6) Blog minimum word enforcement for future + currently scheduled posts (server authoritative)
7) SEO correctness: canonical/hreflang/sitemap consistency across locales

PHASE 3 — Translation/i18n implementation details (must do)
1) Confirm the i18n provider wraps ALL route trees:
   - Pre-Nursing
   - Anatomy
   - Physiology
   - Lessons index and lesson detail routes
   - Tools pages, compare pages, pricing/upgrade, login/register
2) Identify and remove hard-coded English strings in A&P:
   - Convert to translation keys
   - Add missing FR entries
3) Ensure dynamic content fetch returns localized content:
   - Update GET content endpoints to accept locale
   - Resolve translation record selection
   - Controlled fallback + admin-only “missing translation” banner
4) Add a “Missing FR coverage” admin report:
   - Endpoint: GET /api/admin/i18n/missing?locale=fr
   - UI table: content type, slug, missing fields count
   - Filter by category (Pre-Nursing vs A&P etc.)

PHASE 4 — AdminView + credentials system (must do)
1) Create a single shared fetch utility used by all admin modules:
   - credentials: "include"
   - attach adminView=true when admin session present
2) Make server endpoints consistent:
   - Public: published-only + tier gating
   - Admin: adminView=true bypass tier/status (authenticated + authorized only)
3) Ensure publish/save returns the saved object with slug/status and client uses it directly.

PHASE 5 — Publish/scheduler reliability (must do)
1) Remove all window.location.reload() in admin flows
2) After publish/save:
   - setState(savedItem)
   - router.replace/view route
   - invalidate caches (if present)
3) Scheduler:
   - Idempotent publishing
   - Detailed logs
   - Failure states persist and visible in admin logs

PHASE 6 — Blog minimum word rule for future + currently scheduled posts
1) Add BLOG_MIN_WORDS config (db > env > default 1500)
2) Compute wordCount (strip HTML/markdown)
3) Save draft: allowed; set belowMinimum
4) Manual publish: block with 422 unless admin bypass
5) Scheduled publish: also enforce; if too short:
   - do not publish
   - mark job failed with reason POST_TOO_SHORT
   - keep post scheduled or revert to draft (choose one and apply consistently)
6) Admin UI: live word count + warning + disable publish unless override

PHASE 7 — Verification (non-negotiable)
After implementing, run and document these checks:
1) Switch EN→FR on:
   - Pre-Nursing landing
   - Anatomy landing
   - Physiology landing
   - a lesson detail page under each category
   - pricing/upgrade
   - compare page
Result must be 100% FR UI; content uses FR variant or controlled fallback.
2) Admin views:
   - open a paid/tier-gated lesson while logged in as admin
   - open a draft lesson as admin
Both must load without being forced into editor.
3) Publish:
   - publish a lesson without reload; confirm it opens view and persists after refresh
4) Scheduler:
   - confirm scheduled blog post under minimum does not publish and logs POST_TOO_SHORT
5) SEO:
   - confirm canonical/hreflang present on A&P FR pages and lesson pages

PHASE 8 — Final output format
When finished, output:
1) Files changed list
2) New endpoints list
3) Summary of how translations are now guaranteed on switch
4) Summary of adminView + credentialed fetch behavior
5) Test checklist results

Proceed now. Start by locating the Anatomy/Physiology route tree and showing exactly why it doesn’t load French dictionaries today (provider wrapping, namespace loading, or hard-coded strings).
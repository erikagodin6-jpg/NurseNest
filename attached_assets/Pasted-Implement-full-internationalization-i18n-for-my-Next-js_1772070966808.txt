Implement full internationalization (i18n) for my Next.js / React website with multi-locale routing, translation extraction, hreflang, RTL handling, and sitemap support.

IMPORTANT FIRST STEP – Router Detection (Critical)

Before making ANY changes, detect which Next.js routing system the project uses.

• If the project contains an /app directory → It uses the App Router  
• If the project contains a /pages directory → It uses the Pages Router  

You MUST implement i18n using the correct router model. Do not mix patterns. Do not assume.

Routing Requirements

Use locale path prefixes only (no query parameters, no hash-based routing).

Locales to support:

en (English)
fr (French)
es (Spanish)
fil (Filipino/Tagalog)
hi (Hindi)
zh (Chinese)
ar (Arabic)
ko (Korean)
pt (Portuguese)
pa (Punjabi)
vi (Vietnamese)
ht (Haitian Creole)
ur (Urdu)
ja (Japanese)
fa (Farsi/Persian)

URL structure must follow:

/en/...
/fr/...
/es/...
/fil/...
/hi/...
/zh/...
/ar/...
/ko/...
/pt/...
/pa/...
/vi/...
/ht/...
/ur/...
/ja/...
/fa/...

Do NOT use query parameters for language switching.

i18n Library Rules

Use a well-supported Next.js-compatible i18n solution.

• If App Router is detected → Prefer next-intl  
• If Pages Router is detected → Prefer react-i18next or next-i18next  

Do NOT build a custom translation engine.

Primary Tasks

1) Extract ALL user-facing text and replace hard-coded strings with translation keys:

- lessons.tsx → all lesson system titles + all lesson names  
- pre-nursing.tsx → ALL UI text  
- anatomy.tsx → ALL UI text  

User-facing text includes headings, buttons, labels, placeholders, helper text, empty states, filters, errors, tooltips, and any visible UI strings.

2) Generate stable, consistent translation keys.

Example structure:

pages.preNursing.title
pages.anatomy.title
common.startLesson
common.searchPlaceholder
ui.filters.sortBy
lessons.systems.cardiology
lessons.items.hyperkalemia

Keys must be readable and consistent. Avoid random naming.

3) Translation Resources

Create locale JSON files for ALL locales:

/locales/en.json
/locales/fr.json
/locales/es.json
/locales/fil.json
/locales/hi.json
/locales/zh.json
/locales/ar.json
/locales/ko.json
/locales/pt.json
/locales/pa.json
/locales/vi.json
/locales/ht.json
/locales/ur.json
/locales/ja.json
/locales/fa.json

Rules:

• en.json is authoritative source  
• Use existing translations if available  
• Only auto-translate missing strings  
• Do NOT translate brand name "NurseNest"  
• Preserve medical accuracy  

4) Router-Specific Implementation

If App Router detected:

• Use app/[locale]/ directory structure  
• Implement next-intl configuration correctly  
• Ensure layouts and navigation work per-locale  
• Preserve SSR / static behavior  

If Pages Router detected:

• Use pages/[locale]/ or middleware-based locale handling  
• Implement next-i18next or react-i18next correctly  
• Do not break existing routes  

5) Locale Switching

Implement a language switcher that:

• Preserves the current path  
• Falls back to English if translation missing  
• Persists locale selection (cookie or storage)  
• Does NOT use query parameters  

6) RTL Support (Critical)

Arabic (ar) and Farsi (fa) must render RTL.

• Apply dir="rtl" at HTML/root level  
• All other languages remain LTR  
• Do not redesign layout  

7) SEO Requirements

Implement hreflang across all indexable pages.

• Add alternates for ALL locales  
• Include x-default → English  
• Canonical tags must be locale-self-referencing  
• Prevent duplicate-content conflicts  

8) Sitemap Requirements

Update sitemap generation:

• ONLY include https://www.nursenest.ca URLs  
• NEVER include replit.app URLs  
• Include ALL localized URLs  
• Output must be valid XML  

If project is large → Use sitemap_index.xml referencing locale-specific sitemaps.

9) Safety Rules

• Preserve styling and UI structure  
• Preserve business logic  
• Add fallback behavior → English  
• Ensure build passes TypeScript & lint  
• Provide summary of changes  

Deliverables

• Router-correct i18n implementation  
• Updated components with translation calls  
• Locale JSON files  
• Language switcher  
• RTL handling  
• hreflang & canonical tags  
• Valid sitemap output  
Stop all retries on the NOT NULL email migration.

New critical finding:
- ALL 16 production users have NULL email
- ZERO production users have email populated

This means the issue is not isolated bad data. It is a schema/application design mismatch.

Do NOT backfill placeholder emails yet.
Do NOT force users.email to NOT NULL yet.
Do NOT copy development DB to production.

I need a root-cause audit first.

TASKS

1. Determine whether users.email is actually supposed to be required in this product.
   Answer clearly:
   - Is email required for all user accounts by business logic?
   - Or are some/all users created without email by design?

2. Trace the full user creation flow in production code.
   Audit every path that can create or upsert a user:
   - signup
   - login
   - OAuth/social auth
   - magic link/auth callback
   - guest/anonymous users
   - admin-created users
   - seed/demo/test users
   - mobile onboarding/bootstrap
   - webhook-created users
   Show me exactly where the users table is written to.

3. Determine where identity/email actually lives.
   Check whether:
   - email exists in an auth provider table but is not being copied into public.users
   - email is stored in a separate profile/account table
   - username is the real identifier instead of email
   - legacy rows were created before email support existed

4. Inspect production schema and related tables.
   Show:
   - users table columns
   - constraints
   - foreign keys
   - any auth/account/profile tables connected to users
   - whether there is already a unique constraint on username or another identifier

5. Compare development vs production assumptions.
   Identify exactly what code or migration introduced the assumption that users.email must now be NOT NULL.

6. Decide the correct fix based on actual architecture:
   CASE A: If email is truly required for all users
   - find the real source for each user's email
   - backfill production safely from the source of truth
   - then make users.email NOT NULL
   - patch all creation flows so email always persists

   CASE B: If email is optional or unused
   - revert/remove the migration that sets users.email NOT NULL
   - keep email nullable
   - align ORM/types/validation with production reality

7. Before making changes, show me:
   - root cause in plain English
   - which case applies (A or B)
   - exact files/functions causing the mismatch
   - exact migration/code change you propose

IMPORTANT
- Do not loop
- Do not regenerate the same failing migration
- Do not apply fake placeholder emails unless we explicitly decide email must be mandatory
- Do not overwrite production data
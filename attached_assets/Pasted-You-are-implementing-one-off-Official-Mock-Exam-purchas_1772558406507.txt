You are implementing one-off “Official Mock Exam” purchases (single, 3-pack, 10-pack, bundles) in NurseNest, with server-side paywalls. This must work alongside existing subscriptions/roles.

OUTPUT RULES:
- Respond ONLY with: files changed list, unified diffs, manual test checklist, automated test checklist.
- No narration.

GOALS:
1) Users can buy mock exams as one-off packs (like UWorld) without a subscription.
2) Official Mock sessions are gated by either:
   A) active subscription entitlement OR
   B) available mock exam credits for that exam scope.
3) Readiness exams remain free preview (limited review).
4) Server-side enforcement must be authoritative.

DATA MODEL:
Create tables (or add columns) as needed:

A) purchases
- id, userId, provider ("stripe"), providerCheckoutSessionId, providerPaymentIntentId (if available)
- status ("pending"|"paid"|"failed"|"refunded")
- createdAt, updatedAt

B) entitlements OR credit_ledger (choose one consistent approach)
Recommended: credit_ledger
- id, userId
- creditType: "MOCK_OFFICIAL"
- scope: "RExPN"|"NCLEXPN"|"NCLEXRN"|"AANP"|"ANCC"|"ALL_NURSING"|"ALL_ALLIED"
- quantity (integer, can be positive grant or negative consume)
- sourcePurchaseId nullable
- sessionId nullable (when consumed)
- createdAt

Add helper query views/functions:
- getAvailableCredits(userId, creditType, scope): sum(quantity) across matching scopes with fallback:
   - direct scope match, else ALL_NURSING/ALL_ALLIED if applicable.

C) products (optional but helpful)
- sku, name, creditType, scope, creditsGranted, priceId (stripe), active

STRIPE:
- Use existing Stripe integration in codebase.
- Create Stripe Checkout sessions for these SKUs:
  1) MOCK_1_[scope] grants 1 credit
  2) MOCK_3_[scope] grants 3 credits
  3) MOCK_10_[scope] grants 10 credits
  4) MOCK_BUNDLE_[scope] grants N credits
- On webhook checkout.session.completed (or payment_intent.succeeded depending on current setup), mark purchase paid and insert credit_ledger grants.
- Handle refunds if your webhook supports it: insert negative ledger entries.

PAYWALL / AUTHORIZATION LOGIC:
Implement server-side guard function canStartOfficialMock(user, scope):
- return true if user has subscription entitlement that includes official mocks
- else return true if available credits for (MOCK_OFFICIAL, scope) > 0
- else false

When starting an Official Mock:
- perform authorization check on server
- if allowed via credits (not subscription), consume 1 credit:
   - insert credit_ledger row with quantity = -1 and attach sessionId
   - do this in a DB transaction with session creation
- if subscription, do not consume credits

Readiness exams:
- always allowed
- enforce limited review/rationales on server

UI:
- On mock exam setup page:
  - Add “Buy Official Mocks” section with cards: Single, 3-Pack, 10-Pack, Bundle
  - Show current credits remaining for selected scope
  - “Start Official Mock” button:
     - if not entitled and credits=0, show paywall modal with buy options and subscription CTA
- On account page:
  - show credit balance by scope

SECURITY:
- Never trust client claims about subscription or credits.
- All enforcement happens on server endpoints that create/start sessions and fetch review/rationales.

TESTS (integration):
Add tests using existing stack (vitest/jest/supertest etc):
1) Free user with 0 credits cannot start Official Mock -> 403 code MOCK_PAYWALL
2) Free user can start Readiness exam -> 200
3) User with 1 credit can start Official Mock -> 200 and credits decremented
4) User with subscription can start Official Mock -> 200 and credits not decremented
5) Credits are scoped correctly: scope match preferred; ALL_NURSING fallback works

PRICING CONFIG (do not hardcode in code):
- Use environment or DB products table mapping SKUs -> Stripe priceIds.

DELIVERABLE:
- Implement DB migrations, server routes, webhook handling, UI purchase flow, and tests.
- Return diffs + checklists only.
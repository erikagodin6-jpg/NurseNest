0) Immediate emergency containment (do this first)
Goal: stop new users from downloading the full bank today.
A. Remove/disable client imports behind a feature flag
Add env flag VITE_QBANK_SOURCE=server|bundle
Default to server
If bundle is still needed temporarily for dev, keep it only on localhost.
B. Ship a hotfix that prevents rendering QBank unless authenticated
This is not security (bundle still leaks), but it prevents casual access while you migrate.
1) Delete premiumRouteGuard or make it strict-by-tier (no “any paid tier”)
You already added requireExactTier() — good. Now:
Rule
Any endpoint that touches tiered content must use:
requireAuth
requireExactTier("rpn" | "rn" | "np")
optionally requireAdmin for admin stuff
Action
Replace:
premiumRouteGuard(...)
with:
requireExactTier("rpn") etc.
Hard requirement: every premium route must declare its required tier explicitly. No inference.
2) Fix the identity bug: resolveTierFromRequest must NEVER accept userId from client input
This is critical: right now you have privilege escalation via ?userId=adminId.
Correct model
resolveAuthUser() determines the authenticated user from:
session cookie, or
JWT in httpOnly cookie, or
Authorization header (token) that maps to a user server-side
It must ignore req.body.userId, req.query.userId, params, and headers like x-user-id.
Implementation rule
Delete all fallbacks that accept userId from request.
If an endpoint needs a userId (admin actions), require admin and take it explicitly.
Example policy
normal endpoints: operate on req.user.id only
admin endpoints: can accept targetUserId, but requireAdmin must wrap it
3) Build protected server-side Question API (this is the real paywall)
Because content is currently compiled into the bundle, nothing else matters until you move it.
3A) Data model (minimum viable)
Create DB table (or whatever your persistence is) like:
questions
id (uuid/int)
tier (rpn|rn|np)
exam (rexpn|nclexpn|nclexrn|np)
topic (string)
difficulty (string)
stem (text)
options (json/text array)
correctIndex (int) (optional if you don’t want to send to client in practice mode)
rationale (text) (only send if entitled / after answer)
createdAt
3B) Endpoint design (secure + scalable)
You want endpoints that don’t allow tier override.
Fetch questions (practice)
GET /api/qbank (no tier param!)
server uses req.user.tier
filters DB by tier = req.user.tier
returns paginated items
Answer checking
POST /api/qbank/attempt
user submits questionId + selected option
server validates question belongs to user’s tier before scoring
server returns correct + rationale (or only after “submit”)
This prevents scraping “correct answers + rationales” in a single unauthenticated bulk download.
3C) Rate limiting + pagination
hard cap limit <= 50
require cursor pagination
log abnormal access patterns
4) Migrate your existing question files into the DB
You have 85 files, ~4.5MB.
Migration script approach
Write a one-off script that imports each file and inserts rows with the correct tier/exam/topic.
Keep a hash field to prevent duplicate inserts.
Important: Don’t store “tier” inferred from file path if paths aren’t consistent. Explicit mapping table is safer.
5) Remove all question imports from the client bundle
Once the API is live + data is in DB:
delete client/src/lib/question-pool.ts imports
replace with API calls
verify production build bundle does not include stems/options/rationales
This is the moment the paywall becomes real.
6) Add DB-layer tier filtering as a safety net (everywhere)
Even if middleware fails, queries must enforce:
WHERE userId = req.user.id for user-owned resources
WHERE tier = req.user.tier for tiered content
admin can override only under requireAdmin
7) Coverage audit: enumerate ALL endpoints and apply correct guards
You already found unguarded surfaces:
flashcard CRUD
decks
notes
progress
tests
What needs tier checks vs ownership checks?
If it’s user-generated content (their own flashcards/notes): ownership check is enough
If it can generate or access tiered/premium educational content: tier check is required
Your specific example /api/user-flashcards/ai-generate must be tier-locked because it creates premium-grade outputs that can be used as a substitute for paid access.
8) Automated tests (non-negotiable)
Add at least:
RN user cannot access NP endpoints (403)
RN user cannot request NP tier content even if they pass tier param (should be ignored)
user cannot escalate tier by passing userId anywhere (should be ignored entirely)
Also add a “bundle leak test”:
build client
grep for a known stem string
fail CI if found
Clear answer to the question: proceed now
Yes — proceed with the full fix immediately in this order:
Remove client-bundled questions (or flip to server source behind a feature flag today)
Lock identity (kill userId-from-request paths)
Replace premiumRouteGuard with requireExactTier per route
Build QBank API + migrate data + delete client question imports
Add DB filtering + tests
Concrete “Do this next” checklist (fastest path)
If you want the most efficient implementation sequence:
Day 1 (stop leak + stop escalation)
Delete userId fallbacks in resolveTierFromRequest
Replace premiumRouteGuard with requireExactTier on the 6 routes
Add a temporary “QBank disabled in production unless server mode” flag
Ship
Day 2–3 (real paywall)
Create questions table
Import script from the 85 files
Build /api/qbank + /api/qbank/attempt
Switch client to fetch from API
Remove question imports from client
Ship
Day 4 (hardening)
rate limits
audit remaining endpoints
CI bundle leak test
security logging
If you paste the route list (or routes.ts) and confirm whether you’re using sessions or JWT for resolveAuthUser, I’ll give you an exact mapping table of:
endpoint → required guard(s) → tier requirement → ownership requirement
so you (or Replit AI) can implement with zero ambiguity.
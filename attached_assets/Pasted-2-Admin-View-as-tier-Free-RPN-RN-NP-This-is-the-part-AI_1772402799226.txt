2) Admin “View as tier” (Free / RPN / RN / NP)

This is the part AI often fails because it tries to do it client-only. You need server-enforced impersonation with a secure mechanism.

Correct way (secure + actually works)

Add an admin-only feature that issues a short-lived “preview entitlement override” token.

That token is stored in an httpOnly cookie (or passed in Authorization header) and the server uses it to override entitlements during requests.

Rules

Only admins can set it.

It must expire quickly (ex: 30 minutes).

It must be obvious in the UI (“Viewing as: RN Paid — Exit Preview”).

The override must apply everywhere: API gating, stats gating, downloads, etc.

Data model options

Option A (simplest): store preview mode in session/JWT
Option B (more explicit): DB table admin_impersonation_sessions

admin_impersonation_sessions

id

admin_user_id

mode (FREE|RPN|RN|NP)

expires_at

created_at

Middleware logic (what must happen)

When request comes in:

Authenticate user

If user is admin AND has valid preview session -> set effective tier to preview tier

Else use actual entitlement tier from subscriptions

Prompt chunk to add (copy/paste)
ADMIN "VIEW AS TIER" (must be server-enforced):

Add an admin-only feature to preview the site as different tiers:
- FREE
- RPN_PAID
- RN_PAID
- NP_PAID

Requirements:
- Add UI in Admin panel: dropdown "View site as" with the 4 modes + button "Exit preview"
- This must work across the entire app, including server-gated APIs, not just frontend hiding.
- Implement a secure preview override:
  - Only admins can set it
  - Store preview mode in an httpOnly cookie or a signed token
  - Expires in 30 minutes
- Add endpoints:
  POST /api/admin/preview-mode  { mode: 'FREE'|'RPN'|'RN'|'NP' } -> sets cookie/token
  DELETE /api/admin/preview-mode -> clears cookie/token
- Modify entitlement resolution middleware:
  - Determine "effectivePlanTier" used for all gating
  - If admin preview cookie is present and valid -> effectivePlanTier = preview mode
  - Else effectivePlanTier = real subscription tier (from normalized entitlements)
- Add a visible banner when preview is active: "Preview Mode: RN Paid (Exit)"
- Include audit logging: admin_user_id, mode, timestamp, ip (minimal)
3) The one change that makes both features reliable: “Effective Tier” everywhere

Tell Replit AI explicitly:

Create a single helper on the server: getEffectiveTier(req)

Use it in every gated route:

peer stats

flashcard premium features

downloads

question limits

friend comparisons

AI rationales

Add this prompt line (important):

Create a single server utility getEffectivePlanTier(req) and refactor all premium gating to use it.
4) If you want the “View as…” to include real accounts too (optional)

There are two different admin needs:

Tier Preview (what you asked): simulate entitlements without becoming someone

Impersonate User (more powerful): literally view as a specific user account (requires extra safeguards)

If you also want “impersonate a user”, say so—but the tier preview is usually enough for QA.

Copy/paste “combined add-on” (short version)

If you want a single addendum you can append to your master prompt, paste this:

ADD THESE TWO FEATURES:

1) PayPal integration:
- Add PayPal subscription checkout alongside Stripe for the same tiers (RPN/RN/NP).
- Normalize all billing into subscriptions table (provider=stripe|paypal) and a single entitlements resolver.
- Add PayPal webhooks to update subscription status/period end.
- Refactor premium middleware to check entitlements, not Stripe directly.

2) Admin "View as tier":
- Admin-only dropdown to preview site as FREE, RPN paid, RN paid, NP paid.
- Must be server-enforced: implement preview override via signed httpOnly cookie/token expiring in 30 minutes.
- Add POST/DELETE endpoints to set/clear preview mode.
- Refactor all gating to use getEffectivePlanTier(req).
- Show a banner when preview is active + exit button.
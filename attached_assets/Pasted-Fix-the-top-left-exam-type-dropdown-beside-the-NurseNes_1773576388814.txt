Fix the top-left exam-type dropdown beside the NurseNest logo.

Problem:
The dropdown/button is rendering incorrectly. In the navbar, beside the logo, I can only see the icon and arrow, but I cannot see the dropdown label text at all. The trigger must clearly show its text at all screen sizes and in all themes.

What to fix:
1. Find the component for the top-left navbar dropdown beside the NurseNest logo.
2. Ensure the trigger displays:
   - icon
   - visible text label
   - chevron/down arrow
3. Fix any CSS causing the text to disappear, including:
   - text color matching background
   - opacity too low
   - overflow hidden
   - width too narrow
   - text-indent
   - font-size too small
   - line-height issues
   - z-index overlap
   - flex alignment problems
   - accidental `display:none`, `visibility:hidden`, or `color: transparent`
4. Make the trigger large enough to fit the full label without clipping.
5. Use inline-flex with proper spacing so icon, text, and arrow align horizontally.
6. Keep the styling clean and premium:
   - rounded border
   - subtle shadow or border
   - strong contrast
   - same NurseNest brand styling as the rest of the navbar
7. Do not let the text collapse on tablet or desktop.
8. On mobile, allow a shorter label if needed, but it must still remain visible.
9. Preserve the current dropdown functionality and routing.
10. Do not change unrelated navbar spacing or menu structure.

Implementation requirements:
- Inspect the dropdown trigger component and its parent container.
- Check for inherited text color issues from theme variables.
- Check whether the text span is being hidden by a utility class like `sr-only`, `hidden`, `truncate`, `overflow-hidden`, `w-0`, or a responsive class.
- Ensure the text span has:
  - visible color
  - `display: inline-block` or equivalent
  - enough width
  - `whitespace: nowrap`
- Ensure the button has:
  - `display: inline-flex`
  - `align-items: center`
  - `gap`
  - sensible min-width
  - padding left and right
- If there is a localization or language issue, make sure the label falls back to a default visible text string.

Expected result:
The dropdown beside the logo should clearly show its label text, not just the icon and arrow. It should look polished, balanced, and readable in light mode and across breakpoints.

After fixing it:
- verify on desktop width
- verify on tablet width
- verify on mobile width
- make sure no navbar items shift awkwardly
- make sure the dropdown still opens normally
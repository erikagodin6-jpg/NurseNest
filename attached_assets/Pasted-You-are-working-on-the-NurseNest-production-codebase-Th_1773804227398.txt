You are working on the NurseNest production codebase.

The current fail-safe exam protection screen is appearing in production with this message:

“We're having trouble loading this exam right now.
Your access is protected. Your progress, account, and subscription are safe.
Reference: INC-MMVH4TV2-ET5X
Retry (3 attempts remaining)
Return to Dashboard
Report Issue”

This confirms the fallback UI is working, but the actual exam recovery/load path is still failing.

Your task is to trace and permanently fix the FULL EXAM LOAD FAILURE PIPELINE behind this fallback.

PRIMARY GOAL

When exam loading fails, the system must:
1. determine exactly why it failed
2. preserve progress
3. retry intelligently
4. recover automatically when possible
5. only show fallback UI when necessary
6. successfully reload the exam for the user whenever possible
7. never dead-end valid paying users

==================================================
SECTION 1 — TRACE THE FAILURE PATH
==================================================

Audit the full live exam load pipeline from click to render, including:

- frontend exam start/resume page
- exam initialization request
- entitlement/subscription validation
- exam session lookup/creation
- question ID selection
- question batch fetch
- exam state hydration
- frontend render path
- retry path
- safe mode path
- incident reference generation
- report issue path

Find exactly where the load is failing.

Instrument every step with structured logs:
- incident reference
- user id
- exam id
- session id
- route
- function
- elapsed time
- memory state
- payload size
- retry count
- failure reason
- stack trace
- safe mode status

==================================================
SECTION 2 — CLASSIFY FAILURE TYPES
==================================================

Implement explicit error classification for exam load failures.

Possible classes:
- entitlement failure
- missing exam session
- corrupted exam session
- question batch missing
- database timeout
- oversized payload
- memory protection rejection
- malformed question data
- frontend parse failure
- frontend render failure
- stale resume pointer
- schema mismatch
- deployment/version mismatch
- network timeout

Every exam failure must map to a classified reason code, not a generic error.

==================================================
SECTION 3 — MAKE RETRY ACTUALLY RECOVER
==================================================

Refactor retry behavior so Retry is meaningful.

Retry must:
- attempt targeted recovery, not just repeat the same broken request
- re-fetch only missing exam metadata
- re-fetch only missing question batch
- rebuild session pointer if stale
- repair malformed local state if possible
- invalidate corrupted client cache
- retry with capped backoff
- stop retry storms
- preserve progress during all retry attempts

If first retry fails:
- try alternate recovery path

If second retry fails:
- attempt fresh server-side rehydration from persisted progress

If third retry fails:
- offer resume-safe exit and issue reporting with complete diagnostics

==================================================
SECTION 4 — SERVER-SIDE EXAM RECOVERY
==================================================

Implement a hardened recovery service for exams.

Requirements:
- validate exam session integrity
- reconstruct session from persisted progress if in-memory state is lost
- rebuild question order from stored question IDs
- fetch question content in small batches only
- skip or replace invalid question records safely
- never require full exam reload if only one batch is broken

Add:
- session checksum/version validation
- recovery from partial writes
- recovery from stale frontend state
- graceful rebuild after restart/deploy

==================================================
SECTION 5 — CLIENT-SIDE HARDENING
==================================================

Refactor the exam UI so it can survive partial failures.

Requirements:
- do not assume full exam payload exists
- support incremental hydration
- render when first valid batch arrives
- isolate broken question card data
- prevent one malformed question from crashing entire exam
- preserve timer/progress safely
- store minimal recovery metadata locally
- clear corrupted local exam cache automatically when needed

Retry button should:
- call a real recovery function
- display recovery stage
- not just refresh blindly

==================================================
SECTION 6 — INCIDENT REFERENCE SYSTEM
==================================================

Upgrade the incident reference system so every reference maps to useful diagnostics.

For every incident reference:
- store timestamp
- user id
- exam id
- session id
- route
- classified failure type
- memory state
- safe mode state
- request summary
- recovery attempts
- final disposition

Admin/support must be able to search an incident reference and see the actual cause.

==================================================
SECTION 7 — NEVER DEAD-END RULE
==================================================

If exam cannot fully recover, provide controlled fallback options:
- resume from last stable question
- restart exam without losing entitlement
- switch to reduced-load mode
- load one-question-at-a-time mode
- return to dashboard safely
- report issue with prefilled diagnostics

Do NOT leave user stranded on a generic error screen.

==================================================
SECTION 8 — FIND THE MOST LIKELY CURRENT BUGS
==================================================

Specifically inspect for:
- exam route returning too much data
- question objects too large
- corrupted exam session format
- progress pointer referencing missing question
- frontend expecting fields that no longer exist
- deploy mismatch between backend response and frontend type expectations
- retry button wired incorrectly
- localStorage/sessionStorage stale exam payloads
- memory safe mode blocking exam batch fetches
- database query returning null or malformed records
- exam initialization generating invalid question IDs

==================================================
SECTION 9 — REQUIRED DELIVERABLES
==================================================

1. Fix the code directly.
2. Show exactly where the failure was happening.
3. Show exactly which files were changed.
4. Explain the root cause in plain language.
5. Explain how retry was improved.
6. Explain how recovery now works.
7. Ensure the exam can recover safely after transient failures.
8. Ensure fallback remains trust-preserving for paying users.

SUCCESS CRITERIA

- exam loads successfully after transient failures
- retry performs real recovery
- progress is preserved
- incident references become diagnosable
- one bad batch/question does not kill the full exam
- users are never dead-ended
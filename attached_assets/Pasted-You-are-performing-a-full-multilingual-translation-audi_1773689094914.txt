You are performing a **full multilingual translation audit and repair for the entire NurseNest website (https://www.nursenest.ca)**.

The goal is to guarantee that **every page, lesson, UI element, navigation label, button, and system message is fully translated in every supported language** and that switching languages never leaves untranslated English fragments.

This audit must scan **the entire codebase and all content sources**.

CRITICAL REQUIREMENTS

• Do NOT break the existing routing system
• Do NOT remove existing translations
• Do NOT modify exam tier structure (RPN, RN, NP)
• Do NOT change URL structures unless fixing translation routing
• Do NOT allow untranslated text to appear in any language

All languages must remain synchronized.

---

STEP 1 — DISCOVER ALL SUPPORTED LANGUAGES

Scan the project and identify every active language folder and locale configuration.

Common examples may include:

en
fr
es
pt
zh
ja
ko
ar
fa
ur
vi
pa
ht
hi

Verify that every language declared in the translation system actually exists.

If a language is configured but missing translation files, create them.

---

STEP 2 — AUDIT THE ENTIRE SITE FOR TRANSLATABLE CONTENT

Scan and extract all user-visible text from:

Navigation menus
Buttons
Forms
Alerts
Error messages
Hero sections
Lesson titles
Lesson content
Quiz questions
Quiz answers
Practice explanations
Blog posts
Footer links
Tooltips
Modal windows
Authentication pages
Search results
Admin-generated content

Build a master translation key list.

---

STEP 3 — VERIFY TRANSLATION COVERAGE

For every translation key:

Check whether it exists in every language file.

Example structure:

en.json
fr.json
es.json
pt.json
zh.json
ja.json
ko.json
ar.json
fa.json
ur.json

If a key is missing in any language:

Automatically generate the translation using high-quality translation.

Do NOT leave keys blank.

---

STEP 4 — AUDIT LESSON CONTENT TRANSLATIONS

Scan every lesson page under:

/learn/

Ensure the following are translated for every language:

Lesson titles
Lesson body content
Headings
Clinical examples
Exam tips
Image captions
Practice questions
Answer explanations

If translated versions do not exist:

Generate full translations.

Do not leave partially translated lessons.

---

STEP 5 — VERIFY LANGUAGE SWITCHING

Audit the language switcher functionality.

Ensure that when a user changes languages:

The user stays on the same page.

Example:

/en/learn/hyperkalemia-effects-on-heart
→ switch language
/fr/learn/hyperkalemia-effects-on-heart

If a translation does not exist:

Fallback to English while displaying a translation notice.

Never break the page.

---

STEP 6 — ENSURE CONSISTENT URL STRUCTURE

Each page must exist in all supported languages using the same structure.

Example:

/en/learn/glasgow-coma-scale
/fr/learn/glasgow-coma-scale
/es/learn/glasgow-coma-scale
/zh/learn/glasgow-coma-scale

Do not generate duplicate slug variations.

---

STEP 7 — VALIDATE HREFLANG TAGS

Every page must include hreflang declarations linking all language versions.

Example:

hreflang="en"
hreflang="fr"
hreflang="es"
hreflang="zh"
hreflang="ja"
hreflang="ko"

Include an x-default fallback.

---

STEP 8 — DETECT PARTIAL TRANSLATIONS

Scan pages for mixed language content such as:

English headings inside translated pages
Untranslated buttons
Placeholder text in English

Automatically replace these with proper translations.

---

STEP 9 — TRANSLATE DYNAMIC CONTENT

Audit dynamic content sources such as:

AI-generated lessons
AI-generated blog posts
Practice questions
Admin-created resources

Ensure the translation pipeline automatically generates versions for all supported languages.

---

STEP 10 — ADD A TRANSLATION VALIDATION SYSTEM

Implement a validation script that runs during build.

This script must:

Scan all translation files
Detect missing keys
Detect untranslated strings
Prevent deployment if translation coverage is incomplete.

---

STEP 11 — VERIFY FRONTEND DISPLAY

Test the site in every language to confirm:

Navigation appears translated
Buttons are translated
Lessons load properly
Practice questions display correctly
No untranslated English text remains.

---

STEP 12 — FINAL REPORT

After completing the audit, generate a report listing:

Missing translations fixed
New translations generated
Broken language routes repaired
Translation coverage percentage for each language.

Target translation completeness:

100% for all supported languages.

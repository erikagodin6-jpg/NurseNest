You are working on the real NurseNest production codebase.

The mobile app scaffold is now complete and running in a separate mobile workspace. Your task is to produce the exact backend integration contract required to connect the app to the real NurseNest system.

Create a COMPLETE MOBILE BACKEND INVENTORY and CONTRACT.

Required outputs:
1. inventory all existing routes relevant to mobile:
   - auth
   - user/profile
   - subscription/entitlements
   - questions/practice
   - exams
   - CAT exams
   - flashcards
   - lessons/study guides
   - progress/analytics
   - categories/topics
   - settings/preferences
   - notifications if present

2. for each route document:
   - method
   - path
   - auth required or not
   - request params
   - request body
   - response shape
   - pagination/batching behavior
   - error response shape
   - whether it is mobile-safe as-is

3. inspect the real backend data models and document true shapes for:
   - User
   - AuthSession
   - Subscription / Entitlement
   - Question
   - QuestionOption
   - QuestionRationale
   - Exam
   - ExamSession
   - ExamResult
   - FlashcardDeck
   - Flashcard
   - ProgressSummary
   - CategoryPerformance
   - StudyStreak
   - Lesson
   - UserSettings
   - NotificationPreferences

4. identify mobile integration risks:
   - oversized payloads
   - full-exam delivery
   - missing pagination
   - missing resume support
   - inconsistent auth responses
   - web-coupled responses
   - fields unsafe to expose
   - missing error normalization

5. produce recommended mobile-safe endpoints for:
   - login
   - current session/profile
   - entitlement check
   - categories
   - practice session start/fetch
   - question batch fetch
   - answer submit
   - exam start
   - exam resume
   - exam question batch fetch
   - exam answer save
   - exam complete
   - exam results
   - flashcard decks/cards/progress
   - progress summary/streak/category breakdown
   - lessons list/detail
   - settings/preferences

6. create these docs in the repo:
   - MOBILE_API_CONTRACT.md
   - MOBILE_ENDPOINT_MATRIX.md
   - MOBILE_GAP_ANALYSIS.md

Critical constraints:
- do not build a second backend
- do not assume full exam payload delivery
- preserve entitlement accuracy
- optimize for batching, retry, and resume safety
- document exactly what the mobile workspace needs to implement the production adapter with minimal UI changes
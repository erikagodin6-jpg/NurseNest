Urgent production regression fix for NurseNest:

Problem:
Yesterday the lesson availability issue was fixed, but now the published site has regressed and none of the lessons appear available again. This is a production regression. Do not blame browser cache unless you can prove it with evidence. Investigate the actual root cause in code, build output, runtime config, API responses, and published deployment behavior.

Goal:
Restore lesson availability on the published site and identify exactly why the fix reverted.

Important constraints:
- Do not redesign anything
- Do not change branding, copy, navigation, tiered menus, pricing, paywalls, or SEO unless required to fix lesson availability
- Do not make random refactors
- Do not guess
- Trace the real production data flow from source to published UI

What to investigate, in order:

1. Identify what changed between the working state yesterday and the broken published state today
- Check git history, deployment history, recent merges, and recent AI-generated edits
- Find the exact files changed since the lesson fix
- Determine whether the regression came from:
  - route changes
  - content source changes
  - lesson publish flags
  - filtering logic
  - environment variables
  - API endpoints
  - build output differences
  - server-side fallback behavior
  - locale routing
  - auth/paywall gating logic
  - stale static/generated manifest files

2. Audit the lesson data source end to end
Trace where lesson cards/pages come from on production:
- database
- JSON/static content
- CMS/admin content
- generated content files
- route manifests
- API endpoints
- build-time imports

For each source, verify:
- lessons still exist
- lessons are marked published/store-ready/visible as intended
- locale mappings still exist
- slugs still resolve
- lesson indexes are populated
- no filter is excluding all lessons

3. Audit lesson listing logic
Inspect all lesson list pages and loaders:
- /en/lessons
- tier-specific lessons pages
- body system pages
- allied/new grad lesson surfaces if applicable

Check for regressions in:
- filters
- role gating
- locale filtering
- publishedOnly flags
- category matching
- empty state fallbacks
- bad query params
- tier mismatches
- accidental requirement for subscription when content should still show in listing
- client-side filtering that removes all lessons after hydration

4. Audit individual lesson route resolution
Check whether lesson detail pages still resolve in production even when listings are empty.
If detail routes work but listings are empty, isolate the bug to indexing/filter/render logic.
If both are broken, trace the slug/content resolution path.

5. Audit production API/network behavior
Inspect all network requests used by published lesson pages.
For every lesson-related request, verify:
- request URL
- response status
- response payload
- whether production differs from local/dev
- whether 404/500/empty arrays are returned
- whether locale or tier params are wrong
- whether published build points to wrong base URL or API origin

6. Audit environment and deployment config
Check for regressions in:
- env vars
- production base URL
- Vite env exposure
- server runtime config
- feature flags
- admin publish flags
- build-time generated data files
- paths/aliases
- locale defaults
- route manifests

Compare local working behavior vs deployed behavior.
If local works and production fails, identify the exact environment/config mismatch.

7. Audit build artifacts
Check whether the production build actually includes the lesson data/routes/components expected.
Inspect:
- generated dist files
- route bundles
- manifests
- lesson JSON/static assets
- lazy-loaded lesson modules
- locale-specific route output

Confirm the published build contains what the app expects.

8. Check for stale or overwritten generated files
If lesson pages or indexes are generated by scripts/admin tools:
- verify generation ran successfully
- verify output files were committed or deployed
- verify no older file overwrote the fixed one
- verify no build step clears lesson indexes before publish

9. Check auth/paywall regression
Make sure lessons are not being incorrectly hidden because of:
- changed auth state initialization
- broken free vs paid tier logic
- SSR/client hydration mismatch
- lesson cards only rendering for authenticated users by mistake
- subscription gate wrapping the whole listing instead of only locked actions/content

Users should still be able to see available lesson listings and expected preview surfaces according to intended access rules.

10. Check locale and routing regression
Because the site is localized, verify lesson availability on:
- /en/*
- /fr/*
- /es/*
and any other enabled locales

Check for:
- English working but localized pages empty
- localized slug mismatches
- language switcher generating bad lesson URLs
- lesson lists filtering on wrong locale key

11. Add targeted logging while debugging
Add temporary, precise logs to show:
- source lesson count before filtering
- lesson count after each filter
- active locale
- active tier
- auth/subscription state
- publish visibility flags
- API response length
- final rendered lesson count

Use this to identify the exact stage where lessons disappear.

12. Fix only the root cause
Once identified, implement the smallest reliable fix.
Do not patch symptoms.
Do not hardcode lessons.
Do not bypass publish logic in a way that breaks admin control.

13. Prevent recurrence
After fixing, add safeguards:
- a regression test for lesson listing not being empty when published lessons exist
- a test for lesson detail route resolution
- a test for locale lesson availability
- a test for production build data presence if applicable

14. Final verification checklist
Verify on the published build:
- lesson library loads
- expected lesson cards appear
- clicking a lesson opens the correct page
- free/paid gating still behaves correctly
- no console errors
- no white screen
- no broken locale routes
- no accidental menu changes
- no SEO regressions
- no hidden lessons caused by client-side hydration mismatch

Deliverables:
- fix the code
- list exactly which files changed
- identify the exact root cause
- explain why it worked yesterday and broke again today
- explain how the new fix prevents the regression from returning
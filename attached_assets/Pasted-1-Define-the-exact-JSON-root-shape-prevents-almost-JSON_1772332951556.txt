1) Define the exact JSON root shape (prevents “almost JSON”)
Add:
AI JSON output MUST match this root shape:

{
  "meta": { "topic": string, "tier": string, "region": string },
  "sections": [
    {
      "sectionId": string,
      "title": string,
      "blocks": Block[]
    }
  ],
  "questions": [
    {
      "id": string,
      "stem": string,
      "choices": string[],
      "answerIndex": number,
      "rationale": string,
      "whyWrong": string[]
    }
  ]
}

No markdown. No extra keys. Valid JSON only.
Why: without a root contract, models often return different shapes per run.
2) Require zod schema + guardrails in code (prevents runtime mess)
Add:
Implement a Zod schema for the full JSON structure and validate every AI response.
If invalid:
- run exactly 1 retry with a "Fix JSON to match schema" prompt
- if still invalid, surface a user-friendly error in GuidedModeView and stop compilation cleanly.
Why: “safeParse” without Zod often becomes hand-wavy parsing that breaks later.
3) Deterministic pagination rule (prevents “manual layout creep”)
Add:
In Guided Mode, compilation must NEVER require manual layout.
Enforce:
- auto-pagination for every section block list
- text autofit down to a minimum font size
- if still overflow, split content to the next page (do not shrink indefinitely)
Why: this keeps your “compiler” promise.
Final combined prompt (what to actually paste)
Take what you already have and append the 3 clauses above. That’s it.
Quick sanity check after Replit AI applies changes
When you run it, test these 5 things:
Generate 45-page cram guide → should fully build without drag/drop
Toggle Include Images (Low) → should insert placeholders even if image gen fails
Switch Theme after compile → should re-skin without regenerating text
Force an AI failure (bad JSON) → should retry once then show error cleanly
Enable Auto Store-Ready → should run the pipeline after compile
If any of those fail, paste the specific error output and I’ll give you a tight “third-pass” prompt targeted to the failing piece (usually JSON validation or pagination).
If you want, I can also give you a ready-to-drop Zod schema for that JSON root so Replit AI doesn’t improvise it poorly.
You are a senior full-stack engineer adding analytics to my NurseNest platform (Next.js App Router + TypeScript + Tailwind).

GOAL:
Implement privacy-aware analytics so I can track:
- Unique visitors, sessions, page views
- Top pages, time on page (approx), scroll depth (optional)
- Click events: CTAs, tier switches, “Start Quiz”, “Start Session”, “Get Started”, “Pricing”, “Checkout”
- Funnels: Landing → Account create → Lesson view → Quiz start → Subscription start
- Search queries used inside site (if site search exists)
- Content engagement: which lessons/topics are most used (RPN/RN/NP, Patho/Pharm, OSCE usage)

CONSTRAINTS:
- Must not slow down the site noticeably
- Must respect privacy and comply with Canada/US norms (PIPEDA/PHIPA-aware approach)
- Do NOT collect sensitive personal data or health data
- Do NOT record full text inputs (no capturing user answers, free text, names, emails)
- Prefer anonymized analytics; avoid invasive session replay unless explicitly enabled later

IMPLEMENTATION OPTIONS:
Implement ONE of these stacks (choose best for a small startup):
Option A (simple privacy-first): Plausible or Umami (page views + events)
Option B (product analytics): PostHog (events + funnels, optional self-host)
Option C (Google Analytics 4) (industry standard but heavier privacy/cookie requirements)

Pick the best option and implement it end-to-end.

REQUIREMENTS:

1) PAGEVIEW TRACKING
- Track page views for all public routes and logged-in routes
- Track referrer/source (UTM params)
- Distinguish CA vs US site context (region) as a property

2) EVENT TRACKING
Create a small analytics utility:
- track(eventName, properties)
Add event calls to key actions:
- click_get_started
- click_view_demo
- click_pricing
- select_tier (rpn/rn/np)
- select_region (ca/us)
- lesson_opened (id, topic, tier, track patho/pharm)
- flashcards_start_session (topics, tier)
- quiz_start / quiz_complete
- osce_opened / clinical_lab_opened
- signup_started / signup_completed
- checkout_started / subscription_purchased (do NOT log payment details)

3) DASHBOARD / REPORTS
- Ensure the chosen analytics tool provides:
  - visitors over time
  - top pages
  - top events
  - referral sources
  - funnels (if using PostHog/GA)
Provide instructions for viewing reports and recommended dashboards.

4) CONSENT / PRIVACY UI
- If using GA4 or any cookie-based tracking, implement a consent banner:
  - Accept analytics / Reject analytics
  - Store consent preference
  - Do not load analytics until consent is granted
- If using a cookieless privacy-first tool, still include a short Privacy section and allow opt-out toggle (nice-to-have)

5) SECURITY / DATA HYGIENE
- Ensure analytics keys are stored in environment variables
- Do not expose secret keys client-side
- Avoid collecting PII (no email, name, IP storage unless anonymized by provider)
- Add a short “Analytics” clause to the Privacy Policy explaining what is tracked

DELIVERABLES:
- Which analytics tool you chose and why (short)
- Exact code changes with file paths:
  - analytics client utility (lib/analytics.ts)
  - Next.js layout integration for pageview tracking
  - event hooks added to key buttons/components
  - consent banner component (if needed)
- Environment variable list (e.g., NEXT_PUBLIC_..., server keys)
- QA checklist: verify events fire and dashboards populate

IMPORTANT:
Give copy/paste-ready code, not pseudocode.
If the site already has any analytics, integrate rather than replacing.
You are a senior full-stack engineer and CMS architect working inside my existing NurseNest codebase. Your task is to build (or fix) an Admin Menu that lets me edit my website content INLINE in the browser (true WYSIWYG/inline editing on the actual page), and to FIX my AI blog publishing workflow so I can edit, save drafts, and publish/schedule posts. This is urgent because blog publishing is a top SEO driver.

You MUST first inspect my repository and reuse my existing stack (framework/router, auth, database/ORM, UI components, Tailwind tokens, storage, and any existing admin/blog code). Do NOT invent a new tech stack. Preserve the NurseNest aesthetic exactly.

GOALS (NON-NEGOTIABLE)
1) Inline editing across the site: I can click “Edit” from an Admin Menu and edit pages directly in the browser on the live layout.
2) Blog CMS must work end-to-end: create → AI draft → inline edit → save draft → preview → publish now or schedule → confirm it appears on the public blog feed and is indexable.
3) Admin Menu must be accessible from anywhere on the site when I’m logged in as admin (e.g., a small fixed “Admin” button or toolbar).
4) Robust permissions: only admin users can see the admin toolbar and access editing/publishing endpoints.
5) Audit log: track who edited what and when (at minimum for blog posts and pages).
6) Zero regressions: existing public pages must render exactly the same when not in edit mode.

PHASE 0 — DISCOVERY (DO THIS FIRST)
- Identify:
  - Framework (Next.js? App Router vs Pages Router?)
  - Where pages are defined (static, MDX, DB-driven, CMS-driven)
  - Current blog implementation (where posts live, how they render, any API routes)
  - Why my existing AI blog “won’t let me edit and post” (permission bug? draft state stuck? missing publish route? DB write failing? validation error? UI bug?)
- Output:
  - A concise diagnosis
  - A step-by-step plan to fix with minimal changes to working systems

PHASE 1 — ADMIN AUTH + ROLE GATING
- Implement or fix admin role checks:
  - Middleware/server-side gate for /admin and all mutation APIs
  - Client-side guard only for UI convenience; server remains source of truth
- Add an easy method to set the first admin in development (e.g., env var ADMIN_EMAIL), but keep it secure.

PHASE 2 — ADMIN MENU / TOOLBAR (GLOBAL)
Create a persistent Admin UI that appears only for admins:
- A small fixed button “Admin”
- Clicking opens a side panel/menu with:
  - Edit Page (toggles inline edit mode on the current page)
  - Pages Library (list editable pages/sections)
  - Blog Manager (drafts/published/scheduled)
  - Lessons Manager (if applicable)
  - Flashcards/Questions Manager (if applicable)
  - Media Library (uploads)
  - SEO tools (basic metadata editor)
- Must match existing NurseNest UI components and styling tokens (no new design system).

PHASE 3 — INLINE EDITING SYSTEM (WYSIWYG ON REAL PAGE)
Implement a safe inline editing pattern compatible with my existing content architecture:

A) If content is DB-driven:
- Use content “sections/blocks” with ids
- In edit mode, overlay editable fields with:
  - Click-to-edit text (contenteditable or editor component)
  - Inline controls to add/remove/reorder blocks
  - Drag-and-drop image upload
  - Save/Cancel/Preview buttons

B) If content is file/MDX-driven:
- Implement an Admin “content layer”:
  - Store editable overrides in DB keyed by route + section id
  - Render overrides on top of baseline content
  - This enables inline edits without rewriting repo files
- Provide a migration path to convert the most important pages to DB blocks if needed.

Inline editor requirements:
- Text: headings, paragraphs, lists, bold/italic
- Images: drag-and-drop upload, alt text required, captions optional
- Reorder blocks via drag handle
- Autosave optional but at minimum: explicit Save + optimistic UI
- Preview mode shows exact public rendering
- Accessibility: enforce alt text; do not break semantic headings

PHASE 4 — BLOG MANAGER (FIX AND COMPLETE)
This is the priority. Build/fix a full workflow:

Blog post fields:
- title, slug, excerpt, body, cover image, tags, category
- status: draft | pending_review | scheduled | published
- publishAt
- SEO: metaTitle, metaDescription, canonical, OG image
- updatedAt, createdAt, authorId

Admin Blog Manager UI:
- List view with filters (draft/scheduled/published)
- Edit view that supports BOTH:
  1) Inline editing on the real blog post page (preferred)
  2) A dedicated editor screen (fallback)

Critical fixes:
- Ensure I can open the AI blog post I created previously, edit it, save draft, and publish.
- Add clear error reporting if publish fails (show validation error, DB error, auth error).
- Confirm published posts appear:
  - On /blog index
  - On the correct /blog/[slug] page
  - In the sitemap (if you have one)
  - With correct meta tags

Scheduling:
- Implement publishing scheduled posts automatically using the existing hosting constraints.
- If cron is not available, implement a lightweight server “publisher” job:
  - Runs on server start and then periodically (setInterval or background loop if supported)
  - Or trigger publish check on any site request (safe throttled) as a fallback

PHASE 5 — MEDIA LIBRARY
- Central media upload endpoint:
  - Validate type/size
  - Store using existing storage solution (S3/Supabase/Cloudinary/local)
  - Save alt text metadata
- Integrate image selector into inline editor and blog editor.

PHASE 6 — SEO SAFETY
- Ensure blog posts are indexable:
  - Correct canonical
  - Proper meta tags
  - Robots tags not blocking
  - Blog pages accessible without auth
- If there’s a sitemap generator, ensure new posts are included.

PHASE 7 — AUDIT LOG + QA
- Add audit logging for page edits and blog edits:
  - actorId, route/entity, before/after json, timestamp
- QA checklist:
  - Admin can edit and publish a post end-to-end
  - Non-admin cannot access edit mode or mutation APIs
  - Public pages unchanged when not in edit mode
  - No console errors
  - Build passes

DELIVERABLES
1) Repo discovery summary + root cause of the blog editing/publishing failure
2) Files changed list
3) DB migrations if applicable
4) Routes/endpoints list
5) Setup instructions (env vars, admin email, storage config)

START NOW
- First run repo discovery and identify exactly why the AI blog post cannot be edited/published.
- Then implement the admin toolbar + blog manager fixes first before expanding inline editing to other pages.
- Make reasonable assumptions and proceed; document assumptions briefly.
You are working on the NurseNest production codebase.

We have a CRITICAL multi-system production failure affecting reliability, exam access, content integrity, and alert correctness.

This is not a single bug. It is a cascading reliability incident.

OBSERVED FAILURES

1. MEMORY FAILURE
- Repeated “Service Down: memory” alerts for over 30 hours
- Runtime memory repeatedly around 188–189 MB out of 192–193 MB
- Utilization ~98%
- App is running with effectively zero memory headroom

2. CONTENT INTEGRITY FAILURE
Published content items are being reported with zero valid items:
- Restless Leg Syndrome Rpn
- Macular Degeneration Rpn
- Gestational Diabetes Rpn
- Bell’s Palsy Rpn

Alert details indicate:
- type: zero_valid_items
- severity: critical

This means published content exists in the system even though it has no valid usable underlying question/item payload.

3. SYNTHETIC EXAM FLOW FAILURE
Synthetic test failed:
- exam_open_flow
- message: “This operation was aborted”
- type: synthetic_test_failure
- severity: critical

This means the core exam opening path is failing under monitoring.

4. ALERT TEMPLATE / NOTIFICATION ROUTING FAILURE
The above non-payment issues are being sent under an email card labeled:
- “Payment Failed”

This is incorrect and dangerous because it misclassifies incidents and causes operational confusion.

PRIMARY OBJECTIVE

Implement a FULL PRODUCTION STABILIZATION AND CORRECTNESS FIX so that:
- memory alerts stop recurring
- exam opening works reliably
- invalid content cannot remain published
- alerts are correctly categorized and rendered
- failures degrade gracefully instead of breaking customer experience

==================================================
SECTION 1 — ROOT CAUSE ANALYSIS
==================================================

Perform a full trace and identify exact root causes for:
1. repeated memory exhaustion
2. exam_open_flow abort failure
3. zero_valid_items published content issue
4. incorrect “Payment Failed” template used for reliability alerts

Do not assume one cause. Validate each separately and explain where they interact.

==================================================
SECTION 2 — MEMORY STABILIZATION
==================================================

Audit and fix all major memory pressure points:
- heavy exam payload construction
- AI/content generation in web process
- unbounded caches
- global arrays/maps that grow
- retry loops
- repeated polling
- request data retained in memory
- oversized query results
- expensive transformations / duplicate serialization
- background jobs running inside main app process

Implement:
- bounded memory-safe loading
- chunking/pagination for exam content
- lazy-loading of heavy rationale/metadata
- route-level memory logging
- emergency degradation mode at high memory
- safe timeouts and concurrency caps
- process health reporting

Also document the minimum safe production RAM requirement.

==================================================
SECTION 3 — EXAM OPEN FLOW HARDENING
==================================================

Trace the complete exam_open_flow end-to-end:
- frontend request initiation
- auth/session lookup
- entitlement checks
- exam lookup
- question/content fetch
- validation
- response serialization
- client render

Find exactly where “This operation was aborted” happens.

Then implement:
- graceful fallback instead of hard abort
- retries only where safe
- request timeout handling
- frontend error boundary
- backend circuit breaker
- lightweight fallback exam metadata response
- logging with a request ID across the flow

The user must never hit a dead end or broken screen if the exam cannot fully load.

==================================================
SECTION 4 — CONTENT INTEGRITY ENFORCEMENT
==================================================

Implement strict content validity rules.

A content item must NEVER be publishable if it has zero valid items/questions.

Build:
- pre-publish validation
- publish blocker for zero-valid-item content
- automatic quarantine/unpublish for already-published invalid content
- admin dashboard surfacing of invalid content with exact reason
- repair tooling or re-generation hooks for invalid items

Audit the four known broken RPN content items and determine why each ended up published with zero valid items.

Possible causes to inspect:
- generator produced malformed items
- validation logic too weak
- tier/language filters removed all items after publish
- serialization/storage corruption
- publish flag bypassed validation
- migration/import bug

==================================================
SECTION 5 — ALERTING / NOTIFICATION CORRECTNESS
==================================================

Audit the support/ops alert system.

Reliability alerts must NOT be rendered as “Payment Failed”.

Create proper alert type separation for:
- payment_failure
- service_down
- synthetic_test_failure
- content_integrity_failure
- reliability_warning

Ensure:
- template title matches alert type
- severity is displayed correctly
- email/dashboard labels are accurate
- alert routing is not misleading
- deduplication/rate-limiting prevents spam

==================================================
SECTION 6 — USER PROTECTION
==================================================

Implement production-safe user protections:
- if exam content is invalid, do not let the user open a broken exam
- instead show a graceful message and offer alternate valid exam content
- if specific content is quarantined, remove it from public listings automatically
- if backend is degraded, serve a lightweight read-only experience instead of crashing
- protect paying users from dead ends

==================================================
SECTION 7 — DELIVERABLES
==================================================

Return:
1. Exact root cause(s) for each failure category
2. Exact files changed
3. Exact code paths fixed
4. Invalid content records found and status after remediation
5. Alert template/routing fixes
6. Memory stabilization changes
7. Verification steps proving:
   - no published zero-valid-item content remains
   - exam_open_flow passes
   - alert templates are correctly categorized
   - memory behavior is improved

NON-NEGOTIABLE REQUIREMENTS
- Do not provide superficial advice only
- Do not stop after raising memory
- Do not leave broken content published
- Do not leave reliability alerts mislabeled as payment failures
- Do not allow exam routes to hard-fail without fallback
- Prioritize reliability and customer trust over elegance

Start by tracing:
1. the highest-memory routes
2. the exam_open_flow failure point
3. the publish pipeline allowing zero-valid-item content
4. the alert template selection logic
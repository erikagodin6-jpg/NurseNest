You are a senior full-stack engineer and product monetization architect working inside my existing NurseNest codebase. Your task is to implement a Quizlet-style flashcard system monetization model optimized specifically for nursing students.

You MUST inspect the repository first and reuse ALL existing infrastructure (framework, routing, database, auth, Tailwind tokens, components, storage, billing if present). Do not invent a new stack. Preserve my NurseNest aesthetic exactly.

OBJECTIVE
Implement a monetization system that maximizes conversion likelihood for price-sensitive, exam-driven nursing students using a hybrid model:

• Free tier with meaningful utility
• Low-friction one-time deck upgrades
• Subscription positioned as friction removal

This is NOT a standard SaaS paywall implementation.

PRICING MODEL (DEFAULT — DO NOT CHANGE WITHOUT STRONG JUSTIFICATION)
Free Plan:
- Users can create decks and study
- Hard limit: 50 TOTAL flashcards per account (server-enforced)

Deck Upgrade:
- One-time purchase per deck
- Price: $2.99 CAD
- Raises deck limit to 300 cards
- Users do NOT need subscription to upgrade decks

Subscription (only if billing/subscription system already exists):
- ~$7.99–9.99 CAD monthly
- Removes all card/deck limits (or sets very high cap)
- Adds advanced features flag (analytics/spaced repetition/etc)

MONETIZATION PHILOSOPHY (CRITICAL)
The system must encourage upgrades WITHOUT feeling like a paywall.

Users must feel:

❌ NOT “blocked by limits”
✅ BUT “expanding study capacity”

Avoid generic upgrade language.

DATA MODEL REQUIREMENTS
Extend or create schemas using existing ORM patterns:

User Entitlements:
- Determine free vs premium via existing plan logic
- Track flashcard usage count

Deck:
- id, ownerId, title, visibility, etc
- isUpgraded: boolean
- upgradedLimit: integer (default 300 when upgraded)
- upgradedAt timestamp

Flashcards:
- id, deckId, front, back, etc

Purchases (if no existing purchase table):
- id, userId, deckId
- type = 'deck_upgrade'
- status = pending/paid/refunded/failed
- paymentProvider = stripe (if Stripe exists)

ENTITLEMENT LOGIC (NON-NEGOTIABLE)
Enforce ALL limits server-side.

When creating/updating a flashcard:

1) If user has premium subscription → allow up to PREMIUM_MAX
2) Else if deck.isUpgraded → allow up to deck.upgradedLimit (300)
3) Else → enforce FREE_GLOBAL_MAX (50 total across account)

IMPORTANT:
FREE_GLOBAL_MAX applies ONLY to non-upgraded decks.

Implement concurrency-safe checks using DB transactions or atomic operations so limits cannot be bypassed via parallel requests.

USER EXPERIENCE REQUIREMENTS

When user hits free limit:

DO NOT display generic errors.

Display psychologically optimized messaging:

"Your study capacity is full. Most exam-focused decks require 120–200 cards."

Provide options:

• Upgrade this deck – $2.99
• Continue with free plan (dismiss)

When user tries to exceed deck limit:

"This deck has reached its study capacity."

Buttons:

• Upgrade deck capacity
• Cancel

DISPLAY COUNTERS (VERY IMPORTANT)
Deck Editor UI must show:

Free decks:
"Cards used: 37 / 50 free limit"

Upgraded decks:
"Cards used: 112 / 300"

Premium:
"Unlimited cards"

Avoid alarming or punitive wording.

PAYMENTS IMPLEMENTATION
If Stripe already exists → reuse.

Otherwise implement Stripe Checkout + Webhooks.

Required routes:

POST /api/billing/deck-upgrade/create-checkout
- Validate ownership
- Prevent double upgrades
- Include metadata (userId, deckId)

POST /api/billing/stripe-webhook
- Verify signature
- On success → set deck.isUpgraded = true
- Write audit log if system exists

REFUND LOGIC
If deck upgrade refunded:

- Revert deck to free rules
- If deck exceeds free limits → block new cards but do not delete existing ones
- Show UI guidance to user

CONVERSION OPTIMIZATION (CRITICAL)
Upgrade prompts must feel academically logical, not commercial.

Use framing related to:

• Exam preparation
• Study capacity
• Deck expansion
• Removing study friction

Avoid language like:

❌ “Buy now”
❌ “Upgrade to premium”

Prefer:

✅ “Increase deck capacity”
✅ “Remove study limits”

AESTHETIC REQUIREMENTS
ALL flashcard and billing UI must reuse:

- Existing layout
- Existing Tailwind tokens
- Existing buttons/cards/shadows
- Existing typography scale

No new visual design systems.

TESTING REQUIREMENTS
Implement tests for:

- Free limit enforcement
- Upgraded deck limits
- Premium bypass behavior
- Webhook upgrades
- Parallel request safety

DELIVERABLES
1) Repo discovery summary
2) Files modified/added
3) Schema migrations
4) Route list
5) Setup instructions (env vars + Stripe steps)

START NOW
First inspect the repository and output a discovery summary.
Then implement sequentially with build-safe steps.
Document assumptions briefly without blocking progress.
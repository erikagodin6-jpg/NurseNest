Refactor the lesson page header so the layout is fixed and consistent across all lessons.

Goal:
The top lesson area must stay visually neat and the navigation/action buttons must always remain in the same position, regardless of lesson title length, chip count, previous/next lesson text length, or whether optional controls are shown.

Problems to fix:
1. The next lesson button changes position between lessons.
2. The header feels uneven because content length causes layout shift.
3. Buttons like “My Notes” and “Edit Inline” should stay aligned in predictable places.
4. Breadcrumbs, lesson title, chips, progress, and tabs should not push action buttons around.

Implement this with a stable desktop-first responsive layout.

DESKTOP LAYOUT REQUIREMENTS:
- Create a main lesson header wrapper with a max width and centered content.
- Use CSS grid for the hero/header area:
  - Left column: lesson metadata and title content
  - Right column: action buttons
- Keep the right column width fixed so buttons never shift horizontally.
- Suggested grid:
  - grid-template-columns: minmax(0, 1fr) 220px;
  - gap: 24px;
  - align-items: start;

HEADER STRUCTURE:
1. Row 1: breadcrumbs
2. Row 2: “Back to Overview”
3. Row 3: previous/next lesson navigation in a fixed-height row
4. Row 4: main hero section
   - left: title + chips
   - right: action stack (“My Notes”, “Edit Inline”)
5. Row 5: progress card
6. Row 6: lesson tabs

PREVIOUS/NEXT NAV REQUIREMENTS:
- Put previous and next lesson links in a dedicated row with fixed spacing.
- Use a 3-part layout:
  - left: previous lesson link
  - center: empty flexible spacer
  - right: next lesson link
- The right-side next lesson button/link must always sit in the exact same horizontal position.
- Truncate long lesson names with ellipsis instead of allowing wrapping.
- Set fixed max widths for prev/next labels.
- Use min-height on the nav row so it never collapses differently between lessons.

Example:
- display: grid;
- grid-template-columns: minmax(0, 1fr) auto minmax(0, 1fr);
- previous aligned left
- center spacer or divider
- next aligned right
- white-space: nowrap
- overflow: hidden
- text-overflow: ellipsis

TITLE AREA REQUIREMENTS:
- Reserve stable spacing for the lesson title and chips.
- Prevent chips from pushing buttons out of alignment.
- Use a chip row that wraps only within the left content area.
- Add max-width to title block so it does not collide with the action column.
- Keep title/action alignment anchored to the top.

ACTION BUTTON REQUIREMENTS:
- “My Notes” and “Edit Inline” must always appear in the same right column location.
- Stack them vertically with consistent spacing.
- Right-align the action column.
- Use fixed button widths where appropriate.
- If one button is conditionally hidden, preserve layout with a placeholder or keep the stack height stable.

PROGRESS SECTION:
- The progress card should sit below the hero in its own full-width row.
- Progress percentage should align consistently to the right.
- Do not let progress content affect hero layout above.

TABS SECTION:
- Put Pre-Test / Clinical Content / Post-Test in a separate row below the progress card.
- Ensure toggles like “Show Pre-Test” and “Show Post-Test” do not shift the tabs.
- Tabs should be in a stable flex/grid row with justified spacing.

RESPONSIVE BEHAVIOR:
- On mobile/tablet, collapse to one column.
- Action buttons move below the title area.
- Previous/next navigation stays in one dedicated row with stable spacing.
- Preserve consistent margins and padding.
- Prevent overlap or jumpiness.

STYLE CLEANUP:
- Tighten vertical rhythm.
- Use consistent section spacing.
- Reduce visual clutter.
- Make the top feel more polished and premium.
- Keep the current NurseNest aesthetic: clean, soft, modern, minimal, rounded.

TECHNICAL REQUIREMENTS:
- Refactor the lesson header component into clearly separated subcomponents:
  - LessonBreadcrumbs
  - LessonTopNav
  - LessonHero
  - LessonActions
  - LessonProgressCard
  - LessonTabs
- Remove layout shift caused by variable text lengths.
- Use CSS grid/flex intentionally instead of content-driven placement.
- Add truncation utilities for long lesson names.
- Add min-height values for shared rows to stabilize layout.
- Do not use absolute positioning unless necessary.
- Keep the implementation maintainable.

Please implement the full UI refactor and show me:
1. updated component structure
2. updated JSX/TSX
3. updated CSS/Tailwind classes
4. any helper utilities for truncation and fixed layout behavior
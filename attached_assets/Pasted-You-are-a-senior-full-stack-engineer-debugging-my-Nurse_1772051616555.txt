You are a senior full-stack engineer debugging my NurseNest blog. In developer preview, my articles appear, but on the live published site it shows “No articles available.” There is no visible error. This indicates a production-only issue. Your job is to find the root cause and fix it so the live site lists published posts correctly.

NON-NEGOTIABLE
- Do not add placeholder data.
- Do not hide the issue with UI hacks.
- Fix the real production data fetching and rendering path.
- Add logging that helps verify the fix in production without exposing secrets.

PHASE 0: REPRO AND TRACE
1) Identify exactly where the blog index gets its posts:
   - /blog page component
   - data fetching function
   - API route or direct DB query
2) Compare dev vs prod code paths:
   - Are you using build-time fetching (getStaticProps / generateStaticParams / SSG) that runs with missing prod env vars?
   - Are you using client-side fetch hitting a different domain or blocked by CORS?
   - Are you filtering by status='published' and prod posts are draft/scheduled?
   - Are you reading from a local dev DB but prod points to empty DB?
   - Are you using preview content source but prod uses a different source?
3) Add temporary safe diagnostics:
   - On /blog in production, log server-side: number of posts returned, filter criteria used, and whether DB connection succeeded.
   - Show admins a hidden debug panel (admin-only) with count + status breakdown.

MOST COMMON ROOT CAUSES TO CHECK (YOU MUST CHECK EACH)
A) Environment variables missing in production (DATABASE_URL, API base URL, auth)
B) Production DB is not the same as dev DB or is empty
C) The query filters out everything (status, publishAt <= now, category, locale)
D) Blog index is statically generated at build time and built with empty data, then cached
E) API route is not deployed or path differs in prod
F) Wrong domain is being fetched (replit subdomain vs nursenest.ca) and returns empty
G) Auth middleware accidentally blocks public blog reads in prod
H) Caching layer returns stale “empty” response

FIX REQUIREMENTS
1) Ensure blog index fetches posts in a production-safe way:
   - Prefer server-side rendering for the list OR ISR with correct revalidation.
   - Ensure it uses production DB and correct env vars.
2) Ensure posts appear if status='published' and publishAt <= now (or publishAt null allowed).
3) If scheduling exists, ensure posts transition to published correctly.
4) Ensure the live /blog page updates when new posts are published:
   - If SSG: add revalidation
   - If SSR: ensure no cache is forcing empty results
5) Ensure sitemap includes published posts (if sitemap exists).
6) Remove temporary debug output after confirmation, but keep useful structured server logs.

DELIVERABLES
- A short root cause report: what differed between dev and prod and why it resulted in “No articles available.”
- Code changes list with file paths
- Exact env vars required for production
- Verification steps:
  - Confirm DB has N published posts
  - Confirm /blog returns N posts in prod
  - Confirm at least one post page /blog/[slug] renders

START NOW
- Inspect the repo, locate the blog list query, and implement the fix.
- Do not ask me questions. Make reasonable assumptions and proceed.
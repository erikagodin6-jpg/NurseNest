Refactor the Cram Guide / Survival Guide document generator so it cannot output empty section pages and it supports large question counts via chunking.

Observed bug:
Generated PDF/pages contain only cover + TOC + section divider pages, with almost no content in sections. Only a few practice questions appear (e.g., Q1–Q5), and rationales are missing even when enabled. This indicates generation/parse/compile is failing and the pipeline is silently continuing.

Requirements:

1) Add HARD validation gates after each generation step:
   - After generating each section (Pathophysiology, Signs & Symptoms, Assessment, Labs, Meds, Interventions, Complications, Clinical Pearls), verify the section has non-empty content blocks.
   - If empty or below minimum size, automatically retry generation up to 3 times.
   - If still failing, STOP compile and surface an error in the UI with the raw model output + reason (do not silently create empty pages).

2) Stop generating the whole document in one LLM call.
   Implement chunked generation:
   - Generate blueprint first (page plan).
   - Then generate each section in its own call with its own token budget and strict JSON schema.
   - Compile sections after each one passes validation.

3) Enforce a strict JSON schema and robust parsing:
   - The model must return JSON only.
   - If JSON parsing fails, retry with a “return JSON only” repair prompt.
   - Add server logs showing: prompt, model output length, parse errors, and which section failed.

4) Practice questions generation must be separate and pageable:
   - If user requests N questions, generate them in batches of 25–50 (configurable).
   - Each batch returns JSON: [{id, stem, options, answer, rationale, trap_note}]
   - Validate: count matches batch size, each has answer + rationale.
   - Paginate questions into pages (e.g., 4–6 questions per page depending on rationale length).
   - Ensure rationales render (if template expects them, do not omit).

5) Add a “generic content detector” scoring function for sections:
   - FAIL if section is mostly headings with no substantive bullets/tables.
   - FAIL if any required block types are missing (for survival guide: recognition cues, red flags, first actions, do-not-do, exam traps, mini-case).
   - Score 0–100; require >=80 or retry.

6) Add UI feedback:
   - Show per-section status: generated / failed / retrying
   - Show quality score
   - Show number of questions generated so far (e.g., 150/300)

7) Default topic inventory:
   If the document title is generic (e.g., “REx-PN Survival Guide”), do NOT let the model guess scope.
   Use a built-in high-yield topic list (HF, ACS, COPD vs asthma, pneumonia, sepsis, hypo/hyperK, hypo/hyperglycemia, stroke, AKI/CKD, GI bleed, FVD/FVO, anaphylaxis, dysrhythmias, DKA/HHS recognition, UTI→sepsis).
   Generate survival content around that inventory.

Do NOT modify the blog generator.
Only modify the document generator pipeline + compile step.

Deliver:
- Updated server routes/services for chunked generation + retries
- JSON schema definitions
- Validation + logging
- Question batching + rationale rendering
- How to test with a 45-page guide + 100 questions and confirm sections are populated.
Upgrade NurseNest Cram Guide / Store system into a Canva-style design + AI content generator with premium aesthetics and add PayPal checkout across all purchasable products.

NON-NEGOTIABLE OUTCOMES
1) Canva-like Builder: drag-and-drop blocks, template library, brand style presets, live preview, PDF export.
2) AI Content Generator: produces exam-aligned structured content (JSON), not unstructured paragraphs, and can auto-fill templates.
3) Aesthetics: NurseNest “clinical yet cute” presets; logo on every page; optional preview watermark for public.
4) Full Admin Control: create/edit/delete, change price in dollars, download full PDF (admin-only), generate public preview with watermark.
5) Payments: Stripe + PayPal enabled for all products with a shared entitlement/download system.

A) CANVA-LIKE BUILDER UI (ADMIN)
Replace current CRUD form with 3-panel builder:
Left: Block Library (drag blocks)
Center: Canvas (multi-page, A4/Letter toggle, snap grid, zoom, reorder)
Right: Style Panel (theme preset, palette, fonts, spacing, logo placement, watermark controls)

Required features:
- Blocks: Title, Subtitle, Section Header, Text, Bullet list, Two-column, Callout, Clinical Pearls, Exam Traps, Labs Table, Med Table, Algorithm Steps, Mnemonic Box, Image placeholder, QR code block, Footer.
- Drag, resize, reorder
- Duplicate block/page
- Undo/redo
- Autosave
- Template library with at least 10 templates
- Theme presets:
  1) Soft Pastel (default)
  2) Clinical Minimal
  3) Bold Study Mode
  4) High Contrast
- Brand enforcement toggle:
  - Lock palette to NurseNest brand colors
  - Rounded corners default 16px
  - Soft shadow default
  - Auto spacing rules

B) CONTENT MODEL (STRUCTURED)
Store “design” as JSON:
- pages[] each has blocks[] with type + content + style props
Store “content” as structured JSON:
- topic, examTarget, sections[], tables[], medCards[], algorithms[], mnemonics[], practiceQuestions[]
The builder renders from this JSON.

C) AI CONTENT GENERATOR
Add “Generate Content” button inside builder:
Inputs:
- Topic
- Exam target (REx-PN / NCLEX-PN / NCLEX-RN / NP)
- Region rules (CA vs US variants)
- Depth (Quick sheet vs Full cram guide vs Bundle)
Outputs:
- Strict JSON matching schema:
  sections: {title, bullets, clinicalPearls, redFlags, examTraps}
  tables: labs/meds comparisons
  algorithms: stepwise actions
  practiceQuestions: {stem, options, answer, rationale}
The generator must:
- Use concise nursing exam language
- Include prioritization frameworks (ABCs, safety, Maslow, infection control)
- Include meds + monitoring + contraindications where appropriate
- Avoid filler

D) PREMIUM AESTHETICS AUTOLAYOUT
Add “Auto Layout” button:
- Takes generated content JSON
- Chooses a template
- Populates blocks
- Applies consistent typography hierarchy
- Inserts icons/placeholders where appropriate
- Creates final “1-page cheat sheet” summary page automatically

E) PUBLIC PREVIEW WITH WATERMARK
Public product pages must show:
- Watermarked preview PDF (first 3 pages default)
- Watermark diagonal text: “NURSENEST PREVIEW • DO NOT DISTRIBUTE”
- Low opacity, large font
- Never expose full PDF file URL publicly
Admin can regenerate preview anytime.

Implementation requirement:
- Generate preview server-side using pdf-lib:
  - Load full PDF
  - Extract first N pages
  - Draw watermark on each page
  - Save preview PDF to previewStorageKey

F) ADMIN MANAGEMENT (CRUD)
Admin list must allow:
- Edit (opens builder)
- Change price in dollars (server converts to cents for storage)
- Delete (soft delete)
- Download full PDF (admin-only stream)
- Preview full PDF (admin-only)
- Generate/regenerate preview

G) PAYMENT PROVIDERS: STRIPE + PAYPAL (EVERYWHERE)
Create a unified purchase system:
- Products have priceDollars input (admin) stored as priceCents internally
- Checkout UI offers “Pay with Card (Stripe)” and “PayPal”
- After successful payment, create entitlement: userId + productId + provider + transactionId + purchasedAt
- Downloads are authorized by entitlement.

Stripe:
- Keep existing Stripe checkout.
- When price changes, create new Stripe Price and set active stripePriceId.

PayPal:
- Implement PayPal Checkout (Smart Buttons) using server-side Orders API:
  Endpoints:
  - POST /api/paypal/create-order {productId} -> returns orderID
  - POST /api/paypal/capture-order {orderID, productId} -> verifies capture, creates entitlement
- Verify PayPal webhook OR capture result server-side before entitlement.
- Store paypalCaptureId on entitlement.
- Ensure currency is consistent (CAD vs USD based on site/region).

H) SECURE DELIVERY
- GET /api/products/:slug/download requires auth + entitlement.
- Stream full PDF from private storage key.
- Do not return permanent public URLs.

OUTPUT REQUIRED
- Provide: DB schema changes, React component plan, exact Express routes + middleware for admin/auth, pdf-lib watermark code, PayPal integration code paths, and how to wire checkout buttons into product pages.
- Do not leave placeholders like “implement later.” Provide working code-level implementation.
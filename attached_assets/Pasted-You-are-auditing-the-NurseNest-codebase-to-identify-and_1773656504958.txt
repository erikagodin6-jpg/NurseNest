You are auditing the NurseNest codebase to identify and fix empty nursing lessons.

The problem:
Some lessons appear in the UI across RPN, RN, and NP tiers but do not have content.  
These lessons lead to blank pages or 404 errors.

This must be fixed globally and systematically.

IMPORTANT:
Do not modify the tier navigation structure.
Do not rename lessons that already exist.
Do not change working pages.

Only identify and correct empty or incomplete lessons.

--------------------------------------------------

STEP 1 — AUDIT ALL LESSON SOURCES

Search the codebase and database for all lesson definitions:

- lesson config files
- lesson JSON
- CMS entries
- database lesson tables
- static lesson markdown files

Look in folders such as:

/lessons
/content
/src/data
/src/content
/server/data
/server/routes
/database

Extract the following for every lesson:

- lesson id
- title
- tier (RPN, RN, NP)
- body system
- slug
- content field
- status (draft/published)

Create an internal list of lessons where:

content == null
OR content length < 200 characters
OR page component does not exist.

--------------------------------------------------

STEP 2 — REMOVE EMPTY LESSONS FROM UI

For lessons that have no content:

IF status == draft
THEN do NOT show them in navigation.

Modify the lesson loader logic so that only lessons where:

status === "published"
AND content length > 200

are rendered.

Example logic:

visibleLessons = lessons.filter(
  lesson => lesson.status === "published" && lesson.content?.length > 200
)

--------------------------------------------------

STEP 3 — PREVENT FUTURE EMPTY LESSONS

Add validation when lessons are created.

When saving a lesson require:

title
tier
system
content >= 200 characters

If validation fails:

block publishing.

--------------------------------------------------

STEP 4 — CREATE PLACEHOLDER CONTENT FOR IMPORTANT LESSONS

For highly searched topics that already appear in navigation, create temporary educational placeholders instead of removing them.

Example placeholder structure:

Title
Short intro paragraph
Key learning objectives
Clinical relevance
"Full lesson coming soon"

Minimum length: 300 words

This prevents SEO loss.

--------------------------------------------------

STEP 5 — VERIFY ALL NURSING TIERS

Audit these tiers individually:

RPN
RN
NP

Verify:

No empty lessons exist
No navigation items lead to blank pages
Every visible lesson has real content

--------------------------------------------------

STEP 6 — FIX ROUTING ERRORS

Check that every lesson slug:

/rpn/lesson/*
/rn/lesson/*
/np/lesson/*

has a matching content file or database record.

If not:

remove the route OR generate placeholder content.

--------------------------------------------------

STEP 7 — TEST

After fixing:

Build the project
Start the server
Test lesson navigation in:

RPN
RN
NP

Ensure:

No blank pages
No 404 lesson links
No lessons without content

--------------------------------------------------

FINAL RESULT

All nursing tiers should only display lessons that have actual content.

There should be:

0 empty lesson pages
0 broken lesson routes
0 navigation links to blank lessons
YOU ARE REPLIT AI. You are a senior full-stack SaaS architect working INSIDE the existing NurseNest codebase (Vite + React + Express + DB + Stripe). Implement a revenue-optimized Allied Health Exam Prep SUBSITE on a SUBDOMAIN that is fully separated from nursing so it does NOT dilute NurseNest nursing SEO or navigation, while still sharing the same backend infrastructure.

NON-NEGOTIABLES
1) Do NOT break or refactor existing nursing routes, nursing tiers, nursing Stripe products, nursing sitemap, or nursing layout.
2) Allied must be isolated by hostname (subdomain) + separate sitemap + separate nav/branding + separate pricing products.
3) Shared backend is allowed (same DB, same services), but front-end must never mix content between nursing and allied.
4) Enforce all paywalls server-side (no paid data in client bundles).
5) Performance: lazy load, paginate QBank, cache where sensible.

TARGET SUBDOMAIN
- allied.nursenest.ca (Squarespace DNS already points allied -> same IP as production)

GOALS (END STATE)
- www.nursenest.ca (or nursenest.ca) remains Nursing-only (authority, SEO, pricing).
- allied.nursenest.ca becomes “NurseNest Allied” (Healthcare Exam Academy) with its own careers, SEO, Stripe products, sitemap, and analytics.
- Same codebase + same backend. Hostname detection switches layouts + allowed routes.

--------------------------------------------------------------------------------
A) HOSTNAME-BASED SUBSITE SHELL (FRONTEND)
--------------------------------------------------------------------------------
1) Implement hostname detection at app bootstrap:
   - If window.location.hostname starts with "allied." OR equals "allied.nursenest.ca":
       render <AlliedApp /> with AlliedLayout + AlliedRoutes
     else:
       render <NursingApp /> with NursingLayout + NursingRoutes (existing)

2) Create AlliedLayout:
   - Similar design language (pastel, rounded) but different accent color and header label:
     Brand: “NurseNest Allied”
     Tagline: “Healthcare Exam Academy”
   - Separate top nav (NO nursing links except a small footer link back to nursing)
   - Separate footer
   - Separate pricing CTA components

3) Route separation rules:
   - On nursing hostnames: allied routes should 301/302 redirect to allied subdomain equivalents.
   - On allied hostname: nursing-only routes should 301/302 redirect to nursing hostname equivalents (or show a friendly “Go to NurseNest Nursing” page).
   - Implement a shared helper `resolveHostRedirect(path, hostname)`.

4) Allied routes (MUST exist):
   - /                       (Allied homepage)
   - /pricing                (Allied pricing)
   - /careers                (career directory)
   - /:careerSlug            (career landing)
   - /:careerSlug/qbank
   - /:careerSlug/mock-exams
   - /:careerSlug/study-plan
   - /:careerSlug/flashcards
   - /:careerSlug/sims
   - /:careerSlug/tools
   - /blog                   (Allied blog index - optional but scaffold)
   - /blog/:slug             (Allied blog post - optional scaffold)
   - /account                (shared auth area but shows allied menu)
   - /admin/allied           (admin for allied only, protected)

5) Nursing nav must NOT show Allied. Only add a minimal footer link:
   “Allied Health Exam Prep” -> https://allied.nursenest.ca

--------------------------------------------------------------------------------
B) CAREERS TO SUPPORT (PHASE 1)
--------------------------------------------------------------------------------
Implement these careers under allied:

1) Registered Respiratory Therapist (RRT)          slug: rrt
2) Paramedic / Advanced Care Paramedic             slug: paramedic
3) Pharmacy Technician                             slug: pharmacy-tech
4) Medical Laboratory Technologist (MLT)           slug: mlt
5) Diagnostic Imaging (Radiologic / MRI / Sonography) slug: imaging

Each career must have:
- Landing page with blueprint overview + module list + “Start free diagnostic”
- QBank (filter careerType)
- Mock exams (blueprint weighted)
- Study plan generator
- Flashcards with spaced repetition
- Case sims (unfolding scenarios)
- Tools page (career-specific simulators)
- Analytics dashboard (readiness, weak areas, trends)

--------------------------------------------------------------------------------
C) DATA MODEL + BACKEND (SHARED ENGINE, SEPARATE CONTENT)
--------------------------------------------------------------------------------
1) Add enum/string `careerType` to all relevant content models:
   careerType values: nursing, rrt, paramedic, pharmacyTech, mlt, imaging

2) Extend/ensure tables/collections:
   - questions
   - flashcards
   - mockExams (templates) and examSessions (attempts)
   - studyPlans
   - caseSims
   - blueprints
   - performanceStats
   - contentPages (optional for SEO landing pages)
   All must include `careerType`.

3) Add `countryMode` support (CA, US, global) at question and blueprint level.
   - For allied, default to global unless career requires region-specific values.

4) Server-side authorization:
   - Every endpoint that returns paid content must check subscription status for Allied products.
   - Do not ship full question banks to free users.
   - Use pagination and only return small batches.

5) Prevent mixing:
   - All queries must filter by careerType; never return nursing items on allied endpoints and vice versa.

--------------------------------------------------------------------------------
D) REVENUE-OPTIMIZED FEATURES (MANDATORY)
--------------------------------------------------------------------------------
Implement these features for EACH career vertical:

1) QBank:
   - Filter by topic, difficulty (1–5), questionType, countryMode
   - “Tutor Mode” (show rationale immediately) and “Exam Mode” (show at end)
   - Bookmarks, incorrect-only, flagged questions
   - Pagination + server-side randomization option
   - Minimum seed scaffolding per career (even placeholder) to ensure UI works

2) Mock Exams:
   - Blueprint-weighted exam generator (pull distribution from blueprint JSON)
   - Timed mode + pause/resume
   - Review mode + analytics by topic
   - Prevent repeating same items too frequently (session history based)

3) Adaptive Testing:
   - Implement a simple adaptive algorithm:
     start at difficulty 3 -> adjust +/- 1 based on streak/performance while respecting blueprint proportions.
   - Provide readiness score at end.

4) Readiness Predictor:
   - Compute passProbabilityEstimate from rolling performance:
     weighted accuracy by difficulty + recency weighting + topic coverage.
   - Display “Readiness Meter” with confidence bands.
   - (Don’t claim medical certainty; use language like “estimated readiness”.)

5) Weak Area Analyzer:
   - Identify bottom topics by accuracy and timeSpent.
   - Auto-generate a targeted 10–25 question drill.

6) AI Study Plan Generator:
   - Inputs: exam date, hours/week, baseline diagnostic score, focus topics.
   - Outputs: daily plan with quizzes + sims + flashcards.
   - Weekly recalibration based on performance.

7) Rapid Drill Mode:
   - 10-question timed “Most Tested” rounds, streak tracking.

8) Case Simulators:
   - Unfolding scenarios with decision nodes and debrief.
   - Support NGN-like multi-step case sets (even if the actual allied exams are mostly MCQ, keep it for value).

9) Printable Cram Bundles:
   - Generate/export PDF cram bundle per career (structure only if full PDF pipeline exists; otherwise create an “export later” queue).
   - Offer theme selection at purchase.

10) Progress Dashboard:
   - Topic mastery heatmap
   - Trend chart (accuracy over time)
   - Time-on-task
   - Breakdown by blueprint categories
   - Saved study plan progress

Retention mechanics (allied only):
- Daily streak
- Exam countdown (user sets date)
- Weekly summary email scaffold (store events; actual sending optional)

--------------------------------------------------------------------------------
E) CAREER-SPECIFIC FEATURES + QUESTION TYPES + LESSON REQUIREMENTS
--------------------------------------------------------------------------------
Implement these per career:

(1) RRT (slug rrt)
Question types supported:
- MCQ
- SATA
- Ordered response (vent sequence / priority)
- Calculation (ABG/acid-base steps where applicable)
- Image interpretation (CXR basics)
Key features:
- ABG interpretation engine (tool)
- Ventilator mode decision engine (tool)
- Oxygen device selector drill (tool)
Lessons/modules required (MVP):
- Resp physiology, gas exchange
- Acid–base & ABGs
- Mechanical ventilation basics (modes, settings)
- COPD/asthma, pneumonia
- ARDS basics
- Oxygen therapy & delivery devices
- Pulmonary function tests
- Airway management basics
- Infection control & ventilator bundles
- Neonatal RT basics (light)

(2) Paramedic (slug paramedic)
Question types:
- MCQ
- Ordered response (algorithms)
- Calculation (drug dosing)
- ECG recognition (image/strip ID)
- Case branching
Key features:
- ECG rapid-fire drill (tool)
- Trauma algorithm flow simulator (tool)
- Emergency drug calculator (tool)
Lessons required:
- Primary/secondary survey
- Shock & trauma
- Cardiac rhythms + ACLS concepts (educational)
- Airway management
- Stroke/AMI pathways
- Sepsis recognition basics
- Pediatric emergencies basics
- Disaster triage
- Field pharmacology basics

(3) Pharmacy Tech (slug pharmacy-tech)
Question types:
- Calculation (dose, concentration, ratio, drip, mL/hr)
- MCQ
- SATA
- Label interpretation
- Matching (drug classes)
Key features:
- Dosage calc generator (tool)
- IV drip calculator (tool)
- Compounding simulator (tool)
Lessons required:
- Pharm math (core)
- Dosage forms, routes
- Sterile vs non-sterile compounding
- Controlled substances handling
- Medication safety
- Pharmacy workflow/operations
- High-alert meds overview (educational)
- Law/ethics overview (general)

(4) MLT (slug mlt)
Question types:
- Case interpretation (lab trends)
- MCQ
- SATA
- Critical value identification
- QC scenario analysis
- Image/morphology (optional scaffold)
Key features:
- Lab trend simulator (tool)
- QC failure analyzer (tool)
- Micro ID decision tree (tool - MVP)
Lessons required:
- Hematology basics
- Chemistry basics
- Microbiology basics
- Urinalysis
- Transfusion medicine basics
- Quality control & assurance
- Critical values/reporting workflows
- Lab safety

(5) Imaging (slug imaging)
Question types:
- MCQ
- Ordered response (positioning steps)
- Image labeling (anatomy)
- Radiation safety scenarios
- Contrast reaction case
Key features:
- Anatomy labeling simulator (tool)
- Radiation safety drill (tool)
- Positioning technique engine (tool - MVP)
Lessons required:
- Radiographic anatomy basics
- Positioning fundamentals
- Radiation physics & safety
- Contrast agents & reactions
- MRI safety basics
- CT basics (overview)
- Image recognition: normal vs abnormal (basic)

IMPLEMENTATION NOTE:
If you can’t build full image recognition now, scaffold the UI and store image questions with placeholders and a future upload pipeline.

--------------------------------------------------------------------------------
F) STRIPE + PAYWALLS (SEPARATE FROM NURSING)
--------------------------------------------------------------------------------
1) Create separate Stripe products for Allied:
- allied_pro_monthly
- allied_pro_annual
- allied_cram_addon (optional)
These must not touch nursing products.

2) Access model:
FREE (no subscription):
- 25 questions total per career OR 25/mo
- 1 mini mock exam
- limited flashcards
PRO:
- full QBank, mocks, analytics, study plan, sims, tools
Enforce this server-side. Client must never receive full content when locked.

3) Checkout pages:
- allied subdomain has its own /pricing and /checkout
- nursing checkout remains unchanged

--------------------------------------------------------------------------------
G) SEO SEPARATION (CRITICAL)
--------------------------------------------------------------------------------
1) Create separate sitemap:
- https://allied.nursenest.ca/sitemap-allied.xml
Only include allied published routes.
Do NOT include in nursing sitemap.

2) Add canonical tags appropriate to host:
- allied pages canonical to allied subdomain
- nursing pages canonical to nursing host

3) Robots/meta:
- Do NOT noindex allied by default.
- Ensure allied pages are indexable.

4) Create career landing SEO pages (MVP scaffolding):
- /rrt/rt-practice-questions
- /paramedic/paramedic-practice-test
- /pharmacy-tech/pharmacy-tech-calculations
- /mlt/mlt-practice-questions
- /imaging/radiography-practice-test
(Use internal templates, not hard-coded.)

--------------------------------------------------------------------------------
H) ADMIN: ALLIED CONTENT OPS (HIGH SCALE)
--------------------------------------------------------------------------------
Implement /admin/allied (admin-only) with:
1) Career toggles (publish/unpublish)
2) Blueprint editor per career:
   - categories, weights, subtopics
3) Question bank manager:
   - upload JSON
   - bulk generate (AI)
   - quality validation (no duplicates, rationale length, tag presence)
4) Flashcard manager
5) Case sim manager
6) Pricing manager (display only; Stripe IDs managed in env)
7) Analytics per career:
   - subs count (if accessible)
   - active users
   - question attempts
   - top weak topics

AI generation tools (admin buttons):
- Generate 50 questions
- Generate 200 questions
- Generate 10 case sims
- Generate 200 flashcards
Each must output strict JSON matching schemas and include:
- careerType
- topic/subtopic
- difficulty
- questionType
- rationaleLong >= 500 words (configurable)
- “Why wrong options are wrong”
- Safety/escalation note (where applicable)
Reject output that fails validation.

--------------------------------------------------------------------------------
I) MARKETING INTEGRATION (DO THIS SAFELY)
--------------------------------------------------------------------------------
1) On nursing site:
- Add ONLY a small footer link to allied:
  “Allied Health Exam Prep” -> https://allied.nursenest.ca
- Optionally 1 blog post that mentions allied as “separate platform” with a single link.
Do NOT add allied cards to nursing homepage or nav.

2) On allied site:
- Build standalone SEO: career hubs, topic pages, free practice test pages.
- Add email capture:
  “Get a free diagnostic + weekly study plan”
Store leads in DB (email + career + consent) and scaffold future email sending.

--------------------------------------------------------------------------------
J) REDIRECTS + DOMAIN CONFIG
--------------------------------------------------------------------------------
1) Add a server middleware that inspects Host header:
- If host starts with allied:
   allow allied routes; redirect nursing-only paths to nursing host
- Else:
   allow nursing routes; redirect allied paths to allied host

2) Add secure handling of SSL/proxy headers (x-forwarded-host, x-forwarded-proto) to build correct absolute URLs.

--------------------------------------------------------------------------------
K) DELIVERABLES
--------------------------------------------------------------------------------
1) Implement all above with minimal disruption.
2) Provide a concise “Files changed” summary.
3) Provide env variables required (Stripe product IDs, base URLs).
4) Seed minimal content for each career so every page renders.
5) Add basic tests or smoke checks for:
   - host detection
   - redirect rules
   - paywall enforcement
   - sitemap separation

DO NOT:
- Change nursing sitemap
- Rename existing nursing routes
- Remove existing nursing features
- Alter existing nursing Stripe products

BUILD THIS NOW. END.
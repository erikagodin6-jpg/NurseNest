You are working on my production website (NurseNest). Objective: fix Google Search Console Crawl Stats “host problems last week” for the non-www host (nursenest.ca) and improve crawl/indexing efficiency, WITHOUT changing any layout, UI styling, page structure, routes, or existing functionality. Do not remove any features. Do not refactor unrelated code. Keep behavior identical for end users and admins.

Hard constraints:
- No visual/layout changes.
- No route changes, no renaming slugs, no changing content.
- No breaking changes to auth, admin tools, paywall, or editors.
- Only add minimal code/config needed to resolve crawl and canonicalization issues.
- Prefer server/config changes over UI changes.
- If a change is risky, gate it behind env flags and default to safe behavior.

Context from Google Search Console (Feb 19–24, 2026):
- www.nursenest.ca: ~14,614 crawl requests, “No problems”
- nursenest.ca (non-www): 23 crawl requests, “Problems last week”
- Response breakdown: 99% 200, <1% 301, <1% robots.txt not available, <1% 5xx, <1% 304
- File type: HTML 57%, JavaScript 41%

Tasks (implement all):

1) Enforce ONE canonical host (www)
- Force 301 redirect from https://nursenest.ca/* to https://www.nursenest.ca/* for all paths (preserve path + query string).
- Ensure no redirect loops and no mixed host in internal links.
- Confirm redirect happens before app routing.
Implementation options (choose appropriate for our stack):
  a) If using Cloudflare/hosting rules: add redirect rule.
  b) If using Next.js: implement in next.config.js redirects().
  c) If using Express/Node server: add middleware to redirect based on host header.
Pick the least invasive option that matches existing architecture.

2) Canonical tags and sitemap consistency
- Ensure every HTML page outputs <link rel="canonical" href="https://www.nursenest.ca/<path>"> (same path, no trailing slash changes unless already standardized).
- Ensure sitemap URLs use ONLY https://www.nursenest.ca (no non-www).
- Ensure internal navigation and generated links also use www.

3) robots.txt reliability
- Ensure robots.txt is always available at:
  https://www.nursenest.ca/robots.txt
  https://nursenest.ca/robots.txt  (may redirect to www but must return a valid robots file)
- Make robots.txt return 200 quickly and not depend on app rendering.
- Include the correct Sitemap: line with the www sitemap URL.
- Do not accidentally block important routes.
- Do not block CSS/JS needed for rendering.

4) Reduce crawl waste from JS/resource loading (without changing UI)
- Keep current rendering strategy, but ensure pages are indexable without requiring heavy client-only rendering.
- If any pages are purely client-rendered, add minimal SSR/SSG or dynamic rendering hints ONLY where needed for SEO (head tags, canonical, structured data) without changing UI.
- Ensure meta tags (title/description/robots) render server-side.
- Do NOT change how the page looks or behaves.

5) Fix any sporadic 5XX, 301 chains, or “robots.txt not available”
- Find server logs for Googlebot requests if available.
- Identify endpoints that sometimes 500 under crawl load.
- Add guardrails: timeouts, caching headers, static serving for robots/sitemap, and proper status codes.
- Ensure no accidental 403/401 for public pages due to missing credentials.

6) Verification checklist (must produce)
After changes, output a checklist with curl commands and expected results:
- curl -I https://nursenest.ca/robots.txt  (should 301 -> www then 200)
- curl -I https://www.nursenest.ca/robots.txt (200)
- curl -I https://nursenest.ca/sitemap.xml (301 -> www then 200)
- curl -I https://www.nursenest.ca/sitemap.xml (200)
- curl -I https://nursenest.ca/<sample-page> (301 -> www)
- curl -I https://www.nursenest.ca/<sample-page> (200 + canonical)
Also verify in Google Search Console:
- URL Inspection for both hosts (non-www should resolve to www canonical)
- Request indexing for sitemap and a few pages

Output requirements:
- Provide a short plan (what files/config you will touch).
- Provide the exact code/config diff (or code blocks) for the chosen approach.
- Explain why each change is required and why it does NOT alter layout or functionality.
- Keep changes minimal and reversible.
Perform a mobile performance optimization pass for https://www.nursenest.ca/en based on the latest PageSpeed Insights results.

Current mobile findings to fix
- Performance score: 59
- FCP: 6.9s
- LCP: 8.4s
- TBT: 40ms
- CLS: 0
- Reduce unused JavaScript: est savings 529 KiB
- Reduce unused CSS
- Render-blocking requests
- 9 long main-thread tasks found
- 3 animated elements found
- Some images display with incorrect aspect ratio
- Some text/background contrast issues exist

Goal
Improve mobile performance significantly, especially:
- FCP
- LCP
- overall first-load speed
without breaking layout, SEO, translations, lessons, test banks, or navigation.

Priority 1 — Improve LCP / hero rendering
Audit the above-the-fold homepage content and identify the current LCP element.
Then optimize it aggressively.

Do the following:
- if the hero image/banner/logo is the LCP element, compress it and serve modern formats like WebP/AVIF where supported
- preload only the true LCP asset
- lazy-load below-the-fold images and illustrations
- avoid loading heavy sliders/carousels above the fold on mobile
- simplify mobile hero rendering if needed while keeping the design cohesive
- ensure correct width/height attributes are set on images
- fix incorrect image aspect ratios

Priority 2 — Reduce unused JavaScript
There is too much unused JS on initial load.

Do the following:
- code-split noncritical homepage sections
- lazy-load below-the-fold components
- defer widgets, heavy dashboards, modals, analytics extras, animations, and nonessential interactive features until after first paint
- audit shared bundles to remove code from other tiers/pages that should not load on the homepage
- tree-shake unused dependencies
- avoid hydrating components that are static on first load

Priority 3 — Reduce render-blocking resources
- identify CSS/JS blocking first paint
- inline only critical above-the-fold CSS
- defer noncritical CSS
- defer nonessential scripts
- preload only essential fonts/assets
- eliminate duplicate or unnecessary font weights/styles
- use font-display: swap

Priority 4 — Improve caching and delivery
- set efficient cache headers for static assets
- fingerprint static assets properly
- ensure CDN/static asset caching is working
- compress responses with Brotli/gzip
- confirm image and JS asset caching is long-lived where safe

Priority 5 — Animation and main-thread cleanup
- audit the 3 animated elements and reduce non-composited animations
- replace expensive animations with transform/opacity-based animation only
- disable nonessential animations on mobile if needed
- reduce work during initial mount/render
- break up long tasks where possible

Priority 6 — Accessibility issues found in PageSpeed
- fix insufficient text/background contrast
- keep current aesthetic but meet accessible contrast standards
- do not lower design quality

Priority 7 — Best-practice hardening if easy and safe
- fix missing source maps for large first-party JS if build config supports it
- review CSP/HSTS/COOP/trusted-types recommendations if deployment layer supports them
- do not break app functionality while doing this

Technical constraints
- do not redesign the site
- do not remove key homepage content
- do not break translations/locales
- do not break SEO metadata
- do not break lessons/test banks/exams
- do not remove analytics entirely unless a script is clearly nonessential and blocking rendering
- prefer performance improvements through code-splitting, deferred loading, image optimization, and critical CSS handling

Validation requirements
After changes:
- rerun mobile performance checks
- confirm FCP and LCP materially improve
- confirm homepage still looks correct on mobile
- confirm no broken navigation/menu/header behavior
- confirm no layout shift regressions
- confirm images render at correct aspect ratio
- confirm accessibility contrast issues are fixed

Stretch goal
Aim for:
- Performance score 75+ on mobile initially
- better FCP and LCP without sacrificing visual quality
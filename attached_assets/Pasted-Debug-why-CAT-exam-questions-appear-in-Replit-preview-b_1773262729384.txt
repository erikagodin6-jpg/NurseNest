Debug why CAT exam questions appear in Replit preview but show as 0 questions on the published site.

Investigate and fix this systematically.

Check all of the following:

1. Compare preview vs published environment
- verify whether preview and production are using the same database
- inspect all environment variables and secrets, especially DATABASE_URL and any exam/content-related variables
- confirm production has the same required secrets as preview

2. Inspect the CAT questions data source
- identify exactly where CAT questions are stored
- verify the questions actually exist in the production database
- verify the expected table/collection contains rows in production
- confirm the production query is pointed to the correct table/collection

3. Inspect the frontend request on the published site
- find the API endpoint used to fetch CAT questions
- log the request and response in production
- determine whether production returns an empty array, an error, or filtered-out results
- check browser console and network behavior

4. Inspect backend filtering logic
- check whether CAT questions are filtered by fields such as:
  isPublished
  status
  visible
  tier
  role
  examType
  category
  subscription
  storeReady
- make sure production is not filtering out valid CAT questions accidentally
- confirm the examType/category slug used in production matches the actual CAT question records

5. Inspect deployment state
- verify the published site is using the latest build
- confirm the app was redeployed after the CAT questions were added/imported
- clear any stale caching issues if present

6. Inspect for preview-only mock data
- determine whether preview is showing local/mock/hardcoded/admin-cached question data instead of real backend data
- replace mock data with real persisted database data if needed

7. Add debugging output
- add temporary logs to show:
  total CAT questions found in database
  total CAT questions returned by API
  filters applied to CAT questions
  why questions are excluded
- expose enough logging to identify the mismatch between preview and production

8. Fix the issue fully
- make the published site show the correct CAT question count
- ensure preview and production behave consistently
- ensure CAT questions are loaded from the real persisted source
- ensure the correct route, exam key, and filters are being used

After fixing, show me:
- root cause
- files changed
- database or env changes needed
- whether I need to redeploy or reseed production data
Audit and fix the live production blank white screen issue on NurseNest without changing the tiered menus or overall site structure.

Goal:
The site loads to a white page in production. Find the root cause and fix it safely. Do not make cosmetic changes. Do not refactor unrelated areas. Do not change navigation architecture unless a broken route is the direct cause.

Tasks:
1. Check the browser console and production build output for runtime errors.
2. Inspect the app entry points, router, locale detection, and hydration/render flow.
3. Verify that the root React mount is succeeding.
4. Check for any component that crashes on first render because of:
   - undefined data
   - window/document usage during SSR or build
   - bad imports
   - missing environment variables
   - invalid lazy loading
   - broken theme or translation provider
   - route guard redirect loops
5. Verify that the default route / and /en both render correctly in production.
6. Add defensive guards so the app never renders a blank page silently.
7. Add an ErrorBoundary around the main app shell so future crashes show a fallback instead of a white screen.
8. Log the exact root cause in comments near the fix.

Specific files/areas to inspect:
- main client entry
- App.tsx / router config
- locale/i18n initialization
- auth/session bootstrap
- theme provider
- any recent hero page or navigation changes
- any Suspense/lazy routes
- any code that reads localStorage/sessionStorage/window on initial render
- any API bootstrap call that blocks rendering

Required fixes:
- Prevent blank screen on runtime failure
- Ensure at least the homepage shell renders even if one widget fails
- Add null checks and fallback UI for missing data
- Fix broken imports or route definitions
- Ensure production env vars fail gracefully

Validation:
- Confirm / loads
- Confirm /en loads
- Confirm main homepage content renders
- Confirm no fatal console errors remain
- Confirm no redirect loop
- Confirm navigation still works
- Confirm no changes to tiered menus

Output:
1. Show the exact root cause
2. Show the exact files changed
3. Show the minimal patch applied
4. Confirm production-safe deployment steps
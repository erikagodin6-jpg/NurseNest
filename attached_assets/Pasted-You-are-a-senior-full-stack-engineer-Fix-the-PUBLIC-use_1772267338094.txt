You are a senior full-stack engineer. Fix the PUBLIC (user-facing) language behavior end-to-end.

Current bug:
- The language switcher updates hero/nav only.
- Lesson/module titles, lesson bodies, and activity text do NOT change for users.
- Static lessons/modules are rendered from TypeScript data (contentMap and rpnSystems/rnSystems/npSystems), so they bypass i18n unless we wire them into it.

User-facing requirements (non-negotiable):
1) Auto-detect language on first visit (no login/admin needed)
- On first page load, set language to:
  a) URL language prefix if present (/fr/..., /es/...)
  else b) saved preference (localStorage/cookie)
  else c) browser/OS language (navigator.language, e.g., fr-CA -> fr)
  else d) default to English
- Persist the chosen language to localStorage (and cookie optional).

2) Language switcher must affect ALL user content immediately
When a user clicks a language:
- Update global language state
- Persist preference
- Immediately update (without refresh):
  - Static lesson list card titles
  - Static lesson detail title + body
  - Static activity/module text (prompts, buttons, feedback)
- Fallback to English when translation missing (never blank).

3) No AI translation at runtime for users
- Do not auto-generate translations on GET requests.
- Do not call any AI when users browse.
- Translations must be pre-existing (static JSON or stored in DB) and fast to load.

Implementation (Path A - static TS lessons):
A) Create a translation source used by the PUBLIC site
- Create client/src/data/translations/lessons.i18n.json keyed by lessonId, with:
  title: {en, fr, ...}
  body: {en, fr, ...} (or sections)
  activities: {en, fr, ...}
- Add helper getI18n(obj, lang, fallback="en") => obj?.[lang] ?? obj?.[fallback]

B) Make IDs the source of truth in listings
- Update rpnSystems/rnSystems/npSystems so diseases do NOT store name as the display text.
- Diseases store id only (and any language-neutral metadata).
- Render name via getI18n(lessonTranslations[id].title, currentLang).

C) Lesson detail rendering
- Wrap baseLesson = contentMap[id] with getLocalizedLesson(id, lang) that overlays translation fields onto the base English lesson.
- Ensure lesson-detail re-renders when language changes (lang in dependencies; no stale memoization).

D) Activities/modules
- Identify all user-facing activity strings (prompts, hints, button labels, feedback).
- Ensure these strings are sourced from i18n or translations JSON and update on language change.

E) Routing (choose one, but must be consistent):
Option 1 (recommended for SEO): language-prefixed routes
- /en/lessons/:id
- /fr/lessons/:id
Language switch updates route prefix and rerenders content.
Option 2: single route + internal language state
- Still must detect browser language and persist.

Choose the approach that matches the existing router. Do not break existing URLs.

Verification (must provide proof):
- Add temporary console logs for:
  - current language
  - rendered lesson title in list for a known lessonId
  - first 80 characters of rendered lesson body on detail page
  - rendered activity prompt text
Toggle language and show logs change accordingly.
- Provide screenshots of the same lesson list + lesson detail in two languages.
- Remove temp logs afterward.

Deliverables:
- List of files changed
- Translation JSON path + sample entry
- Explanation of language detection priority (URL > saved > browser > default)
- Manual test checklist
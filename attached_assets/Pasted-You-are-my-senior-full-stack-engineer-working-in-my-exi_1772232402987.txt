You are my senior full-stack engineer working in my existing NurseNest codebase (Vite + React frontend, Express backend, DB already exists, Stripe/subscriptions exist).

Implement an SEO → Free Practice → Subscription conversion layer.

Requirements:
1) Create dynamic SEO practice landing pages:
   Route: /practice/:examType/:topicSlug
   Examples: /practice/nclex/hyperkalemia, /practice/rexpn/prioritization, /practice/np/dka, /practice/nclex-nextgen/cardiac-case-study

2) Each page must have:
   - long-form SEO content (stored in DB as markdown/HTML blocks)
   - a “Free Practice” widget with 5–10 questions
   - a conversion CTA section that triggers upgrade flow
   - internal linking module (“Related topics”, “Related lessons”, “Related flashcards”)
   - FAQ section + FAQ schema
   - canonical tags + breadcrumbs

3) Gate interactivity, not content:
   - Page content is viewable without login (indexable)
   - Practice widget requires login to submit answers (or allow anon with cookie-based daily limit; choose simplest consistent approach)
   - Free users: 5 questions per topic per day (configurable)
   - Paid users: unlimited

4) Add server-side enforcement:
   - Create tables for practice_sessions and practice_attempts (or reuse existing test tables)
   - Track daily attempts per user+topic
   - Return error code FREE_LIMIT_REACHED
   - On limit reached, UI shows upgrade modal

5) Add “Mock Exam” landing pages:
   Route: /mock-exam/:examType
   - Includes 3 question preview for free users
   - Full exam requires paid subscription

6) Do not index:
   - results pages
   - personalized dashboards
   - any paid-only question banks
   Ensure robots/meta tags prevent indexing where appropriate.

7) Analytics:
   - Log events: practice_started, question_answered, free_limit_reached, upgrade_clicked, subscribed
   - Build an internal admin dashboard page showing conversion per topicSlug and examType

Deliverables:
- DB migrations
- backend routes + middleware for limits
- frontend routes + reusable PracticePage template
- SEO meta utilities (canonical, schema)
- minimal tests for limit enforcement and publish/indexing rules

Start by scanning the repo for existing question bank models, subscription middleware, routing, and admin dashboard patterns. Implement incrementally with production-ready code.
You are my senior full-stack engineer in NurseNest (Vite + React + Express + DB). Implement “Real Exam Mimic Mock Exams” with per-exam configuration and strict realism mode.

GOAL
Mock exams must mimic real exams as closely as possible including:
- timing window
- min/max items
- CAT stopping rules where applicable
- optional break timing
- scoring style (partial credit where required)
- unscored/pretest items
- mandatory case study sets where applicable
- no-backtracking rules

CREATE AN EXAM BLUEPRINT REGISTRY (DB + Admin UI)
exam_blueprints table:
- exam_id (NCLEX-RN, NCLEX-PN, REx-PN, AANP-FNP, ANCC-FNPBC, etc.)
- is_cat (bool)
- min_items
- max_items
- time_limit_minutes (includes breaks + intro)
- optional_breaks (json array of minutes from start; e.g., [120, 210] for NCLEX)
- has_pretest_items (bool)
- pretest_count
- requires_case_studies (bool)
- case_study_sets_min (e.g., 3 for NCLEX)
- case_study_items_per_set (e.g., 6)
- scoring_rules (json):
    - per question_type: dichotomous vs partial_credit method
    - exam-specific partial credit algorithm (e.g., SATA poly; NGN partial credit buckets)

DEFAULT BLUEPRINTS (seed)
NCLEX-RN and NCLEX-PN:
- is_cat = true
- min_items=85, max_items=150
- time_limit=300 minutes
- optional_breaks at 120 and 210 minutes
- pretest_count=15
- requires_case_studies=true, case_study_sets_min=3, 6 items each

REx-PN:
- is_cat = true
- min_items=90, max_items=150
- time_limit=240 minutes
- optional breaks allowed (no forced schedule, but allow admin to add)
- scoring: dichotomous except SATA partial credit (effective Jan 2026 behavior)

AANP-FNP:
- is_cat=false
- total_items=150
- time_limit=180 minutes
- pretest_count=15

ANCC-FNPBC:
- is_cat=false
- total_items=175
- time_limit=210 minutes
- pretest_count=25
- passing is scaled; we show pass/fail and percent correct, but do NOT claim a universal “minimum correct”.

MOCK EXAM MODES (User-facing)
- Realistic Mode (default for paid): no backtracking, timed, breaks, CAT/blueprint rules enforced
- Tutor Mode (optional): allow review, explanations after each question, pause timer

CAT SIMULATION (for NCLEX + REx-PN)
Implement a CAT simulator:
- Maintain an ability estimate (theta) updated after each scored item
- Stop rules:
  1) 95% confidence interval: stop when CI is entirely above or below passing standard
  2) Maximum-length: stop at max_items
  3) Run-out-of-time: if time expires:
     - if answered < min_items => fail
     - else decide based on final ability estimate vs standard (use last stable theta)
- Enforce content constraints:
  - For NCLEX: include at least 3 NGN case study sets (18 items) distributed through the exam
  - Enforce question-type distribution targets based on blueprint (admin adjustable)

SCORING + REPORTING
- Separate “scored items” vs “pretest items” internally; do not reveal which.
- Implement partial credit where required per blueprint scoring_rules.
- Show:
  - overall outcome (pass/fail for CAT simulation)
  - readiness score
  - performance by topic/body system/question type
  - time management metrics
- Do NOT claim “you need X correct to pass” for CAT exams.

BREAKS + TIMER
- Timer always counts down from blueprint time_limit_minutes.
- At optional break marks:
  - show break modal with Start Break / Skip
  - break time counts toward total time (like real exam behavior)
- Allow unscheduled breaks (pause screen) but do NOT pause timer in Realistic Mode.

ADMIN CONTROLS
- Admin can edit exam blueprints
- Admin can run “blueprint validation” tests
- Admin preview mode that exactly mirrors learner experience

DELIVERABLES
- DB migrations + seeds
- Admin UI for blueprint registry
- Mock exam engine updates (CAT + fixed)
- QA tests for stop rules, timeouts, break events, case-study requirements
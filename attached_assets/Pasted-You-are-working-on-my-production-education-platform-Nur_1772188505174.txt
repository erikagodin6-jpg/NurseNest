You are working on my production education platform (NurseNest).

Objective:
Improve Google indexing ratio and strengthen topical authority WITHOUT changing layout, visual design, routes, URL structure, paywall behavior, admin tools, or existing content. No UI changes. No feature removals. No refactors unrelated to SEO/indexing.

Do NOT:
- Change page layout
- Rename slugs
- Break internal navigation
- Modify blog generator
- Remove any content
- Alter subscription tiers or gating
- Introduce breaking changes

Focus strictly on technical SEO and indexing optimization.

----------------------------------------
PHASE 1: INDEX CONTROL & CANONICALIZATION
----------------------------------------

1) Identify low-value or non-primary pages and apply:
   <meta name="robots" content="noindex, follow">

Candidates include:
- Filter pages
- Tag pages
- Query parameter variants
- Preview/draft routes
- Admin-accessible content
- Paginated variations (if thin)
- Auto-generated flashcard-only pages (if not standalone lessons)

IMPORTANT:
Do NOT noindex:
- Core lessons
- Blog posts
- Cornerstone guides
- Tier-based educational content
- Major category hubs

2) Ensure every indexable page has:
   - Correct canonical tag (www version only)
   - Self-referencing canonical
   - No duplicate canonical conflicts

3) Ensure sitemap.xml:
   - Includes ONLY indexable URLs
   - Uses https://www.nursenest.ca
   - Excludes noindex pages automatically

----------------------------------------
PHASE 2: INTERNAL LINKING OPTIMIZATION
----------------------------------------

Without changing layout:

1) Automatically add contextual internal links at the bottom of lesson pages:
   - Related Pathophysiology
   - Related Pharmacology
   - Related Diagnostics
   - Related Exam Strategy
   - Same-topic higher/lower tier version (RPN → RN → NP)

2) Ensure minimum internal link density:
   - At least 5 contextual links per lesson
   - No orphan pages

3) Add breadcrumb structured data (JSON-LD only, no UI change)

----------------------------------------
PHASE 3: STRUCTURED DATA (NO VISUAL CHANGE)
----------------------------------------

Add JSON-LD schema where appropriate:

- Article schema for blog posts
- EducationalArticle for lessons
- FAQ schema if page contains Q&A
- BreadcrumbList schema
- Organization schema (site-wide)

Render schema server-side.

----------------------------------------
PHASE 4: RENDERING & META STABILITY
----------------------------------------

1) Ensure:
   - Title tag renders server-side
   - Meta description renders server-side
   - Canonical renders server-side
   - No JS-only head tags

2) Verify no public pages return:
   - 403
   - 401
   - 5xx under crawl

----------------------------------------
PHASE 5: INDEX QUALITY REPORT
----------------------------------------

Generate a report listing:

- Total URLs
- Indexable URLs
- Noindex URLs
- Orphan URLs
- Duplicate canonical conflicts
- Pages with low word count (<400 words)

Do NOT automatically delete anything.
Only flag.

----------------------------------------
OUTPUT REQUIREMENTS
----------------------------------------

1) Provide summary of changes.
2) Show exact code/config modifications.
3) Confirm no layout or route changes.
4) Confirm paywall and admin behavior unchanged.
5) Provide post-deployment validation checklist using:
   - curl tests
   - Search Console inspection
   - sitemap validation

All changes must be minimal, reversible, and isolated to SEO logic only.
You are my senior full-stack engineer + SEO architect working inside NurseNest (Vite + React + Express + DB + i18n + Admin Dashboard).

Implement a COMPLETE multilingual SEO pillar + cluster system so NurseNest can rank in ALL 15 languages, with fully translated pillar pages, topic clusters, internal linking, hreflang, and language-specific sitemaps.

IMPORTANT:
- Do not leak paid content.
- Do not overwrite human-reviewed translations.
- Preserve existing English content as the source of truth.
- Ensure clean, clinical, brand-consistent UI.

====================================================
PART 1 — MULTILINGUAL URL + ROUTING STANDARD (FOUNDATION)
====================================================

Adopt one consistent structure for all public content:

/{lang}/
Examples:
- /en/nclex-study-guide
- /fr/guide-nclex
- /es/guia-nclex
- /pt/guia-nclex
- /hi/nclex-adhyayan-guide (or localized slug rules)

Rules:
- Default language: /en
- Always include language prefix for SEO pages (no mixing).
- Store canonical per-language URL.
- Add middleware to resolve language from URL prefix and set locale.
- If user changes language, redirect to the equivalent translated page if available; else fallback to English with a visible notice.

====================================================
PART 2 — PILLAR PAGE SYSTEM (TEMPLATED, TRANSLATABLE)
====================================================

Create a dedicated content type: SEO Pillar Page

DB tables:
seo_pages
- id
- page_type (pillar|cluster|comparison|glossary_index|diagnostic_landing)
- exam (NCLEXRN|NCLEXPN|REXPN|CNPLE|AANP_FNP|ANCC_FNPBC)
- language_code
- title
- slug
- meta_title
- meta_description
- content_html (or markdown, choose one standard)
- toc_json (optional)
- faq_json (array of {q,a})
- internal_links_json (optional list of suggested links)
- is_public (bool)
- is_indexable (bool)
- canonical_url
- last_updated
- translation_status (en_source|auto|human_reviewed)

Also create:
seo_page_map
- page_group_id (ties all languages of same page)
- language_code
- seo_page_id

Template requirements:
- 2,500–4,000 words for pillars
- 1,200–2,000 words for clusters
- 8–12 FAQs for pillars; 5–8 FAQs for clusters
- Include internal linking modules:
  - “Start here”
  - “Related lessons”
  - “Practice sets”
  - “Glossary terms”
- Add a “Last updated” line on every SEO page.

Pillar pages to create (minimum set, per language):
1) NCLEX-RN Study Guide (pillar)
2) NCLEX-PN Study Guide (pillar)
3) REx-PN Study Guide (pillar)
4) CNPLE NP Study Guide (pillar)
5) AANP FNP Study Guide (pillar)
6) ANCC FNP-BC Study Guide (pillar)

Topic Cluster Pages per pillar (minimum 12 per exam):
- Cardiac
- Respiratory
- Neuro
- Endocrine/Diabetes
- Renal/Electrolytes
- GI
- OB/Maternity
- Pediatrics
- Mental Health
- Pharm High-Risk Meds
- Delegation/Prioritization
- Infection Prevention/Safety

Comparison pages (optional but recommended, per language):
- NCLEX-RN vs NCLEX-PN
- REx-PN vs NCLEX-PN
- CNPLE vs AANP vs ANCC (NP exams)

Glossary:
- /{lang}/glossary (index)
- /{lang}/glossary/{term-slug}

====================================================
PART 3 — MULTILINGUAL TRANSLATION WORKFLOW FOR SEO PAGES
====================================================

Implement an admin “SEO Translation Workflow”:

- English pages are the source of truth (translation_status=en_source).
- For each seo_page_group_id:
  - Track which languages exist and their status.
- Admin dashboard features:
  - “Create all missing language versions” (creates drafts)
  - “Generate draft translations” (optional, admin-controlled)
  - “Manual editing” UI with side-by-side English reference
  - “Mark as human reviewed”
  - “Never overwrite human_reviewed content”
  - “Diff viewer” to show what changed in English and needs re-translation

Translation rules:
- Translate title, meta title/description, headings, body, FAQs.
- Localize units/terminology if you already separate Canada vs US values elsewhere.
- Maintain medical accuracy; avoid literal translation of acronyms without explanation.
- Allow language-specific slug overrides.

====================================================
PART 4 — INTERNAL LINKING + TOPIC CLUSTER AUTOMATION
====================================================

Build an internal link engine for SEO pages:

- Each pillar must link to all cluster pages for that exam in that language.
- Each cluster page must link back to its pillar (top + bottom).
- Add “Related topics” module using shared tags (same language only).
- Ensure links always point to same-language pages when available.
- If translation missing, link to English with a “(English)” suffix and add a missing-translation flag internally.

Also create “Breadcrumbs” for all SEO pages:
Home > Exam > Topic > Page

====================================================
PART 5 — MULTILINGUAL SEO TECHNICAL REQUIREMENTS
====================================================

A) HREFLANG
For every seo_page_group_id:
- Output hreflang tags for all languages that have a page.
- Include x-default pointing to English or language selector.

B) CANONICALS
- Each language page canonicalizes to itself (not English).

C) SITEMAPS
Generate:
- /sitemap.xml (index)
- /sitemap-en.xml
- /sitemap-fr.xml
... (one per language)
Include:
- SEO pages (pillar/cluster/comparison/glossary)
Exclude:
- paywalled question pages
- private user dashboards
Set lastmod from last_updated.

D) INDEXING RULES
- Pillar/cluster pages: indexable true
- Paywalled lessons: noindex (unless you have a public preview page)
- Free diagnostic landing pages: indexable true
- Prevent thin pages:
  - Block publishing indexable=true if wordcount < 1200 (clusters) or < 2500 (pillars), unless admin overrides.

E) SCHEMA MARKUP
Add JSON-LD:
- Article schema for SEO pages
- FAQPage schema for FAQ blocks
- BreadcrumbList schema
- (Optional) HowTo schema for “How to use study plan generator” sections

====================================================
PART 6 — CONTENT GENERATION WITHOUT RELYING ON AI (OPTIONAL)
====================================================

Provide a structured “Pillar Page Builder” in admin (no AI required):

Fields that auto-render into formatted content:
- Intro summary
- Exam format summary
- High-yield topic list
- Study plan recommendations (bulleted)
- Common mistakes section
- Practice strategy section (timing, SATA, prioritization)
- FAQ builder
- Internal link selector

Allow CSV import for seo_pages:
- language_code, exam, slug, title, meta, content, faq_json, last_updated

====================================================
PART 7 — QUALITY CONTROL + REPORTING
====================================================

Add Admin QC dashboards:

1) Multilingual Coverage Report
- For each seo_page_group_id: which languages exist, missing count, last updated

2) SEO Completeness Report
- Wordcount thresholds met?
- Meta title/description present?
- FAQ present?
- Schema present?
- Internal links count
- Orphan cluster pages

3) Indexing Health Report
- Indexable page count by language
- Sitemap generated pages count
- Broken links by language

====================================================
PART 8 — DELIVERABLES
====================================================

Deliver:
- DB migrations for seo_pages and mapping
- Admin UI:
  - SEO Pages manager (create/edit/publish)
  - Translation workflow dashboard
  - Pillar builder form
  - QC dashboards
- Frontend routes for /{lang}/... SEO pages + glossary
- Automated hreflang + schema injection
- Language sitemaps + sitemap index
- Tests for:
  - hreflang correctness
  - canonical correctness
  - sitemap generation
  - missing translation fallback behavior
  - indexability gating rules

Constraints:
- Do not break existing blog generator
- Preserve admin copy/paste ability
- Do not expose premium questions
- Maintain clinical, clean NurseNest UI
- Keep performance fast: do not load all languages at once; cache per-language pages.

Start implementation in this order:
1) seo_pages schema + language routing
2) pillar/cluster page rendering routes + hreflang/canonicals
3) admin SEO page manager + translation workflow
4) language sitemaps + schema
5) QC dashboards
6) pillar builder + CSV import
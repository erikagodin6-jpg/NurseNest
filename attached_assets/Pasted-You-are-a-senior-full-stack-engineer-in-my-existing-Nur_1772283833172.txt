You are a senior full-stack engineer in my existing NurseNest codebase. Implement an AI-powered content generation + scheduling workflow INSIDE my admin panel for TWO content types: (1) Exam Questions and (2) Flashcards. This must mirror the UX pattern of my existing admin blog editor + scheduler (which already works). Do NOT break the blog generator/scheduler. Reuse existing patterns, components, auth, and scheduler infrastructure wherever possible.

High-Level Goal
From the Admin UI, I want to:
- Choose tier (RPN/LVN, RN, NP)
- Choose exam targets (REx-PN, NCLEX-PN, NCLEX-RN, AANP, ANCC, Canadian NP)
- Choose locale targeting (Canada, US, Both)
- Choose topic(s) and subtopics
- Select question types distribution (MCQ, SATA, prioritization, pharm, labs, etc.)
- Click “Generate Batch” to create drafts (not published yet)
- Edit drafts as needed
- Schedule publish (one-off or batch cadence like “25/day at 9am”)
- Publish immediately if desired
- See logs, cost estimates, daily caps, and generation history

Strict Constraints
- AI generation must be OFF by default behind feature flags.
- Must implement daily caps and token/cost guardrails.
- Must never auto-publish AI content unless I explicitly schedule or click publish.
- Must respect tier gating and adminView access rules (admin can view all drafts/tier content with credentials).
- Must not modify the blog generator or blog scheduler behavior.

PART 1 — Use the same Admin + Scheduler architecture as Blog
1) Identify the existing Blog Admin Editor + Scheduler pattern:
- Admin UI components and routes
- API routes
- DB schema for drafts/publish/scheduled
- Scheduler runner / job queue
- Admin credentialed fetch approach (credentials: include + adminView param)
2) Reuse these patterns for exam questions + flashcards so the behavior matches blog:
- Draft save
- Publish
- Schedule
- Admin preview for drafts
- Public access restrictions for non-published or paid content

PART 2 — Add AI Generation Service (server-side)
A) Create a dedicated server module: services/aiContentGenerator.ts (or equivalent)
Responsibilities:
- Accept a structured request payload for generating exam questions or flashcards.
- Build a prompt using the “Item Writer” templates I already use.
- Call the LLM provider (OpenAI or my existing provider) via a single wrapper.
- Return strictly structured JSON that matches my DB models (no freeform text).

B) Feature Flags + Limits (mandatory)
- ENV flags:
  ENABLE_AI_ADMIN_TOOLS=false
  MAX_AI_ITEMS_PER_DAY=200
  MAX_AI_TOKENS_PER_DAY=300000 (or similar)
  MAX_AI_COST_PER_DAY=10.00 (if cost tracking available)
- Enforce caps server-side per day and per admin user:
  - If cap exceeded: return 429 with clear error
- Log each generation:
  - adminId, timestamp, contentType, itemsRequested, itemsCreated, tokensUsed, estimatedCost, promptVersion

C) JSON Schema Output Enforcement
- The AI must output valid JSON only, conforming to schema:
ExamQuestion:
{
  "slug": "stable-unique-slug",
  "stem": "...",
  "choices": [{"key":"A","text":"..."},...],
  "correctAnswer": "B",
  "rationale": {"correct":"...", "incorrect":{"A":"...","C":"...","D":"..."}},
  "topic": "...",
  "subtopic": "...",
  "difficulty": "easy|moderate|hard",
  "questionType": "mcq|sata|priority|pharm|labs",
  "tier": "RPN|LVN|RN|NP",
  "examTarget": ["REx-PN","NCLEX-PN",...],
  "localeTarget": "CA|US|BOTH",
  "tags": ["..."]
}

Flashcard:
{
  "deckSlug": "...",
  "front": "...",
  "back": "...",
  "tags": ["..."],
  "topic": "...",
  "subtopic": "...",
  "tier": "...",
  "examTarget": [...],
  "localeTarget": "CA|US|BOTH"
}

- If the model returns invalid JSON, implement a repair/retry ONCE (max 1 retry) and then fail with a clear error.
- Validate on server with zod (or existing validation library).

PART 3 — Database Writes (drafts only)
- Create endpoints:
  POST /api/admin/ai/exam-questions/generate
  POST /api/admin/ai/flashcards/generate
- These must:
  - require admin authentication
  - enforce caps
  - validate JSON schema
  - upsert into DB with status="draft" (never published automatically)
  - attach metadata: source="ai", promptVersion, generationBatchId
  - return created IDs/slugs and summary stats
- Provide batchId for tracking and for bulk scheduling/publishing.

PART 4 — Admin UI Implementation (matches Blog UX)
Add new admin pages (or tabs) that mirror blog:
1) AdminExamQuestionManager
- Filters: status, tier, examTarget, localeTarget, topic, batchId
- Bulk actions: schedule publish, publish now, archive
- “AI Batch Generator” panel:
  - Fields: Tier, Exam target(s), Locale target, Topic(s), difficulty mix, question-type distribution, quantity
  - Button: Generate Draft Batch
  - After generation: show summary (created count, batchId, token/cost estimate)
2) AdminFlashcardManager
- Same list + filters + bulk actions
- “AI Batch Generator” panel:
  - Choose deck, topic, tier, examTarget, localeTarget, quantity
  - Option: generate cloze vs basic
  - Button: Generate Draft Batch

Scheduling UX (must match Blog)
- Single item schedule
- Batch schedule:
  - e.g., “Publish 25 per day at 09:00 America/Toronto”
  - e.g., “Publish all in batch at a specific datetime”
- Use existing scheduler infrastructure. If scheduler already supports RRULE-like recurrence, reuse it. If not, implement a simple batch cadence scheduler:
  - store cadence settings and compute publishAt for each item deterministically.

Important UI rules
- No window.location.reload()
- After create/generate/publish/schedule, update state from API response.
- Fetch calls from admin must always include credentials and adminView=true for gated content.
- Never auto-open editor on fetch failure. Show explicit Not Found / Access / Draft UI.

PART 5 — Scheduler Runner Extensions
- Extend scheduler runner to also publish:
  - scheduled exam questions
  - scheduled flashcards
- Idempotent publishing:
  - only publish if status=="scheduled" AND publishAt<=now
  - set status="published", publishedAt=now
  - log success/failure
- Add guard: if an item is missing required fields, fail and log.

PART 6 — Logging + Audit + Safety
- Add an Admin “AI Activity” log page:
  - generation batches, counts, tokens, estimated cost, failures
- Add “Delete batch” option (admin only) that sets items to archived (do not hard delete unless your system already does).
- Add rate limiting per admin user.
- Ensure prompts avoid PHI and do not include real patient identifiers.

PART 7 — Use my item-writer prompt templates
Use the following prompt content as the base template for generation (embed it into the server prompt builder). The templates must produce licensing-style items with rationales and exam focus.

Template: Exam Questions
- scenario-based when possible
- mix of question types (MCQ, SATA, prioritization, pharm, labs)
- tier-appropriate expectations (RPN vs RN vs NP)
- include correct answer + rationales (correct and incorrect)
- include clinical pearl
- include CA/US lab units when relevant

Template: Flashcards
- front/back concise, high-yield
- tags, topic/subtopic
- include normal ranges (CA/US) when relevant
- avoid fluff

PART 8 — Acceptance Criteria / Tests
- Admin can generate 100 exam questions as drafts, see them immediately in list, edit, then schedule publish.
- Admin can generate 200 flashcards into a deck as drafts, bulk schedule them 25/day at 9am.
- Public cannot see drafts.
- Paid gating works for public, but admin can preview everything.
- Scheduler publishes on time and logs results.
- Daily caps prevent runaway costs.
- Blog generator/scheduler remains unchanged and passes regression tests.

Deliverables
1) List all files changed/added.
2) List all new endpoints.
3) Show the schema validation used.
4) Show how caps and feature flags are enforced.
5) Provide a manual regression checklist.

Proceed now. Start by locating the blog editor + scheduler patterns and cloning them for exam questions and flashcards with shared utilities where safe.
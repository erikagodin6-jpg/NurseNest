You are working on the live NurseNest production codebase.

This is a CRITICAL production incident. The platform is in an active failure loop and must be stabilized immediately.

The system is currently showing all of the following:

- repeated memory alerts every few seconds/minutes
- emergency mode activated repeatedly
- RSS exceeded critical threshold
- runtime memory around 188–189 MB out of 192–193 MB (~98%)
- synthetic exam flow failures, including exam_open_flow aborted
- published content items with zero valid items
- reliability alerts being mislabeled as “Payment Failed”
- customers unable to reliably load exams/pages
- prior fixes have not solved the problem

This is NOT a single bug. This is a cascading multi-system reliability incident involving infrastructure, memory pressure, exam loading, content integrity, alerting, and failure handling.

PRIMARY OBJECTIVES

1. Stop the production meltdown loop immediately
2. Stop the nonstop error email flood
3. Stabilize memory and prevent repeated emergency mode activation
4. Make exam opening and core user flows reliable
5. Prevent invalid/empty content from being published or shown
6. Correct the alerting system so alerts are categorized properly
7. Protect users from dead ends and broken pages
8. Produce a clear root-cause report and verification checklist

==================================================
PHASE 1 — IMMEDIATE INCIDENT CONTAINMENT
==================================================

Treat this as a live outage.

Implement immediate containment in this order:

1. Raise deployment/runtime memory to a production-safe level
- Treat ~192 MB as critically underprovisioned and unacceptable
- Set a minimum safe production target
- Prefer 4 GB or higher if required by workload
- Document the final recommended production memory baseline

2. Restart or redeploy cleanly after scaling
- ensure stale processes are cleared
- ensure emergency mode does not instantly re-enter due to bad baseline state

3. Enable hard load shedding when memory is high
When memory exceeds warning/critical thresholds:
- block or degrade heavy routes
- disable nonessential expensive features
- return lightweight fallback responses instead of attempting full processing
- do not let the app continue trying heavy operations when already in distress

4. Temporarily suppress alert storms
Implement alert deduplication, cooldowns, grouping, or rate limiting so the user does not receive the same critical email every few seconds
- preserve incident visibility
- stop spam
- group repeated incidents into a single rolling incident where appropriate

==================================================
PHASE 2 — MEMORY STABILIZATION
==================================================

Perform a full audit of memory pressure and memory leaks.

Inspect and fix all likely causes, including but not limited to:
- loading too many exam questions at once
- loading all rationales/metadata/images up front
- oversized API responses
- repeated serialization of large payloads
- unbounded global caches
- arrays/maps/objects growing over time
- background jobs running in the main web process
- repeated timers/intervals/listeners
- retry loops on server or client
- synthetic monitors repeatedly hitting broken heavy routes
- AI/content generation retaining large objects in memory
- in-process storage of exam attempts or sessions
- frontend infinite refetch/re-render loops
- loading all DB rows into memory instead of paging

Implement:
- bounded memory-safe patterns
- pagination/chunking
- lazy loading
- cache limits and eviction
- route-level memory logging
- concurrency caps
- request timeouts
- memory threshold actions at 70/80/90%+
- graceful degradation under pressure

Add instrumentation for:
- RSS
- heap used / heap total
- route-level latency
- payload sizes
- concurrent heavy jobs
- request counts on critical routes
- memory trend over time

Identify which routes are the highest-memory offenders and fix them first.

==================================================
PHASE 3 — EXAM FLOW HARDENING
==================================================

Trace and harden the full exam_open_flow end-to-end:
- frontend initiation
- auth/session
- entitlement checks
- exam lookup
- content/question fetch
- validation
- serialization
- response delivery
- frontend render

Find exactly where “This operation was aborted” occurs.

Then implement:
- graceful fallback instead of hard abort
- safe timeout handling
- retry only where appropriate
- frontend error boundaries
- backend circuit breakers
- smaller/lighter exam payloads
- request IDs and structured logs across the flow
- alternate response if full exam content cannot be loaded

Users must never hit a dead-end or broken exam screen.
If a full exam cannot load, serve a stable fallback experience.

==================================================
PHASE 4 — CONTENT INTEGRITY ENFORCEMENT
==================================================

Audit the publish pipeline and content validation system.

Known failures include published content items with zero valid items, including:
- Restless Leg Syndrome Rpn
- Macular Degeneration Rpn
- Gestational Diabetes Rpn
- Bell’s Palsy Rpn

Determine the exact cause for each:
- generator returned malformed/empty items
- validation too weak
- publish bypassed validation
- tier/language filtering removed all items
- storage/serialization corruption
- migration/import problem
- data integrity checks missing

Implement strict enforcement:
- content with zero valid items must never be publishable
- add pre-publish validation blockers
- automatically quarantine or unpublish already-broken content
- remove invalid content from public listings automatically
- surface exact reasons in admin
- add repair/regeneration tooling where needed

No published user-facing item should exist with zero valid underlying content.

==================================================
PHASE 5 — ALERTING / EMAIL / INCIDENT CORRECTNESS
==================================================

Audit the alerting and notification system.

Current issue:
Reliability incidents are being emailed under incorrect labels such as “Payment Failed.”

Fix alert classification and template routing.

Create properly separated alert types and templates for:
- payment_failure
- service_down
- memory_critical
- synthetic_test_failure
- content_integrity_failure
- emergency_mode_activated
- reliability_warning

Ensure:
- titles match actual alert type
- severity is correct
- content is not misleading
- repeated alerts are deduplicated/grouped
- operators are not spammed every few seconds with identical messages

Build incident grouping logic:
- same root incident should update an open incident rather than creating endless duplicate notifications
- include first seen, last seen, count, current severity, and affected components

==================================================
PHASE 6 — USER PROTECTION / RESILIENCE
==================================================

Implement platform-wide resilience so paying and free users are protected from broken experiences.

Requirements:
- never return a blank page or dead end if a route fails
- if exam content is unavailable, show a safe fallback and alternate options
- if specific content is invalid, remove it from listings automatically
- if the system is degraded, provide lightweight read-only or reduced functionality mode
- preserve entitlements and core access where possible
- prioritize usable experience over full feature richness during incidents

==================================================
PHASE 7 — INFRASTRUCTURE AND ARCHITECTURE AUDIT
==================================================

Audit whether the current architecture is incorrectly mixing heavy workloads into the live web app.

Check whether the following are running inside the main web process:
- AI generation
- PDF/image generation
- bulk exam/content generation
- analytics aggregation
- background jobs
- synthetic tests
- scheduled validation jobs

If so, separate or isolate heavy workloads appropriately.
The main request/response web process must remain lightweight and reliable.

==================================================
PHASE 8 — DELIVERABLES
==================================================

Return all of the following:

1. Root cause analysis for each failure category:
- memory exhaustion
- emergency mode loop
- exam_open_flow abort
- zero_valid_items published content
- mislabeled alerts
- alert spam / duplicate incident notifications

2. Exact files changed

3. Exact code paths fixed

4. Which routes/jobs were highest-memory and what changed

5. Which content records were invalid and how they were remediated

6. What alerting/template/deduplication changes were made

7. What infrastructure/runtime changes were made

8. Remaining risks, if any

9. A verification checklist proving:
- memory is stable
- emergency mode is no longer looping
- exam_open_flow passes
- no zero-valid-item content remains published
- alerts are categorized correctly
- alert spam is controlled
- users see graceful fallbacks instead of broken pages

==================================================
NON-NEGOTIABLE REQUIREMENTS
==================================================

- Do not give superficial advice only
- Do not stop at “increase memory”
- Do not leave broken content published
- Do not leave alert emails mislabeled
- Do not allow heavy exam routes to take down the app
- Do not let emergency mode merely log the problem; it must actively reduce load
- Do not allow repeated identical alerts to spam the operator every few seconds
- Prioritize reliability, containment, and customer trust over elegance or feature completeness

Start with:
1. immediate containment of memory and alert spam
2. tracing highest-memory routes and jobs
3. tracing exam_open_flow failure
4. tracing publish pipeline failures
5. tracing alert template routing and deduplication
Then implement the fixes systematically.
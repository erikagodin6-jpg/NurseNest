You are a senior full-stack engineer and product designer. Build an in-app “Digital Product Builder” inside NurseNest so I can create and design professional nursing study products (PDFs + thumbnails) myself, directly in the admin panel. This tool will be used to create marketable cram booklets, quick guides, cheat sheets, printable posters, and bundles to sell in my Marketplace.

NON-NEGOTIABLE CONSTRAINTS
- Must be production-ready, stable, and fast.
- Must NOT break existing blog generator/scheduler or current admin tools.
- Must match NurseNest brand style (pastel clinical aesthetic, rounded corners, clean typography).
- Must support medically correct visuals generated by an image generator that produces images with NO TEXT in the image.
- Must allow me to export:
  1) Print-quality PDF (300 DPI equivalent output, correct sizing)
  2) Web thumbnail image(s) (e.g., 1200x630 and 600x600)
- Must store product design projects so I can edit later.
- Must allow insertion of my NurseNest logo and consistent brand elements.

GOALS
1) Build a Canva-like (simplified) drag-and-drop designer
2) Build medically accurate image generation workflow (text-free images)
3) Build export pipeline: PDF + thumbnails
4) Integrate with Marketplace so a product can be created from a design project

PART 1 — DATA MODEL
Create tables (or equivalent storage) for:
A) design_projects
- id
- title
- slug
- type (booklet, one-pager, poster, bundle)
- pageSize (Letter, A4, 8.5x11, 5x7, etc.)
- orientation (portrait/landscape)
- brandThemeId
- createdAt/updatedAt
- createdByAdminId

B) design_pages
- id
- projectId
- pageNumber
- canvasJson (serialized editor state: objects, positions, styles)
- backgroundColor
- createdAt/updatedAt

C) design_assets
- id
- projectId
- assetType (uploadedImage, generatedImage, icon, logo)
- url
- width/height
- tags
- createdAt

D) exported_files
- id
- projectId
- exportType (pdf, thumbnail, preview)
- url
- settingsJson
- createdAt

PART 2 — ADMIN UI: DIGITAL PRODUCT BUILDER
Create Admin route: /admin/product-builder

A) Project Dashboard
- New Project button
- List projects with:
  - last edited
  - export status
  - quick actions: Edit / Duplicate / Export / Create Marketplace Product
- Templates:
  - “Cram booklet (10 pages)”
  - “One-page cheat sheet”
  - “Poster”
  - “Bundle cover”

B) Editor UI (Drag-and-Drop Canvas)
Implement a canvas-based editor with:
- Multi-page support (left page list)
- Canvas center workspace with rulers + grid + snap-to-grid
- Object layers panel (like Canva)
- Properties panel (right side):
  - font, size, color
  - alignment
  - borders
  - rounded corners
  - shadows
  - opacity
  - spacing/padding
- Toolbar:
  - Add Text
  - Add Shape (rectangle, rounded box, line)
  - Add Icon
  - Add Image
  - Add Logo (NurseNest logo library)
  - Add Section Template (prebuilt content blocks)
- Drag, resize, rotate, arrange layers (bring forward/back)
- Copy/paste elements
- Lock/unlock layers

C) Brand System (Global)
- Brand palette presets (pastel sets)
- Typography presets (heading/body/caption)
- Saved components (e.g., “Clinical Pearl box”, “Red Flags box”, “Medication Table header”)
- One-click “Apply brand style to page”

PART 3 — MEDICAL IMAGE GENERATOR (NO TEXT)
Add an “Image Generator” panel in editor:

Requirements:
- Generate medically correct illustrations with no text overlays
- Styles:
  - clean vector medical diagram
  - pastel clinical illustration
  - realistic anatomy illustration (optional)
- Inputs:
  - anatomy/system (e.g., heart, lungs, nephron)
  - concept (e.g., COPD emphysema alveoli)
  - view type (cross-section, labeled WITHOUT TEXT, simplified)
  - color palette (match brand)
- Output:
  - PNG with transparent background option
  - Stored to design_assets and insertable by drag-and-drop
- Ensure generator prompt always includes: “no text, no labels, no words, no letters, no watermark”
- Provide regenerate / variations
- IMPORTANT: Do not embed any patient images or PHI.

PART 4 — ASSET LIBRARY
Implement asset library:
- Upload images (PNG/JPG/SVG where supported)
- Icons library (basic medical icons)
- Saved generated images
- NurseNest logo variants (light/dark, mono, color)
- Tagging + search

PART 5 — EXPORT PIPELINE
A) PDF Export
- Export all pages to a single PDF
- Print-quality settings:
  - correct page dimensions
  - high resolution rendering
  - embedded fonts
- Include bleed margins option (optional)
- Save exported PDF to exported_files table

B) Thumbnail Generation
- Generate:
  - 1200x630 (OG social share)
  - 600x600 (square product thumbnail)
  - optional: 1080x1350 (Instagram)
- Thumbnail should be:
  - first page or selected “Cover Page”
  - include product title text overlay IF AND ONLY IF text is in the design (do not add text onto generated medical images)
- Save thumbnails to exported_files
- Auto-attach thumbnail to marketplace product creation flow

C) Preview
- Generate low-res preview images for admin (fast load)

PART 6 — MARKETPLACE INTEGRATION
Add “Create Marketplace Product from Project” button:
- Pre-fill product fields:
  - title, description template, tier/exam targeting tags
  - attach exported PDF as the downloadable file
  - attach thumbnail image as coverImageUrl
- Allow bundle creation by selecting multiple projects/files

PART 7 — QUALITY & USABILITY FEATURES
Implement:
- Autosave every 10 seconds + manual Save
- Version history (at least last 10 saves) or “Duplicate Project” fallback
- Undo/redo
- Keyboard shortcuts
- Export progress indicator
- Error handling with helpful messages

PART 8 — SECURITY
- Admin-only access
- Exported files stored in secure storage
- Download links should be signed or gated
- Do not allow public access to raw design JSON

PART 9 — DELIVERABLES
After implementation provide:
1) Files changed/added
2) Any new dependencies
3) DB migrations
4) Routes/endpoints
5) Manual test checklist:
   - Create project, add shapes/text/icons/images, generate medical image, export PDF, export thumbnails
   - Create marketplace product from project
   - Reload editor and confirm project persists

IMPLEMENTATION NOTES
- Use a stable canvas library suitable for drag-and-drop editing (e.g., Fabric.js or Konva). Choose one and implement cleanly.
- Rendering for export must match what is seen on screen (WYSIWYG).
- Keep UI responsive and consistent with NurseNest styling.

Proceed now. Start by implementing project storage + basic canvas editor (text, shapes, images), then add image generator panel, then export pipeline, then marketplace integration.
STACK
- Framework: [Next.js version]
- Hosting: [Replit / Vercel / other]
- Styling: Tailwind CSS
- Content types: [landing pages, lesson pages, blog posts, flashcards, pricing]
- Audience/locale: Canada, English (primary); timezone America/Toronto
- Brand: NurseNest (nursenest.ca), pastel modern UI

PRIMARY SEO GOALS
1) Indexability: every indexable page must be crawlable, canonicalized, and included in XML sitemap.
2) Site structure: clear URL taxonomy, internal linking, breadcrumb structure for deep content.
3) Rich results: correct structured data where appropriate (Organization, WebSite, BreadcrumbList, Article, FAQPage, Product/Offer if applicable).
4) Performance: LCP/INP/CLS optimized, minimal JS, optimized images, font strategy, caching.
5) Content SEO support: reusable metadata system (title, description, OG/Twitter), unique per page, guardrails to prevent duplicates.
6) Accessibility: WCAG-aligned semantics and headings.

DELIVERABLES (must do all)
A) Provide an SEO architecture plan: URL structure, metadata conventions, internal linking rules.
B) Implement code changes with explanations and file paths (exact code, copy-paste ready).
C) Add:
   - Dynamic metadata (Next.js Metadata API / Head management)
   - robots.txt (with correct disallow/allow rules)
   - sitemap.xml (dynamic, includes lastmod, excludes noindex pages)
   - canonical tags and hreflang if needed
   - 404/410 strategy and redirects for moved pages
D) Add structured data JSON-LD components and apply them to:
   - Home (Organization + WebSite)
   - Blog posts (Article)
   - FAQ page sections (FAQPage)
   - Lessons (BreadcrumbList + possibly HowTo only if appropriate)
E) Implement technical best practices:
   - Heading hierarchy enforcement (single H1/page)
   - Open Graph + Twitter card
   - Image optimization (next/image), width/height, lazy loading
   - Font loading strategy (next/font), preconnect if required
   - Caching headers where appropriate
F) Core Web Vitals improvements:
   - Remove unnecessary client components
   - Code splitting, reduce bundle size
   - Avoid layout shifts (reserved space)
G) Provide a validation checklist:
   - Google Search Console checks
   - Rich Results Test
   - Lighthouse targets (Performance, SEO, Accessibility)
   - Crawl test steps

CONSTRAINTS
- Do not use “SEO plugins”. Do it properly in code.
- No keyword stuffing. No autogenerated spam.
- Follow Google Search Essentials: use noindex for thin/private/paywalled pages where appropriate.
- Ensure paywalled content is handled correctly: public preview indexable if substantial; full content gated. Provide a recommended approach.

INPUTS I WILL PROVIDE
- Current routes list: [paste or infer from file tree]
- Existing sitemap/robots if any: [paste]
- Existing metadata patterns/components: [paste]

OUTPUT FORMAT
1) First: architecture plan (bulleted).
2) Second: exact code changes by file with full snippets.
3) Third: checklist + how to test.
Ask me only if something blocks implementation; otherwise make reasonable assumptions and proceed.




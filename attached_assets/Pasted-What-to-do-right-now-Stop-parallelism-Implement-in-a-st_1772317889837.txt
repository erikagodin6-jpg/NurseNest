What to do right now

Stop parallelism. Implement in a strict order that unblocks runtime and revenue:

Fix build/runtime errors (missing routes/pages).

Admin CRUD (edit/delete/price dollars/admin download).

Watermarked public preview.

Image Lab in builder.

Exam context switcher + hub pages (can be basic stubs first).

Never “wait for subagents.” Create the hub pages as stubs now so the app compiles and routes resolve. Replace later.

Immediate fix: Hub pages must exist (no more “Expected — hub pages don’t exist yet”)

Create these pages today even if they’re placeholders:

src/pages/hubs/NCLEXRNHub.tsx

src/pages/hubs/NCLEXPNHub.tsx

src/pages/hubs/REXPNHub.tsx

src/pages/hubs/NPHub.tsx

Each can be a simple component with <Helmet> title + a “Coming soon” card + internal links. This eliminates the route errors and lets you restart cleanly.

App.tsx routing pattern (safe + simple)

Add routes to these components directly (no subagent dependency).

If you use lazy loading, ensure the import paths are correct and files exist.

Exam context switcher: lock the contract end-to-end (so it doesn’t keep breaking)
Define one shared enum (client + server)

Use one set of allowed values and validate everywhere:

export const EXAM_TARGETS = [
  "REX_PN",
  "NCLEX_PN",
  "NCLEX_RN",
  "NP",
] as const;

export type ExamTarget = typeof EXAM_TARGETS[number];
Client

Replace the tier dropdown with an ExamTarget selector bound to ExamTarget.

Persist selection in builder draft JSON (design.examTarget).

Pass examTarget to runAiTool() and to the server.

Server

Validate examTarget using a schema (zod/valibot/joi).

Default to "NCLEX_RN" if missing (but don’t accept arbitrary strings).

Prompt assembly rule

Don’t scatter “if examTarget…” logic everywhere. Centralize:

buildExamContext(examTarget) → returns a short context string

buildRegionContext(region) → returns CA/US constraints

composeAiPrompt(tool, content, examContext, regionContext)

That prevents constant regressions.

Your biggest monetization blockers: Admin CRUD + Preview (do these before more hub UX)

You already have export/publish/Stripe/download. The missing pieces are control + safe preview.

Admin CRUD (minimum endpoints + UI actions)

Implement these routes first:

GET /api/admin/products

PUT /api/admin/products/:id

DELETE /api/admin/products/:id (soft delete)

POST /api/admin/products/:id/price with { priceDollars: number } → server converts to cents

GET /api/admin/products/:id/download → stream full PDF (admin-only)

Admin UI must show per product:

Edit

Price in dollars

Save

Delete

Download

Watermarked preview

Implement:

POST /api/admin/products/:id/generate-preview (uses pdf-lib; first N pages + watermark)

GET /api/products/:slug/preview (public stream of preview only)

Absolute rule: no public URL to the full PDF.

Image generator inside builder (don’t overbuild)

Ship a first version that works:

Prompt textarea (long)

Negative prompt

Text-free toggle appends: no text, no letters, no words, no numbers, no watermark, no labels

Generate button → returns stored image assets

Insert into canvas

Even without fancy editing tools, this immediately upgrades aesthetics.

Paste this to your dev AI to stop the “waiting” behavior
Stop narrating and waiting for subagents. Ship in deterministic order.

1) Create stub hub pages immediately (NCLEX-RN/PN, REx-PN, NP) so routes compile.
2) Fix App.tsx routes to point to real files.
3) Implement Admin CRUD now: edit, soft delete, price in dollars, admin download endpoint + UI actions.
4) Implement watermarked public preview now: generate-preview (pdf-lib watermark, first N pages) + public preview stream endpoint.
5) Add Image Lab to builder: long prompt + negative prompt + text-free toggle + generate + insert to canvas.
6) After the above is working, finish exam context switcher end-to-end with a single validated enum shared client/server.

No “taking stock”, no “waiting for subagents”, no TODOs. Provide runnable code changes and a manual QA checklist with PASS/FAIL.

If you paste the actual build error message/stack (one screenshot or the console text), I’ll tell you exactly where the missing import/route is and the fastest fix. But even without it, the “stub hub pages now” step removes the most common failure mode you described.
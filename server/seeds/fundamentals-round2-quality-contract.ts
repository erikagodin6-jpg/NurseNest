// Documentation-only companion to audit-rn-fundamentals-qbank-round2.ts.
// The executable audit remains the source of truth.
export const RN_FUNDAMENTALS_ROUND2_QUALITY_CONTRACT = {
  minimumQuestions: 200,
  minimumPerBatch: 45,
  regionScope: "BOTH",
  requiresUniqueStems: true,
  requiresValidAnswerContracts: true,
  requiresCorrectAnswerRationale: true,
  requiresPerDistractorRationales: true,
} as const;

import { rnLeadershipBankBatch1Questions } from "../../client/src/data/exam-questions/rn-leadership-bank-batch1";
import { rnLeadershipBankBatch2Questions } from "../../client/src/data/exam-questions/rn-leadership-bank-batch2";
import { rnLeadershipBankBatch3Questions } from "../../client/src/data/exam-questions/rn-leadership-bank-batch3";
import { rnLeadershipRegionalBankBatch1Questions } from "../../client/src/data/exam-questions/rn-leadership-regional-bank-batch1";
import type { ExamQuestion } from "../../client/src/data/exam-questions/types";
const shared:ExamQuestion[]=[...rnLeadershipBankBatch1Questions,...rnLeadershipBankBatch2Questions,...rnLeadershipBankBatch3Questions];const all:ExamQuestion[]=[...shared,...rnLeadershipRegionalBankBatch1Questions];
function fail(m:string):never{throw new Error(m)}function correct(q:ExamQuestion){return q.t==="sata"&&q.ca?q.ca:[q.a]}
if(rnLeadershipBankBatch1Questions.length!==50)fail(`RN_LEAD_BATCH1:${rnLeadershipBankBatch1Questions.length}`);if(rnLeadershipBankBatch2Questions.length!==50)fail(`RN_LEAD_BATCH2:${rnLeadershipBankBatch2Questions.length}`);if(rnLeadershipBankBatch3Questions.length!==50)fail(`RN_LEAD_BATCH3:${rnLeadershipBankBatch3Questions.length}`);if(shared.length!==150)fail(`RN_LEAD_SHARED:${shared.length}`);if(all.length!==170)fail(`RN_LEAD_TOTAL:${all.length}`);const can=rnLeadershipRegionalBankBatch1Questions.filter(q=>q.regionScope==="CAN"),us=rnLeadershipRegionalBankBatch1Questions.filter(q=>q.regionScope==="US");if(can.length!==10)fail(`RN_LEAD_CAN:${can.length}`);if(us.length!==10)fail(`RN_LEAD_US:${us.length}`);
const seen=new Set<string>();for(const [i,q] of all.entries()){const stem=q.q.trim().toLowerCase();if(q.q.trim().length<25)fail(`RN_LEAD_THIN_STEM:${i}`);if(seen.has(stem))fail(`RN_LEAD_DUP:${q.q}`);seen.add(stem);if(!Array.isArray(q.o)||q.o.length<4)fail(`RN_LEAD_OPTIONS:${i}`);const c=correct(q);if(!c.length||c.some(x=>!Number.isInteger(x)||x<0||x>=q.o.length))fail(`RN_LEAD_ANSWER:${i}`);if(new Set(c).size!==c.length)fail(`RN_LEAD_ANSWER_DUP:${i}`);if(!q.r||q.r.trim().length<40)fail(`RN_LEAD_RATIONALE:${i}`);const wrong=q.o.length-c.length;if(!Array.isArray(q.dr)||q.dr.length!==wrong)fail(`RN_LEAD_DR:${i}:${q.dr?.length??0}/${wrong}`);if(q.dr.some(x=>x.trim().length<20))fail(`RN_LEAD_DR_THIN:${i}`);if(q.t==="sata"&&(!q.ca||q.ca.length<2))fail(`RN_LEAD_SATA:${i}`)}for(const q of rnLeadershipRegionalBankBatch1Questions){if(q.regionScope==="CAN"&&q.countryCode!=="CA")fail(`RN_LEAD_CAN_COUNTRY:${q.q}`);if(q.regionScope==="US"&&q.countryCode!=="US")fail(`RN_LEAD_US_COUNTRY:${q.q}`);if(!q.topic||!q.cognitiveLevel||!q.difficulty||!q.licensingBody)fail(`RN_LEAD_META:${q.q}`)}console.log({status:"PASS",shared:150,regional:20,total:170,canada:can.length,unitedStates:us.length,distractorRationales:all.filter(q=>Array.isArray(q.dr)).length});
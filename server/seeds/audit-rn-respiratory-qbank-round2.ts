import { rnRespiratoryRound2Batch1Questions } from "../../client/src/data/exam-questions/rn-respiratory-round2-batch1";
import { rnRespiratoryRound2Batch2Questions } from "../../client/src/data/exam-questions/rn-respiratory-round2-batch2";
import { rnRespiratoryRound2Batch3Questions } from "../../client/src/data/exam-questions/rn-respiratory-round2-batch3";
import { rnRespiratoryRound2Batch4Questions } from "../../client/src/data/exam-questions/rn-respiratory-round2-batch4";
import { rnRespiratoryRound2Batch5Questions } from "../../client/src/data/exam-questions/rn-respiratory-round2-batch5";
import type { ExamQuestion } from "../../client/src/data/exam-questions/types";
const batches=[rnRespiratoryRound2Batch1Questions,rnRespiratoryRound2Batch2Questions,rnRespiratoryRound2Batch3Questions,rnRespiratoryRound2Batch4Questions,rnRespiratoryRound2Batch5Questions] as const;const all:ExamQuestion[]=batches.flatMap(x=>[...x]);
function fail(m:string):never{throw new Error(m)}function correct(q:ExamQuestion){return q.t==="sata"&&q.ca?q.ca:[q.a]}
for(const [i,batch] of batches.entries())if(batch.length<40)fail(`RN_RESP_ROUND2_BATCH_${i+1}_TOO_THIN:${batch.length}`);if(all.length<200)fail(`RN_RESP_ROUND2_MINIMUM:${all.length}/200`);const seen=new Set<string>();for(const [i,q] of all.entries()){const stem=q.q.trim().toLowerCase().replace(/\s+/g," ");if(stem.length<25)fail(`RN_RESP_ROUND2_THIN_STEM:${i}`);if(seen.has(stem))fail(`RN_RESP_ROUND2_DUP:${q.q}`);seen.add(stem);if(!Array.isArray(q.o)||q.o.length<4)fail(`RN_RESP_ROUND2_OPTIONS:${i}`);const c=correct(q);if(!c.length||c.some(x=>!Number.isInteger(x)||x<0||x>=q.o.length))fail(`RN_RESP_ROUND2_ANSWER:${i}`);if(new Set(c).size!==c.length)fail(`RN_RESP_ROUND2_ANSWER_DUP:${i}`);if(!q.r||q.r.trim().length<40)fail(`RN_RESP_ROUND2_RATIONALE:${i}`);const wrong=q.o.length-c.length;if(!Array.isArray(q.dr)||q.dr.length!==wrong)fail(`RN_RESP_ROUND2_DR:${i}:${q.dr?.length??0}/${wrong}`);if(q.dr.some(x=>x.trim().length<20))fail(`RN_RESP_ROUND2_DR_THIN:${i}`);if(q.t==="sata"&&(!q.ca||q.ca.length<2))fail(`RN_RESP_ROUND2_SATA:${i}`)}console.log({status:"PASS",batches:batches.map(x=>x.length),authored:all.length,minimumRequired:200,uniqueStems:seen.size,distractorRationales:all.filter(q=>Array.isArray(q.dr)).length});
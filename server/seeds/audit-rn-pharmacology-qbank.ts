import { rnPharmacologyBankBatch1Questions } from "../../client/src/data/exam-questions/rn-pharmacology-bank-batch1";
import { rnPharmacologyBankBatch2Questions } from "../../client/src/data/exam-questions/rn-pharmacology-bank-batch2";
import { rnPharmacologyBankBatch3Questions } from "../../client/src/data/exam-questions/rn-pharmacology-bank-batch3";
import { rnPharmacologyRegionalBankBatch1Questions } from "../../client/src/data/exam-questions/rn-pharmacology-regional-bank-batch1";
import type { ExamQuestion } from "../../client/src/data/exam-questions/types";
const shared:ExamQuestion[]=[...rnPharmacologyBankBatch1Questions,...rnPharmacologyBankBatch2Questions,...rnPharmacologyBankBatch3Questions];const all:ExamQuestion[]=[...shared,...rnPharmacologyRegionalBankBatch1Questions];
function fail(m:string):never{throw new Error(m)}function correct(q:ExamQuestion){return q.t==="sata"&&q.ca?q.ca:[q.a]}
if(rnPharmacologyBankBatch1Questions.length!==50)fail(`RN_PHARM_BATCH1:${rnPharmacologyBankBatch1Questions.length}`);if(rnPharmacologyBankBatch2Questions.length!==50)fail(`RN_PHARM_BATCH2:${rnPharmacologyBankBatch2Questions.length}`);if(rnPharmacologyBankBatch3Questions.length!==50)fail(`RN_PHARM_BATCH3:${rnPharmacologyBankBatch3Questions.length}`);if(shared.length!==150)fail(`RN_PHARM_SHARED:${shared.length}`);if(all.length!==170)fail(`RN_PHARM_TOTAL:${all.length}`);const can=rnPharmacologyRegionalBankBatch1Questions.filter(q=>q.regionScope==="CAN"),us=rnPharmacologyRegionalBankBatch1Questions.filter(q=>q.regionScope==="US");if(can.length!==10)fail(`RN_PHARM_CAN:${can.length}`);if(us.length!==10)fail(`RN_PHARM_US:${us.length}`);
const seen=new Set<string>();for(const [i,q] of all.entries()){const stem=q.q.trim().toLowerCase();if(q.q.trim().length<25)fail(`RN_PHARM_THIN_STEM:${i}`);if(seen.has(stem))fail(`RN_PHARM_DUP:${q.q}`);seen.add(stem);if(!Array.isArray(q.o)||q.o.length<4)fail(`RN_PHARM_OPTIONS:${i}`);const c=correct(q);if(!c.length||c.some(x=>!Number.isInteger(x)||x<0||x>=q.o.length))fail(`RN_PHARM_ANSWER:${i}`);if(new Set(c).size!==c.length)fail(`RN_PHARM_ANSWER_DUP:${i}`);if(!q.r||q.r.trim().length<40)fail(`RN_PHARM_RATIONALE:${i}`);const wrong=q.o.length-c.length;if(!Array.isArray(q.dr)||q.dr.length!==wrong)fail(`RN_PHARM_DR:${i}:${q.dr?.length??0}/${wrong}`);if(q.dr.some(x=>x.trim().length<20))fail(`RN_PHARM_DR_THIN:${i}`);if(q.t==="sata"&&(!q.ca||q.ca.length<2))fail(`RN_PHARM_SATA:${i}`)}for(const q of rnPharmacologyRegionalBankBatch1Questions){if(q.regionScope==="CAN"&&q.countryCode!=="CA")fail(`RN_PHARM_CAN_COUNTRY:${q.q}`);if(q.regionScope==="US"&&q.countryCode!=="US")fail(`RN_PHARM_US_COUNTRY:${q.q}`);if(!q.topic||!q.cognitiveLevel||!q.difficulty)fail(`RN_PHARM_META:${q.q}`)}console.log({status:"PASS",shared:150,regional:20,total:170,canada:can.length,unitedStates:us.length,distractorRationales:all.filter(q=>Array.isArray(q.dr)).length});
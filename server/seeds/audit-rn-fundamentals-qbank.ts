import { rnFundamentalsBankBatch1Questions } from "../../client/src/data/exam-questions/rn-fundamentals-bank-batch1";
import { rnFundamentalsBankBatch2Questions } from "../../client/src/data/exam-questions/rn-fundamentals-bank-batch2";
import { rnFundamentalsBankBatch3Questions } from "../../client/src/data/exam-questions/rn-fundamentals-bank-batch3";
import { rnFundamentalsRegionalBankBatch1Questions } from "../../client/src/data/exam-questions/rn-fundamentals-regional-bank-batch1";
import type { ExamQuestion } from "../../client/src/data/exam-questions/types";
const shared:ExamQuestion[]=[...rnFundamentalsBankBatch1Questions,...rnFundamentalsBankBatch2Questions,...rnFundamentalsBankBatch3Questions];const all:ExamQuestion[]=[...shared,...rnFundamentalsRegionalBankBatch1Questions];
function fail(m:string):never{throw new Error(m)}function correct(q:ExamQuestion){return q.t==="sata"&&q.ca?q.ca:[q.a]}
if(rnFundamentalsBankBatch1Questions.length!==50)fail(`RN_FUND_BATCH1:${rnFundamentalsBankBatch1Questions.length}`);if(rnFundamentalsBankBatch2Questions.length!==50)fail(`RN_FUND_BATCH2:${rnFundamentalsBankBatch2Questions.length}`);if(rnFundamentalsBankBatch3Questions.length!==50)fail(`RN_FUND_BATCH3:${rnFundamentalsBankBatch3Questions.length}`);if(shared.length!==150)fail(`RN_FUND_SHARED:${shared.length}`);if(all.length!==170)fail(`RN_FUND_TOTAL:${all.length}`);const can=rnFundamentalsRegionalBankBatch1Questions.filter(q=>q.regionScope==="CAN"),us=rnFundamentalsRegionalBankBatch1Questions.filter(q=>q.regionScope==="US");if(can.length!==10)fail(`RN_FUND_CAN:${can.length}`);if(us.length!==10)fail(`RN_FUND_US:${us.length}`);
const seen=new Set<string>();for(const [i,q] of all.entries()){const stem=q.q.trim().toLowerCase();if(q.q.trim().length<25)fail(`RN_FUND_THIN_STEM:${i}`);if(seen.has(stem))fail(`RN_FUND_DUP:${q.q}`);seen.add(stem);if(!Array.isArray(q.o)||q.o.length<4)fail(`RN_FUND_OPTIONS:${i}`);const c=correct(q);if(!c.length||c.some(x=>!Number.isInteger(x)||x<0||x>=q.o.length))fail(`RN_FUND_ANSWER:${i}`);if(new Set(c).size!==c.length)fail(`RN_FUND_ANSWER_DUP:${i}`);if(!q.r||q.r.trim().length<40)fail(`RN_FUND_RATIONALE:${i}`);const wrong=q.o.length-c.length;if(!Array.isArray(q.dr)||q.dr.length!==wrong)fail(`RN_FUND_DR:${i}:${q.dr?.length??0}/${wrong}`);if(q.dr.some(x=>x.trim().length<20))fail(`RN_FUND_DR_THIN:${i}`);if(q.t==="sata"&&(!q.ca||q.ca.length<2))fail(`RN_FUND_SATA:${i}`)}for(const q of rnFundamentalsRegionalBankBatch1Questions){if(q.regionScope==="CAN"&&q.countryCode!=="CA")fail(`RN_FUND_CAN_COUNTRY:${q.q}`);if(q.regionScope==="US"&&q.countryCode!=="US")fail(`RN_FUND_US_COUNTRY:${q.q}`);if(!q.topic||!q.cognitiveLevel||!q.difficulty)fail(`RN_FUND_META:${q.q}`)}console.log({status:"PASS",shared:150,regional:20,total:170,canada:can.length,unitedStates:us.length,distractorRationales:all.filter(q=>Array.isArray(q.dr)).length});
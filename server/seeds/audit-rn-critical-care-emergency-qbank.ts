import { rnCriticalCareEmergencyBankBatch1Questions } from "../../client/src/data/exam-questions/rn-critical-care-emergency-bank-batch1";
import { rnCriticalCareEmergencyBankBatch2Questions } from "../../client/src/data/exam-questions/rn-critical-care-emergency-bank-batch2";
import { rnCriticalCareEmergencyBankBatch3Questions } from "../../client/src/data/exam-questions/rn-critical-care-emergency-bank-batch3";
import { rnCriticalCareEmergencyRegionalBankBatch1Questions } from "../../client/src/data/exam-questions/rn-critical-care-emergency-regional-bank-batch1";
import type { ExamQuestion } from "../../client/src/data/exam-questions/types";
const shared:ExamQuestion[]=[...rnCriticalCareEmergencyBankBatch1Questions,...rnCriticalCareEmergencyBankBatch2Questions,...rnCriticalCareEmergencyBankBatch3Questions];const all:ExamQuestion[]=[...shared,...rnCriticalCareEmergencyRegionalBankBatch1Questions];
function fail(m:string):never{throw new Error(m)}function correct(q:ExamQuestion){return q.t==="sata"&&q.ca?q.ca:[q.a]}
if(rnCriticalCareEmergencyBankBatch1Questions.length!==50)fail(`RN_CRIT_BATCH1:${rnCriticalCareEmergencyBankBatch1Questions.length}`);if(rnCriticalCareEmergencyBankBatch2Questions.length!==50)fail(`RN_CRIT_BATCH2:${rnCriticalCareEmergencyBankBatch2Questions.length}`);if(rnCriticalCareEmergencyBankBatch3Questions.length!==50)fail(`RN_CRIT_BATCH3:${rnCriticalCareEmergencyBankBatch3Questions.length}`);if(shared.length!==150)fail(`RN_CRIT_SHARED:${shared.length}`);if(all.length!==170)fail(`RN_CRIT_TOTAL:${all.length}`);const can=rnCriticalCareEmergencyRegionalBankBatch1Questions.filter(q=>q.regionScope==="CAN"),us=rnCriticalCareEmergencyRegionalBankBatch1Questions.filter(q=>q.regionScope==="US");if(can.length!==10)fail(`RN_CRIT_CAN:${can.length}`);if(us.length!==10)fail(`RN_CRIT_US:${us.length}`);
const seen=new Set<string>();for(const [i,q] of all.entries()){const stem=q.q.trim().toLowerCase();if(q.q.trim().length<25)fail(`RN_CRIT_THIN_STEM:${i}`);if(seen.has(stem))fail(`RN_CRIT_DUP:${q.q}`);seen.add(stem);if(!Array.isArray(q.o)||q.o.length<4)fail(`RN_CRIT_OPTIONS:${i}`);const c=correct(q);if(!c.length||c.some(x=>!Number.isInteger(x)||x<0||x>=q.o.length))fail(`RN_CRIT_ANSWER:${i}`);if(new Set(c).size!==c.length)fail(`RN_CRIT_ANSWER_DUP:${i}`);if(!q.r||q.r.trim().length<40)fail(`RN_CRIT_RATIONALE:${i}`);const wrong=q.o.length-c.length;if(!Array.isArray(q.dr)||q.dr.length!==wrong)fail(`RN_CRIT_DR:${i}:${q.dr?.length??0}/${wrong}`);if(q.dr.some(x=>x.trim().length<20))fail(`RN_CRIT_DR_THIN:${i}`);if(q.t==="sata"&&(!q.ca||q.ca.length<2))fail(`RN_CRIT_SATA:${i}`)}for(const q of rnCriticalCareEmergencyRegionalBankBatch1Questions){if(q.regionScope==="CAN"&&q.countryCode!=="CA")fail(`RN_CRIT_CAN_COUNTRY:${q.q}`);if(q.regionScope==="US"&&q.countryCode!=="US")fail(`RN_CRIT_US_COUNTRY:${q.q}`);if(!q.topic||!q.cognitiveLevel||!q.difficulty||!q.licensingBody)fail(`RN_CRIT_META:${q.q}`)}console.log({status:"PASS",shared:150,regional:20,total:170,canada:can.length,unitedStates:us.length,distractorRationales:all.filter(q=>Array.isArray(q.dr)).length});
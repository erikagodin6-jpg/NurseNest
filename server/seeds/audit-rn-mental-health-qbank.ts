import { rnMentalHealthBankBatch1Questions } from "../../client/src/data/exam-questions/rn-mental-health-bank-batch1";
import { rnMentalHealthBankBatch2Questions } from "../../client/src/data/exam-questions/rn-mental-health-bank-batch2";
import { rnMentalHealthBankBatch3Questions } from "../../client/src/data/exam-questions/rn-mental-health-bank-batch3";
import { rnMentalHealthRegionalBankBatch1Questions } from "../../client/src/data/exam-questions/rn-mental-health-regional-bank-batch1";
import type { ExamQuestion } from "../../client/src/data/exam-questions/types";
const shared:ExamQuestion[]=[...rnMentalHealthBankBatch1Questions,...rnMentalHealthBankBatch2Questions,...rnMentalHealthBankBatch3Questions];const all:ExamQuestion[]=[...shared,...rnMentalHealthRegionalBankBatch1Questions];
function fail(m:string):never{throw new Error(m)}function correct(q:ExamQuestion){return q.t==="sata"&&q.ca?q.ca:[q.a]}
if(rnMentalHealthBankBatch1Questions.length!==50)fail(`RN_MH_BATCH1:${rnMentalHealthBankBatch1Questions.length}`);if(rnMentalHealthBankBatch2Questions.length!==50)fail(`RN_MH_BATCH2:${rnMentalHealthBankBatch2Questions.length}`);if(rnMentalHealthBankBatch3Questions.length!==50)fail(`RN_MH_BATCH3:${rnMentalHealthBankBatch3Questions.length}`);if(shared.length!==150)fail(`RN_MH_SHARED:${shared.length}`);if(all.length!==170)fail(`RN_MH_TOTAL:${all.length}`);const can=rnMentalHealthRegionalBankBatch1Questions.filter(q=>q.regionScope==="CAN"),us=rnMentalHealthRegionalBankBatch1Questions.filter(q=>q.regionScope==="US");if(can.length!==10)fail(`RN_MH_CAN:${can.length}`);if(us.length!==10)fail(`RN_MH_US:${us.length}`);
const seen=new Set<string>();for(const [i,q] of all.entries()){const stem=q.q.trim().toLowerCase();if(q.q.trim().length<25)fail(`RN_MH_THIN_STEM:${i}`);if(seen.has(stem))fail(`RN_MH_DUP:${q.q}`);seen.add(stem);if(!Array.isArray(q.o)||q.o.length<4)fail(`RN_MH_OPTIONS:${i}`);const c=correct(q);if(!c.length||c.some(x=>!Number.isInteger(x)||x<0||x>=q.o.length))fail(`RN_MH_ANSWER:${i}`);if(new Set(c).size!==c.length)fail(`RN_MH_ANSWER_DUP:${i}`);if(!q.r||q.r.trim().length<40)fail(`RN_MH_RATIONALE:${i}`);const wrong=q.o.length-c.length;if(!Array.isArray(q.dr)||q.dr.length!==wrong)fail(`RN_MH_DR:${i}:${q.dr?.length??0}/${wrong}`);if(q.dr.some(x=>x.trim().length<20))fail(`RN_MH_DR_THIN:${i}`);if(q.t==="sata"&&(!q.ca||q.ca.length<2))fail(`RN_MH_SATA:${i}`)}for(const q of rnMentalHealthRegionalBankBatch1Questions){if(q.regionScope==="CAN"&&q.countryCode!=="CA")fail(`RN_MH_CAN_COUNTRY:${q.q}`);if(q.regionScope==="US"&&q.countryCode!=="US")fail(`RN_MH_US_COUNTRY:${q.q}`);if(!q.topic||!q.cognitiveLevel||!q.difficulty)fail(`RN_MH_META:${q.q}`)}console.log({status:"PASS",shared:shared.length,regional:20,total:all.length,canada:can.length,unitedStates:us.length,distractorRationales:all.filter(q=>Array.isArray(q.dr)).length});
import crypto from "crypto";import { pool } from "../storage";import { npUsAnccFnpCapPublishedBank as bank } from "../../client/src/data/exam-questions/np-us-ancc-fnp-cap-published";import "../../client/src/data/lessons/respiratory-np-cap-ancc-fnp-coverage";function h(s:string){return crypto.createHash("sha256").update(s.trim().toLowerCase()).digest("hex").slice(0,32)}async function run(){if(bank.length!==20)throw new Error("ANCC_CAP_SEED_COUNT");await pool.query(`ALTER TABLE exam_questions ADD COLUMN IF NOT EXISTS stem_hash TEXT`);await pool.query(`ALTER TABLE exam_questions ADD COLUMN IF NOT EXISTS correct_answer_explanation TEXT`);await pool.query(`ALTER TABLE exam_questions ADD COLUMN IF NOT EXISTS incorrect_answer_rationale JSONB`);const hashes:string[]=[];for(const q of bank){const hash=h(q.stem);hashes.push(hash);const ex=await pool.query(`SELECT id FROM exam_questions WHERE tier='np' AND exam='ANCC-FNP' AND country_code='US' AND topic='Community-Acquired Pneumonia' AND (stem_hash=$1 OR lower(trim(stem))=lower(trim($2))) LIMIT 1`,[hash,q.stem]);if(ex.rows.length)continue;await pool.query(`INSERT INTO exam_questions (id,tier,exam,question_type,status,stem,options,correct_answer,rationale,correct_answer_explanation,incorrect_answer_rationale,difficulty,body_system,topic,country_code,licensing_body,language_code,cognitive_level,question_format,region_scope,is_mock_exam_eligible,is_adaptive_eligible,career_type,domain,tags,stem_hash,created_at,updated_at) VALUES ($1,'np','ANCC-FNP','multiple_choice','published',$2,$3,$4,$5,$6,$7,$8,'Respiratory','Community-Acquired Pneumonia','US','ANCC','en',$9,'MCQ','US',true,true,'nursing',$10,$11,$12,NOW(),NOW())`,[q.id,q.stem,JSON.stringify(q.options),JSON.stringify([q.correctAnswer]),q.correctAnswerExplanation,q.correctAnswerExplanation,JSON.stringify(Object.fromEntries(q.options.map((o,i)=>[o,q.optionRationales[i]]))),q.difficulty,q.cognitiveLevel,q.clientNeedsSubcategory,["np","ancc-fnp","cap","respiratory"],hash])}const v=await pool.query(`SELECT COUNT(*)::int total,COUNT(*) FILTER(WHERE correct_answer_explanation IS NOT NULL AND length(trim(correct_answer_explanation))>0)::int rationales,COUNT(*) FILTER(WHERE incorrect_answer_rationale IS NOT NULL AND jsonb_typeof(incorrect_answer_rationale)='object' AND jsonb_object_length(incorrect_answer_rationale)=4)::int option_rationales FROM exam_questions WHERE tier='np' AND exam='ANCC-FNP' AND country_code='US' AND topic='Community-Acquired Pneumonia' AND status='published' AND stem_hash=ANY($1::text[])`,[hashes]);const r=v.rows[0];if(r.total!==20||r.rationales!==20||r.option_rationales!==20)throw new Error(`ANCC_CAP_POSTSEED:${JSON.stringify(r)}`);console.log(JSON.stringify(r))}run().then(()=>process.exit(0)).catch(e=>{console.error(e);process.exit(1)});
import { rnOncologyBankBatch1Questions } from "../../client/src/data/exam-questions/rn-oncology-bank-batch1";
import { rnOncologyBankBatch2Questions } from "../../client/src/data/exam-questions/rn-oncology-bank-batch2";
import { rnOncologyBankBatch3Questions } from "../../client/src/data/exam-questions/rn-oncology-bank-batch3";
import { rnOncologyRegionalBankBatch1Questions } from "../../client/src/data/exam-questions/rn-oncology-regional-bank-batch1";
import type { ExamQuestion } from "../../client/src/data/exam-questions/types";
const shared:ExamQuestion[]=[...rnOncologyBankBatch1Questions,...rnOncologyBankBatch2Questions,...rnOncologyBankBatch3Questions];const all:ExamQuestion[]=[...shared,...rnOncologyRegionalBankBatch1Questions];
function fail(m:string):never{throw new Error(m)}function correct(q:ExamQuestion){return q.t==="sata"&&q.ca?q.ca:[q.a]}
if(rnOncologyBankBatch1Questions.length!==50)fail(`RN_ONC_BATCH1:${rnOncologyBankBatch1Questions.length}`);if(rnOncologyBankBatch2Questions.length!==48)fail(`RN_ONC_BATCH2:${rnOncologyBankBatch2Questions.length}`);if(rnOncologyBankBatch3Questions.length!==52)fail(`RN_ONC_BATCH3:${rnOncologyBankBatch3Questions.length}`);if(shared.length!==150)fail(`RN_ONC_SHARED:${shared.length}`);if(all.length!==170)fail(`RN_ONC_TOTAL:${all.length}`);const can=rnOncologyRegionalBankBatch1Questions.filter(q=>q.regionScope==="CAN"),us=rnOncologyRegionalBankBatch1Questions.filter(q=>q.regionScope==="US");if(can.length!==10)fail(`RN_ONC_CAN:${can.length}`);if(us.length!==10)fail(`RN_ONC_US:${us.length}`);
const seen=new Set<string>();for(const [i,q] of all.entries()){const stem=q.q.trim().toLowerCase();if(q.q.trim().length<25)fail(`RN_ONC_THIN_STEM:${i}`);if(seen.has(stem))fail(`RN_ONC_DUP:${q.q}`);seen.add(stem);if(!Array.isArray(q.o)||q.o.length<4)fail(`RN_ONC_OPTIONS:${i}`);const c=correct(q);if(!c.length||c.some(x=>!Number.isInteger(x)||x<0||x>=q.o.length))fail(`RN_ONC_ANSWER:${i}`);if(new Set(c).size!==c.length)fail(`RN_ONC_ANSWER_DUP:${i}`);if(!q.r||q.r.trim().length<40)fail(`RN_ONC_RATIONALE:${i}`);const wrong=q.o.length-c.length;if(!Array.isArray(q.dr)||q.dr.length!==wrong)fail(`RN_ONC_DR:${i}:${q.dr?.length??0}/${wrong}`);if(q.dr.some(x=>x.trim().length<20))fail(`RN_ONC_DR_THIN:${i}`);if(q.t==="sata"&&(!q.ca||q.ca.length<2))fail(`RN_ONC_SATA:${i}`)}for(const q of rnOncologyRegionalBankBatch1Questions){if(q.regionScope==="CAN"&&q.countryCode!=="CA")fail(`RN_ONC_CAN_COUNTRY:${q.q}`);if(q.regionScope==="US"&&q.countryCode!=="US")fail(`RN_ONC_US_COUNTRY:${q.q}`);if(!q.topic||!q.cognitiveLevel||!q.difficulty)fail(`RN_ONC_META:${q.q}`)}console.log({status:"PASS",batch1:50,batch2:48,batch3:52,shared:150,regional:20,total:170,canada:can.length,unitedStates:us.length,distractorRationales:all.filter(q=>Array.isArray(q.dr)).length});
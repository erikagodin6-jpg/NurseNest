import { rnCommunityPublicHealthBankBatch1Questions } from "../../client/src/data/exam-questions/rn-community-public-health-bank-batch1";
import { rnCommunityPublicHealthBankBatch2Questions } from "../../client/src/data/exam-questions/rn-community-public-health-bank-batch2";
import { rnCommunityPublicHealthBankBatch3Questions } from "../../client/src/data/exam-questions/rn-community-public-health-bank-batch3";
import { rnCommunityPublicHealthRegionalBankBatch1Questions } from "../../client/src/data/exam-questions/rn-community-public-health-regional-bank-batch1";
import type { ExamQuestion } from "../../client/src/data/exam-questions/types";
const shared:ExamQuestion[]=[...rnCommunityPublicHealthBankBatch1Questions,...rnCommunityPublicHealthBankBatch2Questions,...rnCommunityPublicHealthBankBatch3Questions];const all:ExamQuestion[]=[...shared,...rnCommunityPublicHealthRegionalBankBatch1Questions];
function fail(m:string):never{throw new Error(m)}function correct(q:ExamQuestion){return q.t==="sata"&&q.ca?q.ca:[q.a]}
if(rnCommunityPublicHealthBankBatch1Questions.length!==51)fail(`RN_COMM_BATCH1:${rnCommunityPublicHealthBankBatch1Questions.length}`);if(rnCommunityPublicHealthBankBatch2Questions.length!==49)fail(`RN_COMM_BATCH2:${rnCommunityPublicHealthBankBatch2Questions.length}`);if(rnCommunityPublicHealthBankBatch3Questions.length!==53)fail(`RN_COMM_BATCH3:${rnCommunityPublicHealthBankBatch3Questions.length}`);if(shared.length!==153)fail(`RN_COMM_SHARED:${shared.length}`);if(all.length!==173)fail(`RN_COMM_TOTAL:${all.length}`);const can=rnCommunityPublicHealthRegionalBankBatch1Questions.filter(q=>q.regionScope==="CAN"),us=rnCommunityPublicHealthRegionalBankBatch1Questions.filter(q=>q.regionScope==="US");if(can.length!==10)fail(`RN_COMM_CAN:${can.length}`);if(us.length!==10)fail(`RN_COMM_US:${us.length}`);
const seen=new Set<string>();for(const [i,q] of all.entries()){const stem=q.q.trim().toLowerCase();if(q.q.trim().length<25)fail(`RN_COMM_THIN_STEM:${i}`);if(seen.has(stem))fail(`RN_COMM_DUP:${q.q}`);seen.add(stem);if(!Array.isArray(q.o)||q.o.length<4)fail(`RN_COMM_OPTIONS:${i}`);const c=correct(q);if(!c.length||c.some(x=>!Number.isInteger(x)||x<0||x>=q.o.length))fail(`RN_COMM_ANSWER:${i}`);if(new Set(c).size!==c.length)fail(`RN_COMM_ANSWER_DUP:${i}`);if(!q.r||q.r.trim().length<40)fail(`RN_COMM_RATIONALE:${i}`);const wrong=q.o.length-c.length;if(!Array.isArray(q.dr)||q.dr.length!==wrong)fail(`RN_COMM_DR:${i}:${q.dr?.length??0}/${wrong}`);if(q.dr.some(x=>x.trim().length<20))fail(`RN_COMM_DR_THIN:${i}`);if(q.t==="sata"&&(!q.ca||q.ca.length<2))fail(`RN_COMM_SATA:${i}`)}for(const q of rnCommunityPublicHealthRegionalBankBatch1Questions){if(q.regionScope==="CAN"&&q.countryCode!=="CA")fail(`RN_COMM_CAN_COUNTRY:${q.q}`);if(q.regionScope==="US"&&q.countryCode!=="US")fail(`RN_COMM_US_COUNTRY:${q.q}`);if(!q.topic||!q.cognitiveLevel||!q.difficulty||!q.licensingBody)fail(`RN_COMM_META:${q.q}`)}console.log({status:"PASS",shared:153,regional:20,total:173,canada:can.length,unitedStates:us.length,distractorRationales:all.filter(q=>Array.isArray(q.dr)).length});
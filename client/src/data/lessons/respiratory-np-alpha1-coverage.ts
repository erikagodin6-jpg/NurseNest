import { respiratoryNpAlpha1FullLessons } from "./respiratory-np-alpha1-full";import { getNpAlpha1Cram } from "./respiratory-np-alpha1-cram";import { npCaCnpleAlpha1PublishedBank,npUsAanpFnpAlpha1PublishedBank } from "../exam-questions/np-alpha1-cnple-aanp-fnp-published";
type Q=(typeof npCaCnpleAlpha1PublishedBank)[number]|(typeof npUsAanpFnpAlpha1PublishedBank)[number];const cells:Array<{exam:"CNPLE"|"AANP-FNP";countryCode:"CA"|"US";regionScope:"CAN"|"US";fullLessonKey:string;questions:readonly Q[]}>=[{exam:"CNPLE",countryCode:"CA",regionScope:"CAN",fullLessonKey:"alpha1-antitrypsin-deficiency-np-ca-cnple-2026",questions:npCaCnpleAlpha1PublishedBank},{exam:"AANP-FNP",countryCode:"US",regionScope:"US",fullLessonKey:"alpha1-antitrypsin-deficiency-np-us-aanp-fnp-2026",questions:npUsAanpFnpAlpha1PublishedBank}];function n(s:string){return s.toLowerCase().replace(/[^a-z0-9]+/g," ").trim().replace(/\s+/g," ")}const ids=new Set<string>(),fps=new Map<string,string>();export const npAlpha1CoverageMatrix=cells.map(c=>{const label=`np:${c.countryCode}:${c.exam}:Alpha-1 Antitrypsin Deficiency`;if(!respiratoryNpAlpha1FullLessons[c.fullLessonKey])throw new Error(`NP_A1_FULL_MISSING:${label}`);const cram=getNpAlpha1Cram(c.exam);if(!cram||cram.fullLessonKey!==c.fullLessonKey||cram.countryCode!==c.countryCode)throw new Error(`NP_A1_CRAM_INVALID:${label}`);if(c.questions.length!==20)throw new Error(`NP_A1_COUNT_INVALID:${label}/${c.questions.length}`);const stems=new Set<string>(),dist=[0,0,0,0];for(const q of c.questions){if(ids.has(q.id))throw new Error(`NP_A1_DUP_ID:${q.id}`);ids.add(q.id);const s=n(q.stem);if(stems.has(s))throw new Error(`NP_A1_DUP_STEM:${label}/${q.id}`);stems.add(s);const fp=`${s}::${q.options.map(n).join("||")}`;const prior=fps.get(fp);if(prior)throw new Error(`NP_A1_CROSS_COPY:${q.id}/${prior}`);fps.set(fp,q.id);if(q.tier!=="np"||q.exam!==c.exam||q.countryCode!==c.countryCode||q.regionScope!==c.regionScope||q.topic!=="Alpha-1 Antitrypsin Deficiency")throw new Error(`NP_A1_SCOPE:${q.id}`);if(q.options.length!==4||q.optionRationales.length!==4||q.correctAnswer<0||q.correctAnswer>3||!q.correctAnswerExplanation.trim()||q.optionRationales.some(r=>!r.trim()))throw new Error(`NP_A1_ANSWER:${q.id}`);if(q.difficulty>4||!q.clinicalReasoning.trim()||!q.clinicalPearl.trim()||!q.keyTakeaway.trim()||q.references.length<2)throw new Error(`NP_A1_METADATA:${q.id}`);dist[q.correctAnswer]++;}if(dist.join(",")!=="5,5,5,5")throw new Error(`NP_A1_BALANCE:${label}/${dist.join(",")}`);return{tier:"np" as const,countryCode:c.countryCode,exam:c.exam,bodySystem:"Respiratory" as const,topic:"Alpha-1 Antitrypsin Deficiency" as const,fullLessonKey:c.fullLessonKey,fullLessonPresent:true as const,cramLessonPresent:true as const,qualifyingQuestionCount:20 as const,questionDeficit:0 as const,cramDeficit:0 as const,answerPositionCounts:dist};});export const npAlpha1CoverageSummary={cells:npAlpha1CoverageMatrix.length,totalQualifyingQuestions:npAlpha1CoverageMatrix.reduce((a,c)=>a+c.qualifyingQuestionCount,0),totalQuestionDeficit:0,totalCramDeficit:0};if(npAlpha1CoverageSummary.cells!==2||npAlpha1CoverageSummary.totalQualifyingQuestions!==40)throw new Error(`NP_A1_SUMMARY:${JSON.stringify(npAlpha1CoverageSummary)}`);

import { getPracticalNursingArdsCram } from "./respiratory-practical-nursing-ards-cram";
import { respiratoryPracticalNursingArdsFullLessons } from "./respiratory-practical-nursing-ards-full";
import { rpnCaArdsRexpnPublishedBank } from "../exam-questions/rpn-ca-ards-rexpn-published";
import { pnUsArdsNclexPublishedBank } from "../exam-questions/pn-us-ards-nclex-published";
type Q=(typeof rpnCaArdsRexpnPublishedBank)[number]|(typeof pnUsArdsNclexPublishedBank)[number];const cells:Array<{countryCode:"CA"|"US";regionScope:"CAN"|"US";exam:"REX-PN"|"NCLEX-PN";fullLessonKey:string;questions:readonly Q[]}>=[{countryCode:"CA",regionScope:"CAN",exam:"REX-PN",fullLessonKey:"ards-rpn-ca-2026",questions:rpnCaArdsRexpnPublishedBank},{countryCode:"US",regionScope:"US",exam:"NCLEX-PN",fullLessonKey:"ards-pn-us-2026",questions:pnUsArdsNclexPublishedBank}];function n(v:string){return v.toLowerCase().replace(/[^a-z0-9]+/g," ").trim().replace(/\s+/g," ");}const ids=new Set<string>();const fps=new Map<string,string>();
export const practicalNursingArdsCoverageMatrix=cells.map(cell=>{const label=`${cell.countryCode}:${cell.exam}:Acute Respiratory Distress Syndrome`;if(!respiratoryPracticalNursingArdsFullLessons[cell.fullLessonKey])throw new Error(`PN_ARDS_FULL_MISSING:${label}`);const cram=getPracticalNursingArdsCram(cell.countryCode);if(!cram||cram.fullLessonKey!==cell.fullLessonKey||cram.regionScope!==cell.regionScope||cram.exam!==cell.exam)throw new Error(`PN_ARDS_CRAM_SCOPE_INVALID:${label}`);if(cell.questions.length!==20)throw new Error(`PN_ARDS_COUNT_INVALID:${label}/${cell.questions.length}`);const stems=new Set<string>(),dist=[0,0,0,0];for(const q of cell.questions){if(ids.has(q.id))throw new Error(`PN_ARDS_DUP_ID:${q.id}`);ids.add(q.id);const s=n(q.stem);if(stems.has(s))throw new Error(`PN_ARDS_DUP_STEM:${label}/${q.id}`);stems.add(s);const fp=`${s}::${q.options.map(n).join("||")}`;const prior=fps.get(fp);if(prior)throw new Error(`PN_ARDS_UNCHANGED_CROSS_COUNTRY_COPY:${q.id}/${prior}`);fps.set(fp,q.id);if(q.servingTier!=="rpn"||q.exam!==cell.exam||q.countryCode!==cell.countryCode||q.regionScope!==cell.regionScope||q.bodySystem!=="Respiratory"||q.topic!=="Acute Respiratory Distress Syndrome"||q.questionType!=="multiple_choice")throw new Error(`PN_ARDS_SCOPE_INVALID:${label}/${q.id}`);if(q.options.length!==4||q.optionRationales.length!==4||q.correctAnswer<0||q.correctAnswer>3||!q.correctAnswerExplanation.trim()||q.optionRationales.some(r=>!r.trim()))throw new Error(`PN_ARDS_ANSWER_CONTRACT_INVALID:${label}/${q.id}`);if(q.difficulty>4||!q.clinicalReasoning.trim()||!q.clinicalPearl.trim()||!q.keyTakeaway.trim()||q.references.length<2)throw new Error(`PN_ARDS_METADATA_INVALID:${label}/${q.id}`);if(new Set(q.options.map(n)).size!==4)throw new Error(`PN_ARDS_DUP_OPTIONS:${label}/${q.id}`);dist[q.correctAnswer]++;}if(dist.join(",")!=="5,5,5,5")throw new Error(`PN_ARDS_BALANCE_INVALID:${label}/${dist.join(",")}`);return{tier:"rpn" as const,countryCode:cell.countryCode,exam:cell.exam,bodySystem:"Respiratory" as const,topic:"Acute Respiratory Distress Syndrome" as const,fullLessonKey:cell.fullLessonKey,fullLessonPresent:true as const,cramLessonPresent:true as const,qualifyingQuestionCount:20 as const,questionDeficit:0 as const,cramDeficit:0 as const,answerPositionCounts:dist};});
export const practicalNursingArdsCoverageSummary={cells:practicalNursingArdsCoverageMatrix.length,totalQualifyingQuestions:practicalNursingArdsCoverageMatrix.reduce((a,c)=>a+c.qualifyingQuestionCount,0),totalQuestionDeficit:0,totalCramDeficit:0};if(practicalNursingArdsCoverageSummary.cells!==2||practicalNursingArdsCoverageSummary.totalQualifyingQuestions!==40)throw new Error(`PN_ARDS_SUMMARY_INVALID:${JSON.stringify(practicalNursingArdsCoverageSummary)}`);

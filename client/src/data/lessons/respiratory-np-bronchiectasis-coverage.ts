import { respiratoryNpBronchiectasisFullLessons } from "./respiratory-np-bronchiectasis-full";import { getNpBronchiectasisCram } from "./respiratory-np-bronchiectasis-cram";import { npCaCnpleBronchiectasisPublishedBank,npUsAanpFnpBronchiectasisPublishedBank } from "../exam-questions/np-bronchiectasis-cnple-aanp-fnp-published";
type Q=(typeof npCaCnpleBronchiectasisPublishedBank)[number]|(typeof npUsAanpFnpBronchiectasisPublishedBank)[number];const cells:Array<{exam:"CNPLE"|"AANP-FNP";countryCode:"CA"|"US";regionScope:"CAN"|"US";fullLessonKey:string;questions:readonly Q[]}>=[{exam:"CNPLE",countryCode:"CA",regionScope:"CAN",fullLessonKey:"bronchiectasis-np-ca-cnple-2026",questions:npCaCnpleBronchiectasisPublishedBank},{exam:"AANP-FNP",countryCode:"US",regionScope:"US",fullLessonKey:"bronchiectasis-np-us-aanp-fnp-2026",questions:npUsAanpFnpBronchiectasisPublishedBank}];function n(s:string){return s.toLowerCase().replace(/[^a-z0-9]+/g," ").trim().replace(/\s+/g," ")}const ids=new Set<string>(),fps=new Map<string,string>();export const npBronchiectasisCoverageMatrix=cells.map(c=>{const label=`np:${c.countryCode}:${c.exam}:Bronchiectasis`;if(!respiratoryNpBronchiectasisFullLessons[c.fullLessonKey])throw new Error(`NP_BX_FULL_MISSING:${label}`);const cram=getNpBronchiectasisCram(c.exam);if(!cram||cram.fullLessonKey!==c.fullLessonKey||cram.countryCode!==c.countryCode)throw new Error(`NP_BX_CRAM_INVALID:${label}`);if(c.questions.length!==20)throw new Error(`NP_BX_COUNT_INVALID:${label}/${c.questions.length}`);const stems=new Set<string>(),dist=[0,0,0,0];for(const q of c.questions){if(ids.has(q.id))throw new Error(`NP_BX_DUP_ID:${q.id}`);ids.add(q.id);const s=n(q.stem);if(stems.has(s))throw new Error(`NP_BX_DUP_STEM:${label}/${q.id}`);stems.add(s);const fp=`${s}::${q.options.map(n).join("||")}`;const prior=fps.get(fp);if(prior)throw new Error(`NP_BX_CROSS_COPY:${q.id}/${prior}`);fps.set(fp,q.id);if(q.tier!=="np"||q.exam!==c.exam||q.countryCode!==c.countryCode||q.regionScope!==c.regionScope||q.topic!=="Bronchiectasis")throw new Error(`NP_BX_SCOPE:${q.id}`);if(q.options.length!==4||q.optionRationales.length!==4||q.correctAnswer<0||q.correctAnswer>3||!q.correctAnswerExplanation.trim()||q.optionRationales.some(r=>!r.trim()))throw new Error(`NP_BX_ANSWER:${q.id}`);if(q.difficulty>4||!q.clinicalReasoning.trim()||!q.clinicalPearl.trim()||!q.keyTakeaway.trim()||q.references.length<2)throw new Error(`NP_BX_METADATA:${q.id}`);dist[q.correctAnswer]++;}if(dist.join(",")!=="5,5,5,5")throw new Error(`NP_BX_BALANCE:${label}/${dist.join(",")}`);return{tier:"np" as const,countryCode:c.countryCode,exam:c.exam,bodySystem:"Respiratory" as const,topic:"Bronchiectasis" as const,fullLessonKey:c.fullLessonKey,fullLessonPresent:true as const,cramLessonPresent:true as const,qualifyingQuestionCount:20 as const,questionDeficit:0 as const,cramDeficit:0 as const,answerPositionCounts:dist};});export const npBronchiectasisCoverageSummary={cells:npBronchiectasisCoverageMatrix.length,totalQualifyingQuestions:npBronchiectasisCoverageMatrix.reduce((a,c)=>a+c.qualifyingQuestionCount,0),totalQuestionDeficit:0,totalCramDeficit:0};if(npBronchiectasisCoverageSummary.cells!==2||npBronchiectasisCoverageSummary.totalQualifyingQuestions!==40)throw new Error(`NP_BX_SUMMARY:${JSON.stringify(npBronchiectasisCoverageSummary)}`);

import { respiratoryNpOsaFullLessons } from "./respiratory-np-osa-full";import { getNpOsaCram } from "./respiratory-np-osa-cram";import { npCaCnpleOsaPublishedBank,npUsAanpFnpOsaPublishedBank } from "../exam-questions/np-osa-cnple-aanp-fnp-published";type Q=(typeof npCaCnpleOsaPublishedBank)[number]|(typeof npUsAanpFnpOsaPublishedBank)[number];const cells=[{exam:"CNPLE",countryCode:"CA",regionScope:"CAN",fullLessonKey:"osa-np-ca-cnple-2026",questions:npCaCnpleOsaPublishedBank},{exam:"AANP-FNP",countryCode:"US",regionScope:"US",fullLessonKey:"osa-np-us-aanp-fnp-2026",questions:npUsAanpFnpOsaPublishedBank}] as const;function n(s:string){return s.toLowerCase().replace(/[^a-z0-9]+/g," ").trim().replace(/\s+/g," ")}const ids=new Set<string>(),fp=new Map<string,string>();export const npOsaCoverageMatrix=cells.map(c=>{const label=`np:${c.countryCode}:${c.exam}:Obstructive Sleep Apnea`;if(!respiratoryNpOsaFullLessons[c.fullLessonKey])throw new Error(`NP_OSA_FULL:${label}`);const cram=getNpOsaCram(c.exam);if(!cram||cram.fullLessonKey!==c.fullLessonKey)throw new Error(`NP_OSA_CRAM:${label}`);if(c.questions.length!==20)throw new Error(`NP_OSA_COUNT:${label}`);const stems=new Set<string>(),dist=[0,0,0,0];for(const q of c.questions as readonly Q[]){if(ids.has(q.id))throw new Error(`NP_OSA_DUP_ID:${q.id}`);ids.add(q.id);const s=n(q.stem);if(stems.has(s))throw new Error(`NP_OSA_DUP_STEM:${q.id}`);stems.add(s);const sig=`${s}::${q.options.map(n).join("||")}`;const prior=fp.get(sig);if(prior)throw new Error(`NP_OSA_CROSS_COPY:${q.id}/${prior}`);fp.set(sig,q.id);if(q.tier!=="np"||q.exam!==c.exam||q.countryCode!==c.countryCode||q.regionScope!==c.regionScope||q.topic!=="Obstructive Sleep Apnea")throw new Error(`NP_OSA_SCOPE:${q.id}`);if(q.options.length!==4||q.optionRationales.length!==4||!q.correctAnswerExplanation.trim()||q.optionRationales.some(r=>!r.trim())||q.difficulty>4||!q.clinicalReasoning.trim()||!q.clinicalPearl.trim()||!q.keyTakeaway.trim()||q.references.length<2)throw new Error(`NP_OSA_CONTRACT:${q.id}`);dist[q.correctAnswer]++;}if(dist.join(",")!=="5,5,5,5")throw new Error(`NP_OSA_BALANCE:${label}/${dist.join(",")}`);return{tier:"np" as const,countryCode:c.countryCode,exam:c.exam,bodySystem:"Respiratory" as const,topic:"Obstructive Sleep Apnea" as const,fullLessonKey:c.fullLessonKey,fullLessonPresent:true as const,cramLessonPresent:true as const,qualifyingQuestionCount:20 as const,questionDeficit:0 as const,cramDeficit:0 as const,answerPositionCounts:dist};});export const npOsaCoverageSummary={cells:2,totalQualifyingQuestions:40,totalQuestionDeficit:0,totalCramDeficit:0};
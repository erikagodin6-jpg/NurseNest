import { respiratoryNpTuberculosisFullLessons } from "./respiratory-np-tuberculosis-full";import { getNpTuberculosisCram } from "./respiratory-np-tuberculosis-cram";import { npCaCnpleTuberculosisPublishedBank,npUsAanpFnpTuberculosisPublishedBank } from "../exam-questions/np-tuberculosis-cnple-aanp-fnp-published";
type Q=(typeof npCaCnpleTuberculosisPublishedBank)[number]|(typeof npUsAanpFnpTuberculosisPublishedBank)[number];const cells:Array<{exam:"CNPLE"|"AANP-FNP";countryCode:"CA"|"US";regionScope:"CAN"|"US";fullLessonKey:string;questions:readonly Q[]}>=[{exam:"CNPLE",countryCode:"CA",regionScope:"CAN",fullLessonKey:"tuberculosis-np-ca-cnple-2026",questions:npCaCnpleTuberculosisPublishedBank},{exam:"AANP-FNP",countryCode:"US",regionScope:"US",fullLessonKey:"tuberculosis-np-us-aanp-fnp-2026",questions:npUsAanpFnpTuberculosisPublishedBank}];function n(s:string){return s.toLowerCase().replace(/[^a-z0-9]+/g," ").trim().replace(/\s+/g," ")}const ids=new Set<string>(),fps=new Map<string,string>();export const npTuberculosisCoverageMatrix=cells.map(c=>{const label=`np:${c.countryCode}:${c.exam}:Tuberculosis`;if(!respiratoryNpTuberculosisFullLessons[c.fullLessonKey])throw new Error(`NP_TB_FULL_MISSING:${label}`);const cram=getNpTuberculosisCram(c.exam);if(!cram||cram.fullLessonKey!==c.fullLessonKey||cram.countryCode!==c.countryCode)throw new Error(`NP_TB_CRAM_INVALID:${label}`);if(c.questions.length!==20)throw new Error(`NP_TB_COUNT_INVALID:${label}/${c.questions.length}`);const stems=new Set<string>(),dist=[0,0,0,0];for(const q of c.questions){if(ids.has(q.id))throw new Error(`NP_TB_DUP_ID:${q.id}`);ids.add(q.id);const s=n(q.stem);if(stems.has(s))throw new Error(`NP_TB_DUP_STEM:${label}/${q.id}`);stems.add(s);const fp=`${s}::${q.options.map(n).join("||")}`;const prior=fps.get(fp);if(prior)throw new Error(`NP_TB_CROSS_COPY:${q.id}/${prior}`);fps.set(fp,q.id);if(q.tier!=="np"||q.exam!==c.exam||q.countryCode!==c.countryCode||q.regionScope!==c.regionScope||q.topic!=="Tuberculosis")throw new Error(`NP_TB_SCOPE:${q.id}`);if(q.options.length!==4||q.optionRationales.length!==4||q.correctAnswer<0||q.correctAnswer>3||!q.correctAnswerExplanation.trim()||q.optionRationales.some(r=>!r.trim()))throw new Error(`NP_TB_ANSWER:${q.id}`);if(q.difficulty>4||!q.clinicalReasoning.trim()||!q.clinicalPearl.trim()||!q.keyTakeaway.trim()||q.references.length<2)throw new Error(`NP_TB_METADATA:${q.id}`);dist[q.correctAnswer]++;}if(dist.join(",")!=="5,5,5,5")throw new Error(`NP_TB_BALANCE:${label}/${dist.join(",")}`);return{tier:"np" as const,countryCode:c.countryCode,exam:c.exam,bodySystem:"Respiratory" as const,topic:"Tuberculosis" as const,fullLessonKey:c.fullLessonKey,fullLessonPresent:true as const,cramLessonPresent:true as const,qualifyingQuestionCount:20 as const,questionDeficit:0 as const,cramDeficit:0 as const,answerPositionCounts:dist};});export const npTuberculosisCoverageSummary={cells:npTuberculosisCoverageMatrix.length,totalQualifyingQuestions:npTuberculosisCoverageMatrix.reduce((a,c)=>a+c.qualifyingQuestionCount,0),totalQuestionDeficit:0,totalCramDeficit:0};if(npTuberculosisCoverageSummary.cells!==2||npTuberculosisCoverageSummary.totalQualifyingQuestions!==40)throw new Error(`NP_TB_SUMMARY:${JSON.stringify(npTuberculosisCoverageSummary)}`);

import { getRnTuberculosisCram, rnTuberculosisResolvedFullLessonKey } from "./respiratory-rn-tuberculosis-cram";
import { rnCaTuberculosisAuthoritativeBank } from "../exam-questions/rn-ca-tuberculosis-authoritative";
import { rnUsTuberculosisAuthoritativeBank } from "../exam-questions/rn-us-tuberculosis-authoritative";
type Q=(typeof rnCaTuberculosisAuthoritativeBank)[number]|(typeof rnUsTuberculosisAuthoritativeBank)[number];
const cells:Array<{countryCode:"CA"|"US";regionScope:"CAN"|"US";questions:readonly Q[]}>=[{countryCode:"CA",regionScope:"CAN",questions:rnCaTuberculosisAuthoritativeBank},{countryCode:"US",regionScope:"US",questions:rnUsTuberculosisAuthoritativeBank}];
function n(v:string){return v.toLowerCase().replace(/[^a-z0-9]+/g," ").trim().replace(/\s+/g," ");}const ids=new Set<string>();const fps=new Map<string,string>();
export const rnTuberculosisCoverageMatrix=cells.map(cell=>{const label=`${cell.countryCode}:NCLEX-RN:Tuberculosis`;const cram=getRnTuberculosisCram(cell.countryCode);if(!cram||cram.fullLessonKey!==rnTuberculosisResolvedFullLessonKey||cram.regionScope!==cell.regionScope||cram.topic!=="Tuberculosis")throw new Error(`RN_TB_CRAM_SCOPE_INVALID:${label}`);if(cell.questions.length!==20)throw new Error(`RN_TB_COUNT_INVALID:${label}/${cell.questions.length}`);const stems=new Set<string>(),dist=[0,0,0,0];for(const q of cell.questions){if(ids.has(q.id))throw new Error(`RN_TB_DUPLICATE_ID:${q.id}`);ids.add(q.id);const s=n(q.stem);if(stems.has(s))throw new Error(`RN_TB_DUPLICATE_STEM:${label}/${q.id}`);stems.add(s);const fp=`${s}::${q.options.map(n).join("||")}`;const prior=fps.get(fp);if(prior)throw new Error(`RN_TB_UNCHANGED_CROSS_COUNTRY_COPY:${q.id}/${prior}`);fps.set(fp,q.id);if(q.tier!=="rn"||q.exam!=="NCLEX-RN"||q.countryCode!==cell.countryCode||q.regionScope!==cell.regionScope||q.bodySystem!=="Respiratory"||q.topic!=="Tuberculosis"||q.questionType!=="multiple_choice")throw new Error(`RN_TB_SCOPE_INVALID:${label}/${q.id}`);if(q.options.length!==4||q.optionRationales.length!==4||q.correctAnswer<0||q.correctAnswer>3||!q.correctAnswerExplanation.trim()||q.optionRationales.some(r=>!r.trim()))throw new Error(`RN_TB_ANSWER_CONTRACT_INVALID:${label}/${q.id}`);if(q.difficulty<1||q.difficulty>4||!q.cognitiveLevel.trim()||!q.clinicalReasoning.trim()||!q.clinicalPearl.trim()||!q.keyTakeaway.trim()||q.references.length<2)throw new Error(`RN_TB_METADATA_INVALID:${label}/${q.id}`);if(new Set(q.options.map(n)).size!==4)throw new Error(`RN_TB_DUPLICATE_OPTIONS:${label}/${q.id}`);dist[q.correctAnswer]++;}if(dist.join(",")!=="5,5,5,5")throw new Error(`RN_TB_BALANCE_INVALID:${label}/${dist.join(",")}`);return{tier:"rn" as const,countryCode:cell.countryCode,exam:"NCLEX-RN" as const,bodySystem:"Respiratory" as const,topic:"Tuberculosis" as const,fullLessonKey:rnTuberculosisResolvedFullLessonKey,fullLessonPresent:true as const,cramLessonPresent:true as const,qualifyingQuestionCount:20 as const,questionDeficit:0 as const,cramDeficit:0 as const,answerPositionCounts:dist};});
export const rnTuberculosisCoverageSummary={cells:rnTuberculosisCoverageMatrix.length,totalQualifyingQuestions:rnTuberculosisCoverageMatrix.reduce((a,c)=>a+c.qualifyingQuestionCount,0),totalQuestionDeficit:rnTuberculosisCoverageMatrix.reduce((a,c)=>a+c.questionDeficit,0),totalCramDeficit:rnTuberculosisCoverageMatrix.reduce((a,c)=>a+c.cramDeficit,0)};if(rnTuberculosisCoverageSummary.cells!==2||rnTuberculosisCoverageSummary.totalQualifyingQuestions!==40||rnTuberculosisCoverageSummary.totalQuestionDeficit!==0||rnTuberculosisCoverageSummary.totalCramDeficit!==0)throw new Error(`RN_TB_COVERAGE_SUMMARY_INVALID:${JSON.stringify(rnTuberculosisCoverageSummary)}`);

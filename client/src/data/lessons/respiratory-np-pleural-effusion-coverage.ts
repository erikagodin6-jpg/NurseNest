import { respiratoryNpPleuralEffusionFullLessons } from "./respiratory-np-pleural-effusion-full";import { getNpPleuralEffusionCram } from "./respiratory-np-pleural-effusion-cram";import { npCaCnplePleuralEffusionPublishedBank,npUsAanpFnpPleuralEffusionPublishedBank } from "../exam-questions/np-pleural-effusion-cnple-aanp-fnp-published";
type Q=(typeof npCaCnplePleuralEffusionPublishedBank)[number]|(typeof npUsAanpFnpPleuralEffusionPublishedBank)[number];const cells:Array<{exam:"CNPLE"|"AANP-FNP";countryCode:"CA"|"US";regionScope:"CAN"|"US";fullLessonKey:string;questions:readonly Q[]}>=[{exam:"CNPLE",countryCode:"CA",regionScope:"CAN",fullLessonKey:"pleural-effusion-np-ca-cnple-2026",questions:npCaCnplePleuralEffusionPublishedBank},{exam:"AANP-FNP",countryCode:"US",regionScope:"US",fullLessonKey:"pleural-effusion-np-us-aanp-fnp-2026",questions:npUsAanpFnpPleuralEffusionPublishedBank}];function n(s:string){return s.toLowerCase().replace(/[^a-z0-9]+/g," ").trim().replace(/\s+/g," ")}const ids=new Set<string>(),fps=new Map<string,string>();export const npPleuralEffusionCoverageMatrix=cells.map(c=>{const label=`np:${c.countryCode}:${c.exam}:Pleural Effusion`;if(!respiratoryNpPleuralEffusionFullLessons[c.fullLessonKey])throw new Error(`NP_PEFF_FULL_MISSING:${label}`);const cram=getNpPleuralEffusionCram(c.exam);if(!cram||cram.fullLessonKey!==c.fullLessonKey||cram.countryCode!==c.countryCode)throw new Error(`NP_PEFF_CRAM_INVALID:${label}`);if(c.questions.length!==20)throw new Error(`NP_PEFF_COUNT:${label}/${c.questions.length}`);const stems=new Set<string>(),dist=[0,0,0,0];for(const q of c.questions){if(ids.has(q.id))throw new Error(`NP_PEFF_DUP_ID:${q.id}`);ids.add(q.id);const s=n(q.stem);if(stems.has(s))throw new Error(`NP_PEFF_DUP_STEM:${label}/${q.id}`);stems.add(s);const fp=`${s}::${q.options.map(n).join("||")}`;const prior=fps.get(fp);if(prior)throw new Error(`NP_PEFF_CROSS_COPY:${q.id}/${prior}`);fps.set(fp,q.id);if(q.tier!=="np"||q.exam!==c.exam||q.countryCode!==c.countryCode||q.regionScope!==c.regionScope||q.topic!=="Pleural Effusion")throw new Error(`NP_PEFF_SCOPE:${q.id}`);if(q.options.length!==4||q.optionRationales.length!==4||q.correctAnswer<0||q.correctAnswer>3||!q.correctAnswerExplanation.trim()||q.optionRationales.some(r=>!r.trim()))throw new Error(`NP_PEFF_ANSWER:${q.id}`);if(q.difficulty>4||!q.clinicalReasoning.trim()||!q.clinicalPearl.trim()||!q.keyTakeaway.trim()||q.references.length<2)throw new Error(`NP_PEFF_METADATA:${q.id}`);dist[q.correctAnswer]++;}if(dist.join(",")!=="5,5,5,5")throw new Error(`NP_PEFF_BALANCE:${label}/${dist.join(",")}`);return{tier:"np" as const,countryCode:c.countryCode,exam:c.exam,bodySystem:"Respiratory" as const,topic:"Pleural Effusion" as const,fullLessonKey:c.fullLessonKey,fullLessonPresent:true as const,cramLessonPresent:true as const,qualifyingQuestionCount:20 as const,questionDeficit:0 as const,cramDeficit:0 as const,answerPositionCounts:dist};});export const npPleuralEffusionCoverageSummary={cells:npPleuralEffusionCoverageMatrix.length,totalQualifyingQuestions:npPleuralEffusionCoverageMatrix.reduce((a,c)=>a+c.qualifyingQuestionCount,0),totalQuestionDeficit:0,totalCramDeficit:0};if(npPleuralEffusionCoverageSummary.cells!==2||npPleuralEffusionCoverageSummary.totalQualifyingQuestions!==40)throw new Error(`NP_PEFF_SUMMARY:${JSON.stringify(npPleuralEffusionCoverageSummary)}`);

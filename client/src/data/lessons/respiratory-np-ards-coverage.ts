import { respiratoryNpArdsFullLessons } from "./respiratory-np-ards-full";import { getNpArdsCram } from "./respiratory-np-ards-cram";import { npCaCnpleArdsPublishedBank,npUsAgacnpArdsPublishedBank } from "../exam-questions/np-ards-cnple-agacnp-published";
type Q=(typeof npCaCnpleArdsPublishedBank)[number]|(typeof npUsAgacnpArdsPublishedBank)[number];const cells:Array<{exam:"CNPLE"|"AGACNP";countryCode:"CA"|"US";regionScope:"CAN"|"US";fullLessonKey:string;questions:readonly Q[]}>=[{exam:"CNPLE",countryCode:"CA",regionScope:"CAN",fullLessonKey:"ards-np-ca-cnple-2026",questions:npCaCnpleArdsPublishedBank},{exam:"AGACNP",countryCode:"US",regionScope:"US",fullLessonKey:"ards-np-us-agacnp-2026",questions:npUsAgacnpArdsPublishedBank}];function n(s:string){return s.toLowerCase().replace(/[^a-z0-9]+/g," ").trim().replace(/\s+/g," ")}const ids=new Set<string>(),fp=new Map<string,string>();export const npArdsCoverageMatrix=cells.map(c=>{const label=`np:${c.countryCode}:${c.exam}:ARDS`;if(!respiratoryNpArdsFullLessons[c.fullLessonKey])throw new Error(`NP_ARDS_FULL_MISSING:${label}`);const cram=getNpArdsCram(c.exam);if(!cram||cram.fullLessonKey!==c.fullLessonKey||cram.countryCode!==c.countryCode)throw new Error(`NP_ARDS_CRAM_INVALID:${label}`);if(c.questions.length!==20)throw new Error(`NP_ARDS_COUNT_INVALID:${label}/${c.questions.length}`);const stems=new Set<string>(),dist=[0,0,0,0];for(const q of c.questions){if(ids.has(q.id))throw new Error(`NP_ARDS_DUP_ID:${q.id}`);ids.add(q.id);const s=n(q.stem);if(stems.has(s))throw new Error(`NP_ARDS_DUP_STEM:${label}/${q.id}`);stems.add(s);const sig=`${s}::${q.options.map(n).join("||")}`;const prior=fp.get(sig);if(prior)throw new Error(`NP_ARDS_CROSS_CELL_COPY:${q.id}/${prior}`);fp.set(sig,q.id);if(q.tier!=="np"||q.exam!==c.exam||q.countryCode!==c.countryCode||q.regionScope!==c.regionScope||q.topic!=="Acute Respiratory Distress Syndrome")throw new Error(`NP_ARDS_SCOPE_INVALID:${q.id}`);if(q.options.length!==4||q.optionRationales.length!==4||q.correctAnswer<0||q.correctAnswer>3||!q.correctAnswerExplanation.trim()||q.optionRationales.some(r=>!r.trim()))throw new Error(`NP_ARDS_ANSWER_CONTRACT:${q.id}`);if(q.difficulty>4||!q.clinicalReasoning.trim()||!q.clinicalPearl.trim()||!q.keyTakeaway.trim()||q.references.length<2)throw new Error(`NP_ARDS_METADATA:${q.id}`);dist[q.correctAnswer]++;}if(dist.join(",")!=="5,5,5,5")throw new Error(`NP_ARDS_BALANCE:${label}/${dist.join(",")}`);return{tier:"np" as const,countryCode:c.countryCode,exam:c.exam,bodySystem:"Respiratory" as const,topic:"Acute Respiratory Distress Syndrome" as const,fullLessonKey:c.fullLessonKey,fullLessonPresent:true as const,cramLessonPresent:true as const,qualifyingQuestionCount:20 as const,questionDeficit:0 as const,cramDeficit:0 as const,answerPositionCounts:dist};});export const npArdsCoverageSummary={cells:npArdsCoverageMatrix.length,totalQualifyingQuestions:npArdsCoverageMatrix.reduce((a,c)=>a+c.qualifyingQuestionCount,0),totalQuestionDeficit:0,totalCramDeficit:0};if(npArdsCoverageSummary.cells!==2||npArdsCoverageSummary.totalQualifyingQuestions!==40)throw new Error(`NP_ARDS_SUMMARY_INVALID:${JSON.stringify(npArdsCoverageSummary)}`);

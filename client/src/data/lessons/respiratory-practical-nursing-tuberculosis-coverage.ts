import { getPracticalNursingTuberculosisCram } from "./respiratory-practical-nursing-tuberculosis-cram";
import { respiratoryPracticalNursingTuberculosisFullLessons } from "./respiratory-practical-nursing-tuberculosis-full";
import { rpnCaTuberculosisPublishedBank, pnUsTuberculosisPublishedBank } from "../exam-questions/practical-nursing-tuberculosis-published";
type Q=(typeof rpnCaTuberculosisPublishedBank)[number]|(typeof pnUsTuberculosisPublishedBank)[number];const cells:Array<{countryCode:"CA"|"US";regionScope:"CAN"|"US";exam:"REX-PN"|"NCLEX-PN";fullLessonKey:string;questions:readonly Q[]}>=[{countryCode:"CA",regionScope:"CAN",exam:"REX-PN",fullLessonKey:"tuberculosis-rpn-ca-2026",questions:rpnCaTuberculosisPublishedBank},{countryCode:"US",regionScope:"US",exam:"NCLEX-PN",fullLessonKey:"tuberculosis-pn-us-2026",questions:pnUsTuberculosisPublishedBank}];function n(v:string){return v.toLowerCase().replace(/[^a-z0-9]+/g," ").trim().replace(/\s+/g," ");}const ids=new Set<string>();const fps=new Map<string,string>();
export const practicalNursingTuberculosisCoverageMatrix=cells.map(cell=>{const label=`${cell.countryCode}:${cell.exam}:Tuberculosis`;if(!respiratoryPracticalNursingTuberculosisFullLessons[cell.fullLessonKey])throw new Error(`PN_TB_FULL_MISSING:${label}`);const cram=getPracticalNursingTuberculosisCram(cell.countryCode);if(!cram||cram.fullLessonKey!==cell.fullLessonKey||cram.regionScope!==cell.regionScope||cram.exam!==cell.exam)throw new Error(`PN_TB_CRAM_SCOPE_INVALID:${label}`);if(cell.questions.length!==20)throw new Error(`PN_TB_COUNT_INVALID:${label}/${cell.questions.length}`);const stems=new Set<string>(),dist=[0,0,0,0];for(const q of cell.questions){if(ids.has(q.id))throw new Error(`PN_TB_DUP_ID:${q.id}`);ids.add(q.id);const s=n(q.stem);if(stems.has(s))throw new Error(`PN_TB_DUP_STEM:${label}/${q.id}`);stems.add(s);const fp=`${s}::${q.options.map(n).join("||")}`;const prior=fps.get(fp);if(prior)throw new Error(`PN_TB_UNCHANGED_CROSS_COUNTRY_COPY:${q.id}/${prior}`);fps.set(fp,q.id);if(q.servingTier!=="rpn"||q.exam!==cell.exam||q.countryCode!==cell.countryCode||q.regionScope!==cell.regionScope||q.bodySystem!=="Respiratory"||q.topic!=="Tuberculosis"||q.questionType!=="multiple_choice")throw new Error(`PN_TB_SCOPE_INVALID:${label}/${q.id}`);if(q.options.length!==4||q.optionRationales.length!==4||q.correctAnswer<0||q.correctAnswer>3||!q.correctAnswerExplanation.trim()||q.optionRationales.some(r=>!r.trim()))throw new Error(`PN_TB_ANSWER_CONTRACT_INVALID:${label}/${q.id}`);if(q.difficulty>4||!q.clinicalReasoning.trim()||!q.clinicalPearl.trim()||!q.keyTakeaway.trim()||q.references.length<2)throw new Error(`PN_TB_METADATA_INVALID:${label}/${q.id}`);if(new Set(q.options.map(n)).size!==4)throw new Error(`PN_TB_DUP_OPTIONS:${label}/${q.id}`);dist[q.correctAnswer]++;}if(dist.join(",")!=="5,5,5,5")throw new Error(`PN_TB_BALANCE_INVALID:${label}/${dist.join(",")}`);return{tier:"rpn" as const,countryCode:cell.countryCode,exam:cell.exam,bodySystem:"Respiratory" as const,topic:"Tuberculosis" as const,fullLessonKey:cell.fullLessonKey,fullLessonPresent:true as const,cramLessonPresent:true as const,qualifyingQuestionCount:20 as const,questionDeficit:0 as const,cramDeficit:0 as const,answerPositionCounts:dist};});
export const practicalNursingTuberculosisCoverageSummary={cells:practicalNursingTuberculosisCoverageMatrix.length,totalQualifyingQuestions:practicalNursingTuberculosisCoverageMatrix.reduce((a,c)=>a+c.qualifyingQuestionCount,0),totalQuestionDeficit:0,totalCramDeficit:0};if(practicalNursingTuberculosisCoverageSummary.cells!==2||practicalNursingTuberculosisCoverageSummary.totalQualifyingQuestions!==40)throw new Error(`PN_TB_SUMMARY_INVALID:${JSON.stringify(practicalNursingTuberculosisCoverageSummary)}`);

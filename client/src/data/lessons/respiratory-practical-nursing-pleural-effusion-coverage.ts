import { getPracticalNursingPleuralEffusionCram } from "./respiratory-practical-nursing-pleural-effusion-cram";
import { respiratoryMissingRpnLessons } from "./respiratory-missing-rpn";
import { respiratoryPnUsPleuralEffusionFullLessons } from "./respiratory-pn-us-pleural-effusion-full";
import { rpnCaPleuralEffusionPublishedBank, pnUsPleuralEffusionPublishedBank } from "../exam-questions/practical-nursing-pleural-effusion-published";

type Q=(typeof rpnCaPleuralEffusionPublishedBank)[number]|(typeof pnUsPleuralEffusionPublishedBank)[number];
const cells:Array<{countryCode:"CA"|"US";regionScope:"CAN"|"US";exam:"REX-PN"|"NCLEX-PN";fullLessonKey:string;questions:readonly Q[]}>=[
{countryCode:"CA",regionScope:"CAN",exam:"REX-PN",fullLessonKey:"pleural-effusion-chest-drain-rpn-ca",questions:rpnCaPleuralEffusionPublishedBank},
{countryCode:"US",regionScope:"US",exam:"NCLEX-PN",fullLessonKey:"pleural-effusion-chest-drain-pn-us",questions:pnUsPleuralEffusionPublishedBank}
];
function n(v:string){return v.toLowerCase().replace(/[^a-z0-9]+/g," ").trim().replace(/\s+/g," ");}const ids=new Set<string>();const fps=new Map<string,string>();
export const practicalNursingPleuralEffusionCoverageMatrix=cells.map(cell=>{const label=`${cell.countryCode}:${cell.exam}:Pleural Effusion`;const full=cell.countryCode==="CA"?respiratoryMissingRpnLessons[cell.fullLessonKey]:respiratoryPnUsPleuralEffusionFullLessons[cell.fullLessonKey];if(!full)throw new Error(`PN_PLEURAL_FULL_MISSING:${label}/${cell.fullLessonKey}`);const cram=getPracticalNursingPleuralEffusionCram(cell.countryCode);if(!cram||cram.fullLessonKey!==cell.fullLessonKey||cram.regionScope!==cell.regionScope||cram.exam!==cell.exam)throw new Error(`PN_PLEURAL_CRAM_SCOPE_INVALID:${label}`);if(cell.questions.length!==20)throw new Error(`PN_PLEURAL_COUNT_INVALID:${label}/${cell.questions.length}`);const stems=new Set<string>(),dist=[0,0,0,0];for(const q of cell.questions){if(ids.has(q.id))throw new Error(`PN_PLEURAL_DUP_ID:${q.id}`);ids.add(q.id);const s=n(q.stem);if(stems.has(s))throw new Error(`PN_PLEURAL_DUP_STEM:${label}/${q.id}`);stems.add(s);const fp=`${s}::${q.options.map(n).join("||")}`;const prior=fps.get(fp);if(prior)throw new Error(`PN_PLEURAL_UNCHANGED_CROSS_COUNTRY_COPY:${q.id}/${prior}`);fps.set(fp,q.id);if(q.servingTier!=="rpn"||q.exam!==cell.exam||q.countryCode!==cell.countryCode||q.regionScope!==cell.regionScope||q.bodySystem!=="Respiratory"||q.topic!=="Pleural Effusion"||q.questionType!=="multiple_choice")throw new Error(`PN_PLEURAL_SCOPE_INVALID:${label}/${q.id}`);if(q.options.length!==4||q.optionRationales.length!==4||q.correctAnswer<0||q.correctAnswer>3||!q.correctAnswerExplanation.trim()||q.optionRationales.some(r=>!r.trim()))throw new Error(`PN_PLEURAL_ANSWER_CONTRACT_INVALID:${label}/${q.id}`);if(q.difficulty>4||!q.clinicalReasoning.trim()||!q.clinicalPearl.trim()||!q.keyTakeaway.trim()||q.references.length<2)throw new Error(`PN_PLEURAL_METADATA_INVALID:${label}/${q.id}`);if(new Set(q.options.map(n)).size!==4)throw new Error(`PN_PLEURAL_DUP_OPTIONS:${label}/${q.id}`);dist[q.correctAnswer]++;}if(dist.join(",")!=="5,5,5,5")throw new Error(`PN_PLEURAL_BALANCE_INVALID:${label}/${dist.join(",")}`);return{tier:"rpn" as const,countryCode:cell.countryCode,exam:cell.exam,bodySystem:"Respiratory" as const,topic:"Pleural Effusion" as const,fullLessonKey:cell.fullLessonKey,fullLessonPresent:true as const,cramLessonPresent:true as const,qualifyingQuestionCount:20 as const,questionDeficit:0 as const,cramDeficit:0 as const,answerPositionCounts:dist};});
export const practicalNursingPleuralEffusionCoverageSummary={cells:practicalNursingPleuralEffusionCoverageMatrix.length,totalQualifyingQuestions:practicalNursingPleuralEffusionCoverageMatrix.reduce((a,c)=>a+c.qualifyingQuestionCount,0),totalQuestionDeficit:0,totalCramDeficit:0};if(practicalNursingPleuralEffusionCoverageSummary.cells!==2||practicalNursingPleuralEffusionCoverageSummary.totalQualifyingQuestions!==40)throw new Error(`PN_PLEURAL_SUMMARY_INVALID:${JSON.stringify(practicalNursingPleuralEffusionCoverageSummary)}`);

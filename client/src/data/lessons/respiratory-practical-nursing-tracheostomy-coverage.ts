import { getPracticalNursingTracheostomyCram } from "./respiratory-practical-nursing-tracheostomy-cram";
import { respiratoryPracticalNursingTracheostomyFullLessons } from "./respiratory-practical-nursing-tracheostomy-full";
import { rpnCaTracheostomyPublishedBank, pnUsTracheostomyPublishedBank } from "../exam-questions/practical-nursing-tracheostomy-published";
type Q=(typeof rpnCaTracheostomyPublishedBank)[number]|(typeof pnUsTracheostomyPublishedBank)[number];const cells:Array<{countryCode:"CA"|"US";regionScope:"CAN"|"US";exam:"REX-PN"|"NCLEX-PN";fullLessonKey:string;questions:readonly Q[]}>=[{countryCode:"CA",regionScope:"CAN",exam:"REX-PN",fullLessonKey:"tracheostomy-care-rpn-ca-2026",questions:rpnCaTracheostomyPublishedBank},{countryCode:"US",regionScope:"US",exam:"NCLEX-PN",fullLessonKey:"tracheostomy-care-pn-us-2026",questions:pnUsTracheostomyPublishedBank}];function n(v:string){return v.toLowerCase().replace(/[^a-z0-9]+/g," ").trim().replace(/\s+/g," ");}const ids=new Set<string>();const fps=new Map<string,string>();
export const practicalNursingTracheostomyCoverageMatrix=cells.map(cell=>{const label=`${cell.countryCode}:${cell.exam}:Tracheostomy Care`;if(!respiratoryPracticalNursingTracheostomyFullLessons[cell.fullLessonKey])throw new Error(`PN_TRACH_FULL_MISSING:${label}`);const cram=getPracticalNursingTracheostomyCram(cell.countryCode);if(!cram||cram.fullLessonKey!==cell.fullLessonKey||cram.regionScope!==cell.regionScope||cram.exam!==cell.exam)throw new Error(`PN_TRACH_CRAM_SCOPE_INVALID:${label}`);if(cell.questions.length!==20)throw new Error(`PN_TRACH_COUNT_INVALID:${label}/${cell.questions.length}`);const stems=new Set<string>(),dist=[0,0,0,0];for(const q of cell.questions){if(ids.has(q.id))throw new Error(`PN_TRACH_DUP_ID:${q.id}`);ids.add(q.id);const s=n(q.stem);if(stems.has(s))throw new Error(`PN_TRACH_DUP_STEM:${label}/${q.id}`);stems.add(s);const fp=`${s}::${q.options.map(n).join("||")}`;const prior=fps.get(fp);if(prior)throw new Error(`PN_TRACH_UNCHANGED_CROSS_COUNTRY_COPY:${q.id}/${prior}`);fps.set(fp,q.id);if(q.servingTier!=="rpn"||q.exam!==cell.exam||q.countryCode!==cell.countryCode||q.regionScope!==cell.regionScope||q.bodySystem!=="Respiratory"||q.topic!=="Tracheostomy Care"||q.questionType!=="multiple_choice")throw new Error(`PN_TRACH_SCOPE_INVALID:${label}/${q.id}`);if(q.options.length!==4||q.optionRationales.length!==4||q.correctAnswer<0||q.correctAnswer>3||!q.correctAnswerExplanation.trim()||q.optionRationales.some(r=>!r.trim()))throw new Error(`PN_TRACH_ANSWER_CONTRACT_INVALID:${label}/${q.id}`);if(q.difficulty>4||!q.clinicalReasoning.trim()||!q.clinicalPearl.trim()||!q.keyTakeaway.trim()||q.references.length<2)throw new Error(`PN_TRACH_METADATA_INVALID:${label}/${q.id}`);if(new Set(q.options.map(n)).size!==4)throw new Error(`PN_TRACH_DUP_OPTIONS:${label}/${q.id}`);dist[q.correctAnswer]++;}if(dist.join(",")!=="5,5,5,5")throw new Error(`PN_TRACH_BALANCE_INVALID:${label}/${dist.join(",")}`);return{tier:"rpn" as const,countryCode:cell.countryCode,exam:cell.exam,bodySystem:"Respiratory" as const,topic:"Tracheostomy Care" as const,fullLessonKey:cell.fullLessonKey,fullLessonPresent:true as const,cramLessonPresent:true as const,qualifyingQuestionCount:20 as const,questionDeficit:0 as const,cramDeficit:0 as const,answerPositionCounts:dist};});
export const practicalNursingTracheostomyCoverageSummary={cells:practicalNursingTracheostomyCoverageMatrix.length,totalQualifyingQuestions:practicalNursingTracheostomyCoverageMatrix.reduce((a,c)=>a+c.qualifyingQuestionCount,0),totalQuestionDeficit:0,totalCramDeficit:0};if(practicalNursingTracheostomyCoverageSummary.cells!==2||practicalNursingTracheostomyCoverageSummary.totalQualifyingQuestions!==40)throw new Error(`PN_TRACH_SUMMARY_INVALID:${JSON.stringify(practicalNursingTracheostomyCoverageSummary)}`);

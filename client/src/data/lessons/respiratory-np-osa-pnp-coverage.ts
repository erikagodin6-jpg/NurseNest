import { respiratoryNpOsaPnpFullLessons } from "./respiratory-np-osa-pnp-full";import { npUsPnpOsaCram } from "./respiratory-np-osa-pnp-cram";import { npUsPnpOsaPublishedBank } from "../exam-questions/np-us-pnp-osa-published";import { npUsAanpFnpOsaPublishedBank } from "../exam-questions/np-osa-cnple-aanp-fnp-published";import { npUsAnccFnpOsaPublishedBank } from "../exam-questions/np-us-ancc-fnp-osa-published";import { npUsAgpcnpOsaPublishedBank } from "../exam-questions/np-us-agpcnp-osa-published";import { npUsAgacnpOsaPublishedBank } from "../exam-questions/np-us-agacnp-osa-published";function n(s:string){return s.toLowerCase().replace(/[^a-z0-9]+/g," ").trim().replace(/\s+/g," ")}const fullLessonKey="osa-np-us-pnp-2026" as const;if(!respiratoryNpOsaPnpFullLessons[fullLessonKey])throw new Error(`PNP_OSA_FULL_MISSING`);if(npUsPnpOsaCram.fullLessonKey!==fullLessonKey)throw new Error(`PNP_OSA_CRAM_INVALID`);if(npUsPnpOsaPublishedBank.length!==20)throw new Error(`PNP_OSA_COUNT:${npUsPnpOsaPublishedBank.length}`);const comparison=[...npUsAanpFnpOsaPublishedBank,...npUsAnccFnpOsaPublishedBank,...npUsAgpcnpOsaPublishedBank,...npUsAgacnpOsaPublishedBank];const prior=new Set(comparison.map(q=>`${n(q.stem)}::${q.options.map(n).join("||")}`));const ids=new Set<string>(),stems=new Set<string>();const dist=[0,0,0,0];for(const q of npUsPnpOsaPublishedBank){if(ids.has(q.id))throw new Error(`PNP_OSA_DUP_ID:${q.id}`);ids.add(q.id);const s=n(q.stem);if(stems.has(s))throw new Error(`PNP_OSA_DUP_STEM:${q.id}`);stems.add(s);const fp=`${s}::${q.options.map(n).join("||")}`;if(prior.has(fp))throw new Error(`PNP_OSA_UNCHANGED_ADULT_COPY:${q.id}`);if(q.tier!=="np"||q.exam!=="PNP"||q.countryCode!=="US"||q.regionScope!=="US"||q.topic!=="Obstructive Sleep Apnea")throw new Error(`PNP_OSA_SCOPE:${q.id}`);if(q.options.length!==4||q.optionRationales.length!==4||!q.correctAnswerExplanation.trim()||q.optionRationales.some(r=>!r.trim())||q.difficulty>4||!q.clinicalReasoning.trim()||!q.clinicalPearl.trim()||!q.keyTakeaway.trim()||q.references.length<2)throw new Error(`PNP_OSA_CONTRACT:${q.id}`);dist[q.correctAnswer]++;}if(dist.join(",")!=="5,5,5,5")throw new Error(`PNP_OSA_BALANCE:${dist.join(",")}`);export const npUsPnpOsaCoverageMatrix=[{tier:"np" as const,countryCode:"US" as const,exam:"PNP" as const,bodySystem:"Respiratory" as const,topic:"Obstructive Sleep Apnea" as const,fullLessonKey,fullLessonPresent:true as const,cramLessonPresent:true as const,qualifyingQuestionCount:20 as const,questionDeficit:0 as const,cramDeficit:0 as const,answerPositionCounts:dist}];export const npUsPnpOsaCoverageSummary={cells:1,totalQualifyingQuestions:20,totalQuestionDeficit:0,totalCramDeficit:0};